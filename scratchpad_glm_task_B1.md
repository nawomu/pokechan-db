# GLM実装タスク: B1「弱点ボタン」(リアルバトル)

あなたはこのポケモンDBプロジェクトの実装担当(GLM)です。設計=Claude/実装=あなた/検証=Claude/声の判定=阿部さん、という役割分担です。下記を**正確に**実装してください。

## 0. まず読む(規律の把握・必須)
- `CLAUDE.md`(運用原則。特に「★多言語化ファースト」節)
- `運用メモ_Opus向け_ループとサブエージェントの回し方_2026-07-05.md`(の§2エージェント7要素)
これらの規律に従うこと。

## 1. 編集してよいファイル(厳守)
- **`real_battle.html` のみ編集可**。
- **`real_battle_simulator.html`(エンジン)は編集禁止**。
- i18n辞書 `i18n/ui-*.json` に**日本語(ja)キーのみ**追加してよい(他8言語は後でClaude側が翻訳ワークフローで埋める。あなたは触らない=空でよい、または ja と同じ値を暫定で入れて `data-i18n` 配線)。
- **git commit / push は禁止**。実装したら止めて報告するだけ。

## 2. 作るもの(仕様)
リアルバトル画面の**自分側HPボックス `#pb-self`** に「**弱点**」ボタンを足す。タップすると、**相手の場に出ているポケモンのタイプ相性パネル**をフィールド上に表示する。プレイヤーが「どのタイプの技が相手に効くか」を確認できる機能。

### パネルの内容
相手アクティブポケモンに対して、18タイプの攻撃技を当てたときの倍率を計算し、以下に分類してタイプバッジで表示:
- **こうかばつぐん(弱点)**: ×2 または ×4(×4は「4倍」と明記)
- **いまひとつ**: ×0.5 または ×0.25(×0.25は「1/4」と明記)
- **こうかなし**: ×0
- (×1の等倍は省略してよい)

### 倍率の計算方法
- 相手アクティブポケモンの**実効タイプ**と特性を考慮する。エンジンブリッジ `S`(= `window.__sim`)の以下を使う:
  - `S.typeEffPreview(攻撃タイプ名, 防御タイプ配列)` … 素のタイプ相性倍率(:8299でエクスポート済み)。
  - 相手の実効タイプは `S.sideTypes('opp')`(:8296でエクスポート済み)で取得。無ければ相手ポケモンの `types` を使う。
  - 特性ふゆう(じめん無効)・もらいび/ちょすい/そうしょく/ちくでん等の吸収特性による無効も反映したい。可能なら `S.calcDamage` の結果の `immune`/`reason` を使う、難しければ最低限 `typeEffPreview` の素相性だけでもよい(特性込みは任意・ベストエフォート)。
- 18タイプは `TYPES`(pokechan_data.js:10)の配列。

### 設置場所と描画
- ボタンは `#pb-self`(real_battle.html:742 付近)内、または既存の技オーバーレイ上段のボタン群(「👀 様子を見る」`:2127` / 「📖 技の説明」/「⚙」`:2126-2130`)と同じ場所・同じスタイルで足すのが自然。**既存の「様子を見る」ボタンのマークアップとクリック委譲(`:2203`付近)を踏襲**すること。
- パネルは既存の **`toggleWatchPanel(btn)`(real_battle.html:2295)を複製**して作る(位置計算・zoom補正・クリックで閉じる処理が揃っているので、それを弱点用に作り替える)。関数名は `toggleWeaknessPanel` 等。
- 相手が場にいない/入替中はボタンを押しても無害に(パネルは空 or 非表示)。

## 3. 多言語化(i18nファースト・必須)
- ボタン文言「弱点」、パネル見出し「こうかばつぐん」「いまひとつ」「こうかなし」「4倍」「1/4」は、**生のja文字列を直書きしない**。`I18N.t('real_battle.weakness_xxx', 'ja既定値')` 経由 or `data-i18n` 配線にする。キーは `i18n/ui-real_battle.json`(存在すれば)等に **ja のみ**追加(他言語はClaudeが後で埋める)。
- **タイプ名は必ず `tTypeName(タイプ名)`(real_battle.html:921)を使う**(直接ja名を出さない)。
- 動的に作るパネルは、言語切替(`i18n:changed`イベント)で作り直すか、開くたびに最新の表示言語で生成する(初回描画がi18n準備前に走るレースに注意=CLAUDE.mdのi18n教訓)。

## 4. スタイル
- 既存のバトルUIのトーン(チャンピオンズ寄せ)に合わせる。タイプバッジは既存のタイプ色(`TYPE_COLORS`)を流用。`toggleWatchPanel`のパネルCSSを踏襲。

## 5. 完了後の報告(実装はするがcommitはしない)
最終報告に必ず含める:
1. 変更した関数・行の一覧(real_battle.html のどこに何を足したか)。
2. 追加したi18nキー名の一覧(ja値つき)。
3. 特性込み相性を反映できたか/素相性のみか。
4. 動作確認をしたか(ローカルで開いて押せたか)。できなければその旨。
5. Claude側で検証・翻訳すべき残点。

**実装後は git status で real_battle.html と i18n の変更だけになっていることを確認し、commitせずに終了すること。**
