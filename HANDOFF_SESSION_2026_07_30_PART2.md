# HANDOFF 2026-07-30 PART2 ★これだけ読めば再開できる(引っ越し直前で「止まった」日)

★**読む順**: ① `CLAUDE.md`「★★★★全体の順番」+「★★★データの絶対ルール」 → ② 本書 → ③ 必要なら `HANDOFF_SESSION_2026_07_30.md`(PART1=マスターデータ完成まで)

---

# 0. いまの位置

**① 内部データの一本化 = 完了 → ② ページの引っ越し = ★阿部さんが意図的に止めた(正しい判断だった) → ③ バトル = 凍結中**

**今日の一番大事な出来事は、コードではなく「止めたこと」。** 阿部さんの「漠然と、まだ早い、どこか見逃している」が当たっていて、
実際に **192+75件の説明文が劣化する状態**だった。止めなければ、そのまま生成コンテンツ4,900枚に焼き込まれていた。

---

# 1. 今日やったこと

## 1-1. 本番push(5コミット)
| commit | 中身 |
|---|---|
| `db61dce4` | エンジンのバグ4件(メイカー永続/フォーカスレンズ/メンタルハーブ/メトロノーム) |
| `ed25cfca` | ★マスターデータ `master/` + `pokedb.js` + 番人 `_ssot_guard_test.js` |
| `dd5cbd9c` | AI交代ロジック(相性予防交代/特性を見る/設置物のコスト/先制技の確定KO)テスト7/7 |
| `69d3a48b` | 管理ツール一式 + 自動生成の仕様書 |
| `a81c792b` | 調査・設計の成果物 + 引き継ぎ + **権威コーパスを.gitignore** |

全ハーネス緑を実測で確認してからpush: **sim 825/0・hard 174/0/skip11・番人 悪化なし・AI交代 7/7**。

## 1-2. ★権威コーパスを公開リポジトリから除外した(重要)
- このリポジトリは**公開**(`github.com/nawomu/pokechan-db` = 200 / CNAME=pchamdb.com)
- `reference/_authority_corpus/`(39MB・1,483ファイル)は **ポケモンWiki・ヤックンの本文をそのまま保存したもの**
- → **他所の本文の再公開になる**ので `.gitignore` に追加。**ローカルには残す**(内部参照専用)
- 既存の `review/_showdown_ref/`(著作物・コミットしない)と同じ扱い。併せて `undefined/`・`*.bak_*`・作業中コピーも除外
- push後に検証: 公開側に載ったのは取得スクリプト `tools/_fetch_authority_corpus.js` **のみ**(本文は0件)

## 1-3. ★引っ越しの中身を確認したら、劣化が見つかった(未修正)
`node tools/_master_diff.js` → `review/マスターデータ差分.html` を作り直して**全数で数えた**結果:

| # | 内容 | 件数 | 性質 |
|---|---|---|---|
| ① | 特性の新説明に**サイトのナビ欄が混入**(「関連する項目特性…道具…」) | **26件**(厳密) | 🔧 抽出の失敗=直せる |
| ② | 「説明が新しく付く」9件のうち**実は空**(`うなぎのぼり`・source=unknown) | **1件** | 🔧 差分レポート自体の嘘 |
| ③ | 持ち物で「**(戦闘中1回)**」が消え「**2000VPで購入する**」に入れ替わる | **74件** | ❓ 声の判断が要る |
| ④ | `すながくれ` に「**すなあらしダメージを受けない**」が足されている | 1件 | ❓ **権威が食い違う** |
| ⑤ | `マグマのよろい`/`ほのおのからだ` から「**タマゴ孵化歩数1/2**」が消える | 数件 | ❓ Championsに孵化があるか |

★③は **simに効く仕様(メガシンカは1戦1回)** が消えてショップ情報に入れ替わる。**情報が減っている。**

★④の食い違い(★Claudeは判定していない):
```
Wiki の Champions節 : 「すなあらし状態の時 回避率が1.25倍になる。」          ←ダメージ無効の記述なし
ヤックン /ch/       : 「…命中率が0.8倍になる。また、『すなあらし』によるダメージを受けない。」
```
- 回避1.25倍と命中0.8倍は**同じ値**(1÷1.25=0.8)なので争点は「ダメージ無効」だけ
- Wikiの同ページには「タイプに関係なくすなあらしの効果でダメージを受けなくなる」が**ブラック2・ホワイト2(チェレン)のセリフ=第五世代**として載っている
- 参考: **`ゆきがくれ` には同じ記述が無い**(非対称)
- ★**うちの旧テキスト「すなあらし状態なら回避率が1.25倍。」は WikiのChampions節と一致している**(=旧が正しく、新が汚れている可能性)

## 1-4. 確認カードを作った(★LLM不使用・決定的な突き合わせ)
**`node tools/_build_migration_check.js`** → `reference/_migration_check.json`

- **353枚**(特性192+9 / 持ち物75 / 技5+1 / 名前30 / 学習データ41)
- 1枚に **①旧文 ②新文 ③ヤックンch原文 ④WikiのChampions節原文 ⑤機械的事実 ⑥判定欄(空)**
- ★**並べ替えない・省略しない・上位N件にしない**(理由は§2)
- **引用の実在検査 470件 / NG 0件**(コーパスに無い文字列を1つも書いていない)
- 材料の揃い: 特性201件すべてにヤックンch原文あり / **195件はWikiのChampions節とも照合可** / 持ち物75件すべてWikiにあり
  → **2つの独立した権威で1件ずつ突き合わせられる**

★**未完**: `review/引っ越し内容確認.html`(見る画面)は**まだ作っていない**。JSONまで。

---

# 2. ★★★注意点(今日いちばん大事・次のセッションは必ず読む)

## 2-1. 阿部さんの指摘(原文に近い形で残す)
> 「**一個一個調べないでバーッと回しちゃうところ**の効率とかスピードとか…慎重にやらないとダメなんだよね、一個一個丁寧に。それを**わからずにバーッと飛ばしちゃう**からダメなんだよ。それを**ストップかけるのを知ってる、わかってる人しかできない**んだよね」

> 「ポケモンは**一言葉、言葉の一個一個がすごく複雑で慎重に作られている**ので、**似たようなまとまっているものをちゃんとそこで分けるかどうかの慎重さ**、『これは似てるな』とか、**それなんですよ**」

> 「**チェックは多分俺にしかわからない。本当のポケモンをやったことがある人しかわからない**」

> 「**その一言で表される違いを一瞬でかぎ取らなきゃいけない**。それってさ、**前の昔の記憶がないとできない**んだよ。それがClaudeにはない。**セッションをリセットするから**。俺は『あれ、これ前みたいな、やばいな、なんか違うな』っていうのが**残る**んだよね」

## 2-2. 罪は「一括処理」ではなく「★似ているものを同一視すること」
**憲法に4日前から書いてあった**(`設計_バトルエンジン原理_2026-07-26.md:10,13):
```
困ったらフェーズを足す。まとめない。ごまかさない。楽をしない。分けすぎて問題になることはない。
分割は情報を失わない。統合は失う。
  …同じ箱に入れると順序が曖昧になり、後から読む人(次のセッションのAI含む)が勝手に解釈する = バグの発生源。
```
★**「後から読む人(次のセッションのAI含む)が勝手に解釈する」= まさにこの癖に名前がついていた。**
Claudeはこれを「バトルのフェーズ設計の話」として読み、**「調べ方」にも同じ法が効くという読み方をしていなかった**。

**今日Claudeが実際にやった統合(3回)**:
1. 数字の消失23件を「大半は同義の言い換え」と**Claudeが決めた**(0.75倍=3/4 は同値だが、**決めたのはClaude**)
2. 持ち物を「**74件が同じ問題**」と一括で報告した(1件ずつ文が違うのに)
3. 「**疑わしい順に並べます**」と言った(★**順番を決めるのも重みを決める=統合**)

## 2-3. ★★「機械に回していいか」の線(今日決めた・これを使う)
> **機械や一括に回していいのは、情報を1ビットも「足さず・減らさず・混ぜない」作業だけ。**

| ✅ 回してよい | ❌ 回してはいけない |
|---|---|
| 原文をそのまま引く | 「同じ意味だ」と決める |
| 有る/無いを数える | まとめる・代表例で済ます |
| 差がある**場所**を指す | 要約する・言い換える |
| **全件を漏れなく並べる** | **疑わしい順に並べて上位だけ見せる** |

**理由**: 左は**後から検証できる**(引用が原文に実在するか機械で確かめられる)。右は**判断が消える**——誰がどこで何を同一視したのか追えなくなる=「統合は失う」。

★この線のおかげで、今夜の仕事は**Sonnetを回す必要が無くなった**(決定的スクリプトの方が正確。LLMは言い換えるし、聞かれれば判定を書く)。
★**「Sonnetで回せることがあれば回して」への正直な答え = 回すより正確に引く方が良い仕事**、だった。

## 2-4. ★失敗した仕掛け(残すが、使い方を間違えないこと)
`_build_migration_check.js` の **`authority_gap`(2権威で片方にしか無い言葉)は 195/195 件で反応した=絞り込みには使えない**。
2つのサイトは同じ内容を違う言葉で書くため。**「絞り込みの道具」ではなく「両方を並べて見るための材料」**と読むこと。
★ただし中身には本物が埋まっている(例: `がんじょう` で **Wikiにだけ「一撃必殺技も効かない」**)。**絞り込みは人間の目。**

## 2-4b. ★★説明文は「わざと短い」/ Wikiは「裏取り・背景」も書いている(2026-07-30 阿部さん)
> 「技・特性・持ち物の説明文は**短いんだけど、わざと短くしてる**。それをポケモンWikiとかは**背景の話も、裏取りの話も**書いてる。でも**ちょっとした違いがどういう意味かに気づかないと、誤認=間違った判断になる**。そのちょっとした違いに気づくかどうか、それがまだAIにできない。**全体を覚えてないとできない**。一回一回リセットされると、目の前のニュアンスの違い・表現の違いが**見過ごされる**。だったら**一回一回全部読み込んで、読み直して比べるしかない**。無駄が多いだろうけど、**それしかないんだよ、正確にやるということは**。丁寧にやる、しっかりやる、ゆっくりやる、**何度も確認する、遡る**。ゆっくりだけど、**それが一番の近道**」

**★この2つが今日のバグの発生源そのものだった**(阿部さんの警告が、実物を説明していた):
- **「短さは欠落ではなく設計」** → 短い説明文を「情報が足りない」と見て**補完してはいけない**
- **「Wikiは裏取り・背景も書いている」** → その背景を説明文に**持ち込んではいけない**
- 実例① `すながくれ` = Wikiの**第五世代NPC(チェレン)のセリフ**「すなあらしの効果でダメージを受けなくなる」が新テキストに混入した疑い
- 実例② ナビ欄26件 = ヤックンの**関連リンク欄**が説明文に混入
→ **どちらも「権威ページの、説明文でない部分」を説明文に混ぜた**という同じ型。

## 2-4c. ★Claudeの実例: 断片から判定して間違えた(がんじょう・同日)
`authority_gap` の**断片リスト**(文を読点で切ったもの)を見て、「ヤックンchは一撃必殺に触れていない」と**結論を出した**。
→ **全文を読んだら入っていた**。しかも旧より詳しい(4技を名指し)。**劣化リストに⑥として足す寸前**だった。
```
旧 : 一撃必殺技を無効化。HPが満タンならば…HP1で持ちこたえる。
新 : HPが最大の時…必ず1残る。また、一撃で『ひんし』になる技『じわれ』『ぜったいれいど』
     『つのドリル』『ハサミギロチン』を受けない。          ←抜けていない。むしろ詳しい
```
★**教訓**: 機械の出力が「断片」や「まとめ」の時、**そこから判定してはいけない**(§2-3の❌そのもの)。**必ず全文に戻る。**
★確定した事実(権威2つ一致): **がんじょうは一撃必殺技(じわれ/ぜったいれいど/つのドリル/ハサミギロチン)を無効にする**。
  **かたやぶり系は貫通**(`real_battle_simulator.html:2798`・実装済み)。一撃必殺技の命中率は**4技すべて30**(Wiki)。

## 2-5. 枠の計器(新設)
**`bash tools/_weekly_budget.sh`** → `{"go":true,"pct_est":…,"remain_out":…}`
- 週間%はアプリ表示しか正が無い(`_quota_guard.sh` に既述)ので、**実測1点で校正して出力トークンで比例換算**する
- 校正点: **2026-07-30 17:00頃 アプリ表示「すべてのモデル 77%」= 7/25以降のClaude出力 4,021,667 tok**(GLM除外)
- 90%ライン = 出力 **4,700,649 tok**。★**測れない時は `go:false` に倒す**(暴走より停止)
- ★**週リセット(金22:00)を跨いだら校正は無効**。取り直すこと

## 2-6. GLMの現状(スクショ実測 2026-07-30 16:39)
| | 使用済み | リセット |
|---|---|---|
| GLM 週間 | **94%**(残り6%) | **2026-08-04 15:25(火)** |
| GLM 5時間 | 0% | — |
| Claude 週間(全モデル) | **77%** | **7/31 22:00(金)** |

★**「GLMをとことん回す」は燃料が無いので無理**。リセットは明日ではなく**8/4**。明日金曜にリセットされるのは**Claude側**。
★プラン: 記録では GLM Pro = **$72/月 ≒ ¥11,000前後**(★7/23時点の記録。今日の価格は未確認)。
**今の作業は「大量生成」ではなく「遊んだ人の目でのチェック」なので、5倍の枠は要らない**という判断(GLMが光ったのは量ではなく**視点の違い**=ニャオニクスナイト1件)。

## 2-7. agmsgでClaude同士を回す件 = ★保留(阿部さん判断)
- 技術的には**可能**(`/agmsg spawn claude-code <名前>` が正式機能。tmux無しのMacでは `open -a Terminal` で新窓+`/agmsg actas`を自動投入。相手のwatcherが繋がるまでブロックして待つ=「渡したが動いてない」が起きない)
- Claude Code同士は**リアルタイムpush**(Monitor)で繋がる。**Codexは Monitor が無く turn モードのみ**=一押しが必要
- ★**却下の理由(阿部さん)**: 「**消費が早くなるだけ**」。GLMは別APIだから2窓が成立していたが、**Claude 2窓は同じ週間枠を2倍速で削る**
- 使うなら「量を増やす」ためでなく「**作る側と検証する側を分ける**」ため(§3-3)

---

# 3. ★次にやること(この順)

## 3-1. まず①〜⑤を潰す(★引っ越しより先)
1. **①ナビ欄の混入26件** = 機械的に除去できる(`関連する項目`以降を落とす)。★ただし**26件を1件ずつ確認**してから(他の形の混入が無いか)
2. **②`うなぎのぼり`** = 権威に説明が無い。`_unknowns.json` の4件と同じ扱い。**推測で埋めない**
3. **③持ち物74件** = ★**阿部さんの声の判断待ち**。提案は「メガピクシーにメガシンカさせる。**戦闘中1回だけ**。(2000VPで購入)」= 仕様を残してVPを足す
4. **④すながくれ / ⑤孵化歩数** = ★**阿部さんの判断待ち**(Championsに孵化があるか / ダメージ無効が在るか)
5. **`review/引っ越し内容確認.html` を作る**(353枚を1枚1カード・並べ替えなし・判定欄つき)← ★未完の宿題

## 3-2. ②ページの引っ越し(①〜⑤が済んでから)
1. `pokedb.js` に `master/` を読む選択肢(`?data=v2`)を足す ← ★**まだ着手していない**
2. 二重公開して実機確認(現行を壊さない)
3. **1ページずつ**切り替え(**直読み17枚 → 0枚が進捗計**。script tag 15 + document.write 2=`battle_lab`/`real_battle_simulator`。`data_browser.html` は済)
4. 生成コンテンツ4,900枚+9言語を再生成 → 安定後に旧削除 → ブリッジ撤去
5. 番人に検査を追加(説明文==compose再生成 / オーバーレイ件数 / フラグ列↔POKEMON_WAZA / 名前の正規化)

## 3-3. 検証役をどう置くか(8/4以降・設計の宿題)
GLMが見つけた**ニャオニクスナイト到達不能**が示したこと = **作った本人は見落とす**。
→ 8/4にGLMが復活したら、**作る側(Claude)と検証する側(GLM)を分ける**運用に戻す。**量ではなく視点**のために使う。

## 3-4. ③バトル(①②完了後・凍結中)
特性の不一致90件 / フェーズ差し込み設計4本 / 接地判定 / 高影響11件 / `runSingleAttack`と`runTurn`の二重実装 ほか。
資料は `reference/_ch_ability_check_all.json` / `_phase_design_a1〜a4.json` / `_t11_spec_g1〜g4.json`(**消さない**)。

---

# 4. 🙋 阿部さん判断待ち

1. **③持ち物74件の書き方**(「戦闘中1回」を残すか。★声の領域)
2. **④すながくれ**: Championsに「すなあらしダメージ無効」は在るか(権威が食い違っている)
3. **⑤Championsにタマゴ孵化はあるか**(無ければ「孵化歩数1/2」は消して正しい)
4. **§2-3の線(機械に回していい/いけない)を `CLAUDE.md` に正式に入れてよいか**(今は本書だけにある)
5. **GLM**: 解約 / 継続 / Pro。★残り6%・8/4リセット・今日が解約予定日だった

---

# 5. 阿部さんの構想メモ(今日の会話から・消さない)

> 「Claudeも**メインで回すセッションと、記憶のサブみたいなのがいくつかあって、複合的に協力して回す**形じゃないと。人間の脳もそうでしょう。**部位が分かれている**。今はまだClaudeが部位が分かれてない。それが複数で管理できたら、それが**シンギュラリティ**になってくる」

> 「あとは**目**。今、静止画は読み取れる。動画も24フレームとか10フレームでもいい、1秒間に。それを一瞬で読み取る。1つの画像を**分割**してもいい。結局それって**複数のAIでやらなきゃダメ**じゃん、今の処理能力じゃ。それができて、目ができて、複数でやり出した瞬間に、やっと**人間を超える**」

**★この日の実務との接点**: 「記憶が無いから一瞬でかぎ取れない」は本当。ただし**部分的には仕掛けで補える**——
`ヤックン耳_判断ログ.md` と memory は既にその試みだが、**記録しているのは「結論(ルール)」で、「違和感そのもの」ではない**。
→ 次に効くのは、**「何を見た時に、何が引っかかったか」を実例で残すこと**。
★それでClaudeが阿部さんの鼻を持てるわけではない。**得られるのは「止まって聞く引き金」**。
今日うまくいった理由は「**阿部さんが止めた**」から。引き金が記録されていれば、**Claudeが自分で止まれる**。

---

# 6. 現在の状態(2026-07-30 夜)

- **push済み** = `a81c792b` まで(5コミット)。**作業ツリーは綺麗**。全ハーネス緑
- **未修正** = 上の①〜⑤(直していない。**カードに全部載っている**)
- **未着手** = `pokedb.js` の `?data=v2`(②の1歩目)/ `review/引っ越し内容確認.html`
- **新設した道具** = `tools/_build_migration_check.js`(カード生成・LLM不使用) / `tools/_weekly_budget.sh`(90%で止まる計器)
- **枠** = Claude週間 77%(明日22:00リセット)。今夜はほとんど使っていない
