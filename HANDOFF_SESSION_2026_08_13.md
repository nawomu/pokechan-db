# HANDOFF 2026-08-12〜13 ★これだけ読めば再開できる(持ち物監査R2完走→R3進行 / 2窓体制の初日 / 枠計器の教訓)

★**読む順**: ① `CLAUDE.md`「★★★データの絶対ルール」 → ② メモリ [[master-data-core-philosophy]] → ③ 本書 → ④ 台帳 `reference/_item_audit_round3.json` と帳簿 `reference/_window_ledger.json`

---

# 0. いまの位置(2026-08-13 朝)

**マスターの全数一字一句監査(持ち物→特性→技→覚える技)の続き。持ち物のラウンド3を 97/169 まで消化して停止中。**

| 項目 | 状態 |
|---|---|
| 持ち物ラウンド1 | 169/169 完了・修正適用済み(2026-08-02) |
| 持ち物ラウンド2 | **169/169 完走**(2026-08-12〜13)。修正3件を適用・push済み |
| 持ち物ラウンド3 | **97/169**(残り72件)。レンズ=相互作用と例外/Champions節突き合わせ |
| 特性313件・技919件・覚える技 | 未着手(持ち物の後) |
| ページの屋台骨作り直し・バトル | 監査の後(順番は不変) |

**停止理由**: 枠(セッション/週間)の実残量が誰にも分からない状態になったため。★§4を読むこと。

---

# 1. 今日マスターに入れた修正(全部 push 済み)

すべて **Wiki + 徹底攻略の二重チェックで一致** を確認して適用(§3の新ルール)。

1. **こだわりハチマキ**(`e8c832b1`) — 「物理技の攻撃が×1.5」→「持たせると、同じ技しか出せなくなるが、『こうげき』が1.5倍になる。ダイマックス中は、すべての効果がない。」
   - 旧文は「技威力が1.5倍」とも読める曖昧表現。権威は一貫して**能力値(こうげき)そのもの**が1.5倍
2. **こだわりメガネ**(同上) — 同型。「『とくこう』が1.5倍」
3. **とくせいガード**(`4d709a68`) — 「いかなる効果によっても変更・無効化されなくなる」→「持たせると、**戦闘中に**特性が変わることや、無効化されること、**かたやぶり系の効果で無視されること**が無くなる。」
   - 旧文は範囲が広すぎた(戦闘外のとくせいカプセル/パッチは防げない)。かたやぶり耐性はWikiに明記のある機構なので併せて採用

**変更しないと判断したもの**: しめったいわ等の天気石4種。Wikiの公式説明文は「技や特性であめ状態にした時」と一般形だが、masterは /ch/ 正典どおりの記述なので**出典優先順位ルール上そのまま**(sim実装時は任意の雨ソースで8ターンと解釈する、と台帳に記録)。

---

# 2. ★ラウンド3の収穫(レンズ替えが当たった)

ラウンド1〜2は「文言の精度」だったが、**ラウンド3は sim が静かに間違える種類**を掘り当てている。**枯れ判定は当分先**。

- **かいがらのすず(6件)**: ダメージ0なら回復しない/瀕死になる時は発動しない/とんぼがえりは回復してから交代/わるいてぐせとの順序/奪った側が即時回復/連続技は合算
- **きあいのタスキ(3件)**: 自滅技・反動では耐えない/連続技は1発目のみ/こらえるが優先
- **ふうせん**: ★**かたやぶりが効かない**(道具なので)。特性ふゆうと同じ実装にすると誤る
- **でんきだま**: 攻撃2倍はピカチュウ限定なのに、文言がライチュウにも乗ると誤読させる
- **くろいてっきゅう**: すばやさ0.5倍と「ランク補正・まひ」の**計算順序**が未記載
- ほか しろいハーブ/せんせいのツメ/しんかのきせき/おおきなねっこ/ひかりのねんど/とつげきチョッキ/だっしゅつパック

## ★★系統バグ(未修正・次にやること筆頭)

**`master/items.json` の `boosts` が「発動条件」のコピーになっていて、「何が上がるか」を表していない。**

- 実例: **のどスプレー** = `applies_to: sound_moves` / `boosts: sound_moves`。しかし権威2つとも「上がるのは**とくこう**」(Wiki「自分のとくこうを1段階上げる」/徹底攻略「『とくこう』ランクが1段階上がる」)
- **原因はデータでなく生成器**: `tools/build_master_v2.js` L229 `boosts: looksLikeDescriptor(it.applies_to) ? it.applies_to : null` — applies_to をそのまま複写している
- 影響: `boosts` の全15値のうち `sound_moves` `punch_moves` `multi_hit_moves` `protosynthesis_quark_drive` 等、**条件が入っている値が複数**。CLAUDE.md の「同じ欄に別の意味を入れない」違反で、sim が上昇対象を取り違える
- **直し方**: 生成器で `boosts` を条件から切り離し、1件ずつ権威で「何が上がるか」を確定して埋める(169件に波及するので全数差分+番人で確認)

---

# 3. ★新しい確定ルール(2026-08-12〜13 阿部さん)

## (a) 事実確認は二重チェック → 聞かずに適用・コミットしてよい
> 「基本的に技は俺に確認するよりも、ポケモンウィキとヤックン徹底攻略の2つ、その二重でチェックすれば間違いないと思ってください。俺に聞くよりもそこで調べてどんどん進めてコミットしていっていいです。」

- メモリ [[two-source-verify-then-commit]]。**両者が食い違う/どちらにも無い場合だけ止まって聞く**
- Champions に在るものは従来どおり ヤックン`/ch/` が正典([[champions-plus-alpha-canon-rule]] は不変)

## (b) 監査の丁寧さは削らない
> 「チェック工程が細かいのはわかる、②では漏れが出る懸念がある、丁寧にやらないと意味がない…人間がやるときって、一字一句で丁寧に見るしかない」

- メモリ [[audit-thoroughness-not-negotiable]]。**1件=1エージェント/引用必須/全件反証/ラウンド制/推測で埋めない は不変**
- Claude が出した「反証役を新発見ありの件だけに絞る」案は**却下された**。実際 R3 の重要発見の多くは反証役が拾ったもの
- **コストが問題なら監査以外の無駄を削る**(実際に削った例=§5の収集器)

---

# 4. ★★枠(セッション/週間)の計器 — 今日の一番の教訓

## 結論だけ先に
**ローカルの数字から「枠の総量(cap)」を逆算する道は原理的に閉じている。真値は阿部さんの使用量画面だけ。重い作業の前に画面の%を聞く。**

## なぜそうなったか(順に4回間違えた)

1. **帳簿がセッション専用フォルダにあった** → 他の窓からは実態の1/5にしか見えず、`--warn 80` が**一度も鳴らないまま窓が死ぬ**状態だった。「無言=安全」に見えて実は危険という、計器として最悪の壊れ方
   → 共有1本 `reference/_window_ledger.json` に移した(「データは一つ」がツール自身に適用されていなかった)
2. **推測値を実測に混ぜていた** → 走行中バッチの見積りを spent に入れ、印を出していなかったので 76% が実測に見えた。実態は約107%
   → `confirmed[]`(実測)と `running[]`(見積り・basis必須)に分離。出力は `confirmed_pct` / `estimated_pct` の2本立て、判定は安全側
3. **cap を 9M→35.2M に引き直した(誤り)** → 阿部さんの画面が40%の瞬間に実測して割り出したが、過去ブロックの検算で「消費が増えたのに%が下がる」矛盾が出た
   → **原因**: その朝、**HPのPCの定期スケジュール(Cowork/Opus 4.7)が同時に動いていた**。枠はアカウント単位だが `ccusage` は**そのマシンのローカルログしか読めない**。他マシン/クラウドの消費は原理的に見えない(`MULTI_MACHINE_LIMIT`)
4. **level=stop のまま自分でバッチを再開した** → 根拠は「40%だったから空いている」だが、**その40%がセッションか週間かは未確認**。未確定の数字を確定のように使った
   → `cap_verified: false` を導入。**検証されていない cap では赤/緑を出さない(`level: "unknown"`)**。前提が崩れた閾値で赤を出すと「跨いでよい赤」になり、計器そのものが死ぬ

## 機構的に確定したこと
- **ccusage はサブエージェント消費を写さない**。セッションログ `~/.claude/projects/…/<session>.jsonl` に `"isSidechain":true` の行が**0件**だから(経験則ではなく必然)。→ **完了通知の `subagent_tokens` を書き留めた帳簿が唯一の記録**
- **ccusage blocks はマシン全体を合算**する(2窓の同時刻読みがほぼ一致)。窓ごとに分けたい時は `npx ccusage session --json`(period=セッションUUID)
- **未決着(unknownのまま)**: 帳簿の `subagent_tokens` が cacheRead を含むか不明。ccusage 側は input+output+cacheCreation で cacheRead 除外なので、単位が揃っていない可能性がある。決着実験(1体ワークフローを A=ツール0回 / B=同文脈10回 で流して差の伸び方を見る)は 0812-2窓が担当予定

## 校正のやり方(次にやる人へ)
校正点には**5つ揃わないと使えない**: ①メーター種別(`session` / `weekly-all` / `weekly-fable`) ②画面の% ③その瞬間の ccusage ④帳簿 confirmed ⑤**その時に動いていたマシン・窓の構成**。1つでも欠けた点は捨てる。
既知の基準点: 2026-08-12 21:00頃のスクショ = セッション47% / 週間すべて12% / 週間Fable11%。

---

# 5. 新しい道具(全部 push 済み)

| ツール | 役割 |
|---|---|
| `tools/_usage_monitor.sh` | 枠の見える化。共有帳簿を自動で読む。`--watch --warn N` で危険域だけ通知。**cap未検証なら level=unknown** |
| `reference/_window_ledger.json` | ★共有帳簿(データは一つ)。confirmed/running・校正点・機構メモ・未決着caveat を全部ここに |
| `tools/_gen_item_audit_materials.js` | 監査の照合材料を1件1ファイルで生成(LLM不使用)。master行+/ch/正典+Wiki生ページ+過去ラウンドの既知指摘。**特性版もこれを流用できる** |
| `tools/_collect_audit_round.js` | ワークフロー結果を `journal.jsonl` から台帳へ組み立て(LLM不使用)。二重追記防止・新発見候補の抽出つき |

**★ワークフローは要約(digest)だけ返す形に変えた**(`scratchpad/item-audit-round.js`)。理由=完了通知の巨大テキストを親がもう一度読むコストが監査本体と同じ桁だった。**監査エージェント側は1文字も変えていない**。反証スキーマに `name` を必須化(journalから対にするため)。

---

# 6. ★2窓体制(Claude同士の会話)— 初日の実績

公式のセッション間メッセージング(`SendMessage` / `ListAgents`)が使えるようになり、2窓目(検証役)と往復した。**agmsg と併用可**(公式=割り込みで直接届く・設定ゼロ / agmsg=履歴が残る・他ベンダー可)。

**初日で検証役が見つけた私の穴が5件**。全部「私が自分で書いたルールを自分で破っていた」もので、独りでは見つからなかった:
1. 帳簿の分裂(データは一つ違反) 2. 推測値の混入(推測で埋めない違反) 3. cap校正の矛盾 4. isSidechain調査による機構確定 5. **赤を跨いだ私自身**

**運用の取り決め**: 縄張り(master/ と監査台帳は監査窓が握る)/重い作業の前に一声/枠は共有なので配分を相談。窓ごとの消費内訳は `npx ccusage session --json` で分離できる(実測: 検証窓3.0M/$7.63 vs 監査窓37.7M/$71.37 = **重いのは2窓運用ではなく監査ワークフロー**)。

---

# 7. ★次にやること(この順)

1. **🙋 阿部さんに画面の%を聞く**(現在のセッション / 週間すべて / 週間Fable)。→ 校正点を5つ揃えて記録 → `cap_verified` を true にできるか判断
2. **持ち物ラウンド3の残り72件**。名簿=`scratchpad/r3_names.txt` から台帳済みを引いた分(`_collect_audit_round.js` が二重追記を防ぐ)。材料=`scratchpad/audit_items_r3/`
3. **★boosts 系統バグの修正**(§2末尾)。生成器を直し、169件を全数差分+番人で確認
4. ラウンド3完走 → 集計 → 枯れ判定(規約=2ラウンド連続で新発見ゼロ)。**R3で発見が続いているので、ラウンド4は必要**
5. 持ち物が枯れたら **特性313件**へ。材料生成は `_gen_item_audit_materials.js` の abilities 版を作る(権威=Wiki生ページ310枚+`_authority_corpus_ch/abilities_ch.json`+7/28の三者照合資産 `_ch_ability_check_all.json`)
6. その後 技919件 → 覚える技 → **②ページの屋台骨作り直し** → **③バトル**

---

# 8. 罠(次のセッションが必ず思い出すこと)

1. **計器を跨ぐな**。赤を一度跨ぐと、次のセッションはそれを標準だと思う(CLAUDE.md の「今回だけの例外」と同じ型)。跨ぎたくなったら、跨ぐのでなく**計器を正直にする**(=`level: unknown`)
2. **推測を実測に混ぜるな**。混ぜる時は必ず印を出す(`has_estimate` / `basis`)
3. **ローカルの数字は下限値**。別マシン・クラウドのルーティンが動いていれば実消費はもっと多い
4. **ワークフローの完了通知は巨大**。全結果を返させると親が同額を払う。要約+journal収集が正
5. 1件あたりの実測: R2=117k / **R3=132k**(『詳細な仕様』節の精読ぶん重い)/ 要約方式のR3バッチ5=123k
6. 上限に当たっても **`resumeFromRunId` で損失ゼロ**。止めるのを怖がらない
7. ヤックンは `curl` 403 → **実ブラウザの同一オリジン fetch**(`https://yakkun.com/sv/item.htm` を開いて JS で `tr` を拾う)

---

# 9. コミット一覧(2026-08-12〜13・全push済み)

`e8c832b1` こだわり2件是正 / `a2c782aa` R2バッチ4+材料生成器 / `0a96c730` R2バッチ5a / `30dbd706` R2バッチ5b / `2f56e9ed` **R2完走** / `4d709a68` とくせいガード是正 / `5d9298dc` 生成器拡張 / `0592cdaf` R3バッチ1 / `b6993675` 枠校正(旧) / `8fc5fee9` R3バッチ2 / `08b7cbf9` 枠監視ツール新設 / `0a94da66` 帳簿を共有1本に / `8f1d5b23` 推測と実測を分離 / `1fc6533b`・`79d72589` 校正点と機構メモ / `658d8cce` R3バッチ3 / `788c68bc` 収集器新設 / `8139999a` 予算60% / `e34d9232` cap引き直し(→撤回) / `f5679dca` **capの逆算を放棄** / `89b0ad27` **level=unknown化** / `11ad7f51` R3バッチ5+boosts系統バグ記録
