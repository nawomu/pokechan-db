# 計画: マスター(master/)に足したものを全ページへ流す(2026-09-01)

阿部さん指示(2026-09-01): 「データベースは一つ。ここを元に追加する場所はあちこちある。どこに何を追加すべきか一回調べてから計画的に。ループとゴールとオーケストレーションと省エネで。」

## 0. 調べて分かったこと(全数・推測なし)
1. **マスター→ページの道が無い**。ページは旧生成物 `pokechan_data_all.js`(全国版6ページ)/`pokechan_data.js`(Champions版18ページ)/`items_database.js`(7ページ)を読む。`master/*.json` を読むのは `data_browser.html` だけ。
2. 旧生成物の生成器は**全部壊れている**(入力が消えた `reference/master_*.json`/`pokeapi_master.json` を指す)。しかも `build_master_v2.js` は旧生成物を**入力**にしている=向きが逆(旧→マスター)。
3. 旧生成物にしか無い資産=7項目(`reference/_plans/棚卸し_生成物にしか無い資産_2026-09-01.md`): ①TYPE_*静的表4つ ②`season`配列(M-A履歴245行) ③技`availability`(Showdown由来) ④`ABILITY_DESC`(家の流儀の短文=マスターのヤックン文言と別物・308/309が違う) ⑤`STAT_RANK`(計算可能) ⑥持ち物の構造化30項目 ⑦行の並び順(4パス構造)。
4. ZAにしか無い欠け(`reference/_plans/ZA欠け全数_2026-09-01.md`)=メガ11+見た目フォルム3+ストーン8・特性0 → **本日マスターへ補完済み**。
5. コンテンツ静的ページ(pokemon/ ability/ type/ ×9言語=約4,900枚)は `_gen_content_pages.js` が `pokechan_data.js`(Championsのみ)から生成。全国版の分は元々作られていない。
6. sitemap.xml に全国版ページ(pokemon_db_all_v9 等)が元々載っていない(別件)。

## 1. 方針(設計書§8.1/§8.3 第7段そのまま)
- **一方向にする**: `master/` → 生成器 → `pokechan_data_all.js` / `pokechan_data.js` / `items_database.js` → ページ。**ページの上っ面は触らない**。
- **別で作ってから入れ替える**: 生成器の出力は `*.new.js`。**全数差分ゲート**(既存の全エンティティが旧と同値・差分は「追加分」と「意図した変更一覧」だけ)を通るまで本番ファイルに触らない。
- **資産を先に運ぶ**(§8.3 第0段の教訓): 上の7項目をマスター(or `reference/` の凍結ファイル)に移してから生成器を書く。**旧文言・旧順序は保つ**(変えるかどうかは別途阿部さんの判断=声の領域)。

## 2. 段と客観ゲート(各段=1エージェント・ゲートはスクリプト)
| 段 | やること | ゲート(機械) |
|---|---|---|
| A ✅ | マスターを広げる(M-C 5体/新特性/ナイトZ3 + ZA欠け14体/ストーン8) | 番人✅・data_browser実機✅(済) |
| **B ✅** | 7資産をマスターへ運ぶ: types.json に静的表4つ/pokemon に `seasons`(旧season凍結=`reference/_legacy_seasons.json`)/moves に `availability`/abilities に `desc_house`(旧ABILITY_DESC)/items に構造化30項目/`reference/_legacy_order.json`(旧並び順) | 再ビルド後 `_ssot_guard_test.js` ✅ + 新テスト「legacy-only field = 0」 |
| **C ✅** | 生成器 `tools/build_views.js`(master→3ファイル・同スキーマ)+ 差分器 `tools/_views_diff.js` | **未説明差分 = 0**(許容差分=追加行 + null/""・no文字列・全角半角の正規化のみ。一覧を出力) |
| **D ✅** | 入れ替え: 旧を `bak/` に退避 → new を本番名に → 実機PDCA(Playwright: pokemon_db_all_v9/ability_all/waza-list_all/pokemon_db_v9/party_checker/real_battle=JSエラー0・件数・新行表示・画像) | 全ページ JSエラー0・件数一致 |
| **E ✅** | 逆流を断つ: `build_master_v2.js` の入力を旧生成物から凍結スナップショット `reference/_legacy_snapshot/` へ | 再ビルドで master 差分0 |
| F ✅ | コンテンツページ再生成(`_gen_content_pages.js`)+sitemap(全国版ページの掲載は別件で阿部さん判断) | 生成枚数一致・hreflang監査 |
| G | i18n 追随(`build_i18n_entities.js` の入力を master/ へ) | `i18n_audit_playwright.js` 残ja=0 |
| H | 9/9 レギュM-C切替(HANDOFF_2026_09_01 §1) | 番人✅ |

## 3. 🙋阿部さんの判断が要るもの(段Cまでは旧のまま=判断不要)
1. 特性ポップアップの文言: 旧の家の流儀(短文)のまま か ヤックン文言(マスター effect_ja)に替えるか(声の領域)
2. ページの行の並び: 旧の4パス順のまま か 図鑑順に揃えるか
3. コンテンツ静的ページ(4,900枚)を全国版まで広げるか(枚数が約4倍になる)
4. push のタイミング(段Dの入れ替え後・実機確認済みで)

## 4. 省エネの配分
Fable=設計/ゲート確認/声の判断の仕分けのみ。段B〜Gの実装=Sonnet(1段=1エージェント・ゲートスクリプトで自己判定→Fableが差分を目視)。1段≈15〜30万トークン見込み。

## 5. 実施記録(2026-09-01 同日)
- 段B `77c270e0` / 段C `fe8a4382` / 段E+D `889b888f`。**A〜E完了・F/G/Hが残り**。
- 段Cの許容差分(最終1,951件)の中身: (a)権威値の上書き(PP401/威力30/命中8等)+全国版learnersの差(旧の名前表記由来の693組・実欠け51件・マスターが多い分3,665) (b)隠れ特性スロット修正886 (c)正式名称化110 (d)持ち物是正105 (e)追加行136 (f)STAT_RANK母集団=各ファイル全体。旧と同じにしたもの=技グループ分け(subcategory)/持ち物効果文(effect_house)/Champions技の added・mode/added_in/メガ特性短文。
- 発見した既存バグ: `ability_all.html` が旧パス `reference/abilities_master.json` を取りに行き本番404(7/31以降)→パス1行修正+`.nojekyll`(段①の恒久対応は pokedb.js 化)。
- 残宿題: 全国限定ポケモンの learners 残差51件(体名は `reference/_views_diff_report.json`)/`_legacy_asset_test` を常設ゲートに追加するか/段F(コンテンツページ)・段G(i18n入力をmasterへ)・段H(9/9切替)。
- 段F `8c5d5c15`(同日夜): 阿部さん決定「全国版も広げて」→ 1273体/313特性/18タイプ×9言語=14,463枚(消失0)。`pokemon/all.html` が0行だった既存バグも修正。新17フォルムの8言語名=PokeAPI公式(en/fr)+公式部品の合成(de/es/it/ko/zh・各 `i18n/<lang>.json` の `_meta.added_2026_09_01` に記録)。
- ★阿部さん確定(同日): **レギュは累積**(M-Bで使えた物は全部M-Cでも可+新規26体)。builder規則化済み。Champions版DBのSSN列に🔜M-C。
- 残宿題(段G): i18n辞書は旧名キー(8/1の正式名称化前)のままの穴がある(例: ジガルデ(50%フォルム)/イッカネズミ(3びきかぞく)=8言語とも無し)→ `build_i18n_entities.js` の入力を master/ に向け直して全数再解決。非ja言語の孤児ページ(33体+6特性/言語)の掃除。
