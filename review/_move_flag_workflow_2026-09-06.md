# 技フラグ統合ワークフロー(2026-09-06)

承認: 阿部さん「それですすめていいよ、ループとゴールとオーケストレーション、ワークフローで省エネ」。順番=判断待ち整理→技フラグ統合→行動順設計清書→B064段階/工数/停止点。全体のバトル凍結は維持。

## 実行単位

- 親=統合・ゲート。独立担当=既存フラグの棚卸し、古い判断待ちの整理。担当別にreview文書だけを書き、同じ実装ファイルを取り合わない。
- 一回の収集は最大3技(ブラウザの時間制限に合わせ、保存後に次へ)、URLのmove番号で重複排除。既取得の表は再利用。最大40バッチ/走行、同一失敗は最大2回、その項目を保留して他の仕事を進める。
- 手順: 保存済み根拠の確認→不足分を取得→1技ずつ原文照合→別担当が反証→一致分のみreference入力→生成→全数差分→番人→実機。
- 8マスは単なるbooleanに潰さず、表の文字をまず原文のまま保存する。「通常/貫通/一部」等の条件を確認してから事実の表へ写す。未確認はfalseと区別する。
- キューが空になれば停止。無人CLI再起動・常駐cron・空の待機巡回は作らない。ゴール機能による継続とファイル状態から再開する。

## 保存場所・再開

- `review/_move_flag_inventory_2026-09-06.md`: 既存資料の再利用判断。
- `review/_pending_decisions_2026-09-06.md`: 完了済みと残判断の根拠。
- `review/_move_flag_audit_2026-09-06/sources/yakkun_ch.json`: 実ブラウザで読んだ8マス(引用・URL・時刻)。`tools/_move_flag_collect_server.py` のlocal formへバッチ送信し原子的に保存。
- 元の未コミット差分を `/tmp/astra-main-sync-20260906/` に保全してからmainへfast-forward済み。開始HEAD=`3b67cf31`。`reference/_views_diff_report.json` は着手前から差分あり。
- コーパスはmain側にあるgit管理外の資料を読み取り専用で再利用する。SSOTのコピーは作らない。

## 公式確認

2026-09-06: `node tools/_watch_official_news.js --roster M-C --dry` は「一覧リンクがまだ無い」。公式本文も「詳細は後日発表」と確認。レギュ入力は変更しない。
