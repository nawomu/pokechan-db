# Codex 設計トライアル・ブリーフ(2026-09-05・Fable作成)

目的: **Codexに「③バトル作り直し」の設計を一部任せてみて、Claude(Fable)が評価する**。単独で任せられるかの判定材料を作る(阿部さん 9/5 指示)。
役割: Codex=設計・所見 / Fable=監視・評価・反論 / 阿部さん=決定。**Codexはファイルを書き換えない**(sandbox read-only・出力は標準出力のみ→Fableが `review/` に保存する)。

## 0. 守るルール(プロジェクトの憲法・要約。原文は必ず開いて読む)
- `CLAUDE.md`(運用原則。特に「全体の順番 ①→②→③」「データの絶対ルール(SSOT=master/*.json)」)
- `設計_バトルエンジン原理_2026-07-26.md`(バトル設計の憲法。§11 追加プレイブック D=新ルール)
- `設計_フェーズ語彙v3_2026-07-26.md`(箱=フェーズの一覧・P/S番号)
- `設計_割り込みと分岐の規律_2026-07-26.md`(箱の間の飛び方)
- 原則: **困ったらフェーズを足す。まとめない**(分割は情報を失わないが統合は失う)。「なぜ」を消さない。
- **記憶で断言しない**。根拠は必ず「ファイル名:行番号」か権威ソース(ポケモンWiki日本 / 徹底攻略)。読んでいないものは「未確認」と書く。**でっち上げ禁止**。
- ③の実装は凍結中(①②完了まで)。**今回は設計だけ**。コードを書かない・直さない。
- 判断が要る所(好み・方針)は決めずに「🙋阿部さん判断」として列挙する。

## 1. 現在地(事実)
- ②(全ページを `pokedb.js` 1本に向け替え・旧版削除)は 2026-09-05 昼に完了(commit 16dff9fd)。
- ③のバックログ=`review/_battle_rework_backlog_2026-09-05.md`(168票)。§4=並び順案、§5=🙋、§6=Fable検証注(§5の 4/5/6/8 は決着済み・残る判断は 2/3/7)。
- 設計42の素材=`reference/_phase_design_a1_reorder.json` / `_a2_priority_event.json` / `_a3_skip_can.json` / `_a4_persistent.json`。
- 現エンジン=`real_battle_simulator.html`(9,682行)。`movePriority` L2278・`decideOrder` L2323・行動ループからの呼出 L7017(★バックログ本文の「L6883」は古い。行番号の乖離は自分で確かめること)。

## 2. 課題(今回のトライアル・2本)

### 課題A(設計): B149「action_reorder」+ B150「priority_event」の設計メモ
バックログ §1-F の B149/B150 について、憲法§11プレイブックDに従い、次を書く:
1. **何が問題か**(現コードのどこが・なぜ憲法の原理に反するか。行番号つき)
2. **足すフェーズ/イベント**(フェーズ語彙v3 のどの P/S 番号の間に入るか・名前・入力/出力・誰が購読するか)
3. **実機の根拠**(ターン途中で行動順が変わる実例=例: すばやさ変化後の再評価は第八世代以降どう扱われるか・先制の爪/せんせいのツメ等)。権威ソースの引用元を明記。**Championsでの扱いが分かる範囲で優先**
4. **既存実装との差分**(いたずらごころ/はやてのつばさ/トリックルームが今どこで処理されているか・新設後どこへ移るか)
5. **リスク・やらないこと・🙋阿部さん判断**
6. **`_phase_design_a1_reorder.json` / `_a2_priority_event.json` の先行設計への所見**(同意/不同意と理由。丸写しは不可)

### 課題B(話し合い): バックログ §4 の並び順と §5-2/3/7 への所見
- §4-2 の順序案(①§1-A 14件→②§1-F 骨格6件→③依存5件→④31件…)に**同意するか**。骨格(§1-F)を先にすべきという別解もあり得る(骨格を後にすると14件を2回直す)。理由つきで自分の案を出す。
- §5-2(B064 ダブル基本構造の着手時期)/§5-3(種別の粒度)/§5-7(おもかげやどし の切り分け)について、**事実で決まる部分**と**阿部さんの好みで決まる部分**を分けて所見を書く。

## 3. 出力形式
- 日本語。見出しは上の番号のまま。**根拠は「ファイル:行」を必ず添える**。
- 末尾に「## 自己申告」節: 読んだファイル一覧 / 読めなかった・時間切れのもの / 推測で書いた箇所(あれば正直に)。
- 長さ制限なし。ただし薄い一般論より「行番号つきの具体」を優先。
