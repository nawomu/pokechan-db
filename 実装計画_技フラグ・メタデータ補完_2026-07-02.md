# 実装計画: 技フラグ・メタデータ補完(監査4本の結果統合) — 2026-07-02

出典: 監査#1 `reference/_flags_audit_showdown.json` / #2 `_meaning_audit_report.json` / #3 `_ability_move_class_report.json` / #4 `_compose_consistency_report.json`。
判断ログ2026-07-02(#1〜#10)が阿部さん確定。原則=**effects/フラグ側を直してcompose再生成(個別手書き禁止・R9)**・修正後は必ず回帰(harness3本+diffレポート+意味監査再実行)。

## P1 接触フラグ110技補完(★最優先・sim直撃)
- Showdown `flags.contact` を正典に、全919技の contact を突合修正(Showdown基準278技)。うち166→278へ。
- 修正先=技の基礎データ(全国版ビルダーの源とmaster)。Champions curated 498技のcontactと矛盾が出たら**上書きせずリスト報告**(Champions実値優先の判断は私)。
- 検証: _sim_behavior_all.js+接触特性(せいでんき/さめはだ等)のスポット実走。

## P2 フラグ補完+キー統一(_move_flags.json)
- 補完: 踊り9(つるぎのまい/りゅうのまい/はなびらのまい/ちょうのまい/フェザーダンス/フラフラダンス/ほのおのまい/ソウルビート/アクアステップ) / 風9(ふぶき/こごえるかぜ/ねっぷう/ぼうふう/おいかぜ/すなあらし/ふきとばし/エアカッター/はなふぶき) / 弾7(くちばしキャノン/エレキボール/エナジーボール/かふんだんご/ロックブラスト/がんせきほう/みずあめボム) / 粉6(わたほうし/どくのこな/ふんじん/いかりのこな/ねむりごな/しびれごな)。
- キー統一: slash+slicing→1キー / bullet+ball→1キー(エンジン・タグ・compose全読み手を追従)。
- 過剰削除: スピードスター/フルールカノン/ダイマックスほう(弾でない=SD基準)。ドラゴンエール(音)=要裏取り(Bulbapedia等)→白なら削除。
- 維持(意図した差): かまいたち/ぎんいろのかぜ/あやしいかぜの風(SDはPast扱いで無いだけ・うちは全技収録)。

## P3 Max/Z メタデータ+タグ+compose(判断ログ#4,#5,#10)
- Showdown isMax/isZ/itemUser から: `is_max`(ダイ〜)/`is_gmax`(キョダイ〜)/`z:{user,base_move,crystal}` を技データに構造化。
- タグ新設: 「Zワザ」「ダイマックス」(キョダイ含む)。
- composeテンプレ: Max技=「攻撃技のダイマックス技。」見出し+「レイドでは相手の不思議なバリアのゲージを2つ減らす。」/ Z技=「『◯◯』の専用Zワザ。」(専用のみ)。→意味監査の抜け23+18+Z専用を一括解消。
- G-Max33技の新規収録=**阿部さん判断待ち(保留)**。

## P4 意味要素の系統穴(監査#2)
- 一撃必殺/固定ダメ: 「タイプ相性の影響を受けない」17技+「ゴーストタイプには無効」9技→effects注記+compose。
- 天候必中(ふぶき×ゆき/かみなり×あめ等)10技→effectsに条件必中(**simにも効く**)。
- こんらん等の継続「1〜4ターン」5技→duration(**simにも効く**)。
- 相手全体対象の明記6技/状態回復表記6技。
- 「100%の確率で」の統一ルール=「必ず」に寄せる(現エンジン既定・監査#4で全統一済みを確認)→legacy側が100%表記の47件は**同値として意味監査の辞書に登録**(直すのはチェッカー側)。

## P5 タグ新設: 対象系(判断ログ#9)
- 「相手全体」「場の全員(味方含む)」タグ(waza_picker.js・データ源=target列/effects)。風/踊り/粉/切る/弾もタグ化(フラグ→タグの導出線を1本に)。

## P6 フラグ文ひらがな統一(判断ログ#2)
- 「噛み技。」→「かみつき系の技。」等「〜系の技。」形に統一(compose)。

## P7 ちいさくなる型の全列挙経路統一(判断ログ#8)
- affected_moves全列挙+generation_notes括弧書き。端折り経路を全列挙に一本化。

## P8 技の「使える世代」availability(2026-07-02 阿部さん承認・順番お任せ→スプリント最後に)
- master_moves に `availability:{gen_introduced, gens:[...], champions_seasons}` を追加(源=Showdown moves.json の gen+isNonstandard:"Past"・全数機械導出・手作業ゼロ)。
- わざリスト(全国版)に「世代」列=「第3世代〜」形式チップ(削除技のみ「第1〜7世代」型)。
- 習得の世代別(ポケモン×技×世代×方法)は `reference/_showdown/learnsets.json`(取込済・コード形式)を源に**将来の世代セレクタ**で使う(今回はUI作らない=データ確保済のまま)。仕様の世代差=generation_notes注記(既存方式)。
- 設計詳細=`設計_バトル再現ファースト_逆算_2026-07-02.md`の世代節。

## 回帰ゲート(各P完了ごと)
1. `node tools/build_master.js`→`build_champions_view.js`(unexpected=0) 2. harness3本(PCHAM_DATA=new含む) 3. `node tools/_meaning_audit.js`(抜け件数が減ることを確認) 4. `node tools/_compose_consistency_audit.js`(0維持) 5. 確認ビュー再生成。
全P完了→confirm再生成→**阿部さん耳確認(1回)**→T5(本番差替)。

## 保留(阿部さん判断)
- 「ダメージ。」接頭辞の要不要 / G-Max33技収録 / キック技フラグ予防付与 / 「追加効果」の正式定義(ちからずく用・当面prob<100で代用)。
