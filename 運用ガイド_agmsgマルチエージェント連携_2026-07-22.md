# 運用ガイド: agmsgマルチエージェント連携(GLM・他エージェント共通)

作成: 2026-07-22 JST / 対象: agmsgで別エージェント(GLM実装役・Gemini等)を併用する全作業
관련メモリ: [[glm-agmsg-loop-setup]] / [[glm-worktree-file-edit-only]] / [[no-unattended-auto-restart]] / [[fable-token-economy]] / [[loop-visibility-feedback]] / [[explicit-ask-in-reports]]

このガイドは「**別ターミナルで動く別エージェントを、agmsgで実装役として回す**」ときの**正しい使い方と、踏んだ地雷**をまとめたもの。GLMでも、将来別のエージェント(Gemini/Kimi/Sonnet別セッション等)でも同じ原則。

---

## 0. 体制(役割分担)

| 役割 | 誰 | 何をする |
|------|-----|---------|
| 設計・検証・声判定・commit | **claude-design**(本物Claude・設計役) | 仕様書を書く / 独立にハーネス検証 / 阿部さん報告 / commit・push |
| 実装(量産・機械作業) | **glm-impl**(GLM等) | 仕様を読んで自律実装 + 自己検証 → 完了報告 |
| 声/tasteの最終OK | 阿部さん | 口調・文言・方針分岐の最終判定 |

チーム名 = `pchamdb`。メンバー = claude-design / glm-impl / gemini-impl。

---

## 1. 起動(GLMの場合・設定は全部保存済み)

別ターミナルで:
```
cd "/Users/masamichi/Documents/ポケモンDB"
glm
```
- `glm` = Claude Code CLIの頭脳だけGLM-5.2に載せ換えた別エージェント(`~/.zshrc`のglm関数・キー=`~/.glm_key`)。**Anthropic週間枠を消費しない別API**なのでFable/Opus温存に効く。
- 設計役(本物Claude)は別ターミナルで `claude`。
- 詳細な起動メモ = `~/glm_起動メモ.txt`。

---

## 2. ★★★ 絶対にやってはいけないこと(2026-07-22の実際の事故)

### ❌ 他エージェントの受信箱を `inbox.sh` で覗く
```
# ダメ: claude-design が glm-impl の受信箱を覗く
~/.agents/skills/agmsg/scripts/inbox.sh pchamdb glm-impl   # ← 横取り!
```
**`inbox.sh <team> <他人>` は、覗いた瞬間その人宛メッセージを既読化して消費する。** 送った相手が自分でinboxを見ても「新着なし」になり、**タスクが永遠に届かない**。

**2026-07-22に実際にこれで事故った**: claude-designが「GLMが拾ったか?」を確認したくてglm-implのinboxを覗いた → 送ったT2タスクと検証OK通知が消費された → GLMは何も受け取れず「検証待ち」で停止 → 原因究明にスクショ数往復を浪費。

### ✅ 正しい進捗確認(非侵襲・相手のinboxを触らない)
1. **相手の返信** = 自分(claude-design)のmonitorが自動で届ける(下記§4)。これが第一情報源。
2. **ファイル編集の形跡** = `find <対象ファイル> -newermt '10 minutes ago'`(覗いても壊れない)。
3. **どうしても対話が要る** = 相手に**直接メッセージを送って聞く**(`send.sh ... "状況は?"`)。相手のinboxは**絶対に覗かない**。
4. 履歴だけ見たい = `history.sh`(読み取り専用・消費しない)は可。ただし自分視点の履歴。

> 覚え方: **「受信箱は本人のもの。他人の郵便受けを開けたら郵便は消える」**

---

## 3. monitorモードの挙動(毎回nudgeは不要)

- このプロジェクトは **delivery mode = monitor**(`.claude/settings.local.json`のSessionStartフックでwatcher自動起動)。
- **初回セッション立ち上げ後は、送ったメッセージが相手にリアルタイム自動配信される。毎タスクごとの「inbox確認して」は本来不要。**
- **例外(一押しが要る場面)**: 相手が **確認質問を出して一時停止**しているとき(例: GLMが初回に「glm-implとして進めてよいか?」と聞いて止まった)。このときだけ相手ウィンドウで一押し or 質問に答える。
- 「毎回nudgeが要る」と誤認したことがある(2026-07-22)が、それは上記§2の横取り事故で配信が壊れていたのが真因。**平常時は自動でループする。**

---

## 4. タスクの流し方(テンプレ・毎回これ)

1. **仕様書を書く** = `scratchpad_<agent>_task_<名前>.md`(データ場所・要件・手順・検証コマンド・制約・完了報告先を明記)。生の要件をチャットに散らさず1ファイルに。
2. **agmsgで送る**(要約 + 仕様書ファイル名):
   ```
   ~/.agents/skills/agmsg/scripts/send.sh pchamdb claude-design glm-impl "【タスク名】仕様書を読んでから着手: scratchpad_xxx.md。要約=... ★本番commit/pushしない。完了したら数値と触った行を claude-design に返信。"
   ```
3. **相手が自律実装 + 自己検証** → claude-design に完了報告(monitorが自動で届ける)。
4. **claude-designが独立検証**(相手の自己申告を鵜呑みにせず、自分でハーネスを回して裏取り = [[full-check-beats-sampling]]・Opus 4.8のfalse-green退行対策)。
5. **差分を目視** + 制約遵守を確認。
6. **commit/push**: 声/SSOTに関わらない機械的修正 → 阿部さん承認で即commit。**声・全国版SSOT編集・文言** → 緑でも自動commdiscommitせず**阿部さんに報告してからcommit**(SSOT編集=阿部さん報告とセット)。

---

## 5. 制約(仕様書に必ず書く定型)

- **本番push/commit/git addしない**(検証はclaude-designの役割・commitも設計役が承認後に)。→ 途中コードの巻き込みpush防止 [[agent-file-commit-lockout]]。
- **触ってよいファイルを明示**(2ファイルだけ、等)。エンジン挙動ロジックは触るな等。
- **手書きで生成物を直編集しない**(ソースを直して再ビルド = effects→compose一方通行の思想)。
- **声/tasteの判断はさせない**(公式準拠データはそのまま・独自文言は設計役/阿部さん領域)。迷う所は勝手に埋めず報告。

---

## 6. 実装環境の注意(GLM)

- 今回のagmsg版GLM(`glm`で起動)は**メインリポジトリで動く** = node/python実行OK・**自己検証まで自分でできる**。
- [[glm-worktree-file-edit-only]]の「ファイル編集のみ・検証は親担当」は**隔離worktree版GLM(glm-e1等)専用**の制約。**agmsg版には当てはまらない**。混同しない。

---

## 7. 停滞・枠切れ対応(安全弁・[[no-unattended-auto-restart]])

- GLMは5時間枠ですぐ引っかかる。**停滞検知の心拍**(ScheduleWakeup 7-10分)で「返信来た? ファイル触った?」を確認。
- **無反応が続いても、勝手にSonnet起動や無限リトライをしない**(2026-07-12に無人自動再起動で週間枠94%消費の事故)。
- 停滞したら **阿部さんにping**(「GLM止まってるかも/nudgeして」or「Sonnetで巻き取る?」)して判断を仰ぐ。
- バックログが尽きる/taste・阿部さん判断が要る所に来たら**ループを止めて質問リストをping**([[explicit-ask-in-reports]]・[[loop-visibility-feedback]])。
- 待機中はトークン消費ゼロが原則([[fable-token-economy]])。monitorのイベント駆動を活かし、無駄なポーリングを増やさない。

---

## 8. ターミナル状態の自己確認について

- 相手ターミナルの様子は、**agmsgで直接聞けば分かる**(スクショ不要)。相手のinboxは覗かない(§2)。
- どうしても画面を見たい時は macOS `screencapture`(要・画面録画権限/全画面が写る)も一応可能だが、大げさ。正常運用ではagmsg経路で足りる。

---

## 9. 今回の教訓(1行で)

> **他人の受信箱を覗くな。進捗は「相手の返信(monitor自動)」と「ファイル編集の形跡」で見る。monitorモードなら毎回nudgeは要らない。検証は必ず自分でハーネスを回して裏取り。**
