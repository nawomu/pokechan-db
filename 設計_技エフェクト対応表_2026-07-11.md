# 設計: 技エフェクト対応表(タイプ×名前→絵の形状) 2026-07-11

作成: 演出設計担当(調査・設計フェーズ。**本番ファイル変更・git操作なし**)
背景(阿部さん 2026-07-11): 現行の攻撃演出はクラス判定(punch/slash/bullet等10種)+タイプ色のみで、技の"らしさ"(くさなら葉っぱ、ひこうなら羽、みずなら水しぶき、かくとうはパンチかキック等)が絵に出ていない。技のタイプと名前から「合う絵」を割り当てた対応表を作り、雰囲気を上げたい。**本書は調査・設計のみ。実装は次段。**

対象読者: 次にこの表を real_battle.html / online_battle.html へ実装するサブエージェント・Claude。

---

## 0. 現行実装の把握(調査結果)

### 0-1. 現状のクラス判定(real_battle.html 3139行 `moveClassOf`)

```js
function moveClassOf(mv){
  const f = (mv && mv.flags) || {};
  if (f.sound) return 'sound';
  if (f.punch) return 'punch';
  if (f.slicing || f.slash) return 'slash';
  if (f.bullet || f.ball) return 'bullet';
  if (f.wind) return 'wind';
  if (f.pulse) return 'wave';
  if (f.bite) return 'bite';
  if (mv && /キック|蹴/.test(mv.name || '')) return 'kick';
  return (mv && mv.category === '特殊') ? 'spec' : 'phys';
}
```

- 優先順: sound > punch > slash > bullet > wind > wave(pulse) > bite > kick(技名正規表現) > 物理汎用(phys) / 特殊汎用(spec)。
- クラスに応じて `_RB_CLS_ICON`(🎵👊⚔️🌀🦷🦵)・`_RB_CLS_DOT`(bullet/physは弾ドット)・`_RB_BEAM_CLS`(wave/specはビーム)が決まり、`spawnProjectile`/`spawnBeam` で飛翔、着弾で `burstFx`/`spawnBurstGlyph`(slash=斬線2本、punch/kick/bite=星✦)。
- 色は `TYPE_COLORS[mv.type]` のみ(タイプ色の玉/ビーム/斬線)。**タイプごとの固有グリフは無い**(例: くさ物理もあく物理も同じ「phys」でクラス上区別されない。星✦マークもpunch/kick/biteで共通)。
- Wave2.5で `mv.contact === true` の技は `chargeFx`(本体クローン突進)に分岐。**接触/非接触の動き(motion)はデータ駆動で既に確定済み**。本設計はこの上に「見た目のグリフ(shape)」を足す。

### 0-2. 設計_バトル演出強化_2026-07-10.md(v2仕様)の関連箇所

- 0章の不変制約: エンジン非改変・ログ文面不変・CSS(transform/opacity/filter)+WebAudio自前合成+インラインSVGのみ・追加テキストなし(i18n影響ゼロ)・座標系(動線系=fxPoint()でbody直下fixed/その場系=#f-側append)。
- ⑤共通実装基盤: 「タイプ色玉テンプレ」`div+radial-gradient`(ほのお/みず/どく/ゴースト等の配色例あり)が既に構想されている → 本設計の `orb`/`flame`/`water` 等の shape 実装はこの延長。
- ⑤「プリミティブヘルパー1個に集約」`showEffect({kind, from, to, color, dur, easing, end})` 構想があり、**本設計の shape は showEffect の `kind` 引数の拡張(kind=shape名)として自然に載る**。

→ **本設計はこれらを置き換えるのでなく、`moveClassOf` の後段に `shapeOf(mv)` を足す形で積む**(③実装方針で詳述)。

---

## 1. 形状ボキャブラリ(shapes・全27種)

機械可読定義は `reference/move_fx_map_draft_v1.json` の `shapes` キー。実装方針(emoji/css/svg)と既定モーション(projectile/beam/burst/rain/melee)を持つ。

| shape | 絵 | 実装方針 | 既定motion | 主な用途 |
|---|---|---|---|---|
| fist | 👊 | emoji | melee | パンチ・体当たり全般(既定) |
| foot | 🦵 | emoji | melee | キック系 |
| fang | 🦷 | emoji | melee | かみつき・毒の一撃・トラバサミ |
| blade | 斬線(既存.rb-burstglyph-slash) | css | melee | 斬撃・クロー・矢 |
| note | 🎵 | emoji | beam | 音技 |
| heart | 💗 | emoji | melee | フェアリーの触れ合い |
| star | ⭐ | emoji+css | projectile | きらめき・隕石・手裏剣・コイン |
| skull | 💀 | emoji+css | burst | あく特殊の禍々しい波動 |
| gear | ⚙️ | emoji+css | melee | はがねの機械質感 |
| orb | css(radial-gradient球) | css | projectile | エネルギー球(bullet/ball系の発展形) |
| psi | css(渦・conic-gradient) | css | beam | エスパー・波動・渦潮 |
| ice | ❄️/🧊 | emoji+css | projectile | 氷結晶 |
| sand | css(茶〜黄土色の粒子) | css | rain | 砂・土けむり・ガス |
| web | 🕸️ | emoji+css | projectile | 糸・網・巻きつき |
| leaf | 🍃 | emoji+css | projectile | くさタイプ既定 |
| seed | 🌰 | emoji+css | rain | タネ・木の実・りんご |
| feather | 🪶 | emoji+css | melee | ひこう物理 |
| gust | css(既存windクラス流用) | css | projectile | ひこう特殊・風技 |
| water | 💧 | emoji+css | projectile | 水しぶき |
| flame | 🔥 | emoji+css | projectile | 炎 |
| spark | ⚡ | emoji+svg | beam | 電撃(ジグザグ) |
| rock | 🪨 | emoji+css | projectile | 岩石・鈍器投擲物 |
| dust | css(爆煙+地面クラックsvg線) | css | burst | じめん物理・爆発全般 |
| dragon | css(青紫らせんconic-gradient) | css | beam | 竜のエネルギー |
| sting | emoji(針アイコン想定) | emoji | melee | むし物理(かみつきより鋭い) |
| drill | css(高速回転スパイラル) | css | melee | ドリル技 |
| beam | css(既存.rb-beam発展・極太化) | css | beam | 極太ビーム・レーザー・大砲 |

motion は「その shape が採用されたときの着弾グリフの雰囲気」の既定値であり、**実際に飛翔体か突進かは既存の `mv.contact` 判定がそのまま握る**(③参照)。

---

## 2. タイプ×分類の既定マッピング表(18タイプ×2=36)

機械可読定義は `move_fx_map_draft_v1.json` の `defaults`。

| タイプ | 物理 | 特殊 |
|---|---|---|
| ノーマル | fist | star |
| ほのお | flame | flame |
| みず | water | water |
| でんき | spark | spark |
| くさ | leaf | leaf |
| こおり | ice | ice |
| かくとう | fist | psi |
| どく | fang | orb |
| じめん | dust | sand |
| ひこう | feather | gust |
| エスパー | psi | psi |
| むし | sting | web |
| いわ | rock | rock |
| ゴースト | orb | orb |
| ドラゴン | dragon | dragon |
| あく | fang | skull |
| はがね | gear | gear |
| フェアリー | heart | star |

選定理由の例: ノーマル特殊=star(すごい技のような光の乱舞を既定に。実際は多くがsound flagでnoteに化ける)/ どく物理=fang(毒の牙・爪牙の一撃)/ どく特殊=orb(丸めた毒の塊)/ じめん物理=dust(地割れ+土煙)・特殊=sand(舞う砂塵)/ あく特殊=skull(禍々しい波動。ただしpulse flag技はpsiに化ける=下記flag層参照)。

---

## 3. flagベース中間層(既存moveClassOfの優先順を踏襲)

技の**タイプ**より先に、**flags/技名の正規表現**が形状を決める場面がある(既存 `moveClassOf` が既にこの優先順で「クラス」を決めているのと同じ理由=パンチ系はタイプに関係なく拳の絵が正しい)。**優先順は既存エンジンと完全一致**させ、二重管理・矛盾を避ける。

| 優先順 | 条件 | shape |
|---|---|---|
| 1 | `flags.sound` | note |
| 2 | `flags.punch` | fist |
| 3 | `flags.slicing` \| `flags.slash` | blade |
| 4 | `flags.bullet` \| `flags.ball` | orb |
| 5 | `flags.wind` | gust |
| 6 | `flags.pulse` | psi |
| 7 | `flags.bite` | fang |
| 8 | 技名が `/キック|蹴/` に一致 | foot |
| — | どれにも一致しない | ②のタイプ×分類デフォルトへフォールバック |

この層だけで 326技中 **79技**が個別のタイプ既定より正確な形状に自動的に解決される(例: あくのはどう/りゅうのはどう/みずのはどう→pulse→psi。ハイパーボイス/さわぐ/ばくおんぱ等13技→sound→note。シャドーボール/エナジーボール等→bullet/ball→orb)。

---

## 4. 個別上書き(overrides・61技)

機械可読定義は `move_fx_map_draft_v1.json` の `overrides`(技名の完全一致キー)。**全技を手作業で個別化しない**(北極星=既定で受ける設計。個別化は「flag層でも拾えない・タイプ既定でも拾えない、名前の直感が強い技」だけに絞る)。61技は3パターンに分類できる:

### (a) flag層の取りこぼし(データにフラグが無いが名前が直感を持つ)
例: `ふいうち`(不意打ち=拳打ちなのに `flags.punch` が立っていない)→fist / `いかりのまえば`(「まえば」なのに `flags.bite` が無い)→fang / `きゅうけつ`(吸血=噛んで吸うが bite flag無し・drain flagのみ)→fang / `ふくろだたき`(Beat Up=複数体で殴る)→fist / `けたぐり`・`とびひざげり`・`かかとおとし`・`じだんだ`(蹴り技だが技名に「キック」「蹴」の文字が無く③の正規表現で拾えない)→foot。

### (b) flag層の視覚ミスマッチ(フラグはあるが実際の絵と合わない)
例: `くちばしキャノン`(bullet flag→orbになるが、実態は「くちばしで突く」動作であって丸玉ではない)→feather / `どくばりセンボン`(`ball:false` 明示=bulletとして扱われないが、実際は細い針の乱舞)→web。

### (c) タイプ既定では拾えない固有の絵(名前の固有性が強い)
例: `つのドリル`/`ドリルライナー`/`ドリルくちばし`(回転する錐)→drill / `タネばくだん`/`タネマシンガン`/`かふんだんご`/`りんごさん`(丸玉でなく粒々の種・花粉・果実)→seed / `はかいこうせん`/`かえんほうしゃ`/`ハイドロポンプ`/`だいもんじ`等18技(名前が「ビーム/砲/放射」=一直線の光条であって粒状の既定と違う)→beam / `シャドークロー`/`ドラゴンクロー`/`ブレイククロー`/`フェイタルクロー`(公式データ上slicingフラグは無いが「クロー」を冠する=爪の斬撃イメージが強い)→blade / `りゅうせいぐん`/`メテオビーム`/`みずしゅりけん`/`コインビーム`(隕石・手裏剣・コインという星のきらめき)→star / `だいばくはつ`/`じばく`(自爆技。ノーマル既定fistでは迫力不足=地割れ+土煙のdustを明示指定)→dust / `トラバサミ`(鋼の罠が「噛む」)→fang / `なげつける`/`ボーンラッシュ`/`デカハンマー`(持ち物・骨・大槌=投げつける鈍器)→rock / `まきつく`/`エレキネット`(巻きつき・電気の網)→web / `うずしお`/`しんくうは`(渦潮・真空波=渦巻き)→psi 等。

**全61件の内訳・理由コメントはビルドスクリプト `reference/_build_move_fx_map.js` 内にコード上のコメントとして残してある**(再生成可能・レビュー時に理由を追える)。

### 個別に吟味した技の総数
overrides(61) + flagOrder経由(79) = **140技**が「タイプ既定を上書きする個別判定」を経て形状決定している(残り186技はタイプ×分類の既定36通りで十分にカバー=既定で受ける設計が機能している)。「代表100〜150技程度に絞る」の目安に対して、flag層(既存エンジン流用の自動判定)+overrides(手作業の個別判定)を合わせて140技という妥当な規模に収まった。

---

## 5. カバレッジ機械照合(結果=100%)

照合スクリプト: **`reference/_build_move_fx_map.js`**(node実行で `move_fx_map_draft_v1.json` を再生成しつつ照合)。
入力: `pokechan_data.js` の `WAZA_MAP`(全国版SSOT)から `category` が「物理」または「特殊」の技を技名で重複排除して抽出=**326技**(変化技172技は対象外)。

実行結果(`reference/_move_fx_map_coverage_report.json` に保存):

```
attacking moves total: 326
resolved via: { override: 61, flag: 79, default: 186, none: 0 }
missing (unresolved): 0
defaults table integrity errors: 0
overrides integrity errors: 0
COVERAGE: 100% OK
```

- `missing=0`: 326技全てが overrides→flagOrder→defaults のいずれかで形状に解決される(未解決ゼロ)。
- `defaults table integrity errors=0` / `overrides integrity errors=0`: 各テーブルが参照する shape 名が①のカタログに実在すること、overrides のキーが実在する技名であることも機械検査済み。
- 全326技の解決結果一覧(技名/タイプ/分類/形状/経由)は **`reference/_move_fx_map_resolved_all.json`** に出力済み(実装後のレビュー・waza_list_confirm的な目視チェックに使える)。

形状別の出現数(上位): fist 47 / orb 27 / fang 24 / blade 24 / beam 18 / psi 17 / water 14 / note 13 / flame 13 / rock 12 / leaf 12 / spark 12 / star 10 / ice 10 / gear 9 / sting 9 / foot 8 / feather 8 / dust 7 / gust 6 / sand 5 / web 5 / dragon 5 / seed 4 / drill 3 / heart 3 / skull 1(`ナイトバースト`のみ。あく特殊の他技はpulse/sound flagで psi/note に化けるため)。

---

## 6. 実装方針(次段用メモ)

### 6-1. 差し込み位置
`real_battle.html` 3139行 `moveClassOf(mv)` の**後段**に `shapeOf(mv)` を新設する(既存関数は変えない=motionの判定はそのまま維持)。

```js
// 疑似コード
function shapeOf(mv){
  if (!mv) return null;
  if (MOVE_FX_MAP.overrides[mv.name]) return MOVE_FX_MAP.overrides[mv.name];
  for (const rule of MOVE_FX_MAP.flagOrderCompiled) if (rule.test(mv)) return rule.shape;
  const d = MOVE_FX_MAP.defaults[mv.type];
  return d ? d[mv.category] : null;
}
```

`MOVE_FX_MAP` は `move_fx_map_draft_v1.json` をそのまま(または生成物として)読み込む想定。`flagOrder` の `test` は文字列化していないので、実装時は本設計の③の表を直接JSで書き下ろす(JSONは人間可読のドキュメント兼データ、実装コードは既存 `moveClassOf` の分岐にならって直書きする方が保守しやすい=既存コードとの一貫性を優先)。

### 6-2. 接触技(chargeFx)との関係
- **motion(飛翔体 or 突進)は変えない**。既存の `mv.contact === true` 判定(`attackFx` 3209行)がそのまま握る。
- 接触技(`chargeFx`)は「本体クローンが突進→着弾グリフ」の流れなので、**shape は着弾グリフ(`spawnBurstGlyph`相当)の絵柄として使う**。現行は slash/punch/kick/bite の4種類だけだが、これを shape 27種に拡張する形(例: `くさ`の接触技なら着弾で 🍃 が散る、`はがね`の接触技なら ⚙️ が散る)。
- 非接触技(`attackFx` の従来分岐)は「飛翔体 or ビーム」なので、**shape が飛翔体アイコン/パーティクルの絵柄になる**(現行 `_RB_CLS_ICON` の6種だけを27種の shape 辞書に置き換える形)。
- **motion(shapes カタログの `motion` フィールド)は「shapeが単独で新規描画されるとき」の参考値**であり、real_battle 側の実際の飛翔/突進/ビームの選択は従来通り `moveClassOf` の bullet/wave 判定 + `mv.contact` が優先する。矛盾するケース(例: shape=beam だが moveClassOf 上は bullet 判定でドット扱い)は実装時に**shapeのmotionをbeam系("beam"/"psi"/"dragon"/"gust"/"note"/"spark")なら `_RB_BEAM_CLS` 相当に、それ以外は `_RB_CLS_DOT` 相当に**、という単純な追記ルールで解決できる見込み(要実機確認)。

### 6-3. 実装バッチ分割案
1. **バッチ1(土台)**: `MOVE_FX_MAP` の読み込み+`shapeOf()` 関数+27 shapes の描画プリミティブ(css/emoji)を実装。既存の6アイコン(🎵👊⚔️🌀🦷🦵)をそのまま shape=note/fist/blade/gust/fang/foot にマッピングして**現状と見た目が変わらないことをまず確認**(回帰防止の土台)。
2. **バッチ2(タイプ既定の可視化)**: defaults 36通りの新規shape(flame/water/spark/leaf/ice/psi/orb/dust/sand/feather/rock/dragon/skull/gear/heart/star/sting/web)を実際に描画。①のカタログ通りcss/emoji実装。タイプ色は既存 `TYPE_COLORS` をそのまま使う(色のロジックは変えない)。
3. **バッチ3(flag層+overrides)**: 61個別技+flag層8ルールを接続。ここで drill/seed/beam の3新規shapeを実装。
4. **バッチ4(磨き)**: 阿部さんのFB(下記確認論点)を反映。

各バッチ後に `python3 -m http.server 8000` + Playwright実機で JSエラー0・対象技の発火スクショ・非対象技(既存演出のまま)を確認(PDCA)。

---

## 7. 阿部さん確認論点(絵の趣味が分かれそうな例・5つ)

1. **はがねの物理技は全部「歯車⚙️」でいいか**: `ヘビーボンバー`/`ハードプレス`(体当たり系の重量級ボディプレス)まで歯車の機械質感にすると違和感が出るかもしれない。代替案=鋼タイプ物理は「金属光沢の衝撃(銀色のクラック)」寄りにして、歯車は明確な「機械もの」(`アイアンローラー`のようなホイール技)だけに絞る、など。
2. **自爆技(`だいばくはつ`/`じばく`)の絵**: 今回は「dust(地割れ+土煙)」を割り当てたが、自爆技は他の技と一線を画す派手さ(閃光+爆風)が欲しいかもしれない。専用の "explosion" shape を別枠で新設するかは阿部さんの好み次第(現状はスコープを絞ってdust流用に留めた)。
3. **`なげつける`/`ボーンラッシュ`/`デカハンマー`を "rock"(岩石グリフ)で代用している**点: 骨や道具や大槌は厳密には岩ではない。専用の「投擲物(鈍器)」shapeを別途起こすか、rock流用のままで十分か。
4. **ゴールドラッシュ/コインビームのような「お金」モチーフ**: 今回コインビームは star(きらめき)に寄せたが、ゴールドラッシュは default の gear のまま。金貨らしい専用グリフ(💰的な)を作るかどうかは絵柄の趣味の領域。
5. **フェアリーの物理/特殊を heart/star で塗り分けている**点: `じゃれつく`(物理=heart)は良いが、`ソウルクラッシュ`のような打撃寄りのフェアリー物理技まで heart(ハート)にするとコミカルになりすぎるかもしれない。フェアリー物理を heart 一色にするか、technicalな打撃技は star/blade 寄りに分けるかは相談ポイント。

---

## 8. 成果物一覧

- 本書: `設計_技エフェクト対応表_2026-07-11.md`
- 機械可読ドラフト: `reference/move_fx_map_draft_v1.json`(defaults/overrides/shapes/flagOrder)
- 照合スクリプト(再実行可能): `reference/_build_move_fx_map.js`
- 照合結果: `reference/_move_fx_map_coverage_report.json`
- 全326技の解決結果一覧(レビュー用): `reference/_move_fx_map_resolved_all.json`
