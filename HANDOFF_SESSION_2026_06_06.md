# セッション引き継ぎ — 2026-06-06(ヤックン耳 / 文章化エンジン裏ビルド)

**作成**: 2026-06-06 JST
**ステータス**: 🟡 主要成果はコミット済・本番無変更(legacy維持)。だが**上位ルール(忠実 vs やさしさ)に未解決の矛盾**が発生し、**テンプレ作り直し・②③コミット・開通順は全部、阿部さんの判断待ちで停止中**。→ **まず §8 を読む(今の最前線・何が何待ちか)。**
**次セッションが最初に読む**: `CLAUDE.md`(自動注入)→ **`ヤックン耳_判断ログ.md`(必読・正の台帳)** → 本書(**特に §8**)。

---

## 0. このセッションの一番大事な発見(これが全部の土台)

**Claude は必ず漂流する。根本は「作る快感 > 確かめる規律」**(Goodhart)。英語消し・正規化・全490生成…どれも"測れる手応え"があるから、機械を完成させる方へ流れ、手段(grepゼロ・カバー率)を目的(子どもがヤックンに戻れるか)と取り違える。**作る最中のセッションは自分の漂流を感じない。**

→ 対策は「記憶」でなく「**構造**」:
- **CLAUDE.md(憲法・毎session注入)** に 北極星/ヤックン耳テスト/罠/境界線 を設置。
- **`ヤックン耳_判断ログ.md`(正の台帳)** に 判定済み実例(✓本人)・gold(legacy)・境界線 を蓄積。
- 詳細は memory [[verification-over-production]] / [[yakkun-ear-ledger]]。

**唯一の合否=ヤックン耳テスト**: 何か作ったら、作る姿勢を捨て読者の姿勢で「うちの出力だけ」読み、`description_legacy`(=ヤックン)と並べて ①意味が戻るか ②声が同じか ③機械が漏れてないか。grepゼロ・カバー率・"全部日本語"は**偽の頂上**。

**札(provenance)の不変条件**: `✓本人`=人間の耳だけが確定できる / `◇機構`=Claudeがデータ/legacyに錨を打てた事実のみ(耳ではない) / `★要`=耳待ち。**Claudeは★→✓に上げられない。別Claudeの同意も不可。** verifier も legacy か 人間 にしか錨を打つな。

---

## 1. 今日やったこと(時系列・全てコミット済)

| commit | 内容 |
|---|---|
| `c2484ac` | Wave1 英語prose全廃 + いちゃもんkind衝突修正 + とおせんぼう/フェアリーロック昇格 |
| `1126d5f` | 文章化エンジン試作 + 能力ランク変化テンプレ + 監査漏れ言葉化 |
| `d085f59` | **憲法をCLAUDE.mdへ + 判断ログ(ヤックン耳_判断ログ.md)確立** |
| `1bab4db` | データ衛生: duration正規化 + effect.target英語化(B方針) |
| `cd9e8ce` | **ヤックン耳 意味層 5/5 ✓本人確定** |
| `904ba9a` | compose(監査器)が✓耳反映 + 表示戦略決定を記録 |
| `17c372f` | compose 連続攻撃テンプレ(カバー191→216技) |

### 1-1. データ衛生(`pokechan_data.js`・適用済)
- **duration正規化**: 範囲文字列(`"1-4 turns"`等)→`[min,max]` / 継続系英語enum→和文(`until_user_leaves`→`自分が場を離れるまで`)。simは読まない裏取り済。backup `.durnorm.bak`。
- **effect.target英語化(B方針)**: 日本語scope4値→英語(`相手全体=all_opponents`/`自分以外全体=all_but_self`/`手持ち全員=party`/`交代先=incoming`)。view4辞書(proto_done/cond_proto/effects_ja/compose)をペア修正。backup `.tgtnorm.bak`。
- **★方針=B確定**: データは英語enum(プログラム/i18nピボット)。日本語で読みたい所は view と新版で。**日本語の別コピー(2nd SSOT)は作らない**。

### 1-2. ヤックン耳 意味層 5/5 ✓(`ヤックン耳_判断ログ.md`)
人間(阿部さん)の耳で確定した5技の声(機構はデータ/Bulbapediaで◇裏取り済):
- **いばる**: 損な効果は「〜してしまう」で書く(相手attack+2=自分の損)
- **はらだいこ**: 「HPが先に半分減る→その代わりに攻撃max。今のHPが半分以下だと失敗(`fails_if`)」
- **ミストバースト**: 条件は最後まで・接地の「など」を残す
- **のろい**: 条件を先に・分岐束ね。HP半分払い、**足りなければ瀕死(失敗しない)・控えなしでも成功し敗北**(Bulbapedia+pokemonwiki+本人で決着)。`fails_if`無しが正しい
- **いのちがけ**: 残りHP分→ひんし・**ゴースト無効(immuneデータ済)**・相性無視・「固定」やめ

### 1-3. 文章化エンジン compose.js(=監査器)
`effects(SSOT) → ヤックン音の一文`を生成 → `description_legacy`と並べて検証する**監査器**(`tools/_waza_compose.js` → `review/waza_compose.html`)。
- condition-グループ化(のろいの分岐束ねが効く)・声ルール(半分/全部/1分数スラッシュ/漢字)・機械漏れ検知(全490回帰ガード)。
- 対応kind: 状態付与/拘束/反動/威力倍率/自分瀕死/回復/HPが減る/固定ダメージ/継続削り/能力ランク変化/**連続攻撃**。
- **フル生成可 216/490技**。

---

## 1-4. ★独自性の方針(2026-06-06 阿部さん指示・重大)

**技の説明文は「独自に」作る。ヤックン(legacy)の文言をコピーしない。** プロジェクトの出発点が「既存サイトの丸写しでなく独自に作る」だったのに、急所・確率補完で **legacyとverbatim一致**を"成功(✅)"扱いしていた=逆。**verbatim一致は盗用リスク＝赤信号**。
- **legacy=意味の参照のみ。文言の正解ではない。** ヤックン耳テストの「②声」は"文言を合わせる"でなく"やさしい子ども口調か"。「legacyとそっくり=偽の頂上」。
- **直訳テスト**: legacyを**見ず** effects だけから「初見の他人ならどう訳すか」を生成→意味が戻るか確認。データから独立に作る。
- **独自化のレバー**: 用語を開いて説明(`急所`→`相手の弱いところ`)・確率を子ども向けに(`10%の確率で`→`10回に1回くらい`?=★阿部さん判断)・節を組み替える。
- **例外**: **特性(ABILITY_DESC)は公式準拠なのでそのまま可**。独自必須なのは技の説明文。
- → CLAUDE.md 北極星 ④ ＆ `ヤックン耳_判断ログ.md` 境界線に記録済。

## 2. ★表示戦略=決定済(越えるな)

**本番の説明文は legacy(手書き)のまま。compose生成の「新版」を本番に出さない。**(2026-06-06 阿部さん判断=①維持)
- compose は **監査器＋裏で開発中**。**generatorが全技100%喋れるまで本番化しない**(部分ship=generator+legacyの2ソース=ドリフト)。
- 新版が legacy 並みに良く見えても切替えない。切替は戦略=**人間の判断**。
- 声を直すのも「監査がノイズらない範囲」まで(ship磨きは別=legacyの仕事)。
- → `ヤックン耳_判断ログ.md` 境界線に焼いてある。

---

## 3. 次セッションの作業=裏ビルドのループ(これを回す)

```
開通順に kindテンプレを compose.js に追加
  → review/waza_compose.html で legacy と並べて監査(音確認)
  → 声の疑問は ★ で人間(阿部さん)に確認(Claude自己昇格禁止)
  → node tools/_waza_compose.js の機械漏れ検知で回帰チェック
  → カバー率↑(目標: 頭〜30テンプレで76%・尾はlegacyフォールバック)
```

**開通順(距離マップ・大きい順)**: 急所率上昇(+13) → ひるみ(+13) → 威力可変(+12) → 必中(+9) → 2ターン目に攻撃(+8) → …(以降+3〜5の長い尾)。
現状 216/490(連続攻撃まで)。

**注意**: 100%生成を最初から狙わない。頭の高頻度kindを生成、尾の一発ものは legacy フォールバック(全490に legacy 在り=無言の技ゼロ)。

---

## 4. park(別件・今日は触らない)

- **m.target(move-level の対象範囲)**: 日本語のまま。`real_battle_simulator.html:917-918` が日本語値を読む(load-bearing)。英語化するなら sim とセット=別リスク。effect.target(英語化済)とは別フィールド。
- **Wave2 英語prose(約35件)**: triaged5フィールド以外の自由記述(reason/applies_to/blocks等)。仕分け済・未適用(`review/waza_wave2_triage.html`)。enum70は許可(翻訳禁止)・formula4はD(数式保持)。これを当てると「全proseフィールド英語0」に到達。
- **構造化フェーズ2(defer4)**: ぜったいれいど命中30/20・アシストパワー+20/stage・おいかぜ速2倍4T・メロメロ50%=新スキーマ要。simと整合設計。
- **composeの穴**: トリプルアクセル(power_per_hit)・ちからをすいとる(回復にfraction無し)・たたりめ(condition値`any_non_volatile_status`未マップ)・ドラゴンエール(STAT辞書に急所ランク無し)。
- **いちゃもん/いのちがけ/とおせんぼう/フェアリーロック**: Wave1で構造修正済(kind衝突解消・昇格・ghost immune)。

---

## 5. 手つき(必ず守る・今日3回 盲目ツールに救われた)

- **「足りない/在る/通った」と言う前に必ず実データで裏取り**(カルテD)。今日: のろいshow・targetshow・gitの日本語名8進エスケープ、で3回ツールが盲目→裏取りで回避。
- **git の日本語ファイル名**は8進エスケープ(`"\343..."`)で出る。grep が盲目になる→ `git -c core.quotepath=false` か直接パス指定。
- **commitスコープを絞る**: `pl.m3u8`・画像フォルダ2つ・`*.bak`(`.tagdbbak`/`.tagsbak`は`.gitignore`漏れ)は**除外**。

---

## 6. 主要ファイル
- **憲法**: `CLAUDE.md`(自動注入・北極星/ヤックン耳テスト/罠/境界線)
- **正の台帳**: `ヤックン耳_判断ログ.md`(✓本人 5/5・声表記ルール・境界線・札)
- **監査器**: `tools/_waza_compose.js` → `review/waza_compose.html`(新版↔ヤックン↔元データ)
- **SSOT**: `pokechan_data.js`(WAZA_MAP)
- **condition表示**: `tools/_cond_render.js`(condStrNew・両ビュー共通)
- memory: [[verification-over-production]] / [[yakkun-ear-ledger]] / [[waza-effects-rollout-status]] / [[champions-battle-systems]]

---

## 7. ★★方針転換(2026-06-06 後半・阿部さん指示)— 説明文は「独自」に作る

**背景**: 監査器が legacy(ヤックン)と **verbatim一致**を出し始めた(クラブハンマー「急所に当たりやすい(急所ランク:+1)」等)。阿部さん指摘=**そっくり=盗用に見える**。本プロジェクトの出発点は「既存サイト(ヤックン)の丸写しでなく**独自に作る**」。→ **CLAUDE.md 北極星を阿部さんが更新済**(意味は戻る・**文言は独自**・verbatim=赤信号・直訳テスト追加)。

### ⚠ 重要訂正(2026-06-06 さらに後半・阿部さん)— 「独自化レバー」の解釈ミスを却下
当初この§7に「独自化=ジャーゴンを落とす/用語を開く/確率を子ども言葉に崩す」と書いた(下に取り消し線で残す)。**阿部さんが明確に却下**:「急所に当たりやすい」は勝手な解釈・softening。データ `急所率上昇 stages:1` は **「急所ランクが1個上がる」とそのまま書く**(stagesの数も"急所ランク"も落とすな)。**アレンジ・ひねり・まとめ・省略・AIの加工は不要。子どもに分からなくても、事実をそのまま。** → 詳細は `ヤックン耳_判断ログ.md`「★★そのまま書く」。

### 新しい合否(三本柱)
- ✅ **①意味が戻る** / **③機械が漏れない** … 維持
- 🔄 **②そのまま・忠実に**(データの中身を言葉通りに。アレンジ・softening・要約・AI加工 禁止。「やさしい子ども口調」より忠実が優先)
- ➕ **④legacyの文言を写してないか**(ヤックンの**編集的な言い回し**=「急所に当たりやすい」等 を写すな。ただし**データを忠実に訳した結果**bareな事実(「10%の確率で…まひ状態にする」等)が legacy と似るのは**自然な収束=OK**。独自性は softening でなく「データに忠実」から出る)

### 生成方法(これが核)= データの逐語訳(直訳)
**「直訳」=元データ(effects)の中身を、そのまま・言葉通りに日本語化する。** 子ども向けの作文・意訳ではない。まず"そのまま"で伝わるかを見る(憶測・想像で足さない)。
- `急所率上昇 stages:1` → **急所ランクが1個上がる**
- `状態付与 まひ prob:10` → **10%の確率で相手をまひ状態にする**(確率はそのまま「10%」)
- ~~ジャーゴンを落とす / 用語を開く(まひ→しびれて…) / 確率を「10回に1回」に崩す~~ ← **却下(2026-06-06 阿部さん)。崩さずそのまま。**
- **カテゴリ名(阿部さん要望)**: effect `kind` を頭にラベル付けする案あり(`急所率上昇`→【急所】等)。ただし**呼び名・形は未確定→要・耳**。
- **未確定の耳★**: 状態名の `『』` 囲み(『まひ』 か まひ か)。

### 役割分担(変更なし)
- **技の説明文**=独自作成(上の方法)。
- **特性(ABILITY_DESC)等**=基本もう公式通りなので**そのままでOK**(独自化対象外)。

### この転換が `tools/_waza_compose.js`(監査器)に意味すること
- 旧コンセプト=「effects→**legacyの音を再現**」は**否定された**。verbatim一致は達成でなく赤信号。
- 新コンセプト=「effects→**独自のやさしい一文＋カテゴリ**」。legacyは画面に置いて**意味の戻り確認**だけに使う(声の手本ではない)。
- **保留中(この転換で宙づり)**: ②確率統一・③prob-drop修正 は**コミットせず作業ツリーに保持**。③(prob欠落=「必ず状態異常」化)は**意味の誤り**なので独自方式でも要修正=生き残る。②(かくりつ→確率)は"legacy一致"が動機だったので新方式では**うちの選択**として再判断。**節順番**問題は独自再生成に吸収。
- park(③で露出・未着手): 範囲「相手」/トライアタックのランダム1つ/みずのはどう節内順/反動 1/3 vs 33%/もうどくの厚い説明。**独自方式で作り直す時にまとめて設計**。
- park(2026-06-07 ◇データ欠け・検証担当): **きあいパンチ**=肝の「攻撃するまでに技を受けると失敗」が effects に無い(legacyは散文)。`hit_by_contact_move_before_activation`相当のeffect追加候補(sim非依存裏取りのうえ)。→ 「voiced≠complete」の実例(台帳参照)。
- park(2026-06-06 検証担当が指摘・data-cleanup候補): **ひるみがデータ2系統**(`kind:ひるみ`13技 + `状態付与 value:ひるみ`6技)。表示は「ひるませる」に統一済だが**データは2形式のまま**(target/いちゃもん と同型の内部drift)。後日 `kind:ひるみ` へ一本化(sim非依存を裏取りのうえ)。**今は表示統一でOK・止めない。** ※ひるみ「ひるませる」自体は ◇(全19技 duration無し=動作)で確定・声判定不要(2026-06-06 検証 co-sign)。

### 確認HTML
`review/waza_voice_check.html`(`tools/_waza_voice_check.js`・監査器と同一エンジンをrequire)。阿部さんの耳判定用に①確定②節順番③別件の3段。**この転換後は「独自訳↔legacy(意味参照)」を並べる形へ作り替える**のが次の整備。
`review/waza_style_try.html`(`tools/_waza_style_try.js`)= 「そのまま忠実」版の試作(『』囲む/囲まない の2列＋legacy参照)。

---

## 8. ★★今の最前線 — 未解決の上位ルール矛盾(2026-06-06 最終・阿部さん判断待ち)

**§7 で「そのまま忠実」へ再転換した。それ自体は正しい(`急所率上昇 stages:1`→「急所に当たりやすい」はデータを削った＝✗。「急所ランクが1個上がる」が✓)。** だがこの再転換が、**過去に確定した✓やプロジェクトの★テーマと矛盾**することを検証担当(こっち)が止めて報告した。**この矛盾が解けるまで、テンプレ作り直しに入らない**(推測スタイルで作ると二度手間＝全部やり直しになる)。

### 未解決の矛盾(2つ)
**矛盾① [[plain-language-principle]] と正面衝突**
何日も★だった「**全説明文は小学校低学年にもわかる言葉で**」と、今回の「**子どもに分からなくてもデータのまま**」は両立しない。台帳に両方あると次セッションが割れる。
※あっちは「plain-language＝英語/機械語を出すな の意で、事実を崩していい ではない」と読み替えて記録したが、**これは plain-language の意味を大きく変える再解釈＝阿部さん未確定**。

**矛盾② あなた自身の ✓本人 と衝突(いばる)**
いばるの ✓本人 ＝「相手のこうげきを2段階**上げてしまう**」。だがデータ(`能力ランク変化 opponent attack +2`)を**そのまま**書けば「相手のこうげきが2段階**上がる**」。**「してしまう」は"そのまま"でなくアレンジ。** → いばる✓を残すか「上がる」に上書きか未決。

### 決定の分岐 — (あ) か (い)(これが根。ここが決まれば下が全部決まる)
- **(あ) 忠実＝情報を落とすな・歪めるな(完全・忠実)**。「急所ランクが1個上がる」が正・「急所に当たりやすい」は情報欠落で✗。だが**情報を落とさない言い回し**(いばるの「してしまう」等)は許される → **いばる✓は生きる・plain-languageとも両立**。
- **(い) 一切アレンジしない・データの語そのまま**。「してしまう」も✗(**いばる✓を上書き**)・plain-language は捨てる。
- **検証担当の見立て＝(あ)**(阿部さんの急所への怒りは「情報を落とした」ことへのもので、「子ども向けが悪い」ではなかったはず)。**最終は阿部さんの耳。**

### 阿部さんの耳待ちリスト(これが出るまで停止)
1. **(あ) / (い)** — 根。どちらか
2. **いばる✓「してしまう」** — 残す / 「上がる」に上書き((い)なら上書き)
3. **『』囲み** — 維持(『まひ』) / 外す(まひ)。内容でなく表記(「ゲーム用語」の合図)。台帳の既存✓ルールは「囲む」
4. **カテゴリ名** — 【急所】【どく】等(effect `kind` 由来)の形・呼び名。未確定
5. (確率は「**10%のまま**」＝阿部さん確定済・「10回に1回」に崩さない)

### ★共有ファイルの書き手＝あっち1人(2026-06-06 阿部さん決定・fork停止策)
- **台帳(`ヤックン耳_判断ログ.md`)・本ハンドオフ の書き手＝あっち(ビルド)1人。** こっち(検証)は直接編集しない・発見は文章で渡す→あっちが記録。
- **ただし あっちは共有docを commit する前に こっち(検証)の了解を得る**(＝1人で書く＋もう1人が承認、で fork も独走も防ぐ)。
- コード(`compose.js`等)はあっちのビルドdomain(了解不要・こっちは出力を検証)。
- ※ これより上の本§8は こっち が書いた最後の共有doc編集。以後あっちが owner。

### この決定の波及
- **5/5 ✓ ears の再確認**: (い) なら いばる(「してしまう」)等を見直し。(あ) なら概ね生きる。**(あ)/(い) 確定後に台帳の✓を整合させる**。
- §7 の「独自化レバー(ジャーゴン落とす/用語を開く/確率崩す)」は **却下済**(取り消し線)。次セッションはそれを採用しない。
- §1-3・§7 の合否基準は **§8 の (あ)/(い) 決定で最終確定**。それまで暫定。

### 現在地(全部この決定待ち)
- **停止中**: テンプレ作り直し・②③コミット(確率統一/prob-drop)・開通順(ひるみ以降)。
- **clean**: `pokechan_data.js`・本番表示 無変更。②③＋require化＋確認HTML生成器＝**作業ツリー保持(未コミット)**。
- **コミット済の注意**: ① 急所テンプレ(`5fb0eea`)は「急所に当たりやすい(急所ランク:+1)」を出す＝今の忠実方針では `(急所ランク:+1)` 表記の是非含め**作り直し対象**(revertでなく更新予定・(あ)/(い)決定後)。

---

## 9. 今日の全時系列(物語・ビルド担当の記録)

> §8=「今どの決定待ちか」。本§9=「どう今日ここに来たか」の通し記録(次セッションが文脈を失わないように)。

1. **始まり**: 監査器 `_waza_compose.js` を開通順にテンプレ追加して 100% へ育てる作業の続き(朝=216/490)。距離マップ先頭=急所率上昇(+13)。
2. **急所率上昇テンプレ追加** → 216→**229**(+13)。`_waza_dump_full.js` で `stages:N`/`always_crit` の形を裏取り。単一effect技が legacy と **verbatim一致** → 当時「達成」と思いコミット(`5fb0eea`)。**※これが後で"赤信号"と判明。**
3. **prob-drop 発見**: 急所を埋めて複合技が開通 → `状態付与` が `prob` を落とす意味漏れが露出(「必ず状態異常」化・24技)。legacy は「確率」漢字119/かな0。→ ②確率統一・③prob-drop を作業ツリーに適用、監査器を `require` 可能化、確認HTML `_waza_voice_check.js` を新設。**②③未コミット。**
4. **★方針転換(阿部さん)**: 「verbatim一致=盗用に見える。独自に作る。」CLAUDE.md 北極星を阿部さんが更新(`568e737`)。
5. **独自訳の試行**: 本Claude＋別Claudeが legacy 非参照で独自訳 → **独立2試訳が収束**(再現性の証拠)。そこから「独自化レバー(ジャーゴン落とす/用語を開く/確率を崩す)」と整理し **A/B/C** スタイルを HTML 提案。
6. **★A/B/C 全却下(阿部さん)**: 「急所に当たりやすい」=データを削った勝手な解釈。**`急所率上昇 stages:1`→「急所ランクがひとつ上がる」とそのまま書け。** アレンジ・ひねり・まとめ・省略・AI加工は全部しない。確率は「10%」のまま。**→ §8 の "そのまま忠実" へ再転換。**
7. **言葉のルール確定**: 「1個」→「ひとつ」(和語数詞)。正確で標準的な日本語(公式準拠・適切な漢字)。全ひらがな幼稚表現に寄せない。ヤックンとそっくり同一は避ける(望ましい)。→ 台帳に「★★そのまま書く」「言葉のルール」記録。`waza_style_try.html`(忠実版)・`kotoba_rules.html`(ルール一覧)作成。
8. **検証担当が矛盾を発見・停止**(=§8): "そのまま忠実" が ①plain-language-principle と ②いばる✓「してしまう」と衝突 → **(あ)/(い) の決定待ちで全停止**。

## 10. 成果物・git・次の一手(実務引き継ぎ)

### 今日できた成果物(次セッション/阿部さんが開ける)
- `review/waza_style_try.html` ← **そのまま忠実版**(『』囲む/囲まない2列＋ヤックン参照)。スタイル判断用。
- `review/kotoba_rules.html` ← **言葉のルール一覧**(大原則・✗→✓表・実例・未確定★)。
- `review/waza_voice_check.html` ← prob/急所の確認(①確定②節順番③別件)。**※"verbatim=✅"の枠組みは方針転換で意味反転=作り替え対象。**
- 生成器: `tools/_waza_style_try.js`・`tools/_kotoba_rules.js`・`tools/_waza_voice_check.js`(いずれも監査器を `require`)。

### git 状態(2026-06-06 セッション終盤)
- **コミット済**: `5fb0eea`(急所テンプレ=旧スタイル・作り直し対象)・`568e737`(CLAUDE.md北極星)。
- **コミット(セッション終盤・「全部保存」)**: `tools/_waza_compose.js`(②③+require化)・`review/waza_compose.html`・`ヤックン耳_判断ログ.md`(ルール追記)・`HANDOFF_SESSION_2026_06_06.md`(本書)・新規HTML/生成器3組(`_waza_voice_check`/`_waza_style_try`/`_kotoba_rules`)。※②③は「prob意味修正＋正確な日本語(確率)」で新ルールと整合するため保存。**エンジン全体の"忠実版"作り直し・急所テンプレ更新は (あ)/(い) 決定後**(本コミットは進捗保存であって方針確定ではない)。
- **コミット除外厳守**: `pl.m3u8`・`pokechan_data.js.tagdbbak`/`.tagsbak`・`画像 …/`2フォルダ。
- **`pokechan_data.js`・本番表示=無変更(clean)。**

### 次セッションの一手(順番)
1. **§8 の (あ)/(い) を阿部さんからもらう**(根の決定)。+ 付随★(いばる✓/『』/カテゴリ)。**出るまでテンプレ作り直しに入らない。**
2. 出たら台帳の✓群を (あ)/(い) で整合 → **`_waza_compose.js` を忠実版に作り直し**(まず急所:「急所ランクがひとつ上がる」へ更新=revertでなく更新)。`状態付与` も prob・『』ルール反映。②③はここに吸収してコミット。
3. 開通順(ひるみ→威力可変→必中→…)も**そのまま忠実**で。legacy は意味確認だけ。
4. 役割: ビルド=作る/検証=独立試訳で事実 co-sign。**Claudeは★→✓に上げない・声は人間のみ。**

### 効いた手つき(継続)
- **「足りる/在る/通った」前に実データ裏取り。** git日本語名は `git -c core.quotepath=false`。
- **作る快感>確かめる規律 の漂流は必ず起きる**(今日も「verbatim=達成」と誤認しコミットした=まさにこの罠)。北極星=今は「そのまま忠実か」で外から自分を点検。
