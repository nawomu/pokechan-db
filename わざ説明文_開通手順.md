# わざ説明文づくり — 開通手順(SOP)

最終更新: 2026-06-07 JST / **これはセッションをまたいで不変の作業手順書**。
新セッションで「続きをやってください」と言われたら、**まず本書のとおりに進める**(迷ったら本書に戻る)。

---

## ★これは何の作業か(1行)
`pokechan_data.js` の各技の **effects(構造データ)→ ヤックンの音の説明文** を作るエンジン(`tools/_waza_compose.js`)を、**効果の種類(kind)ごとに1ブロックずつ「開通」**(=テンプレを書いて喋れるようにする)していく。本番表示は **legacy のまま**(generator が全技100%喋れるまで本番化しない)。

---

## 0. 最初に必ず読む(この順番)
1. `CLAUDE.md`(自動注入・★北極星と★わざリスト基準)
2. `review/rules.html`(= `tools/_rules.js`。**作る/チェックの全ルール**。書き方＋言葉)
3. **本書**(`わざ説明文_開通手順.md`)
4. 最新の `HANDOFF_SESSION_YYYY_MM_DD.md`(現在地・次のブロック・park)
5. 必要に応じて `ヤックン耳_判断ログ.md`(判断ログ=具体例 ✗→✓)

---

## 1. 1ブロック(=効果kind1つ)の作業手順 ★これを1つずつ繰り返す

### STEP 1 — 次のブロックを選ぶ
- `review/waza_list_confirm.html` を開き、上部の目次 **「⚠ これから(技数の多い順)」の先頭**を次の対象にする(技数が多い=効果が大きい順)。
- もしくはコマンドで未対応kindを技数降順で出す(下の「コマンド早見表」)。

### STEP 2 — データを裏取り(自分の目で・憶測しない)
- 対象kindを持つ**全技の effects と description_legacy** を `node -e` で出す(コマンド早見表参照)。
- **全パターンを把握する**: 同じkindでもパラメータ違い(無条件/条件付き/形違い)がある。
- ★**意味の分裂に注意**: 同じ意味が**別kind/別キー表記に分裂**していないか横断確認(例: 「2倍」が `ダメージ倍率` と `威力倍率`、「必ず急所」が独立kindと `always_crit`、「ちいさくなる使用時」条件が4表記)。

### STEP 3 — テンプレ(clause)を書く
- `tools/_waza_compose.js` の `function clause(e, m)` に `case '<kind>':` を追加。
- **rules.html の全ルールに従う**(忠実版)。特に:
  - **そのまま忠実**(アレンジ/まとめ/省略/softeningしない)。**略さない**(意味が要る所は縮めない)。
  - **囲みは「 」**。数値はそのまま(10%等)。**機械語(英語/キー名/true/0.125)を出さない**(未知形は `return null` で穴に)。
  - **能力ランクは ±N表記**(こうげき-1、命中率+1 等)。**急所ランクは「ひとつ上がる」**。
  - **条件**は `clause` では付けない(composeが `condition` でグループ前置する)。

### STEP 4 — エンジンを回して機械チェック
```
node tools/_waza_compose.js
```
- **カバー数(フル生成可)が増えたか**・**機械漏れ0か**を確認(機械漏れが出たらテンプレを直す)。
- ※カバー数は「喋れる」だけ。**意味が戻るかは別**(STEP 6)。

### STEP 5 — 確認HTMLを再生成
```
node tools/_waza_list_confirm.js
```
→ `review/waza_list_confirm.html` をブラウザで開く(`open -a "Google Chrome" ...`)。

### STEP 6 — 意味照合(★ここが本番・人間/検証の目)
- 確認HTMLの**該当セクション**で、各技の **効果(新版)↔ ヤック(legacy)** を**意味で照合**。
- **voiced ≠ complete**: 穴フラグが無く「フル生成」でも、**データが肝を落としていれば意味は戻らない**。1技ずつ legacy と見比べる。
- ★**照合して抜けを丁寧に追加**: 新版に**抜けている意味**があれば、STEP 7 で effects に追加して補完する。

### STEP 7 — 抜けていたら effects を補完(データ編集)
- `pokechan_data.js`(SSOT)に不足 effect を追加(例: なみだめの `まもり貫通`)。
- ★**SSOT編集は検証してから書く**: 置換対象がN件きっかり一致するか確認 → 置換 → WAZA_MAP が `JSON.parse` できるか確認 → 期待どおりか確認 → OKなら保存(`node -e` でガードする)。
- effects は **simと説明文の二役** → simが読まなくても説明に要るものは消さない。**sim影響データは検証担当の co-sign 対象**(コミットメッセージに明記)。

### STEP 8 — ルール/台帳/ハンドオフを更新
- 新しく決めた言い方・例外は **`tools/_rules.js`**(→`node tools/_rules.js` で rules.html 再生成)。
- ✗→✓ の具体例は **`ヤックン耳_判断ログ.md`**(台帳)。
- 進捗・次のブロック・park は **ハンドオフ**。

### STEP 9 — コミット(論理単位で分割)
- 例: ①`feat(compose+data):` エンジン＋データ ②`docs(rules+...):` ルール/台帳/CLAUDE/ハンドオフ。
- 末尾に `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`。

### STEP 10 — 検証担当の独立 co-sign
- ビルド(作る)と検証(独立に試訳/データ裏取りして事実を co-sign)は別。**声の最終判定は人間(阿部さん)の耳**。
- → STEP 1 に戻り次のブロックへ。

---

## 2. 絶対の注意事項(やってはいけない / 必ずやる)
- ❌ **Claudeは ★→✓ に上げない**(意味の確定=人間の耳のみ。別Claudeの同意も不可)。
- ❌ **別Claude(検証/ビルド)の主張に錨を打たない**。錨は legacy か 人間 か 自分で引いたデータにのみ。
- ❌ **legacy の文言を写さない**(verbatim一致=盗用リスク=赤信号)。ただし**独自化=softening/言い換えではない**。データに忠実に書けば自然に別物になる。bareな事実一致(10%等)は自然な収束=可。
- ❌ **作る快感に流されない**(カバー率・grepゼロ・「全部日本語になった」は**偽の頂上**)。作る前後で「もう legacy に在るのでは?」を問う。
- ✅ **共有doc(台帳/ハンドオフ/CLAUDE.md)のコミットは検証担当の了解を取る**(阿部さん直接指示時を除く)。
- ✅ **境界(声・SSOT矛盾・スキーマ追加)は止まって阿部さんに聞く**。記憶で断言しない・権威ソース(Bulbapedia等)で裏取り。
- ✅ **本番(pokechan_data.js の表示／waza-list.html)は legacy のまま**。新版を本番に出さない。

---

## 3. コマンド早見表
```bash
# 未対応kindを技数降順で(=次のブロック候補)
node -e 'const fs=require("fs");const t=fs.readFileSync("pokechan_data.js","utf8");function lit(t,m){const at=t.indexOf(m);let i=t.indexOf("{",at),s=i,d=0,S=0,e=0;for(;i<t.length;i++){const c=t[i];if(S){if(e)e=0;else if(c=="\\")e=1;else if(c=="\"")S=0;}else{if(c=="\"")S=1;else if(c=="{")d++;else if(c=="}"){d--;if(d==0)return t.slice(s,i+1);}}}}const map=JSON.parse(lit(t,"const WAZA_MAP ="));const {clause}=require("./tools/_waza_compose.js");const h={};for(const m of Object.values(map))for(const e of (m.battle_data||{}).effects||[])if(!clause(e,m))h[e.kind]=(h[e.kind]||0)+1;Object.entries(h).sort((a,b)=>b[1]-a[1]).forEach(([k,v])=>console.log(v,k));'

# 対象kindの全技 effects + legacy を出す(KIND を置換)
node -e 'const fs=require("fs");const t=fs.readFileSync("pokechan_data.js","utf8");function lit(t,m){const at=t.indexOf(m);let i=t.indexOf("{",at),s=i,d=0,S=0,e=0;for(;i<t.length;i++){const c=t[i];if(S){if(e)e=0;else if(c=="\\")e=1;else if(c=="\"")S=0;}else{if(c=="\"")S=1;else if(c=="{")d++;else if(c=="}"){d--;if(d==0)return t.slice(s,i+1);}}}}const map=JSON.parse(lit(t,"const WAZA_MAP ="));const KIND="状態異常回復";for(const m of Object.values(map)){const eff=(m.battle_data||{}).effects||[];if(eff.some(e=>e.kind===KIND)){console.log("\n■"+m.name);eff.forEach(e=>console.log("  "+JSON.stringify(e)));console.log("  legacy:",m.description_legacy);}}'

node tools/_waza_compose.js          # エンジン実行(カバー/機械漏れ)
node tools/_waza_list_confirm.js     # 確認HTML再生成
node tools/_rules.js                 # rules.html 再生成
open -a "Google Chrome" review/waza_list_confirm.html
```

---

## 4. 主要ファイル
- 基準: `review/rules.html`(`tools/_rules.js`) / 確認: `review/waza_list_confirm.html`(`tools/_waza_list_confirm.js`)
- エンジン: `tools/_waza_compose.js`(`module.exports={compose,clause,map}`) / 条件文: `tools/_cond_render.js`
- SSOT: `pokechan_data.js`(`WAZA_MAP`) / 台帳: `ヤックン耳_判断ログ.md` / 憲法: `CLAUDE.md`
- 進捗: 最新の `HANDOFF_SESSION_*.md`
