# HANDOFF 2026-08-13〜15 ★これだけ読めば再開できる(持ち物監査 完走 → 特性監査 着手)

★**読む順**: ① `CLAUDE.md`「★★★データの絶対ルール」 → ② メモリ [[master-data-core-philosophy]] → ③ 本書 → ④ 計画 `reference/_audit_campaign_plan.json` → ⑤ 帳簿 `reference/_window_ledger.json`

---

# 0. いまの位置

| 領域 | 状態 |
|---|---|
| **持ち物 169件** | ★**監査完了**(枯れ判定成立・R1〜R6) |
| **特性 312件** | ラウンド1を **80/312** まで消化して停止(上限) |
| 技 919件 | 未着手。★**始める前にコーパス検査が必要**(§4) |
| 覚える技 | 未着手 |
| ②ページ屋台骨 / ③バトル | 監査完走まで着手しない(順番は不変) |

---

# 1. 持ち物監査の結末(完了)

**新発見の推移: R1=140 → R2=11 → R3=4 → R4=2 → R5=0 → R6=0**。規約の「2ラウンド連続ゼロ」を満たして完了。

| ラウンド | レンズ |
|---|---|
| R1 | 効果の一字一句 |
| R2 | 数値と条件の一語 |
| R3 | 相互作用と例外 |
| R4 | 世代差とChampions差分・表記ゆれ |
| R5 | 周辺フィールド(メタ情報) |
| R6 | ★逆方向の照合(でっち上げ検出)= master の文節ごとに根拠引用を出せるか |

## master に入れた修正(11種)
こだわりハチマキ/メガネ(能力値が1.5倍)/ とくせいガード(戦闘中限定+かたやぶり系無視の防止)/ ふうせん(攻撃技のダメージ限定)/ いのちのたま(命中時のみ反動)/ しろいハーブ(「ターン終了時」を削除=阿部さん確認)/ メンタルハーブ(かなしばり追加で6種)/ モモンのみ(もうどく追加)/ くろいてっきゅう(設置技3種を追加)/ レジェンズZ-Aのハイフン4件/ **applies_to_pokemon のメガ済み混入**(生成器のバグ)/ **boosts系統バグ**(生成器のバグ・5件)

## 触らなかったもの
- **categoryの分類の粗さ = A案(触らない)**。権威で正誤が決まらず表示専用。屋台骨のページ作り直し時に設計し直す(決定は `_audit_campaign_plan.json` の decisions)
- **sim実装課題 396件** → `reference/_sim_todo_items.json` に退避(説明文はわざと短い設計を守る)。生成器= `tools/_build_sim_todo.js`

---

# 2. 特性監査で見つけた大物(全部根治済み・push済み)

## ★★権威コーパスが壊れていた(一番重要)
ヤックン `/ch/` の旧スクレイプ(2026-07-28)は**本文中のリンク文字が落ちる**取り方で、**28件の効果文が途中で切れていた**。
- 例: **マジックガード** = 「直接のダメージ以外を受けない。**具体的には、**」で終了(本来は道具・技・状態の長い列挙が続く)
- 他: ふゆう / いかく / しめりけ / かげふみ / こぼれダネ / スロースタート 等

→ **全315ページを取り直した**(EUC-JPデコード + img alt復元 + リンク文字を含む textContent)。結果: **途中切れ0件・効果文が回復38件**。旧版は `reference/_authority_corpus_ch/abilities_ch_2026-07-28_旧.json` に保全。

## その他3件
1. **ナビ欄混入59件** — 効果文の末尾に「関連する項目 特性トレース…」。生成器 `stripNavi()` で除去(切り取り位置は全件機械検査済み)
2. **全角『ＡＲシステム』と半角『ARシステム』の重複** — 名前を `zen2han` で正規化して統合(**313→312件**)
3. **★champions_pokemon_count の退行(私のミス)** — コーパス取り直し時にこのフィールドを落とし、**311件全部が0**になった。特性R1のエージェントが「champions=trueなのにcount=0」と大量指摘して発覚。
   → **外部の値を写すのをやめ、Championsのポケモン一覧から自前で数える**形に変更(旧値と 309/312 一致。残り3件=きずなへんげ・わるいてぐせ・スナイパーはうちのデータの方が正しい可能性が高く、監査で確認する)

---

# 3. 新しい道具(全部 push 済み)

| ツール | 役割 |
|---|---|
| `tools/_gen_item_audit_materials.js` | 持ち物の照合材料を1件1ファイルで生成(LLM不使用) |
| `tools/_gen_ability_audit_materials.js` | 特性版。triage(7/28の三者照合90件)も渡す |
| `tools/_collect_audit_round.js` | ワークフロー結果を `journal.jsonl` から台帳へ(LLM不使用・二重追記防止・新発見抽出つき) |
| `tools/_build_sim_todo.js` | 台帳から sim実装課題を抽出(引用つき) |
| `tools/_usage_monitor.sh` | 枠の見える化。**cap未検証なら level=unknown** |
| `reference/_window_ledger.json` | ★共有帳簿(校正点・機構メモ・未決着caveat) |
| `reference/_audit_campaign_plan.json` | ★キャンペーンの自走計画と決定の記録 |
| `scratchpad/item-audit-round.js` / `ability-audit-round.js` | ラウンド実行スクリプト(要約のみ返す方式) |

---

# 4. ★次にやること(この順)

1. **特性ラウンド1の続き(80/312 → 312)**
   - 名簿= `master/abilities.json` の並び順。80件/バッチで残り3バッチ
   - 材料= `scratchpad/audit_abilities_r1/`(消えていたら `node tools/_gen_ability_audit_materials.js <dir>`)
   - 台帳化= `node tools/_collect_audit_round.js <runId> reference/_ability_audit_round1.json`
   - ★指摘が大量に出る(持ち物R1と同じ規模)。**4バッチ終えてから一括で精査・適用**する方が安い(系統的な誤りをまとめて根治できる)
2. **特性R1の発見を適用** → R2以降(レンズ替え)→ 枯れるまで
3. **★技919件に入る前に、技の権威コーパスを検査する**
   - `reference/_authority_corpus_ch/moves_ch.json` には**効果文が入っていない**(タイプ・威力・命中・PP・接触・守るのみ)
   - `yakkun.com/ch/move_list.htm` を実ブラウザで開き、効果の列があるか確認 → あれば特性と同じ手口で取り直し
   - ★**これをやらずに監査に入ると、欠けた権威と比べて「一致」と誤判定する**
4. 技のラウンド1〜 → 覚える技 → **②ページの屋台骨** → **③バトル**

---

# 5. ★注意事項(次のセッションが必ず守る)

## 監査の規律(不変)
- **1件=1エージェント / 引用必須 / 全件反証 / ラウンド制 / 推測で埋めない**。★コストを理由に工程を間引かない([[audit-thoroughness-not-negotiable]]・阿部さん「はしょると事故につながる」)
- 事実は **Wiki+徹底攻略の二重チェックで一致すれば聞かずに適用・コミット**([[two-source-verify-then-commit]])。**食い違う時だけ止まって聞く**
- Champions に在るものは ヤックン`/ch/` が正典。ただし**ゲーム内表示文(WikiのChampions節)の方が詳しい場合がある**(メンタルハーブのかなしばり・モモンのみのもうどくは、ヤックンが落としていた)

## ★監査を始める前に、権威コーパス自体を検査する
今回の最大の学び。**特性で28件、技でも同じ疑い**。持ち物は8/2に取り直していたので無事だった。
検査のやり方(LLM不使用): 効果文が `。` `)` で終わっていない件を数える / ナビ欄の語が混入していないか / 必要なフィールドが全部あるか。

## Championsは状態の呼び名が本編と違う(実証済み)
技名は同じだが**状態名が違う**: 「いちゃもん」がかける状態=Championsでは**連続不可**、「かなしばり」がかける状態=**わざふうじ**。
→ Champions の文を読む時、別物として二重にデータを作らないよう対応づける。

## 枠(セッション/週間)
- **ローカルから cap は逆算できない**(ccusageはマシン単位・サブエージェントは写らない=isSidechain行が0件)。真値は阿部さんの画面だけ([[quota-meter-multi-machine-limit]])
- **赤を跨ぐな**。跨ぎたくなったら計器を正直にする(`cap_verified:false` → `level:"unknown"`)
- 上限に当たっても **`resumeFromRunId` で損失ゼロ**。止めるのを怖がらない
- 最新の校正点(2026-08-15 09:20): **セッション94% / 週間すべて20% / 週間Fable 0%**(セーフガードでOpus 5に自動切替されていた)。**詰まるのは5時間枠の方**

## 省エネの作法
1バッチは大きく / ワークフローは**要約(digest)だけ返す** / 台帳化と材料生成は**LLM不使用のツール** / 親の返事は短く。
★実測: 1件あたり 持ち物R3=132k・要約方式で123k。特性R1=約120k。

## 踏んだ罠
- ワークフロースクリプトを流用する時、**一括置換で `items` などのキーワードまで書き換えるとスキーマが壊れる**(80エージェントが起動即失敗。消費0だったのが救い)
- ヤックンの連続アクセスは**弾かれる**。400ms間隔を空ける。45秒のJS実行制限があるので、長い取得は**バックグラウンドで走らせて状態だけ返す**形にする
- ブラウザから大きなJSONを持ち出す時は、**ローカルにPOST受け口を立てる**のが速い(出力の切り詰めを回避)。https→httpは混在ブロックされるので、いったん `http://localhost:8000/` に移ってから送る(`window.name` は遷移をまたいで残る)

## 2窓体制(Claude同士の会話)
公式のセッション間メッセージング(`SendMessage`/`ListAgents`)で往復できる。初日に検証役が**私の穴を5件**見つけた(帳簿の分裂・推測値の混入・cap矛盾・機構調査・赤跨ぎ)。縄張りを決めること(master/と監査台帳は監査窓が握る)。

## auto-continue(上限リセット時の自動再開)
2026-08-14に公式アナウンス。**まだこのCLIには来ていない**(2.1.232のバイナリに文字列なし)。デスクトップアプリで上限に当たった時のメッセージ内に `☑ Auto-continue when limits reset` が出るので、見えたらオンにする。オンにしたら帳簿の付け方を自動再開ぶんに合わせること。

---

# 6. 検討メモ(後回し)
**master/ をMCPサーバとして公開する案**(デジタル庁が調査データ75,000件をMCP公開した事例)。他セッション・クラウドClaudeからも同じマスターを引けて、「データは一つ」がアクセス経路のレベルでも守られる。★監査完走が先。②ページ作り直しと同じ層で検討(`_audit_campaign_plan.json` の mcp_idea)。
