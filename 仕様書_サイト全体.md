# 仕様書: サイト全体(PchamDB)— まずここを読む

作成 2026-07-28 JST / 指示=阿部さん「**仕様書の一番最初の軸として書いておいてほしい。データベースは一つ、と。ページ全体のものとして。**」
★これは**入口の仕様書**。各領域の詳しい仕様書へはここから辿る。

---

# 第1の軸 ★★★ データベースは一つ

> **データは一つ。絶対に一つ。どのページにも「データは一つ」。複数持たない。各ページごとに作らない。**

**これがこのサイトで一番大事な決まり。** 他のどの決まりよりも先に来る。

- **修正も追加も、必ず元データ(SSOT)を直す。** 生成物・各ページのコピーを手で直さない。
- **ページは参照元1枚 `pokedb.js` だけを読む。** ページから `pokechan_data*.js` を直接読むのは**禁止**。
- **新しいページを作るときも同じ**(技のページ・ログのページ・ランキング・ツール…何であれ)。
  **そのページ用にデータを作らない。** 既にあるデータを**参照する**。足りなければ**元データを広げる**。
- ★**この決まりは、あちこちに重複して書いてよい。**(阿部さん「その都度確認するという意味で、重複はしてもいい」)
  忘れる前提で、目に入る場所すべてに置く。

### なぜ(理由を消さない)
1. **一箇所直せば全部変わる。** ページは現在**4,999枚**。分かれていると、1件直すたびに「どのページが古いまま残っているか」を探す作業が発生し、**検証もページ数ぶん増える**。
2. **多言語化も一本化できる。** データが一本なら、**翻訳を1回当てるだけで9言語・全ページに波及する**(名前・技名・特性名・説明文・見出し・項目名)。分かれていると同じ翻訳を何度も当て直すことになる。
3. **追加が楽になる。** Championsに新しいポケモン/技/持ち物が追加されても、**その1件を調べ直して上書きするだけ**で済む。

### 破ったらどうなるか(2026-07-28 に実際に起きたこと)
- Champions版と全国版が**別体系**になった(技のキーが `reitoupanchi` と `karate-chop` で**共通0件**、特性の説明が195件と309件)
- サーフゴーが「おうごんのからだ」を持つのに**説明が出ない**・エンジンも未実装
- 再ビルドすると**説明文が5件静かに消える**状態
- 名前の切り詰め**17件** → **メガフラエッテ・メガニャオニクス♀♂ が誰もメガシンカできない**
- 危うく**396技のPPを旧世代へ巻き戻す**ところだった

→ 詳細と**失敗の記録** = **`設計_データSSOT一本化_2026-07-28.md`**(§10が本体)

### 守る仕組み(文章だけでは守られないので)
| 層 | 仕組み |
|---|---|
| 読ませる | `CLAUDE.md` 最上位 / この仕様書 / 記憶 |
| **止める** | 生成物のヘッダ・hookでの編集ブロック(予定) |
| **落ちる** | **`node tools/_ssot_guard_test.js`**(番人)。悪化したら赤くなる。★**基準を上げて通すのは禁止** |

---

# 第1.5の軸 ★呼び名は「マスターデータ」(2026-07-29 阿部さん)

> **一本化した大本のデータ = 「マスターデータ」**と呼ぶ。
> ポケモンの名前・能力値・特性・技一覧・持ち物 など、**データのすべての大本**。

- **置き場所は `master/` ただ1つ**。「**マスターデータは `master/` にしかない**」と言い切れる形にする。
  ```
  master/
    ├ pokemon.json      名前(正式)/display_name/種族値/タイプ/特性/フォーム
    ├ moves.json        技(威力・命中・PP・効果・タグ・flags)
    ├ abilities.json    特性
    ├ items.json        持ち物
    ├ learnsets.json    誰がどの技を覚えるか ＋ ★没収技
    └ regulations.json  現行レギュレーション(=M-B)のフラグ
  ```
- **直すのはマスターデータだけ。** `pokechan_data.js` / `pokechan_data_all.js` / 生成ページ / i18n辞書は**全部その生成物**。
- ★**名前の衝突を掃除してから移る**: 現在 `reference/` に **旧 `moves_master.json`(6/25)・`abilities_master.json`(7/3)・`items_master.json`(6/25)** と
  **新 `master_moves.json`・`master_abilities.json`(7/28)** が**両方ある**(GLM棚卸しT12-4で検出)。
  → **`master/` へ移す時に、旧いものは削除**する(「別で作ってから入れ替える」の手順で)。

---

# 第2の軸 データの持ち方(器と中身)

- **器(全部を入れる箱)= 全国版の範囲**(1219体 / 919技 / 310特性 / 167持ち物)
- **中身(値)の正典 = Champions**
  1. **Championsに在るもの → Champions権威の値で上書き**(確認先=ヤックン `/ch/`)
  2. **Championsに無いもの → 最新世代(第九→第八→第七…)の値を参考として入れる**
  3. **後からChampionsに追加・変更されたら → その1件だけ調べ直して上書き**
- ★**「新しく作った方が正しい」とは限らない**(全国版は後から取り直したが、PPは396件が旧世代の値だった)
- **名前は正式名称**(略さない・切り詰めない)。英語もPokeAPIの完全名。
  ★名前の不一致は「**機能が到達不能になる実バグ**」を生む(フラエッテナイト/ニャオニクスナイト/いじげんラッシュ/i18n468件=全部同じ型)

**権威(正典)の確認先**
| 何を | どこで |
|---|---|
| Champions固有の効果・数値 | **ヤックン(ポケモン徹底攻略)の `/ch/` ページ**(URLに `/ch/` があるか必ず確認) |
| 仕組みの詳細・なぜそうなるか | **ポケモンWiki(日本)** |
| 食い違ったら | **Championsページが正典** |
| 取得済みの正典コーパス | `reference/_authority_corpus_ch/`(特性315 / 技497 / 道具75 / 種族値313体) |

---

# 第3の軸 サイトの構成(何があるか)

| かたまり | 枚数 | 中身 |
|---|---|---|
| ルート直下(手作り) | 44 | ポケモンDB / わざリスト / バトルラボ / リアルバトル / オンライン対戦 / 各種ツール |
| 生成コンテンツ | 約550 | `pokemon/` `ability/` `move/` `type/` … |
| 言語別 生成ページ | 約4,300 | `en/` `fr/` `de/` `es/` `it/` `ko/` `zh-Hans/` `zh-Hant/`(各539) |
| **合計** | **4,999** | ★**生成ページはデータが焼き込まれている**=元データを直したら**再生成が要る** |

→ 配線の実測 = **`review/データ配線_現状調査_2026-07-28.html`**

---

# 第3.5の軸 ★本番(リアルバトル)とラボの役割分担(2026-07-29 阿部さん)

> 「**リアルバトルは現行レギュレーションどおり**(技・持ち物・ポケモン、没収技なし)。**ラボは全国版で試せる**。全ポケモンが使えて、**没収技も『使える/使えない』をチェックで選べたら面白い**。」

| | リアルバトル / オンライン対戦 | バトルラボ |
|---|---|---|
| 位置づけ | **本番**(公式の対戦をなぞる) | **実験場**(何でも試せる) |
| ポケモン | **Champions登場のみ**(現行レギュレーション準拠) | **全国版=全ポケモン** |
| 技 | **Championsで使える技のみ**(**没収技は出さない**) | **全技**。★**没収技を使う/使わないをチェックで切替**できる |
| 持ち物 | Champions実在のみ(**75件**) | 全部(169件) |
| データの出どころ | **同じ1本のDB**。違うのは**絞り込み条件だけ** | 同上 |

★**大事なのは「データを分けない」こと**。本番とラボは**同じDBを違う条件で絞り込んでいるだけ**にする。
(データを分けた瞬間に、また今回と同じ「別物になる」問題が起きる)

### なぜ裏で全部作っておくのか(阿部さんの意図)
**Championsに新しいポケモン/技/持ち物が追加されたとき、すぐ反映できるようにするため。**
器(全国版の範囲)に全部入れてあり、値は「Champions優先→無ければ最新世代」で埋めてあるので、
**追加された1件を調べ直して上書きするだけ**で本番に出せる。

### そのために必要なデータの印(新DBに持たせる)
- `champions: true/false` — Championsに登場するか
- `confiscated: true/false` — ★**Championsで没収された技か**(ポケモンごと。2026-07-29にヤックンから取得中)
- `source` / `verified_at` — 値がどこ由来か(Champions確認済み / 最新世代からの暫定)

### ★レギュレーションの扱い(2026-07-29 阿部さん確定)
- **現行レギュレーション = M-B**(その前は M-A)。**過去のレギュレーションは保持しない**(後戻りしないので、持っていても仕方がない)。
- **データに「M-B」のフラグを立て、そのフラグが立っているポケモン・技・持ち物だけをリアルバトルに出す。**
- レギュレーションが更新されたら(M-C・M-D…)、**フラグを新しいものに更新するだけ**。過去分は残さず、**常に現行だけを見る**。
- 権威: ヤックンに **「レギュレーションMBで追加された新ポケモン一覧」**(`/ch/zukan/reg_mb/`)・**「レギュMBで追加された道具一覧」**(`/ch/item.htm?mode=reg_mb`) がある。
  → **追加分を機械で取得して、フラグを立て直せる**。

---

# 第4の軸 新しいページを作るときの決まり

1. **データはそのページ用に作らない。** `pokedb.js` 経由で参照する。足りなければ**元データを広げる**。
2. **多言語化ファースト。** 書いてから訳すのでなく、**最初から9言語前提で設計**する(生の日本語をHTML/JSに直書きしない)。→ [[i18n-first-design]]
3. **表示する値は元データから導出する。** 例=**ポケモンDB右側の技の列**は、フラグ列を持たず `POKEMON_WAZA` から導出し、**グループ分けはタグで決める**(わざリストと同じタグを見る)。→ `設計_データSSOT一本化` §13
4. **自分で画面を見て確認してから完了報告**(PDCAのCheck)。→ [[self-verify-pdca-rule]]

---

# 第5の軸 ★★説明文の作り方(鉄壁のルール・例外なし)

> 阿部さん(2026-07-29):「**説明の作り方とか、effectsと直結するダメージとかも全部仕様書に書いておいて。そうやって作るんだよ、というのは鉄壁のルールなので。**」

## 5.1 技(わざ)の説明文 = **effectsから生成する。手で書かない。**

```
   effects(バトルのデータ) ──compose──▶ 技の説明文 ──▶ わざリスト/技ページ/多言語
        ▲
   ここを直せば、説明文も自動的に正しくなる
```

- **正典の順序**: **①effects(simの土台)を作る → ②composeが日本語に訳す**。**逆は禁止**。
  effectsが空のまま説明文だけ手書きすると、**人間が読む文は埋まっても sim が動かない**(その技がバトルで不発)=**偽の完成**。
- **実装**: `tools/_waza_compose.js` の `compose()` + ビルダー側の `composeDesc()`(ダメージ技には冒頭に「ダメージ。」を付ける後処理)。
  - 全国版: `tools/_build_pokechan_data_all.js` が `composeDesc()` で生成
  - Champions版: `tools/build_champions_view.js` が `master.description_ja`(= compose再生成版)を使う
- ★**2026-07-29 実測**: 本番の説明文と `compose()` の生出力は498技中327件が違って見えるが、**差はほぼ全部「ダメージ。」の有無**=後処理の分。**仕組みは生きている**。
- **手書きオーバーレイ** `reference/moves_desc_override.json` は **「121kindに無いメカが出た時の、新kind実装までの一時しのぎ」**。
  **恒久的に手書きで済ませてはいけない**。新しいメカが出たら **新kindをスキーマ+compose(+sim)に足して、説明文はcompose生成に戻す**。
- **書き方のルール本体** = `review/rules.html`(書き方のルール+言葉のルール)。**二重管理しない**。
- **確認の場** = `review/waza_list_confirm.html`(効果カテゴリー別に「新版 ↔ 参照用」を意味で照合)。
- ★**バトルのフェーズを作り変えても、この道は変わらない**。むしろ**フェーズ設計が整うほど説明文も正確になる**(effectsが正しくなるため)。

### 番人に入れる検査(新DB移行時)
- **「本番の説明文 == effectsから再生成した文」が全技で一致するか**。ズレたら赤。
  → **effectsを直したのに説明文を再生成し忘れた**、が構造的に検出できる。
- オーバーレイが効いている技の件数を出す(**増えていたら赤**=手書きで誤魔化しが増えたサイン)。

## 5.2 特性(とくせい)の説明文 = **公式のゲーム内テキストをそのまま使う**

- **composeを通さない**(技とは別ルート)。`master/abilities.json` の文言をそのまま使う。
- ★**出どころ = ポケモンWikiの「説明文」節にある Champions の行**(= **公式のゲーム内テキスト**)。
  | 特性 | 公式テキスト(Champions) |
  |---|---|
  | どくのトゲ | 接触技を受けると 30%の確率で相手をどく状態にする。 |
  | エレキメイカー | 登場した時から 5ターンの間 全体の場を エレキフィールド状態にする。 |
  | すいほう | ほのおタイプの技で受けるダメージが 半減する。みずタイプの技の威力が 2倍になる。やけど状態にならない。 |
  | たいねつ | ほのおタイプの技で受けるダメージが 半減する。やけど状態で受けるダメージも 半減する。 |
- ★**なぜ「そのまま」で良いのか(2026-07-29 阿部さん判断)**: **特性・持ち物の説明は、どのページも同じ=公式が出している情報だから**。
  → **ヤックン/ポケモンWikiから そのまま流用してよい**。(技の説明文は独自作成が必要=北極星。**特性・持ち物は別扱い**)
- **★唯一の注意点 = 世代で変わる。ナーフが入る。**
  特性は世代ごとに仕様が変更されることがある(実例: **すいほう/たいねつ**は第九世代で「相手のこうげき・とくこう半減」に書き換わったが、**Championsは「ダメージ半減」**)。
  → **流用する時は必ず「最新のポケモン(=Champions)に対応した記述か」を確認する。**
  - 見分け方: ポケモンWikiの「説明文」節に **`Champions` の行があるか**(あればそれが最新)。ヤックンは **URLに `/ch/` があるか**。
  - Champions の記述が無ければ、**最新世代(第九→第八…)のものを入れ、`source` に何世代由来かを残す**([[champions-plus-alpha-canon-rule]])。
- **持ち物も同じ扱い**。

### 実務の手順(特性・持ち物の説明を入れる/直すとき)
1. ポケモンWikiの「説明文」節を見る → **`Champions` の行があればそれを採用**(公式のゲーム内テキスト)
2. 無ければヤックンの `/ch/` ページ(Champions準拠)
3. どちらも無ければ**最新世代**のものを入れ、`source`(何世代由来か)+ `verified_at` を残す
4. **後でChampions版が判明したら上書きする**(印があるのでどれを直すか分かる)

## 5.3 わざリスト・技ページ・多言語も、ここから出る
- **わざリストのタグ/グループ分け**は `master/moves.json` のタグから(→ §13 と同じ。ポケモンDB右側の技列と**同じタグを見る**)。
- **多言語**も同じ大本から。**翻訳は1回当てるだけで全言語・全ページに波及する**。

---

# 第6の軸 AI(コンピュータ対戦相手)の仕組み

★**阿部さんが見る画面 = `review/AIの仕組み.html`**(`node tools/_build_ai_spec.js` で再生成。**数字と行番号はエンジンのソースから直接抜くので、コードを変えればページも変わる**)。
以下はセッションをまたぐ引き継ぎ用の要約。**詳細と最新の数字は必ず生成ページを見る**こと。

## 6.1 ひとことで言うと
**「1手先だけを見て、それぞれの技に点数をつけて、一番高い手を選ぶ」自動プレイヤー。** 機械学習ではなく**ルールと点数表**。
実装 = `real_battle_simulator.html` の `aiScoreMove` / `aiChooseMove` / `aiChooseAction`。

## 6.2 技の点数(2026-07-29 時点の実測値)
| 種類 | 点数 |
|---|---|
| 攻撃技 | 予想ダメージの平均。**確実に倒せるなら +1000**(=実質最優先) |
| 回復技(自分) | (最大HP − 今のHP) × **0.7**(満タンならほぼ0=無駄撃ちしない) |
| 状態異常技(相手) | 相手の残HP × **0.45**(既に状態異常/無効なら0) |
| 積み技(自分) | **40 + 段階×10**。条件=自分のHPが**2/3以上**かつ**+2未満** |
| へんしん | **60**(使える状況のみ) |
| その他の変化技 | **1**(攻撃が全部通らない時の保険) |

## 6.3 撃っても無駄な技は選ばない(実プレイのFBから足された)
PP切れ / こだわりロック中の別技 / **不発が確定している技**(天気不一致のオーロラベール・2ターン目のねこだまし・たくわえ0のはきだす・ふいうち系の `fails_if`・みらいよち重ねがけ 等**12件のゲート**)。

## 6.4 交代の判断
- **A 手詰まり**: どの技にも価値が無い → 相性の良い控えへ
- **B 受け交代**: 相手の最大平均ダメージ ≥ 自分の残HP **かつ** 自分が後攻 **かつ** 倒し返せない → 一番安く受けられる控えへ
- ★**連続では交代しない**(`aiSwitchedLast` で往復防止)/ **確定KOがあるときは必ず攻撃**

## 6.5 AIが見てよい情報
コード内に明記: **「AIはsim内の情報(相手の技)を見てよい設計」= 相手の手札を知っている**(カンニングあり)。
相手側AIは**プレイヤーの行動確定後**に選ぶので、ふいうち系の判定も正確。

## 6.6 分かっている限界(③バトル作り直しの改善候補)
- **1手先しか見ない**(2手先・詰め・交代読みなし)
- 変化技の価値が粗い(壁・追い風・設置技が「1点」の保険に流れることがある)
- ★**点数づけがエンジン本体に直書き** → フェーズ構造に整理する時に「どこで何を判断しているか」を見える形にできる
- ★**フェーズ・ビューア**(1フェーズずつ止めて見る画面)を作れば、**AIが各技に何点つけたか**も表示できる

---

# 各領域の仕様書(ここから辿る)

| 領域 | 仕様書 |
|---|---|
| **★管理ツール(阿部さんが見る画面)** | `管理ツール.html`(ローカル専用・`PchamDB管理ツールを開く.command` をダブルクリックで起動)→ データ一覧 / 内部構造仕様書 / AIの仕組み / バトル仕様書 ほか |
| **データの持ち方・一本化・引っ越し** | **`設計_データSSOT一本化_2026-07-28.md`**(★§10 失敗の記録) |
| **バトルの設計(憲法)** | `設計_バトルエンジン原理_2026-07-26.md` → `設計_フェーズ語彙v3_2026-07-26.md` → `設計_割り込みと分岐の規律_2026-07-26.md` |
| **バトルの機構一覧(自動生成)** | `review/バトル仕様書.html`(618機構×63フェーズ・`node tools/_build_battle_spec.js` で再生成) |
| ルールの実装点検 | `review/現状棚卸し_ルール実装点検_2026-07-28.html`(439規則) |
| 全国版DBの決まり | `review/db_rules.html`(R1〜R8) |
| わざ説明文の決まり | `review/rules.html` + `ヤックン耳_判断ログ.md` |
| 多言語化 | [[i18n-pipeline]] [[i18n-first-design]] [[i18n-ssot-render-rule]] |
| 運用(ループ・自動化) | `ループ運用ルール.md` |

---

# 検査(壊れたら気づける仕組み)

| 検査 | コマンド | 見るもの |
|---|---|---|
| **SSOT番人** | `node tools/_ssot_guard_test.js` | 消える資産 / 説明なし特性 / 名前の切り詰め / DB間の食い違い |
| バトル回帰 | `node tools/_sim_test.js` / `_sim_hard_interaction_test.js` / `_sim_sweep_all.js` | 825 / 174 / 919 |
| 機構別の仕様テスト | `node tools/_spec_*_test.js` | 12本+(メトロノーム8/8 ほか) |
| 多言語 | `node tools/i18n_audit_playwright.js` | 残日本語0・JSエラー0 |

---

# いまの状態(2026-07-28 時点)

- **全体の順番**: **①内部データの一本化(まず調査) → ②ページを作り変える(参照元を `pokedb.js` へ) → ③バトルを作り直す**
  ★**③は①②が終わるまで凍結**(データが動くと全部やり直しになるため)
- 調査は完了: 配線調査(4,999ページ) / 資産の棚卸し4本(`reference/_inv1〜4_*.json`) / Champions正典コーパス4種
- 道具は完成: **`pokedb.js`**(参照元1枚・実機確認済) / **SSOT番人**
- バトル側の調査結果は保存済み(消さない): `reference/_ch_ability_check_all.json`(特性の不一致90件) / `_phase_design_a1〜a4.json` / `_t11_spec_g1〜g4.json`
