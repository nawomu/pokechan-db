# GLMタスク T8: T7で見つかった実バグ候補の権威裏取り→修正

送信元: claude-design → glm-impl
前提: T7(特殊機構11種テスト)で 134ケース中127pass・fail 7件。うち「実バグ候補」を1件ずつ確定させて直す。
成果物: エンジン修正+全ハーネス緑+報告。**commit/push/git addはしない**(検証・commitはclaude-design)。

## 大原則
- ★**バグ報告自体を疑う**。権威ソース(ポケモンWiki日本 https://wiki.pokemonwiki.com/ / Bulbapedia)を**実際にWebFetchで読んでから**判定。仕様どおりなら直さず理由を報告。
- ★引用は**実在する文字列のみ**(捏造厳禁)。
- ★**Championsは第9世代相当**として扱う(世代差がある仕様は最新世代の挙動が正)。ただしChampions独自仕様が `pokechan_data.js` / `items_database.js` の説明文にあればそちらが優先=必ず原文も読む。
- 変更は最小限・出典コメント必須。既存テストの期待値を変える場合は**必ず報告してから**(自己出力をゴールデンにしない)。

## 対象

### A. ミイラが「なまけ」を上書きできない(real_battle_simulator.html:3335付近)
`ABILITY_CHANGE_NG = [ふしぎなまもり, バトルスイッチ, ばけのかわ, なまけ]` に **なまけ(Truant)** が入っている。
報告: Bulbapedia の Mummy 除外リストに Truant は含まれない=本来は上書きされるはず。
→ 権威で「ミイラが上書きできない特性」の正しいリストを確認し、**なまけを除外リストから外すべきか**を判定。
→ ついでに**リスト全体**(ふしぎなまもり/バトルスイッチ/ばけのかわ)も権威と突き合わせ、過不足を報告(追加すべきものがあれば足す)。
→ 注意: T7の担当は「**ふしぎなまもりの除外はエンジンが正しい**(テスト期待値が過剰)」と判定している。この判定が妥当かも確認すること。

### B. すてゼリフ: ランク低下を全て防がれた時の交代(第7世代以降の仕様)
報告: 「第7世代以降はランク低下を全て防がれた場合は交代しない」がエンジン未反映(常に交代する旧仕様)。
→ 権威(ポケモンWiki「すてゼリフ」の世代別仕様)を実読して確認。第9世代の挙動が「防がれたら交代しない」なら修正。
→ 判定に関わるケース: クリアボディ/しろいきり/ミラーアーマー/まもる/みがわり/相手ひんし。**それぞれで交代するのかしないのか**を権威で確認してから実装する(1つの条件で決めつけない)。

### C. マジックミラー/マジックコートの fail 1件
`tools/_spec_magicmirror_test.js` の fail ケースを読み、権威で期待値の正しさを確認→エンジンのバグなら修正・期待値誤りならテスト側を直して理由を報告。

### D. ばけのかわの fail 1件
同上(`tools/_spec_disguise_test.js` または該当スクリプト)。

### E. きあいのタスキ+がんじょう+こらえるの fail 1件
同上(`tools/_spec_endure_test.js` 等)。

## 検証(全部緑が完了条件・数値を報告)
node tools/_sim_test.js(825/0) / node tools/_sim_sweep_all.js(919/0) / node tools/_sim_hard_interaction_test.js(174/0/skip11) / node tools/_sim_behavior_all.js(flag0)
+ 該当する `tools/_spec_*_test.js` を再実行して当該ケースがpassになること。

## 報告(agmsgで claude-design へ)
①A〜Eそれぞれ: 権威URL+**実在する日本語引用** → 仕様 → 現実装 → 判定(実バグ/仕様どおり/テスト期待値誤り) → 修正内容(file:line)
②4ハーネス+specテストの数値 ③直さなかったものと理由
