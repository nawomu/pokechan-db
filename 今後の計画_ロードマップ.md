# 今後の計画 — ロードマップ

最終更新: 2026-06-23 JST / 起票: 阿部さんの口頭メモを整理

このファイルは「次に何をやるか」の**親ドキュメント**。個別の作業手順は各HANDOFFや専用手順書に分け、ここは**全体像と順番**だけ持つ。各案件に「ループ向きか(記事『Loops explained』の4ボックス判定)」を付記 — **ゲートが客観的=ループ化／ゲートが人間の好み=手動のまま**。

---

## ループ4ボックス判定の早見表(この計画の前提)

ある作業をループ(自動反復)に載せてよいのは**4つ全部YESの時だけ**:
1. そのタスクが繰り返す(週次以上)
2. 悪い出力を**機械が自動で弾ける**(テスト/grep/型/ハードルール)
3. エージェントが最後まで自分でやり切れる
4. 「完了」が**客観的**(好み=tasteなら人間が勝つ)

→ 1つでも外れたら手動プロンプトのまま。「順番」: ①手で1回確実に→②スキル化→③ループで包む(ゲート+停止条件)→④スケジュール。飛ばすと寝てる間に課金事故。

---

## 進捗ログ(案件A・SSOT)

- **2026-06-23**: 監査地図作成 = `review/i18n_leak_audit_2026-06-23.md`(全25ページ・ワークフロー26エージェント)。**漏れは静的HTMLでなく動的JS描画に集中**(I18N.pokemon/move/ability/item/type が存在するのに未使用)が判明。
- ✅ **news.html**: DB参照描画化(第1段の手本)。監査でも dataLeaks=0 で裏付け。残: fx_*効果文8件が7言語未登録(QA待ち)。**未コミット**。
- ✅ **party_checker.html (Phase 1-A)**: 技名→`I18N.move(m.key,m.name)` / 持ち物名→`I18N.item` / 効果→`I18N.itemDesc`。ヘルパー `tMoveName/tItemName/tItemEffect` 追加。主スクリプト構文OK・en変換確認済(はたく→Pound等)。`renderTable()`が言語切替で再描画。**未コミット**。保留: form括弧(通常/メガ)=決定#1, カテゴリ見出し=決定#3, 技説明tooltip=決定#2。
- ✅ **バッチ1(WF5エージェント・commit 6bff183)**: pokemon_db_v9/real_battle/real_battle_simulator/battle_simulator の描画関数の固有名詞をI18N化。items_listは既にDB駆動済。全node --check OK・diffスコープ確認済。
- **★多言語化ルール確定(2026-06-23 阿部さんOK)** = メモリ [[i18n-multilingual-rule]]: 名前/分類/持ち物効果/特性効果=PokeAPI既存9言語を配線・JA効果説明は独自維持。**move説明だけ要翻訳**(PokeAPI move descはフレーバーで競技的仕組みを欠く=独自JAを翻訳して使う・英語版から)。
- **次の波**: ①item/ability効果文の配線(PokeAPIデータ既存) ②40語彙キー(分類/能力名/状態/applies)をPokeAPI/公式語彙で整備 ③move説明の翻訳(英語先行・要確認) ④waza_picker 159タグ ⑤バトルログ391(最後)。

### 2026-06-23 続き(自走で完了したコミット群)
- ✅ 技説明 **498件×9言語** フル多言語化(独自JA出典・PokeAPIフレーバー非採用=競技情報保持)。正本 `i18n/move_desc_own.json`。commit cac3d56/da72ccd/f144086
- ✅ 技説明の**表示配線**: party_checker / pokemon_db waza-tip / sim系 / **waza_picker(waza-list+party_checker両用)**。I18N.moveDesc経由。
- ✅ **共有語彙9言語**: common.cat_*(分類)/stat_*(能力)/status_*(状態)/bdg_*(設置物20)/targets.<ja>(対象13)/items_list.applies(12) + 各ページ配線(WaveB/C・WF)。commit 94ba9a2/f9c5e68/9165a22
- ✅ items_list applies を data-i18n 化。
- **★残り(ブラインド自動化NG・要慎重設計)**: ①**バトルログ391**(lineWithFx正規表現連動=ログ翻訳とセット改修) ②**効果フィルタタグ159**(サブ語彙からテンプレ合成=語順i18n設計) ③軽微: 数値付き複合バッジ(まきびし×N等)・モーダル助詞「の」・○×コンパクト記号・ダメージ計算チップ ④**言語QA**(自動翻訳498×9の品質チェック=過去101件修正と同工程)。

---

## ★土台ルール: SSOT表示原則(多言語漏れを"構造的に"起こさない) 〔2026-06-23 阿部さん指示〕

**新規ページ・新規UIで、マスターDBに在るデータ(ポケモン名・技名・タイプ・特性・持ち物・カテゴリ[物理/特殊/変化]など)の表示文字列を、HTMLに直接書かない。** 必ず**IDで参照し、`runtime.js` の `I18N` 経由でマスターDB(`pokechan_data.js` + `i18n/*.json`)から描画**する。固定文(見出し/ボタン)は従来どおり `data-i18n` + `ui-*.json`。

> 一言で: **「新しくHTMLに日本語の固有名詞を手書きしない。データは常に1つの多言語DBから引く。」**

**なぜ**: 名前/技名/タイプは既に9言語DBが在る。にもかかわらず手書きするから、言語切替で日本語のまま残る(例: `news.html` のポケモンチップ・新技テーブルが全部ベタ書き=英語モードでも日本語)。**引く元を一本化すれば、新規ページでもこの漏れは原理的に発生しない。**

**検証ゲート(ループのチェックに組み込む)**: 新規/改修HTMLに対し「マスターDBに在る固有名詞がハードコード文字列で出てないか」を grep → **0 が合格**。voiced≠complete と同様、"見た目英語になった"でなく"DB経由で描画されてる"が合否。

**境界(でっち上げ禁止)**: このルールは「翻訳を強制」ではなく「**必ずi18n層を通す**」こと。公式外国語名が無いもの(Champions独自=コインビーム等)は `I18N` が **ja fallback** を返す=それで正解。手書きの自由文(効果サマリ「50%ひるみ…」等)も新規に増やさない→技は effects/compose、または専用i18nキーに寄せる。

**適用先**: 案件A(漏れ潰し)の再発防止の土台。F(更新)・G(タグ)にも常時適用。**まず `news.html` を「ベタ書き→DB参照描画」に作り替えるのが最小の実証(Aパイロットの一部)。**

→ 確定したら CLAUDE.md か `新技追加手順.md`「🌐 多言語化」へ昇格を提案(共有docなので阿部さん了解の上で)。

---

## 案件一覧(6本)

### A. 多言語化の実機漏れチェック&修正(全ページ・全mouseover)
- **何を**: 本番(https://pchamdb.com)の公開HTML全ページを、各言語に切替えて**実機で触り**、mouseover/ツールチップ/動的ログ含め英語・日本語残りを全部洗い出し→`ui-*.json`/`i18n/*.json`へ配線→再チェック。全ページ・全項目を一巡。
- **なぜ**: 海外流入があるのに隙間が日本語/英語のまま=信頼を落とす。公開済みの品質直結。
- **現状**: 自動audit(`_ui_sweep_result.json`=922キー)は済。**既知の残**(`次回ここから.md`)= title/meta/JSON-LD・著作権定型文・**バトル中の動的ログ(ポケ名入り)**・一部confirm/alert。今回はこの「自動が拾えない実機の隙間」を人の目で潰す。
- **ループ判定**: ◎ **ループ向き**(①繰返す ②grep/audit_i18n_coverage.pyで機械判定 ③やり切れる ④客観)。ゲート=「未翻訳キー0・英語残0」。ただし**実機mouseoverの発見だけは目視**=半自動(発見は人/AI、潰しと再audit は自動)。
- **★確定リーク(2026-06-23調査済・Aのパイロット最適)**: 技リスト上部の**「🏷 詳細タグ」(`#newTagChips`)は未翻訳**。`waza_picker.js` がeffect-kindから**JSで自動生成**し日本語ラベルを直に吐くため、data-i18n配線の外。静的chip(💚回復のみ 等227キー)は翻訳済=対照的。→ まずここを「手で1ページ確実に直す→スキル化→他ページへ展開」のループ・パイロットにする。**Gと同じkind taxonomyなので、ここを直すとGのグループ名も多言語で出る**。
- **注意/境界**: コンテンツページ(ability/move/pokemon/type 等)は**別セッション担当=触らない・commitしない**(メモリ`content-pages-separate-session`)。これらに漏れがあれば**直さず一覧化して阿部さんに渡す**。でっち上げ翻訳禁止(公式外国語名なしはja fallback維持)。

### B. バトルsimのUIをPokémonShowdown風に寄せる
- **何を**: 今の`real_battle_simulator.html`はクリック数が多く本物とイメージが違う。本家(Pokémon Showdown想定)の対戦画面を**スクショで一画面ずつ撮りながら**、画面展開・操作動線をそっくりに寄せる。
- **なぜ**: 「リアル」を名乗る以上、本物の手触りに近づける。クリック数削減=使いやすさ。
- **ループ判定**: △ **半分だけ**。UXの良し悪し=taste=人間判断(ボックス4で落ちる)→**設計は手動**。ただし「参照スクショ ↔ 自分の画面」の**差分出し**は反復作業=AIに回せる(ゲート=チェックリスト項目の充足)。
- **注意/境界**: Showdownの**画像/CSS/アセットを複製しない**(著作権)。真似るのは**動線・情報配置・クリック数**だけ。simの中身(Phase構造v7・effects)は壊さない=表示層だけ寄せる。

### C. バトル用ポケモン画像(全身・前/後ろ)
- **何を**: 今は顔アイコンだけ。バトルには**自分=背中／相手=正面**の全身像が要る。1体ずつ作成/チェック。
- **★方向性の更新(2026-06-23 阿部さん)**: 「子どもの落書き風」は一旦置く。**最優先は"判別できること"**=今のは原型と違いすぎて絵だけ見て何か分からない。**周りの輪郭(シルエット)の線は元を保持**するのが絶対条件。その上で表現は最小:
  - 候補A: **線画(輪郭中心)＋シンプルな色**(色なしだと判別しづらいので簡単な配色は付ける)。
  - 候補B: **レトロなドット絵風(昔のファミコン/GB風)**。今と違うレトロな味で面白い。色つき or 白黒。
  - 共通の鉄則: **シルエットが元ポケモンと一致して一目で分かる**。落書き感より判別性。
- **なぜ**: バトル画面のリアル感・没入はスプライトで決まる。Bと不可分(本物風の画面には判別できる絵が要る)。
- **ループ判定**: ✗→△ 生成は1体ずつ目視判定=taste=手動(声と同じ=人間の目が最終判定)。ただし「全N体の未作成/要修正リスト」管理と規格(サイズ・前後ろ・透過)チェックは機械化可。
- **⚠️ 要決定(方針転換・未確定)**: メモリ`sim-sprite-svg-pipeline`は「テイスト=子どもの落書きで**確定**(2026-06-12・275体SVG完成)」。今回の方向はこれを**覆す**。①スタイル(候補A/B/ハイブリッド)②既存275 SVGを捨てる/別系統で作る/共存 — を決めてから着手。**勝手に上書きしない**。
- **境界(著作権)**: 公式アートの直トレース複製はNG。シルエット忠実は"判別できる独自絵"の範囲で(サイト自体が非公式ファンDB)。

### D. 構築サポートの簡易版(初心者向け・補完即出し)
- **何を**: 今は pokemon_db_v9 で手持ちを選んで比較ページ(party_checker)へ遷移。これを「**ポケモンを置いたら即・補完候補が出る**」簡易・初心者向けに。少クリックで「次に何を入れるべきか」が分かる。
- **なぜ**: 初心者の入口を広げる=サイトの価値の核。
- **ループ判定**: ✗ 新規UX設計=手動。ただしロジック(補完候補の出し方)が固まればテスト化可。
- **注意**: 既存のparty_checker/比較フローは壊さず**別の簡易導線**として足すのが安全。

### E. スマホ対応(全ページ・レスポンシブ)
- **何を**: 海外/スマホ流入が多い前提で、主要ページをスマホ最優先のレイアウトに。
- **なぜ**: トラフィックの実態に合わせる=全案件の効果を底上げする横断改善。
- **ループ判定**: △ 各ページのレイアウト判定は目視だが、「ビューポート幅でのはみ出し/タップ領域」チェックは機械化可。
- **注意**: 横断作業=巨大。一気にやらず「最重要ページ(index/db/sim)から」段階的に。

### G. 技リストのフィルター/グルーピング強化(物理/特殊の整理 + 機能グループ)
- **何を**: `waza-list.html`(技リスト)のフィルターを強化。
  - (1) **物理/特殊/変化のジャンル分けを前面に**: 今は表ヘッダーの小ドロップダウン(`f-class`)でしか分かれてない → 上部のタブ/チップで物理・特殊・変化を分かりやすく。
  - (2) **機能グループのまとめチェック**: 「回復技」「積み技(剣の舞/蝶の舞など能力上昇)」等を**グループ単位でON** → その技/それを持つポケモンだけ絞り込み。グループのチェックボックスを設ける。
- **なぜ**: 「回復技を持つポケモンだけ」「積み技持ちだけ」を一発で見たい=型作り・補完(案件D)の入口にも直結。
- **現状の素材**: 効果kind(effects)に回復/能力上昇の情報がある=グループの中身は**データから機械抽出可能**。静的chipに「💚回復のみ」等は既存=これを"グループ"概念に昇格する形。
- **ループ判定**: ✗→△ UI設計=taste=手動。ただし「どの技が積み技/回復技か」のグループ定義はkindから自動生成+テストで検証可。
- **注意/関連**: グループの分類体系は**Aの「詳細タグ」と同じkind taxonomy**を使う。→ **Aのタグ多言語化を先にやると、Gのグループ名もそのまま多言語で出せる**(二度手間回避)。「ポケモンを選ぶ」用途は party_checker/pokemon_db とも重なる=どのページに置くか実装時に確認。

### F. サイト更新の継続(ニュース・新要素反映)
- **何を**: 追加要素のニュース掲載、新規追加時の多言語化until8言語(=既存ルール`i18n-pipeline`)。
- **ループ判定**: ◎ 定型=ループ/チェックリスト向き。各リリースに**畳み込む**(独立案件にしない)。

---

## 推奨する順番(なぜこの順か)

> 原則: **「公開済みの品質を直す(守り)」を先に、「新しく作る(攻め)」を後に。** かつ**ループが一番安全に効くもの(A)をパイロットに**して型を作る。

### フェーズ1 — まず A(多言語化の実機漏れ潰し) 〔今すぐ・低リスク・高効果〕
理由: 阿部さんの第一優先。公開中の品質直結。**6案件で唯一いちばんループ向き**(客観ゲート)=ループ運用の練習台に最適。記事の「順番」どおり、まず1ページ手で潰し→手順をスキル化→残りページをループで回す。
- **パイロット第一手 = 技リストの「詳細タグ」多言語化**(確定リーク・`waza_picker.js`)。ここで型を作る。

### フェーズ1.5 — G(技リストのフィルター/グルーピング強化) 〔Aの直後・taxonomy共用〕
理由: Gのグループ分類はAの「詳細タグ」と同じkind taxonomyを使う。**Aでタグを多言語化した直後にGを乗せると、グループ名がそのまま多言語で出る**=二度手間にならない。物理/特殊の整理→回復/積み技グループの順で。

### フェーズ2 — B+C(sim画面のリアル化) 〔設計を先に見せる・要・方針確認〕
理由: BとCは不可分。ただし**Cはスプライト方針転換の決定待ち**、**Bは設計(動線)を先に阿部さんに見せてから**着手(`A案は設計を先に見せる`=メモリ`sim-sprint-complete`の作法)。先にスクショ収集+ワイヤー提案→OK後に実装。

### フェーズ3 — E(スマホ対応) 〔横断・段階的〕
理由: 全案件の効果を底上げするが巨大。フェーズ1-2で主要ページが固まってからの方が手戻りが少ない。index/db/sim から段階導入。

### フェーズ4 — D(構築サポート簡易版) 〔新規価値・腰を据えて〕
理由: 新UX設計で一番"taste"が要る=手動でじっくり。土台(スマホ・sim)が整ってからが活きる。

### 常時 — F(ニュース/更新) を各フェーズのリリースに畳み込む

---

## 着手前に阿部さんに確認したい3点(境界線=勝手に進めない)

1. **スプライト方針(C)**: 「子どもの落書きで確定」を覆して「本物寄り全身・前後ろ」に切替える理解でよいか? 既存275体SVGは破棄/共存/別系統のどれか。
2. **コンテンツページの扱い(A)**: ability/move/pokemon/type 等は「別セッション担当=触らない」。ここに多言語漏れがあったら**直さず一覧化して渡す**でよいか。
3. **順番の合意**: 上記フェーズ1→4でよいか。スマホ(E)を品質面で前倒ししたい等あれば差し替える。

---

## 参考(この計画の土台)
- ループの考え方: 本セッションで読んだ Anatoli Kopadze『Loops explained』+ 各記事 → 要点はCLAUDE.mdの既存原則(`verification-over-production`・北極星・役割モデル)と一致。
- 関連メモリ: `i18n-pipeline` / `content-pages-separate-session` / `sim-sprite-svg-pipeline` / `sim-sprint-complete-2026-06-11` / `verification-over-production`
