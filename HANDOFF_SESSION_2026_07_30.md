# HANDOFF 2026-07-30 ★これだけ読めば再開できる(マスターデータ完成〜引っ越し直前)

★**まずこの順で読む**: ① `CLAUDE.md` の「★★★★全体の順番」と「★★★データの絶対ルール」 → ② `仕様書_サイト全体.md`(入口・6つの軸) → ③ 本書
★前日分= `HANDOFF_SESSION_2026_07_29.md` / `HANDOFF_SESSION_2026_07_28_PART2.md`(必要な時だけ)

---

# 0. 全体の順番(絶対・飛ばさない)

**① 内部データの一本化(マスターデータ) → ② 各ページの参照元を `pokedb.js` に向け替える → ③ バトルを作り直す**

- ★**③の個別修正(特性90件・未実装87件・フェーズ差し込み)は①②が終わるまで凍結**
- ★ただし **エンジンのバグ修正とAIロジックはデータと独立**なので凍結対象外(実際にやった)
- **いまの位置 = ①がほぼ完了。②の直前。**

---

# 1. ★いま何ができているか

## 1-1. マスターデータ `master/`(★①の成果・完成)
生成器 = **`node tools/build_master_v2.js`**(★**旧データを1バイトも変更しない**)
```
master/pokemon.json     1,257体(Champions 313)
master/moves.json         919技(Champions 497)
master/abilities.json     313特性(Champions 201)
master/items.json         169持ち物(Champions 75)
master/learnsets.json     313体(覚える技19,538 / ★没収技10,699)
master/regulations.json   M-B 1件
master/_unknowns.json     ★決められなかった値4件(推測で埋めていない)
```
**中身の設計**
- **器(範囲)= 全国版 / 値の正典 = Champions**(`source` + `verified_at` で由来を記録)
- **`name`(正式名称)+ `display_name`(画面用の短い名前)** の2欄
- `champions`(登場するか)/ `regulation`(現行=**M-B**)/ `confiscated`(★そのポケモンで没収された技)
- **技のキーは slug**(★名前をキーにするとZ技の物理/特殊版が**18件潰れる**=実際に潰れて気づいた)
- ★**コインビームは持ち込まない**(存在しない技)
- ★**技の説明文とeffectsは既存を移送**(作り直さない)

## 1-2. 引っ越しの安全確認(★これが関門)
**`node tools/_master_diff.js`** → **`review/マスターデータ差分.html`**(管理ツールから見られる)

| 項目 | 結果 |
|---|---|
| **消える技** | **コインビーム1件だけ** ✅ |
| **消える特性 / ポケモン / 持ち物** | **すべて0件** ✅ |
| **技が減るポケモン** | **0件** ✅ |
| **種族値の変化** | **0件** ✅ |
| 埋まる穴 | **特性の説明9件**(おうごんのからだ等)/ **学習データが空だった38体**(サーフゴー等) |
| 正典に揃うもの | PP**5件** / 特性の説明文192件 / 持ち物の効果75件 / **名前30件**(★画面表示は不変) |

## 1-3. 管理ツール(阿部さんが見る画面・ローカル専用)
起動 = **`PchamDB管理ツールを開く.command` をダブルクリック**(旧名も互換で動く)
| 画面 | 中身 |
|---|---|
| `管理ツール.html` | 入口。全リンク新規タブ・各ページ右下に **🏠 管理ツール** |
| **`data_browser.html`** | データ一覧(エクセル風・タイプ色・棒グラフ・列の出し入れ)。★**参照元は `pokedb.js` 1枚=引っ越し第1号** |
| `review/マスターデータ差分.html` | ★**何が消えるか・何が変わるか**(引っ越しの安全確認) |
| `review/内部構造仕様書.html` | `node tools/_build_internal_spec.js`。★差分の基準は**阿部さんが最後に確認した時点**(`--reviewed`で更新) |
| `review/AIの仕組み.html` | `node tools/_build_ai_spec.js`。★**数字と行番号はソースから直接抜く** |
| ほか | バトル仕様書 / ルール実装点検 / テスト結果 / データ配線調査 |

## 1-4. エンジン(バトル)
- **commit `db61dce4`(pushはまだ)**: メイカーのフィールド永続 / フォーカスレンズの死にコード / メンタルハーブ3→6状態 / メトロノームの計算副作用+交代持ち越し。エンジン `?v=20260729a`
- **★AIの交代ロジックを強化(未commit・テスト7/7)**: `aiChooseAction` の **C)** + `aiBenchTakes`/`aiHazardCost`/`aiSwitchAffordable`
  - 相性による予防交代(残HP半分以上を持っていかれる & 有効打なし & 後攻)
  - **特性を見る**(ふゆう/もらいび/ちょすい等=無効、あついしぼう/たいねつ=半減。全件Champions権威で裏取り)
  - **★設置物のコスト**(ステルスロック+まきびし。出た瞬間に倒れる/半分以上減る控えには逃がさない)
  - **★先制技で倒せるなら殴る**(確定KOが複数なら優先度の高い技)
  - **★判断理由 `st._aiReason`** を記録(テスト+将来のフェーズビューア用)
- 全ハーネス緑: **sim825/0・hard174/0/skip11・sweep919/0・metronome8/8・AI交代7/7**

## 1-5. 権威コーパス(Champions正典)`reference/_authority_corpus_ch/`
**特性315 / 技497 / 道具75 / 種族値313 / 学習データ442(★うちの313体すべてに `matched_ours` で対応)**
取得元 = ヤックン `/ch/`(★curl不可・**Chromeの同一オリジンfetchのみ**・EUC-JP・タイプ名はimg alt復元)

---

# 2. ★★注意事項(今後のために・全部ここに集約)

## 2-1. データの絶対ルール
1. **データは一つ。絶対に一つ。どのページにもデータは一つ。複数持たない。ページごとに作らない。**
2. **直すのはマスターデータ(`master/`)だけ。** 生成物(`pokechan_data*.js`・生成ページ4,900枚・i18n辞書)は**手で直さない**
3. **ページは参照元1枚 `pokedb.js` だけを読む**(`pokechan_data*.js` の直読み禁止)
4. **値の正典=Champions → 無ければ最新世代**。★**「新しく作った方が正しい」とは限らない**(全国版は後から作ったが**PPは396件が旧世代の値**だった)
5. **名前は正式名称**(略さない)。★名前の不一致は「**機能が到達不能になる実バグ**」を生む(フラエッテナイト/ニャオニクスナイト/いじげんラッシュ/i18n468件=全部同じ型)
6. **作り直しは別で作ってから入れ替える**(旧を残す→全数差分→回帰→昇格→**確認後に旧を削除**)
7. **共通する失敗の根**=「**足りないから、もう一つ作る**」。**足りないときは一つのものを広げる**

## 2-2. ★調べ方の教訓(今回4回やらかした)
1. **★「数が合わない」時は、まず自分の取り方を疑う**
   - 種族値39件差 → **番号だけで突合**した私のミス(名前で照合し直したら**0件差**)
   - learners食い違い17,109件 → **データ構造を確認せず**書いた私のミス(実際は**完全一致**)
   - 権威208件 → **フォームを取り漏らし**(313体あるのに208件=サインは出ていた)
   - サーフゴー「権威に無い」→ **URL番号の取り違え**(下記)
2. **★外部サイトの識別子が、自分のDBの識別子と同じだと仮定しない**
   - ヤックンの `/ch/zukan/n<番号>` は**全国図鑑番号ではない**(**サーフゴー=n977**/カヌチャン=n1000/コノヨザル=n1010)
   - **正解=一覧ページから「名前→URL」の対応表を作る**(1,244件)
3. **突き合わせは「名前」と「番号」の両方**。**比較前に全角→半角の正規化**(`１０まんボルト` vs `10まんボルト`)
4. **★「履歴にあるから正しい」とも決めつけない**(コインビームは「阿部さん依頼で追加」の履歴があったが**実在しない技**)
5. **★「?」つきのメモを、確認せずにデータへ昇格させない**(コインビームの発生源は `_research_mb_signature_moves.js:46` の「専用技あり**?**」)
6. **テストが落ちたら、まず「テストの設定が悪いのでは」を疑う**(AIテスト3件とも私の設定ミス)
7. **★権威の抽出が壊れることがある**(メタモンのページ構造が違い「紫」「第1世代」を技名と誤認)→ **生成器に「抽出失敗の検知」を入れた**(実在の技名が1つも無ければうちのデータを使う)

## 2-3. ★ブラウザで取得する時の罠
1. **https のページから `http://localhost` へは送信できない**(ブラウザが遮断)→ `window.name` に載せて localhost へ遷移してから送る。**送信できたかを必ず確認**(sent=0のまま219件が宙に浮いた)
2. **タブを別ページへ移動させると、ページ内の作業結果が全部消える**(実際に219件失った)→ **取得専用タブを分ける**+**こまめにファイルへ追記保存**
3. **1リクエストごとに150ms程度の間隔**を空ける(礼儀)

## 2-4. ★★GLMの運用(阿部さんの重い指摘・2回)
> 「**言ってんのに動いてないとか、注意しなきゃ動かないってよくないよ。俺が『動いてます』って言われて動いてなかったらダメじゃん。誰が責任取るの?**」

**事故**: Claudeが「GLMを回しています/T20実行中」と**確認せずに報告**。実際は**11時間半止まっていた**(着手すらしていなかった)。

**恒久ルール(`ループ運用ルール.md` に記載済み)**
1. **稼働は必ず機械で確認**: `bash ~/.agents/skills/agmsg/scripts/history.sh pchamdb | grep "glm-impl →" | tail -1`(最終発言の時刻)+**生成物が実際にあるか**
2. ★**言葉を機械の事実に固定する**
   - 「**送信しました**」= agmsgで送った(動作は未確認)
   - 「**動いています**」= **最終発言か生成物で確認できた時だけ**
   - 「**停止中です**」= 一押し2回で無反応
3. **30分無音で一押し → 2回で無反応なら「停止中」と判定**し、**黙って待たず阿部さんに窓の確認を依頼**
4. ★**agmsgは届くが、GLMの窓が「入力待ち/許可待ち」だと読まれない**。**Claudeは別セッションの窓にキー入力できない=ここから復帰させられない**
5. **GLMが止まっている間はClaudeが自分で引き取る**(2026-07-30にT20を引き取って完成させた)
6. **GLM前提のスケジュールを組まない**(止まる前提で、Claudeが引き取れる粒度に切る)
7. **GLMを「待ち」にしない**(ブロックされたら必ず代わりの仕事を渡す=控えの指示書を常に1本用意)

**GLMの起動方法**(阿部さんの操作)
```
新しいターミナル → cd ~/Documents/ポケモンDB → glm
起動したら: 「agmsgのinboxを見て、claude-designからの指示に従って作業して」
```
★**窓は1つだけ**(2つ動くと二重作業とファイルの取り合いが起きる)
★復帰したGLMには**「これだけ読めば分かる」形で新しく指示を送る**(長い履歴を読ませると枠を無駄に食う)

## 2-5. 説明文の作り方(鉄壁のルール)
- **技**: **effects → compose で生成。手で書かない**。順序は①effects(simの土台)→②composeが訳す。**逆は禁止**
  - 実測(2026-07-29): 本番と再生成の一致 **1,411/1,417**・**手書きオーバーレイ0件**・**effects空81件は全て純ダメージ技=実穴なし**
- **特性・持ち物**: **composeを通さず、公式のゲーム内テキストをそのまま流用可**(どのページも同じ=公式情報だから)
  - ★**唯一の注意=世代で変わる・ナーフが入る**。流用時は必ず「**Champions対応の記述か**」を確認(WikiのChampions節 / ヤックンの`/ch/`)
  - 実例: すいほう・たいねつは第九世代で「相手のこうげき・とくこう半減」に変わったが、**Championsは「ダメージ半減」**

## 2-6. 本番とラボの役割分担
| | リアルバトル / オンライン | バトルラボ |
|---|---|---|
| ポケモン | Champions登場のみ(**M-Bフラグ**) | 全国版=全ポケモン |
| 技 | Championsで使える技のみ・**没収技は出さない** | 全技+★**没収技のON/OFFを選べる** |
| 持ち物 | Champions実在75件 | 全部169件 |
| データ | ★**同じ1本のDB。違うのは絞り込み条件だけ** | 同上 |
★**レギュレーションは現行M-B 1本**。過去(M-A)は保持しない。更新されたらフラグを変えるだけ。

---

# 3. ★次にやること(この順)

## 3-1. ②ページの引っ越し(いまここから)
1. **`pokedb.js` に `master/` を読む選択肢を足す**(`?data=v2`)。★**供給元の差し替えはこの1枚の中だけで完結する**設計になっている
2. **二重公開**して実機で確認(現行は壊さない)
3. **1ページずつ**参照元を切り替え → ★**内部構造仕様書の「直読み35 → 0」が進捗計**
4. 生成コンテンツ4,900枚+9言語を**再生成**
5. 安定後に**旧を削除** → ブリッジ(`moveByChampKey`/`pokeByName`)撤去 → **逆流(生成物→SSOT)を断つ**
6. **番人に検査を追加**: 説明文==compose再生成 / オーバーレイ件数 / フラグ列↔POKEMON_WAZA / 名前の正規化

## 3-2. 残っている小さな課題
- `master/_unknowns.json` の**4件**(英語slug=PokeAPI照合が必要)
- ポケモン・特性の `slug` が null(同上)
- **技のフラグ列169を捨てる**(全数証明済み=`POKEMON_WAZA`から導出可能)→ ポケモンDB右側の技列とわざリストが**同じタグを見る**形にする

## 3-3. ③バトル(①②完了後)
- **特性の不一致90件**(`reference/_ch_ability_check_all.json`・Championsでの保有体数順)
- **フェーズ差し込み設計4本**(`reference/_phase_design_a1〜a4.json`): 行動順の再評価 / 優先度補正Event / skip→can段 / 持続状態→常設Handler
- **接地判定 Q01 ground_check**(`設計_フェーズ語彙v3` §2.05・保存フラグにしない・攻撃側も引数)
- **高影響11件の実装仕様**(`reference/_t11_spec_g1〜g4.json`)
- ★設計中に見つかった未修正バグ: `runSingleAttack`と`runTurn`の**二重実装**(こんらん・ひるみが片方に欠落)/ ばけのかわがみがわり・タイプ相性より先に評価 / ちょうはつのPP消費順が権威と逆 / マジックルームを15箇所超が自己申告
- ★**フェーズ・ビューア**(1フェーズずつ止めて見る画面)。`_aiReason` があるので**AIの思考も表示できる**

---

# 4. 🙋 阿部さん判断待ち(たまり)
1. **`db61dce4`(エンジンのバグ4件)を本番pushするか**
2. **未commitの分をcommitするか**: 管理ツール一式 / AI交代ロジック / マスターデータ生成器+`master/`(全緑)
3. **②の引っ越しに進んでよいか**(`?data=v2` の二重公開から)
4. GLM: **7/30が解約予定日**。使い切るか / 解約するか / Proプラン($72/月・使用量5倍)を検討するか

---

# 5. 現在の状態(2026-07-30 08:00頃)
- **commit済み**: `db61dce4`(push待ち)。**未commit**: 管理ツール / AIロジック / `master/` / 生成器 / 各仕様書
- **全ハーネス緑**: sim825/0・hard174/0/skip11・sweep919/0・metronome8/8・AI交代7/7
- **GLM**: 復帰済み(`glm-5.2`)。**T21=`master/`の独立検算**を依頼中(週間74%・5時間枠0%)
- **Claude枠**: 12:00まで(見張りMonitor稼働中)
- **自走ループ**: 25分ごとにGLMの枠と稼働を確認して次を投げる形で稼働

---

# 6. 【追記】GLMの独立検算(T21)の結果と、その修正 — 2026-07-30 08:30

## 6-1. ★GLMが見つけた重大な見落とし(検証役を置く価値の実証)
> **「ニャオニクスナイトがまだ到達不能」**
> `items_database` の `applies_to` が基本名『**ニャオニクス**』なのに、マスターデータの名前はフォーム名
> 『ニャオニクス(オスのすがた)/(メスのすがた)』なので**一致せず、メガシンカできない**。

★**Claudeはフラエッテナイトが直ったことで満足し、隣にある同じ型の問題を確認していなかった。**
→ **教訓: 作った本人は見落とす。「作る側」と「検証する側」を必ず分ける。**

## 6-2. 修正した内容
1. **`applies_to` を実在する名前の配列に展開**(`applies_to_pokemon`)
   - ニャオニクスナイト → `["ニャオニクス(オスのすがた)","ニャオニクス(メスのすがた)","メガニャオニクス♀","メガニャオニクス♂"]`
   - フラエッテナイト → `["フラエッテ(えいえんのはな)"]`
   - → **到達不能なメガストーンは0件になった** ✅
2. ★**`applies_to` が2つの意味で使われていたのを分離**(設計の匂い)
   | 意味 | 例 | 新しい欄 |
   |---|---|---|
   | 対象ポケモン(メガストーン) | ニャオニクス / フラエッテ(えいえんのはな) | **`applies_to_pokemon`**(75件) |
   | 補正の対象(能力・技種別) | physical_attack / speed / sound_moves | **`boosts`**(15件) |
   → **同じ欄に別の意味を入れない**(「まとめない・分ける」の原則)

## 6-3. GLMの検算で確認できたこと(問題なし)
- 件数の妥当性: pokemon 1257 = 全国1219 + Champions独自メガ等38(champions 313 ✓)
- **技のPP/威力/命中 = 0差 / 種族値 = 0差**
- **消失: コインビーム1件のみ**(特性0 / ポケモン0 / 持ち物0)
- 特性313 vs 権威315 の差は「権威に在ってmasterに無い」= **0件**(実質一致)
- 学習データ: `confiscated` が `learn` に混ざっていない

## 6-4. マスターデータの最終状態(2026-07-30 08:30)
```
消える: コインビーム1件だけ ✅ / 消失: 特性0・ポケモン0・持ち物0 ✅
種族値の変化: 0件 ✅ / 技が減るポケモン: 0件 ✅
埋まる穴: 特性の説明9件 / 学習データ38体
到達不能なメガストーン: 0件 ✅(フラエッテ・ニャオニクス両方解決)
```
→ **①内部データの一本化は完了。次は②(`pokedb.js` に `?data=v2` を足して二重公開 → 1ページずつ切り替え)。**


---

# 7. 【追記】GLMは週間枠を使い切った(2026-07-30 08:30)

- **5時間枠 100% / 週間 94%**(リセット= **2026-08-04 15:25**)→ **8/4までGLMは実質使えない**(残り6%)
- T21(master/の独立検算)までは完了。成果= `reference/_verify_master.json`
- ★**今日(7/30)はGLM解約予定日**でもある。判断待ち: 解約 / 継続 / Pro($72月・使用量5倍)
- ★**8/4まではClaudeだけで進める前提**でタスクを切る(GLM前提のスケジュールを組まない=2-4の規律)
- GLMには「新しい作業を始めない・書きかけは元に戻して待機」を送信済み
