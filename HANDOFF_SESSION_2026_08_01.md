# HANDOFF 2026-08-01 ★これだけ読めば再開できる(データの土台を固め切った日)

★**読む順**: ① `CLAUDE.md`「★★★データの絶対ルール」+「★★★新しいページを作る時の絶対手順」 → ② 本書 → ③ 必要なら `HANDOFF_SESSION_2026_07_31.md`

---

# 0. いまの位置

**① データ一本化 = 完了 → ②' データの中身の正常化 = ★今日ほぼ完了 → ② ページの作り直し(屋台骨) = ★次はここ → ③ バトル = 凍結中**

- **push済み・作業ツリー綺麗**(docsコミットまで含め全部 origin/main)
- **master の現在地**: ポケモン **1,256体(全行 英語slugつき・空0)**・技919・特性313・持ち物169・**覚える技 1,256体分**・タイプ18・性格25・**備考25体**
- 全ハーネス緑(sim825/0・SSOT番人・ページ番人・pre-commit hook)

---

# 1. 今日の成果(コミット8本・全push済み)

| コミット | 何をしたか |
|---|---|
| `6a52da51` | **pre-commit番人** — commit時に自動でページ/データの番人が走り、**赤だとcommit自体が失敗**。わざと壊して赤・片付けて緑を確認済み。再導入= `bash tools/_precommit_guard.sh --install` |
| `0f3f1f94` | **世代の空き38件根治**(メガ35+パルデアケンタロス3。図鑑No.→種の世代の控え genFromNo)+**verified_at保全**(中身が変わらない行は前の日付を保つ=再ビルド差分3,062行→本物だけ) |
| `89b2938d` | **英語slug 1,254体を検証つき機械照合**(tools/_match_pokemon_slugs.py → reference/_pokemon_slug_map.json)+**覚える技の孤児30体根治**(learnsetsにNAMEMAP+slug配線) |
| `89137813` | **メガストーン75件「戦闘中1回だけ。」復元**(阿部さん決定「残していい」。入手方法の文は一字も変えず差し込み) |
| `aeeeafc8` | **全国版941体の覚える技**(PokeAPI・LLM不使用) — learn=最新作品/learn_legacy=過去世代のみ(**廃止マーク方式**)。生データ9.4MBはgitignore(再取得可能・tools/_fetch_national_learnsets.js=再開可能) |
| `2ae7f41d` | **名前の正式化一式** — ケンタロス重複統合(**1,257→1,254体**)・英語残骸の日本語化(ヒヒダルマ/ガーメイル/コフキムシ/コフーライ)・**ジガルデ案1**・全角→半角(ポリゴン2等)。裏取り=WF(Sonnet調査→別Sonnetが引用の実在を反証) |
| `fc4d1924` | **★備考欄(note)新設**(25体)+**見た目フォルム2体を忠実に追加**(イキリンコ(ブルーフェザー)/イッカネズミ(3びきかぞく))→ **1,256体** |
| (docs) | 本ハンドオフ+CLAUDE.mdの半角ルール |

---

# 2. ★今日の阿部さん決定(全部実装済み or ルール化済み)

1. **push解禁** — 「そのやり方で保持できるならどんどん進めてプッシュしてコミット」→ 以後、検証緑ごとにpush
2. **持ち物74件の「戦闘中1回」= 残す** → 実装済み
3. **覚える技(全国版)= やる。9世代までに廃止された技も「含めて廃止マーク」**。目的=Champions新ポケモン追加への備え。**追加されたら9世代との差分を記録してから上書き**(champions_diff。★記録の道具はまだ無い=Champions追加が実際に起きた時に作る)
4. **屋台骨の設計承認** — 新版を別フォルダで二重掲載(sitemap外+noindex)→確認後に**旧URLのファイルへ上書き差し替え**(URL不変=SEO無傷)→旧版は`_legacy/`退避→安定後削除。+pre-commit hook
5. **★英数字は半角**(ルール化→CLAUDE.md§4+メモリ hankaku-alphanumeric-rule)
6. **ジガルデ=案1**(素の名前=オーラブレイク個体/「・スワームチェンジ」付き=もう1系統)
7. **★見た目だけのフォルムも「忠実に入れる」+masterに備考欄(note)を絶対入れる**(同日中に「なくていい」→反転して確定。「俺というよりClaudeのため」)→ メモリ cosmetic-form-rows-policy
8. **GLM解約済み・今は使えない**(Kimiウェイトリスト待ち。実装=Sonnet中心)
9. **Fable節約で回す** — WFの中身は全部Sonnet・機械取得はLLMゼロのスクリプト。Fableは設計/検証/声だけ

---

# 3. ★次にやること(この順)

## 3-1. 本丸 = ★屋台骨のページ作り直し(新版1枚目から)
`CLAUDE.md`「新しいページを作る時の絶対手順」(4か条)+承認済みの差し替え設計(§2-4)で:
1. 新版の置き場フォルダ名を決める(前回は `new/` 推し・今回設計では `v2/` と書いた。★最初の1枚を作る時にどちらかに確定して統一)
2. 1枚目の対象を選ぶ(候補=直読み17枚の中から。`reference/_page_ledger.json` と内部構造仕様書を見て、小さく確実なページから)
3. **二重掲載期間は sitemap に入れない+noindex**(SEOを割らない)
4. 進捗計 = 内部構造仕様書の「**直読み 17枚 → 0枚**」

## 3-2. 残っている小さい宿題(データ)
| # | 内容 | 状態 |
|---|---|---|
| ① | 特性のナビ欄混入26件(7/30発見) | 未修正。機械的に除去可・ただし1件ずつ確認 |
| ② | `うなぎのぼり` の説明が空(権威にも無い) | 推測で埋めない。`master/_unknowns.json` の扱い |
| ③ | 権威コーパスの特性35件で「効果」節が空(すながくれ/いかく等) | コーパスの取り直しが要る |
| ④ | 没収技18種(キョダイ系)が名前だけ残存 | 表示上の扱いを決める |
| ⑤ | イキリンコ既存3色の weight_kg が null(青だけ2.4) | PokeAPIから3値写すだけ |
| ⑥ | `master/_unknowns.json` 4件(うなぎのぼり/ほのおのたてがみ/ARシステム英語slug) | 継続 |

## 3-3. 保留(意図的に止めたもの)
- **Champions差分記録の道具**(§2-3)= Championsに実際に新ポケモンが来た時に作る
- **バトル(③)は凍結のまま**

---

# 4. ★注意事項(今日踏んだ罠・次のセッションのために)

1. **ポケモンWikiの実体ドメインは `wiki.pokemonwiki.com`**。`wiki.ポケモン.com` は301・punycode直打ちは失敗・**curlとWebFetchは403のことが多い→ claude-in-chrome(実ブラウザ)経由が確実**。WFの検証役が403で未達でも「調査役の引用が取れて内容が一貫」なら、その旨を明記して使う(誤confirmはしない)
2. **PokeAPIは生きて動く** — プルリル/ブルンゲルが雌雄分割され旧slugが404(照合器に理由つきOVERRIDES済み)。ローカルsnapshot(reference/_old_master/pokeapi_master.json)と現APIはズレ得る
3. **中身の形を決めつけて .join/.length しない**(今日も2回: i18n辞書の値がdict/PokeAPI abilitiesがdict配列)。まず1件printして形を見る
4. **番人・hookの罠**: hookは `.git/hooks` から走る→ cwdは `git rev-parse --show-toplevel` で取る。`J()`はROOT起点=絶対パスを渡すと壊れて**例外が握り潰される**(keepDatesで実踏)
5. **同名の技18種(Zワザ物理/特殊)**が居る→ 技を名前でキーにしない。slugで
6. **阿部さんの決定は同日中に反転することがある**(イッカネズミ「なくていい」→「忠実に入れる」)。**最終決定だけをメモリ化**し、反転履歴も1行残す
7. **再ビルドの正しい手順**: 名前を変えたら **build→ _match_pokemon_slugs.py → build の2パス**(slug対応表は名前キーのため)
8. **枠の計器**: 金曜22:00の週間リセットを跨いだので `tools/_weekly_budget.sh` の校正は**取り直し**
9. 道具の早見(前回分+今日の新顔):
   ```
   node tools/build_master_v2.js         … master再生成(名前替え後は→matcher→再buildの2パス)
   python3 tools/_match_pokemon_slugs.py … slug対応表の再生成(検証つき・OVERRIDES内蔵)
   node tools/_fetch_national_learnsets.js … 覚える技のPokeAPI取得(再開可能・150ms間隔)
   bash tools/_precommit_guard.sh --install … pre-commit番人の再導入(クローン後に必要)
   reference/_pokemon_additions.json     … ポケモン行を手で足す入口(出典必須)
   reference/_pokemon_notes.json         … ★備考欄の元(slugキー・裏取り済みの事実だけ)
   reference/_name_normalize.json        … 名前の正式化変換表(display→official)
   ```

---

# 5. 🙋 阿部さん確認待ち(いま特になし・強いて言えば)

1. 新版ページの置き場フォルダ名(`new/` か `v2/` か。次の作業の最初に確定)
2. data_browser の備考列・追加2体の見え方(Cmd+Shift+R)
