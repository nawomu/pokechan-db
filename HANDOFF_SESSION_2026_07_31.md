# HANDOFF 2026-07-31 ★これだけ読めば再開できる(屋台骨の入れ替えを始めた日)

★**読む順**: ① `CLAUDE.md` の「★★★データの絶対ルール」+「★★★新しいページを作る時の絶対手順」 → ② 本書 → ③ 必要なら `HANDOFF_SESSION_2026_07_30_PART2.md`

---

# 0. いまの位置と、最初にやること

**① 内部データの一本化 = 完了 → ② ページの作り直し = ★今日ここに入った → ③ バトル = 凍結中**

★**最初にやること = `00dc83bb` を push していいか、阿部さんに聞く**(未push。全ハーネス緑・作業ツリー綺麗)。

---

# 1. 今日の結論(1行ずつ)

1. **阿部さんが「屋台骨の総入れ替え」を決めた** — 全ページを新版で作り直し、旧版と二重掲載してから旧を消す
2. **`pokedb.js` を「master だけを読む薄いローダ」に作り替えた** — データの複製を作らない形に是正
3. **`data_browser.html` が master に繋がった** — 313体 → **1,257体**(Champions絞り込みで313)
4. **ページの番人を新設した** — 文章のルールでは止まらないので、**赤くなる物**を作った
5. **紛らわしい旧マスター10本を退避した** — 「一番のマスターって何なの?」の原因を除去

---

# 2. ★★★今日いちばん大事なこと(次のセッションは必ず読む)

## 2-1. 阿部さんの言葉(原文に近い形で残す)
> 「**なんか作る時に、もうパッと新しく作っちゃう。確認しないで作っちゃう癖なんだよね。** 多分そうだよね、Claudeの癖なんだよね。
> でも**プログラムを作る場合って絶対 元を意識しなきゃいけない**んだけど、その意識がいつまでたってもないから、
> やっぱりMDに書いておいた方がいいのかな。**それでも忘れるのかな**。」

> 「**内部構造的に一回整理して作り直したい。屋台骨の総入れ替えというか、骨格を全部統一する。**
> 今の既存のページはちょっと置いといて、**新しくもう別で作り直して全部入れ替える**。今公開されているのは旧版、新しく作るのが新版。
> **一回両方を掲載して、うまくいけば片方(昔の方)を消す。**」

> 「**データベース1本。これはずっと言ってるんですけど、どうしてもできない。**
> Claudeのセッションが入れ替わると全部読んでないということになるので、忘れないようにしておいてください。」

## 2-2. ★「MDに書けば直るか」への答え = **直らない。実測した。**
Claudeは今日 `CLAUDE.md` に「**足りないときは一つのものを広げる**」と**自分で書いた1時間後に**、
`data_browser.html` があるのに `review/マスターデータ確認.html` を**新しく作った**。

→ **文章のルールでは止まらない。赤くなる物だけが止める。**
→ だから **`tools/_page_guard_test.js`(ページの番人)** を作った(§3-4)。

★なぜ止まらないのかの構造(記録しておく):
- **新しく作るのは、確実で早い。**既存を広げるには**読んで理解する**必要があり、しかも**壊すかもしれない**
- つまり「作る」は**報酬が即時で失敗が見えない**、「調べて広げる」は**手間が先で成果が地味**
- これは知識でなく**引力**。既存メモリ [[verification-over-production]]「作る快感 > 確かめる規律」と同じ。

## 2-3. ★「決めつけて .join / .length する」が今日も3回出た
| 決めつけ | 実際 | 結果 |
|---|---|---|
| `items_database.js` の件数=トップのキー数 | 入れ子(`items`配列に169) | 「10件」と報告して自分で訂正 |
| `master/pokemon.json` の件数=トップのキー数 | `{meta,count,champions_count,items}` | 仕様書に**「4件」と表示されていた**(実際1,257) |
| `items.boosts` は配列 | **文字列**(`"physical_attack"`) | 持ち物タブが**描画途中で落ちていた** |
★**中身の形を確かめずに数える・並べる**のをやめる。`asText()` のように**形を決めつけない**書き方にする。

## 2-4. ★機械の出力が「断片」や「まとめ」の時、そこから判定するな(前日からの継続)
実例: `authority_gap` の断片リストを見て「がんじょうの説明から一撃必殺が抜けた」と結論 → **全文には入っていた**。
**必ず全文に戻る。**

---

# 3. 今日やったこと(commit `00dc83bb` ★未push)

## 3-1. `pokedb.js` の作り替え(設計の是正)
阿部さんの指摘:「**別にマスターがあれば pokedb.js って別に。結局は確認用のページのためでしょ。そもそもある必要あんの?**」
→ **正しかった。** 前の版は旧データを読む切替器で、master を読むために **2.2MBの複製(`pokedb_v2.js`)**まで作ろうとしていた=**データが2本に増える**。

```
master/*.json           ←★唯一のデータ。直すのはここだけ
     ↑ fetch
pokedb.js(データを1バイトも持たない薄いローダ)
     ↑
ページ
```
- **非同期**になったので `PokeDB.ready.then(...)` で待つ
- **「Champions版 / 全国版」は別データではなく、同じ1本への絞り込み**(`PokeDB.setMode`)
- ★`pokedb_v2.js` と生成器は**削除した**(持っていると必ず腐る)

## 3-2. `data_browser.html` が master に繋がった
- **1,257体**(Champions絞り込み=313) / 技919 / 特性313 / 持ち物169(絞り込み75)
- **「出どころ / 確認日 / Champions / レギュ」の列を追加**
  = 阿部さん「**バージョンとか、どれがOK、マスターの中で見たい**」
- `review/マスターデータ確認.html` は**削除して一本化**(今日 Claude が増やした分を戻した)

## 3-3. master の穴を塞いだ
`TYPES` / `TYPE_COLORS` / `NATURES` が master に無く、旧 `pokechan_data.js` を読むしかなかった。
→ **`master/types.json`(18) / `master/natures.json`(25)** を追加。★値は作らず**そのまま移送**。
→ **旧データへの最後の依存が消えた。**

## 3-4. ★ページの番人 `tools/_page_guard_test.js`(新設)
```
G1 データ直読み … pokechan_data*/items_database/master を直接読むページが増えていないか
G2 ページ増加   … 手作りページが台帳(reference/_page_ledger.json)より増えていないか
G3 役割の重複   … 同じ仕事のページが2枚ないか
```
- ★**わざと壊して赤くなることを確認済み**(効かない番人は意味がないため)
- 増やすなら台帳に「**なぜ既存を広げられないのか**」を1行書かないと通らない
- 現状の直読み18件は台帳で許容済み(=**新しく増えた時だけ赤くなる**)
- 承認して基準を更新するのは `node tools/_page_guard_test.js --accept`(★阿部さんが承認した時だけ)

## 3-5. 紛らわしい旧マスター10本を退避
`reference/master_pokemon.json`(1365件)/ `pokeapi_master.json`(1302)/ `items_master.json`(2180)… が
`master/` と並んでいて「**どれが本物か分からない**」状態だった。
→ **`reference/_old_master/` へ退避**(★消していない)+ README で「これはマスターではない」と明記。
→ 「マスターらしきもの」**17本 → 7本(本物だけ)**。
→ ★**旧マスターに書き戻す2本**(`_fetch_pokeapi_masters.js` / `_fetch_pokeapi_learnsets.js`)に**止め金**を付けた
  (走らせると片付けたファイルが復活するため。`ALLOW_OLD_MASTER_WRITE=1` を付けた時だけ動く)

## 3-6. `CLAUDE.md` を直した(★毎回読み込まれる場所)
1. **古い記述の訂正**: `SSOT = reference/master_*.json` → **`master/*.json`**(理由つきで明記)
2. **新セクション「★★★新しいページを作る時の絶対手順(例外なし)」**を追加
   ```
   1. データは pokedb.js からしか取らない
   2. ページ専用のデータを新しく作らない。足りなければマスターを広げる
   3. JSON/JSをコピーして持ってこない(コピーした瞬間に2本目が生まれる)
   4. 名前・技名・タイプをHTMLに直書きしない(IDでマスターから引く)
   ```
   ＋ 総入れ替えの進め方(旧版は消さない → 新版を別ファイル → 二重掲載 → 確認後に旧を消す)

## 3-7. 内部構造仕様書を「人が読める」ものにした
- **「★一番のマスターは何か」の階層図**を最上部に(①マスター ②配管 ③旧データ ④ページ)
- ★**「マスター経由」という嘘の表記を削除**(`pokedb.js` はマスターではないし、当時の供給先は旧データだった)
- **ファイル名を全部クリックで開けるように**(`internal_home.js` に入れたので**7ページ全部**に効く)
  ★**存在しないファイルは「⚠ 存在しません(削除検討)」**と表示(記述は消さない=履歴)
- **JSの中身を「人が読める説明」で表示**(★解説は書かない。**ファイル内の日本語コメントを抜き出すだけ**=腐らない)

## 3-8. 直した実バグ
| バグ | 原因 |
|---|---|
| 持ち物タブが描画途中で落ちる | `boosts` を配列と決めつけて `.join`(実際は文字列) |
| 読み込み前にタブを押すと空の結果が残る | 非同期化で生まれた競合 → `boot()` でキャッシュを捨てる |
| `_ssot_guard_test` が参照切れ | 私が旧マスターを動かしたため。★**参照先だけ直し、判定ロジックは1行も変えていない** |
| 仕様書の件数が「4件」 | トップのキー数を数えていた(実際1,257) |
| **地図に穴**: `battle_lab.html` が表から消えていた | `document.write` で変数から組み立てるため `src="..."` に文字列が無い → **直読みは15→17枚が正** |
| 「直読み35枚」にバックアップ19枚が混入 | **生きているページ17枚**が追う数字 |
| `.json` が `.js` で切れる | 正規表現の拡張子の順番 |
| 仕様書HTMLが6.7MBに膨張 | 「先頭80行」で切ったが `pokechan_data.js` は**111行で2.3MB**(1行が巨大)→ 文字数でも切る |

---

# 4. ★次にやること(この順)

## 4-1. すぐ
1. **`00dc83bb` を push していいか聞く**(未push)
2. **画面を見てもらう**: `http://localhost:8000/data_browser.html`(★**Cmd+Shift+R** で強制再読み込み。キャッシュで何度も引っかかった)

## 4-2. 前日からの持ち越し(引っ越しの中身の劣化・未修正)
`reference/_migration_check.json`(353枚のカード・`node tools/_build_migration_check.js` で再生成)に全部載っている。
| # | 内容 | 状態 |
|---|---|---|
| ① | 特性の説明にヤックンの関連リンク欄が混入 26件 | 🔧 機械的に除去できる |
| ② | `うなぎのぼり` が空(権威にも無い) | `_unknowns.json` の4件と同じ扱い |
| ③ | 持ち物74件で「**(戦闘中1回)**」が消え「2000VPで購入」に | ❓ **声の判断待ち**(仕様を残す案を提示済み) |
| ④ | `すながくれ` | ✅ **解決**(Wikiの`効果>バトルでの効果`に「すなあらしのダメージを受けなくなる」と明記。ヤックンが正しい) |
| ⑤ | タマゴ孵化歩数 | ✅ **決定**(Championsに孵化は無い。ただし**括弧して入れておく**) |

## 4-3. 今日見つけた新しい宿題
- ★**全部版944体の「覚える技」データが無い**(`master/learnsets.json` は Champions 313体分だけ)。
  「覚える技」タブで944体が「★データ無し」になる。**取り方を相談してから着手**する
- **権威コーパスの特性35件で「効果」節が空**(すながくれ/いかく/はりきり等)。
  リンク付き箇条書きが取れていない。**仕様の本体が抜けている**ので、コーパスの取り直しが要る
- **没収技18種に対応する技が存在しない**(キョダイ〇〇=ダイマックス技。Championsに無いので正しいが、リストには名前だけ残っている)
- **再ビルドで `verified_at` が919件書き変わる**(日付だけ)。毎回2,990行の差分が出て**本当の変更が埋もれる**
- `master/_unknowns.json` の4件(うなぎのぼり/ほのおのたてがみ/ＡＲシステムの英語slug)
- **ポケモン1,257体の `slug` が全部空**(画面で一目で分かるようになった)

## 4-4. 屋台骨の入れ替え(本番)
`CLAUDE.md`「新しいページを作る時の絶対手順」に従って、**新版を別ファイルで作り、旧版と二重掲載 → 確認後に旧を消す**。
★進捗計 = 内部構造仕様書の「**直読み 17枚 → 0枚**」。

---

# 5. 🙋 阿部さん判断待ち

1. **`00dc83bb` を push していいか**
2. **持ち物74件の書き方**(「メガピクシーにメガシンカさせる。**戦闘中1回だけ**。(2000VPで購入)」の案)
3. **全部版944体の覚える技**をどう集めるか(★大きい作業。取り方の相談から)
4. **新版ページの置き場所**(`new/` にまとめる案 / `_v10` と版を上げる案。★私は前者を勧めます)
5. GLM: 解約 / 継続 / Pro(★週枠は8/4 15:25までほぼ無い)

---

# 6. 現在の状態(2026-07-31 夜)

- **commit `00dc83bb`(未push)** / 作業ツリー綺麗(未commit 0件)
- **全ハーネス緑**: sim 825/0・hard 174/0/skip11・SSOT番人 悪化なし・AI交代 7/7・**ページ番人 緑**
- **枠**: ★**今夜22:00に週間リセット**。跨ぐと `tools/_weekly_budget.sh` の校正(77%=出力4,021,667tok)は**無効**になるので取り直すこと
- **道具の早見**
  ```
  node tools/build_master_v2.js       … master/ を作り直す(★直すのはここの入力側)
  node tools/_master_diff.js          … 旧→master で何が消えるか(全数)
  node tools/_build_migration_check.js… 353枚の確認カード(引用の実在検査つき)
  node tools/_build_internal_spec.js  … 内部構造仕様書(--reviewed で基準更新)
  node tools/_ssot_guard_test.js      … データの番人
  node tools/_page_guard_test.js      … ★ページの番人(--accept で台帳更新)
  bash  tools/_weekly_budget.sh       … 週間枠90%で止まる計器
  ```
