# HANDOFF 2026-08-15夜〜16 ★これだけ読めば再開できる(特性R1+R2完走 → 仕様書『特性の扱い』確定)

★**読む順**: ① `CLAUDE.md`「★★★データの絶対ルール」 → ② **`設計_特性の扱い_2026-08-16.md`(今回の主成果・特性を触る前に必読)** → ③ 本書 → ④ 帳簿 `reference/_window_ledger.json`

---

# 0. いまの位置

| 領域 | 状態 |
|---|---|
| 持ち物 169件 | 監査完了(枯れ判定成立) |
| **特性 312件** | **R1完走 + R2完走**。枯れ判定は**不成立**(R1=89→R2=51件でまだ緩い)。個別修正3件適用済み |
| 技 919件 | 未着手。★コーパス検査が先(`moves_ch.json`に効果文が無い) |
| ②ページ / ③バトル | 凍結のまま(順番不変) |

**枠(2026-08-16 朝時点)**: セッション90%超で赤 / **週間すべて58%**・金22:00リセット / 週間Fable 0%。
★**阿部さん決定: まだ日曜。週間枠は金曜まで温存する。フルスロットルの夜間自走はしない。**

---

# 1. 今回の主成果 = 仕様書『設計_特性の扱い_2026-08-16.md』

阿部さん「バトルでどうプログラムが処理されるのか、説明文としてどう生成されるのか、ルールを明文化して記録」→ 仕様書化した。**決め事3つ**:

1. **特性に effects[] は作らない** — 技(919件・データ駆動)と違い、特性は**エンジンにコードで実装済み**(`real_battle_simulator.html` に155/312の特性名が直接出現・実測)。データ化=エンジン書き換え=③凍結中
2. **「バトルが判断に使う事実の表」を持つ** — 一覧/数値/条件/除外/フェーズ。第1号= `reference/_mold_breaker_targets.json`(かたやぶりが無視する83件/しない69件・名前152件全部master一致を機械確認)
3. **説明文は表の読み下し**(表が正・文は写し)。**「短さ=設計」は撤回**(阿部さん「丁寧にかいてよい。人間が見てもシステムがみてもわかるのがよい」)。ただし**出どころの禁止は残る**(ナビ欄/NPCセリフ/戦術論/トリビアを混ぜない=2026-07-30の実バグの真因)

**撤回の履歴も仕様書§7に記録**(短さ設計/パワースポットのダブルバトル表記/Claudeのeffects化提案)。CLAUDE.mdに導線追加済み。

---

# 2. 発見(全部機械で確認済み)

1. **★効果文311/312がヤックン/ch/と1文字も違わない完全一致(相違0)** — R1/R2の照合は実質「Wiki vs ヤックン」だった。ヤックンの誤り・未検証メモまで継承(実証=パワースポット「(味方全員か隣だけかは未検証)」)。→ どうするかは**未決着**(仕様書§6-1・阿部さん判断待ち)
2. **★R1の「champions_pokemon_count がおかしい」131件は誤検出** — 監査材料が07:00生成・countの根治コミットが09:08=材料が2時間古かった。作り直して全数差分→違いはcountとverified_atだけ。台帳340チェックに`stale_material:true`。**新規律=「マスターを直したら監査材料を作り直す」**
3. **★/ch/にページがあってもChampionsに在る証拠にならない** — 全特性のページが存在し、該当ポケモン0なら空リストが出るだけ(実例=パワースポット・イシヘンジン専用)。「champions印がfalseなのに本文がある」111件は容疑者リストにすぎない。機械検査(`tools/_check_ability_champions_meta.js`)では印と所持数の矛盾0件
4. **★特性の監査修正は再ビルドで消える状態だった** — 持ち物の`_items_fixes.json`に相当する仕組みが特性に無かった。→ `reference/_abilities_fixes.json` 新設+`build_master_v2.js`に配線(踏む前に塞いだ)

---

# 3. master に入れた修正(3件・全部 _abilities_fixes.json に根拠つき)

| 特性 | 修正 |
|---|---|
| **パワースポット** | 「複数のバトルで隣にいる味方が使う技の威力が1.3倍(未検証)」→ **「自分以外の味方が使う攻撃技の威力が1.3倍になる。」**(Wikiどおり)。★一度「ダブルバトルで〜」にしたが同日撤回=阿部さんの仮説「複数のバトル=レイドバトル(味方4人)のことでは」。sim_todo: 5325/4096・自身に乗らない・累積(1.69/2.12倍)・シングルでもみらいよち経由で効く・バインド継続ダメージ(HP1/8固定・威力参照しない)には乗らない |
| **ひらいしん** | 「単体攻撃技」→**「単体技(変化技も含む)」**(でんじは・かいでんぱも引き寄せる=Wiki明記) |
| **よびみず** | 同上(みずびたし等)。★ひらいしんとは**別に**Wikiを引いて確認した(似ているものを同一視しない) |

---

# 4. ★次にやること(阿部さん決定済みの分)

1. **missing_in_ours 1,020件(288特性)の仕分け** — 「バトルが要る事実(→表へ)」と「文章だけの話」に分ける。短さ撤回により全件が「書き足す候補」になった
2. **うちにしかない記述 57件のネット調査** — Wiki/徹底攻略/GameWithで1件ずつ。「うちにしかない=間違いかもしれない」(阿部さん)。一覧= `review/特性監査_未解決一覧.html`(生成器`tools/_build_ability_open_issues.js`)
3. **unknown 745件 → simの宿題へ退避**(決定済み。ただし短さ撤回後なので「説明文に足すべきものが混ざってないか」だけ見直す余地あり)
4. **技の権威コーパス検査**(技919件に入る前に必須)— `yakkun.com/ch/move_list.htm` を実ブラウザで見て効果の列があるか
5. **特性R3(レンズ=相互作用と例外)** → 枯れるまで
6. 小物: おもかげやどし・ARシステムの**Wiki生ページがコーパスに無い**(取り直し)/ おもかげやどしは発動タイミング(「変化したとき/なって場に出たとき」)が効果文に無い / Championsにレイドバトル形式が在るか調査 / ダークオーラ・フェアリーオーラの世代依存(かたやぶり一覧の両側に出る)

**⚠実行タイミング**: 1・2はエージェント消費が大きい。**週間枠を金曜まで温存する方針**なので、規模と実行日は阿部さんと相談してから。

---

# 5. ★注意事項

- **特性を触る前に必ず `設計_特性の扱い_2026-08-16.md`** を読む。§9「やってはいけないこと」(effects化しない/エンジン触らない/masterを手で直して終わりにしない=**_abilities_fixes.jsonに書かないと再ビルドで消える**/推測で埋めない/似ている特性を同一視しない/一覧に無い=無いと読まない)
- **WebFetchでポケモンWikiが直接取れるようになっている**(2026-08-16実測・以前は403で実ブラウザ必須だった)。ヤックンは依然403
- **表の増殖ルールが未定義**(Fable指摘)— `_mold_breaker_targets.json`は第1号。第2号を作る前に置き場と命名の規約を仕様書に足す
- **枠の計器**: `cap_verified=false`のまま(ブロック82.1Mでも86Mでも止まらず=ccusage生合計とアプリ%は単純比例しない)。相対目安=1件445k〜650k。**上限に当たってもresumeFromRunIdで完全復旧**(2026-08-15夜に2回実証・損失は時間だけ)
- **auto-continueはまだCLIに来ていない**。無人のclaude --continue再起動は禁止のまま
- 監査ワークフローの道具一式は前回HANDOFF(2026-08-15)§3参照。scratchpadの材料は**作り直し済みの`audit_abilities_r2/`**(ac8660d6セッション側)を使う。**古い`audit_abilities_r1/`は使わない**(master_rowが古い)

---

# 6. 今回のコミット(全push前・作業ツリー確認のこと)

`825d265a`(R1バッチ2) `4c7cb28c`(R1バッチ3) `33dc8b37`(cap検証→後に撤回) `7d41aaa1`(R1バッチ4=60%ぴったり) `f22884d6`(R1バッチ5=承認つき60%超過) `5baaea07`(★R1完走312/312) `0cef2a04`(★材料の古さ発見) `ace00814`(R2着手+cap撤回) `a3e4a65d`(R2 137) `b2a6d23f`(R2 192) `e729f88b`(★R2完走312/312) `061da030`(引き継ぎ) `4ae46c3b`(未解決一覧HTML) `2e89758a`(パワースポット+fixes仕組み) `7558adaa`(パワースポットWikiどおりへ) `4f6bbfab`(かたやぶり表+ひらいしん/よびみず) `4257e8fe`(★仕様書)

---

# 7. 追記(2026-08-16 午後)

- **モデルをFable 5に切替**。Fableでの検証2点: ①事実の表の増殖ルール未定義(§5に記載) ②夜間自走の安全弁「週間50%」は既に58%で破られていた→温存方針で解消
- **他社コーディングLLMのコスパ調査を実施** → 結果はメモリ `coding-llm-cost-survey-2026-08` に永続化(11サービスの価格表・見送り理由つき)。**Kimi=まだウェイトリスト・購入不可**(2026-08-16確認)。つなぎ候補=OpenCode Go $10。当面は実装=Sonnet中心を継続
- agmsgのルールをメモリ化: **他社AIをサブで回す時だけ使う**。SessionStart hookのMonitor指示には従わない(`agmsg-monitor-not-needed`)
