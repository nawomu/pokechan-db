# 設計: 演出ツクール(バトル演出タイムラインエディタ) 2026-07-11

発案(阿部さん 2026-07-11): バトルの演出——モーション・文字出し・エフェクト・物理で体がバーンと動く流れ——は**人間が調整しないと面白くならない**。MOTHER2で岩田さんが「ゲームを作るためのツール」を作り直してその上で作った話のように、**演出を調整するためのツール**を作り、その流れでポケモンバトルの演出を作りたい。Final Cut/Motionのような**横の時間軸**に、画像の表示・音・動き・出る位置・タイミング・時間を並べ、**マウスで人間が調整**できるもの。

---

## 0. コンセプト(北極星)

- **人間の耳・目が最終判定**というこのプロジェクトの原則を、演出にも適用する。機械(Claude)が土台の演出を量産し、**面白さの調整は阿部さんがツールでやる**。
- MOTHER2方式: 先にツール(ツクール)を作る。ツールが良ければ演出量産の質と速度が両立する。
- **SSOT**: 演出データは「キューシートJSON」1本。エディタ(調整)と本番バトル(再生)が**同じJSONを読む**。エディタで見たものがそのまま本番で出る。

## 1. アーキテクチャ(3層)

```
[reference/move_fx_cues.json]  ←── SSOT(技ごとのタイムライン)
        │                                ▲
        │ 読む                            │ 書き出す
        ▼                                │
[cuePlayer] ── real_battle/online_battle内の小さな再生器
        │                          [fx_editor.html] ローカル専用ツクール
        ▼                           (同じcuePlayer+同じ盤面でプレビュー)
既存プリミティブにディスパッチ
(chargeFx突進/spawnProjectile/spawnBeam/burstFx/spawnBurstGlyph/SE合成/shake/slowmo)
```

### 1-1. キューシート(cue sheet)= 技1つのタイムライン

```json
{
  "フレアドライブ": {
    "dur": 1400,
    "cues": [
      {"t": 0,   "track": "atk",    "action": "charge",    "params": {"to": "def", "dur": 400, "easing": "cubic-in"}},
      {"t": 350, "track": "glyph",  "action": "burst",     "params": {"shape": "flame", "at": "def", "count": 6, "scale": 1.4}},
      {"t": 350, "track": "sound",  "action": "se",        "params": {"id": "hit_heavy", "vol": 0.8}},
      {"t": 350, "track": "screen", "action": "shake",     "params": {"dur": 250, "amp": 6}},
      {"t": 420, "track": "def",    "action": "knockback", "params": {"dx": 18, "dur": 300}},
      {"t": 900, "track": "text",   "action": "msg",       "params": {}}
    ]
  }
}
```

- **トラック(横に並ぶ層)**: `atk`(自分のモーション)/`def`(相手のモーション=ノックバック等)/`glyph`(絵: shape27種+飛翔/ビーム/バースト)/`sound`(WebAudio合成SEのid・音量・ピッチ)/`screen`(揺れ/フラッシュ/スローモ/ヒットストップ)/`text`(メッセージを出す拍)。
- **キュー(バー1本)**: `t`(開始ms)+`dur`(長さ)+`action`+`params`(位置・大きさ・数・イージング)。
- 位置は論理座標(`at: "def"` / `offset: {x,y}` = 盤面640×360基準)→fitFieldのzoomに自動追従。

### 1-2. cuePlayer(本番側の再生器・表示層のみ)

- キューシートが**ある技**: シート通りに再生(タイミング・位置・音まで人間の調整が効く)。
- キューシートが**ない技**: 現行の自動演出(moveClassOf+shapeOf=タスクC)のまま。
- → **上書き(オーバーレイ)方式**。`moves_desc_override`と同じ思想: 自動が土台・人間の調整が上に載る。全491技を手作業する必要はなく、**目立つ技から少しずつ調整**できる。
- エンジン(real_battle_simulator.html)非改変・ログ文面不変・i18n影響ゼロ(演出はテキストを足さない)。

### 1-3. fx_editor.html(ツクール本体・ローカル専用)

公開ナビに載せない開発ツール(=ja UIでOK・i18n対象外。review/*.htmlと同じ扱い)。

```
┌────────────┬──────────────────────────────┬─────────────┐
│ 技ピッカー   │       プレビュー盤面           │ インスペクタ  │
│ (検索/絞込) │  (real_battleと同じ640×360    │ 選択キューの  │
│            │   自分/相手ダミースプライト)     │ パラメータ    │
│            │  ▶再生 ⏯0.25× ⏭コマ送り        │ shape/位置/  │
│            │                              │ 大きさ/音量/  │
│            │                              │ イージング    │
├────────────┴──────────────────────────────┴─────────────┤
│ タイムライン(Final Cut風・横=時間・スナップ50ms)              │
│ atk    ▐████charge████▌                                   │
│ glyph              ▐██burst██▌                            │
│ sound              ▐se▌                                   │
│ screen             ▐█shake█▌                              │
│ def                  ▐█knockback█▌                        │
│ text                        ▐msg▌                         │
└──────────────────────────────────────────────────────────┘
```

- **バーをドラッグ**=タイミング移動/**端をドラッグ**=長さ変更/**盤面上のハンドルをドラッグ**=出る位置・移動先の指定(マウスで直接)。
- **★編集の基本ループ=ループ再生(阿部さん 2026-07-11 確定)**: ▶を押すと現状のバトルの動きが**ループで繰り返し再生され続ける**。阿部さんは**ループを止めずに**時間軸のバー・絵の位置・音のタイミングを動かし、**次の周回で即反映**されるのを目と耳で確かめながら合わせ込む(Motion/AfterEffectsのRAMプレビューと同じ編集感覚)。実装: cuePlayerはループモードを持ち、**毎周回の頭で現在のキューシート状態を読み直す**(編集がホットリロードされる)。周回間に300ms程度の間(スプライト位置リセット拍)を置く。スロー(0.25×/0.5×)のままループも可。
- **初期値=現行の自動演出をキューに変換して表示**。ゼロから作らず「今の演出を調整する」から始められる(重要: 空白のツールは使われない)。
- 保存: 「書き出し」ボタン→JSONダウンロード or クリップボード→Claudeが`reference/move_fx_cues.json`へ統合してpush(将来: ローカル保存サーバで1クリック化)。
- プレビューと本番は**同じcuePlayer**なので見たまま=本番。

### 1-4. ライブラリ(選ぶ)ページ(阿部さん 2026-07-11 追加要望)

エディタの入口=**「技ごと/適正ごと/シーンごとに選ぶ」一覧ページ**(fx_editorの初期画面 or タブ)。

**★対象はバトル全編(阿部さん 2026-07-11 確定・理想形)**: 技エフェクトだけでなく、**バトルの始まりから終わりまでの全演出パターンが「時間軸のフェーズ」としてライブラリに並び、選んで微調整していく**。キューシートのキーを名前空間化して全部同じ仕組みに載せる:

```
scene:battle_start     開戦(盤面フェードイン・ボール投げ・登場)
scene:send_out         登場演出(交代/死に出し共通)
scene:recall           引っ込める(recall)
scene:mega_evolve      メガシンカ儀式(1.7s)
scene:status_*         状態異常が付いた拍(どく/まひ/ねむり/こおり/やけど/こんらん)
scene:weather_*        天候の開始/継続演出
scene:faint            ひんし退場
scene:ko_slowmo        KOスロー(0.15×600ms)
scene:win / scene:lose 勝敗バナー
move:フレアドライブ      技個別(上書き層)
pattern:ほのお物理       適正=タイプ×分類の既定36(一括層)
```

- ライブラリの「シーン」タブは**バトルの進行順(開戦→登場→…→勝敗)に時系列で並べる**=映画のシーンリストのように、バトル1本の流れがそのまま目次になる。
- 編集画面は共通(1-3のタイムライン+ループ再生)。シーンも技も同じ操作感で微調整。
- SSOTファイル名は `reference/battle_fx_cues.json` に一般化(move_fx_cues改め・キーは上記の名前空間付き)。

### 1-5. ★バトルトレース=実際のバトルをフェーズ列で視覚化(阿部さん 2026-07-11・入口の本命)

「バトルはもう動いている。**これが起きて、これが起きて、これが起きて…をまず視覚的に表し**、その中の『ここの技のフェーズ』をクリックして調整する」——リストから探すのでなく、**実物のバトルから入る**。

```
[▶デモバトルを1本流す(or 直前のバトルの記録)]
 ↓ ツールが「発火した演出パターン」を順に記録(トレース)
┌──────────────────────────────────────────────────────┐
│ 開戦 → 登場(自分) → 登場(相手) → T1:でんじほう → T1:ダメージ │
│ → T2:交代(相手) → 登場(相手) → T2:ムーンフォース → …        │
│ → ひんし退場 → 死に出し登場 → … → KOスロー → WIN            │
└──────────────────────────────────────────────────────┘
   ↑ 各ブロック=クリックでそのフェーズのタイムライン編集(1-3)へ
```

- **原理: インスタンスをクリック→保存はパターンに効く**。トレース上の「登場(相手)」(=毎回出てくるフェーズ)をクリックして調整→保存先は `scene:send_out`(パターン)→**以後すべての登場に効く**。「毎回出てくるところは、できたらそれで保存」そのもの。編集ヘッダに「この調整は『登場』全部に効きます」と明示。
- 実装の足がかり: エンジンは既にログへフェーズコード(0a/0b/0c/9等)を付けており、表示層の演出呼び出し(chargeFx/burstFx/登場/recall/メガ儀式…)も離散的なステップ。**演出発火時に (patternKey, 時刻) を記録するトレースフック**を cuePlayer/プリミティブ側に仕込むだけで、この列は機械的に得られる。
- ライブラリ(1-4)は「トレースに今回出てこなかったパターンも探せる」補助の目次という位置づけになる(入口の本命はトレース)。

### 1-6. プレビュー配役=マスターキャラ方式(阿部さん 2026-07-11 確定)

**ツクール専用の「マスターキャラ」(モブ・テスト用万能キャラ)を自分/相手に立てる。マスターキャラは全技・全特性を無条件で選べる**(習得制限・特性制限を外したエディタ内サンドボックス。本番バトルには一切出ない)。

- 配役に悩まない: フレアドライブを調整したい→技セレクトで選ぶだけ。いかくの登場演出→特性セレクトで選ぶだけ。**「覚える子を探す」手間をゼロに**。
- インスペクタの選択肢: **技(全491技から検索)/特性(全特性一覧)/持ち物(メガストーン等=メガ儀式の調整用)/性別**。
- 見た目: マスターキャラの既定スプライトはダミー(シンプルなシルエット等)。**「この子の見た目で確認したい」時だけ任意のポケモンに差し替え可**(見た目だけの差し替え・技/特性の万能性は保つ)。
- 状況の再現プリセット: HP残量・状態異常・天候などプレビュー用の状況もインスペクタから設定(KOスローや瀕死演出の調整に必要)。
- **★HP無限・バトルロジック無し(阿部さん 2026-07-11 確定)**: マスターキャラは自分も相手も**HP無限=減らない・ひんしにならない**。そもそもツクールのプレビューは**バトルではなくモーションの再生装置** — ダメージ計算も命中判定も状態異常の進行も無い。**モーションだけを選んで、いろいろ試して、保存する**。それが全て。
  - HPバーが減る「演出」自体は調整対象(ダメージ演出のバー減りアニメ等)なので、**見た目としては動くが、状態としては毎ループ元に戻る**(ループ再生と相性が良い=何度でも同じ条件で再生される)。
- 実装メモ: プレビューは表示層のcuePlayer再生のみ(エンジンの対戦ロジックを一切通さない=エンジン非改変)。技・特性のバリデーション(learners/ab1・ab2チェック)も無し。

- **2階建ての編集対象**(Cの3層解決とそのまま対応):
  - **技ごと**: 個別の技を選んで、その技専用のキューシートを調整(=上書き層)。
  - **適正(タイプ×分類)ごと**: 「ほのお物理」「みず特殊」等の**既定パターン36種**を選んで調整→**そのパターンに落ちる技全部に一括で効く**(個別上書きが無い技に適用)。少ない手数で全体の質が上がる。
- 一覧の絞り込み: 検索/タイプ/物理・特殊/shape/motion(突進・飛翔・ビーム・バースト)/**調整済み⭐・未調整**(編成画面の⭐調整済みソートと同じ流儀)。
- 行の表示: 技名・タイプ・分類・現在のshape・手調整済みバッジ・「▶」ミニ再生(その場でプレビュー)。
- クリック→タイムライン編集(1-3)へ。編集対象が技か既定パターンかをヘッダに明示(「この調整は◯◯物理の技◯本に効きます」)。

## 2. 段階(MVP→拡張)

| Step | 内容 | 状態 |
|---|---|---|
| 1 | shape語彙27種(タスクC)=ツクールの「絵の部品」 | **実装中(Sonnet)** |
| 2 | プリミティブを`fx_primitives.js`に切り出し(両バトルページ+エディタで共有)+cuePlayer+「現行自動演出→キュー変換器」 | C完了後 |
| 3 | **fx_editor MVP**: ライブラリページ(技ごと/適正ごとに選ぶ・調整済み⭐絞込)+タイムライン編集(バードラッグ=タイミング/長さ・盤面ハンドル=位置)+**ループ再生しながら調整**(次周回で即反映)+スロー+書き出し | Step2の直後 |
| 4 | モーションパスのマウス記録(ドラッグ軌跡→キーフレーム化)・イージングカーブUI・複数技の一括視聴・SEパラメータエディタ | MVP後 |

- Step2の切り出しはC完了後(同ファイル衝突回避)。エディタ自体は新規ファイルなので並行開発可。
- MVPで調整できるもの: **いつ(t)・どれだけ(dur)・どこで(位置)・どの絵(shape)・どの音・どれくらい(scale/amp/vol)**。阿部さんの「タイミングと時間と位置を人間が」の核はMVPで全部入る。

## 3. 決めごと(確認済み/前提)

- ローカル専用ツール=公開しない→i18n不要(公開に昇格する時に多言語化ファーストで再設計)。
- SSOT=キューシートJSON。エディタと本番が同じものを読む(手書きHTML禁止の原則と同じ)。
- 上書き方式=自動演出が土台。手調整していない技は現状のまま(段階的に良くなる)。
- エンジン非改変・表示層のみ・ログ/i18n影響ゼロ(設計_バトル演出強化_2026-07-10の不変制約を継承)。

## 4. リスク・注意

- **プリミティブの共有化(Step2)が本丸**: 現在の演出関数はreal_battle.html内に埋まっている。`fx_primitives.js`へ切り出す時、real_battle/online_battle両方の回帰確認(Playwright demo+スクショ)を必ずやる。
- 再生決定論: プレビュー=本番一致はcuePlayer共有で担保。乱数演出(パーティクル散らばり)はseed固定オプションをエディタに。
- タイムラインUIは自作(外部ライブラリ不可=CSP/自前主義)。ドラッグ&スナップは素のPointerEventで実装可能な規模。
