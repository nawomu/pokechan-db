# バトルエンジン設計の原理(一度引いて全体を見る)

作成: 2026-07-26 JST / 発案: 阿部さん「**フェーズを足せば全てが成り立つ。細かく分けて分けまくってフェーズを足す。そうすれば絶対に間違わない**」
位置づけ: **これが土台**。`設計_フェーズ語彙v3_2026-07-26.md`(箱の一覧)と `設計_割り込みと分岐の規律_2026-07-26.md`(箱の間の飛び方)は、本書から導かれる。

---

## 1. 原理(阿部さんの発見)

> **困ったらフェーズを足す。まとめない。ごまかさない。楽をしない。分けすぎて問題になることはない。**

**なぜ正しいか**:
- **分割は情報を失わない。統合は失う。** 2つの処理を別の箱に置けば「どちらが先か」が必ず明示される。同じ箱に入れると順序が曖昧になり、**後から読む人(次のセッションのAI含む)が勝手に解釈する**=バグの発生源。
- うちで起きた再発バグは、ほぼ全部「箱が足りず、性質の違うものが同居していた」ことに帰着する。
  - どくびしT3 = 「設置」と「その場が効く」が同じ箱 → 自分の宣言が自分のガードを叩いた
  - 交代入場特性の全不発 = 「開幕の登場」と「ターン中の交代」が別扱いだった
  - さいせいりょく/しぜんかいふくが分類不能 = **「退場時」という箱が無かった**(2026-07-26に判明)
  - こんがりボディの誤配置 = 「ダメージ0」と「技が当たらなかった」が同じ箱

**分けすぎのコスト**: 箱が増えて読むのが大変になるだけ。**バグにはならない**。一方まとめるコストは**バグそのもの**。→ 迷ったら必ず分ける。

## 2. ただし「足りているか」を人間の気づきに頼らない(本書の追加提案)

原理には1つだけ弱点がある: **「足す」は気づいた時にしかできない**。今日 さいせいりょく で穴に気づけたのは偶然に近い。300の特性・900の技・169の持ち物に対して、気づきだけで完全性は保証できない。

→ **フェーズを手で列挙するのをやめ、"生成規則"から導く**。そうすれば「足りない」を**機械が検出できる**。

### 生成規則

> **フェーズ = 「状態」×「それが変わりうるタイミング」の交差点**

バトルとは**状態が変化していく過程**でしかない。だから:
1. **状態を全部数える**(有限・数えられる)
2. **時間の骨格を決める**(ターン開始 → 行動ループ → ターン終了)
3. **1と2の交差点が、そのままフェーズになる**

この形なら「フェーズが足りない」は **「まだ名前のついていない状態変化が存在する」** と同義になり、**機械で検出できる**(§5)。

## 3. 層(全体像)

```
【層0】状態(State) ── 何が存在し、何が変わりうるか。有限・全列挙可能
   ↓ 交差
【層1】時間骨格(Timeline) ── ターン開始 → 行動ループ(繰り返し) → ターン終了
   ↓ 生成
【層2】フェーズ(Phase) ── 状態×タイミング。全順序を持つ。いくら細かくてもよい
   ↓ 各フェーズの内部は必ず
【層3】3段(可否 can → 実行 do → 反応 react)
   ↓ ここに
【層4】機構(特性309/持ち物169/技kind140)が Handler として登録される
   ↓ 登録は
【層5】P00(バトル開始前の登録フェーズ)で1回だけ行う
   ↓ 実行時
【層6】エンジン = フェーズを順に回して、登録されたHandlerを発火するだけ
```

**完成条件**: 層6が「フェーズを順に回すだけ」になり、エンジンに特例のif文が1つも要らなくなった時。

## 4. 層0=状態の全列挙(実測・2026-07-26時点)

`real_battle_simulator.html` の実データから確認。**約50フィールド/個体**。これが「変わりうるもの」の全部であり、**この一覧が閉じていれば、フェーズの一覧も閉じる**。

| 分類 | 状態の例 | 寿命 |
|---|---|---|
| 個体の数値 | currentHp / rank(6能力) / statOverride / critBoost | per_switch(HPは個体持ち) |
| 個体の識別 | typeOverride / フォルム / megaBase / へんしん | 個体持ち(★交代しても残る) |
| 状態異常 | status(どく/まひ/やけど/ねむり/こおり) / sleepTurns | 個体持ち |
| 一時状態 | confusion / minimized / rooted / aquaRing / magnetRise / flashFire / chargeBoost | per_switch |
| 技の制約 | disable / encore / tormented / imprison / soundBlocked / lastMove / usedMoveNames | per_switch |
| 累積 | stockpile / protectStreak / rampage / slips / subHp | per_switch |
| 予約(未来) | futureSight / wish / healingWish / あくび / ほろびのうた | 予約先による |
| 持ち物 | item / lastConsumedItem / 消費済みフラグ | 個体持ち |
| サイド | tailwindTurns / 壁 / 設置物 / healingWish / megaUsed | per_battle_field |
| 場 | weather / field / room / weatherTurns / fieldTurns | per_battle_field |
| ターン制御 | switchChoice / switchedThisTurn / enduring / subAbsorbed | per_turn |

→ **新しい機構を実装する時、この表に無い状態を触るなら、それは「状態の追加」であり、同時に「フェーズの追加」が必要**というサインになる。

## 5. 完全性を機械で測る(これが「絶対に間違わない」の実体)

原理を保証に変えるための3つの検査。**人間の気づきに頼らない**。

1. **写像の全数検査**: 特性309・持ち物169・技kind140 の**すべて**が、1つ以上のフェーズに割り当てられているか。未割り当て=**穴リスト**として出る(今日の「パーキングエリア」がまさにこれ)。
2. **状態の被覆検査**: 層0の各状態について「これを書き換えるフェーズが存在するか」。存在しない=**名前のない状態変化**=フェーズ不足。
3. **順序の明示検査**: 全フェーズが**全順序**(誰が先か一意)を持つか。同順位が2つあったら、それは分割漏れ。

**この3つが全部通った時だけ「絶対に間違わない」が成立する。** 通っていない箇所は「未確認」として正直に残す(埋めない)。

## 6. 分割の唯一の注意点(分けすぎの害ではなく、分け方の作法)

分けること自体に害は無い。ただし**分けたら必ず順番を明示する**こと。

- ✗ 悪い分け方: 3つに分けたが「どれが先か」を書かない → 実装者ごとに順序が変わる
- ✓ 良い分け方: 3つに分け、**番号で全順序を与える**(P15c → P15c2 → P15d のように)
- **順序そのものが仕様である段**は特に明記する(天候の後勝ち=遅い方有利 / メガの速い順=情報が漏れる / スリップの速い順=勝敗が変わる)

## 7. 新しい機構が来た時の手順(迷わないための決定手続き)

1. **何の状態を変えるか?**(層0の表を引く) → 無ければ状態を追加+寿命を宣言
2. **いつ変えるか?**(層1の骨格のどこか) → 該当フェーズが無ければ**フェーズを足す**
3. **止める側 / 実行 / 反応する側のどれか?**(層3の3段のどれか)
4. **P00に登録を1行足す**(エンジン本体は触らない)
5. **寿命に応じてundoスナップショット対象に入れる**
6. 権威ソースで裏取り。分からなければ**「未確認」と書いて止める**(埋めない)

→ この6ステップで置き場が**一意に決まる**。決まらない = 語彙の穴 = **足すべきフェーズが見つかった**、という設計です。

## 8. 進め方(既存の緑を壊さない)

- 一括書き直し禁止。**パイプライン単位で増分**。各段階で全ハーネス(sim825/0・sweep919/0・hard174/0・behavior flag0・lab)をゲートにする。
- ✅第1歩(2026-07-26完了・commit 13ba5206): ランク変化を名前つきEvent+Handler 9個に。挙動完全維持で全緑。
- 第2歩: ディスパッチャに①発生源タグ ②深さ上限 ④再入禁止(→`設計_割り込みと分岐の規律`)
- 第3歩: P00(登録フェーズ)の新設。持ち物から着手すると効果が見えやすい(オレンのみ/メンタルハーブ等)
- 第4歩: 決定的順序を全体適用(メガ順・スリップ順のgap修正)。**オンライン対戦E2Eを必須ゲートに**
- 第5歩以降: 語彙v3のSectionを1つずつEvent化

## 9. この原理が効く範囲(ダブルバトルまで見据えて)

阿部さん「複雑になればなるほど、ダブルバトルもそうだけど、フェーズを足す。技に合わせてフェーズを足していく」

- **ダブル対応 = 新しいエンジンではなく、フェーズと対象選択の追加**で済む(層6が「順に回すだけ」になっていれば)。
- **新シーズン/新技の追加 = P00に登録を足すだけ**(CEDEC記事「ハンドラー登録を省略すれば自然に除外される」と同じ)。Champions季節制・全部版切替もこの仕組みに乗る。
- 逆に言えば、**今この土台を作らないと、ダブルバトルは今の特例if文の上にさらに特例を積むことになる**。

---

# 第II部: バトル設計の憲法(2026-07-28 追記・今後の全追加作業はここに従う)

> この部は「今後、技・ポケモン・特性・持ち物・ルールが増えるたびに、どう追加するか」の恒久手順。
> **なぜの部分を消すな**。理由を失った規則は次のセッションで必ず破られる(実証済み)。

## 10. 今日までに確定した設計判断の全リスト(なぜ付き)

| 判断 | なぜ |
|---|---|
| **困ったらフェーズを足す。まとめない** | 分割は情報を失わない・統合は失う。再発バグは全部「箱不足で異質なものが同居」に帰着(どくびし/交代入場特性/さいせいりょく/こんがりボディで実証) |
| **正式キー=英語スラグ・番号は飾り** | フェーズを足すたび番号は崩れる。名前ならどこに挿しても既存データが壊れない(P15a2のような継ぎ接ぎが実際に発生した) |
| **フェーズ=「状態」×「変わりうるタイミング」の交差点** | 手で列挙すると気づいた分しか足せない。生成規則なら「足りない」を機械検出できる(PPが状態表に無い→S12が要る、を実際に検出) |
| **1ターン=開始→行動ループ(繰り返し)→終了** | 直線に描くと交代技/死に出しで場が入れ替わる現実を表現できない(盤v2の最大ミス) |
| **共有Sectionは can→(modify)→do→react の段構成** | 「可否」「量の補正」「実行」「反応」は別物。reactが無いとポーリング(itemReactions)という代用品が生まれ、取りこぼす |
| **登録3層: 選出時一括登録→変化イベントで差分再登録→発火時はライブ所持確認** | 毎ターン全機構総なめは無駄で、焼き込みははたきおとす/トリックで嘘になる。登録は「今の構成の純関数」なのでラボの無限編集とも両立 |
| **登録対象=特性+持ち物+技(4×6/8)** | 技は使用時kind解決が基本だが、他人の技に反応する系(おどりこ/ふういん)は見張りフラグが要る |
| **順序そのものが仕様の段がある** | メガ=速い順(先メガ=速さがバレる)・天候=後勝ち(遅い方が有利)・終了スリップ=速い順(勝敗が変わる)。まとめると順序が消える |
| **乱数は「開始時一括」と「そのつど」を区別** | ねむり/あばれ/連続回数は発動時に内部確定が実機準拠。毎ターン再抽選は分布が変わる。先読み値はundo対象 |
| **状態には寿命キーを必ず宣言** | per_switch と per_battle_individual の取り違えが壊す(ばけのかわ/イルカマン)。undoスナップショット対象は寿命で機械決定 |
| **割り込み=即時再帰+5安全装置(発生源タグ/深さ上限/決定的順序/再入禁止/本流復帰)** | ロックステップ(オンライン)とundoがあるため「非決定的な即時再帰」は許されない。どくびし自爆は発生源タグで構造的に消える |
| **野生戦専用=対象外・ダブル用=語彙に置き場だけ作る** | 対象外と未確認を混ぜると「調べ忘れ」に見える。ダブルは後から作り直すより今キーを切る方が安い |
| **エージェントの報告は検証パスが潰すまで事実にしない** | 誤報2件(持ち物フラグ/いわはこび)・出典捏造9件超を実際に検出。作る側だけなら全部通っていた |

## 11. ★追加プレイブック(新しいXが来たらこうする)

### A. 新しい技が来たら
1. 権威ソース(ポケモンWiki日本/徹底攻略)を**一字一句**読む(「〜のとき」「〜する直前」が箱を決める)
2. effects(kind)で表現できるか確認 → できるなら既存kindで登録(データだけで完結・エンジン改変なし)
3. 表現できないメカニズムなら: **新kind+必要なら新フェーズ**をスキーマ→語彙v3→エンジンに足す(**説明文だけ手書きで済ませるのは禁止**=simが動かない偽の完成・[[effects-sim-phase-first]])
4. 6ステップ(原理§7)で属性を決める: 何の状態を変えるか/いつ/3段のどれか/P00登録/寿命/乱数タイミング
5. sim_test にケース追加(期待値は権威由来・自己出力ゴールデン禁止)→全ハーネス緑→`?v` bump→push

### B. 新しい特性・持ち物が来たら
1. 権威を読む → **発火フェーズ(スラグ)を割り当てる**(データに書く。エンジンに特性名のif文を書くのは過渡期のみ・最終形はP00登録)
2. 持ち物は**動的前提**(はたきおとす/トリックで変わる)=発火時ライブ所持確認を忘れない
3. 分類データ(`reference/_phase_assign/`)に追記 → 置き場が無ければ**それは新フェーズ発見の合図**(遠慮なく語彙に足す)
4. Championsロスターの誰かが持つなら実装必須(未実装=本番で無言の不発。2026-07-28に63件発覚した轍)

### C. 新しいポケモン/フォルムが来たら
1. フォルム変化の**トリガーと寿命**を確認(イルカマン=退場で変身・per_battle_individual/ばけのかわ=被弾・per_battle_individual)
2. 名前照合は**完全名**(フォルムラベル単体は事故る=いじげんラッシュT4/i18n468件の轍)
3. per_battle_individual の状態は**undoスナップショット対象に追加**
4. 変身でしか出ない姿は初期選出不可([[transform-forms-not-startable]])

### D. 新ルール(新シーズン/ダイマックス等/ダブル)が来たら
1. **Handler登録の省略=自動除外**を使う(CEDEC準拠・季節制/版切替はP00の登録表を差し替えるだけにする)
2. ダブル: 新エンジンではなく**フェーズと対象選択(target_lock)の追加**で対応(語彙に置き場は確保済み)
3. ターン構造に手を入れる変更は必ず: 実機リファレンス更新→語彙v3更新→増分実装→全ハーネス+オンラインE2E

### E. どの場合も不変の順序
**権威を読む → データ(語彙/分類)を先に直す → エンジンは増分 → テスト緑 → push**。逆順(エンジンに特例if→後付けデータ)は監査D(2026-07-23)が特定した再発バグ製造機そのもの。

## 12. 完成の定義(再掲・変わらない)
①全機構(特性/持ち物/技kind)が1つ以上のフェーズに割り当て済み ②全状態に書き換えフェーズが存在 ③全フェーズが全順序を持つ — **3検査が機械で緑**になり、エンジンが「フェーズを順に回すだけ」になった時。

## 13. ★説明文(compose)への影響 — 流れは不変・ただし1点だけ連動が要る(2026-07-28 阿部さんの問い)

### 流れは変わらない(むしろ今日の変更で正当化された)
**権威(ヤックン/徹底攻略) → effects(sim用データ) → compose(訳す) → 独自の日本語** は不変。
CLAUDE.mdの「effectsはsim前提・composeは訳すだけ」「composeが賢く織り込みたくなったら**phase設計が足りないサイン**」= 今日やった「フェーズを足す」そのもの。基準docも不変: `review/rules.html`(書き方)・`ヤックン耳_判断ログ.md`(具体例)・`わざ説明文_開通手順.md`(手順)・確認は`review/waza_list_confirm.html`。

### ただし1点: composeは旧5値phaseを直読みしている(将来の地雷)
`tools/_waza_compose.js` は `e.phase === 'lasting' && e.trigger === 'turn_end'` 等、**旧5値(on_use/lasting/this_turn/delayed/turn_end)を10箇所以上で直接読んで**日本語を分岐している(L101/112/196/200-201/249-251/262/460/477-479 等)。
- **現時点は無害**: 今日の分類結果は `reference/_phase_assign/` の別ファイルにあり、技データ `battle_data.effects[].phase` は旧5値のまま=composeは正常動作。
- **★危険なのは将来**: エンジンのP00登録化に合わせて**技データのphaseをスラグへ置き換える時、その瞬間にcomposeが全部誤訳する**(「毎ターン回復」が出なくなる等)。
- **規則: 技データのphaseをスラグ化するコミットでは、必ず同じコミットで `_waza_compose.js` の読み替えを行い、`review/waza_list_confirm.html` で意味照合(ヤックン耳テスト)まで通す。** 段階移行するなら `phase_slug` を新設して旧`phase`を残し、composeは旧を読み続ける(=非破壊移行)方が安全。

### むしろ説明文は良くなる(細粒度の恩恵)
- 新語彙は旧5値より細かいので、**推測でなく確定で訳せる**(例: `end_turn_move_sustain` なら「毎ターン終了時に回復」が確定。今は lasting+trigger の組み合わせから推測している)。
- 今日 kind140 に付いた **`fire_level`** も訳語に効く: `per-hit`→「**ヒットするたびに**」/`per-move`→「その技で1回」。
- **`rng_timing`=開始時一括のものは説明文に出さないのが正しい**(ねむりの残りターン等は内部確定値=プレイヤーには見えない。「何ターン眠るかはわからない」が正しい訳)。
- → スラグ移行の際は、この3情報(phase/fire_level/rng_timing)を使って **compose を書き直す好機**。ヤックン耳テスト(意味が戻るか・声がやさしいか・機械が漏れてないか・legacyコピーでないか)は不変の合否基準。

### 13.1 ★スラグ化と同時にやる「説明文の全数再照合」(2026-07-28 阿部さん指示・必須工程)

技データの `phase` をスラグ化したら、**その回で必ず説明文919件を全数照合する**。サンプリング禁止([[full-check-beats-sampling]]=系統バグは全数でないと出ない)。

**手順(3者照合)**
1. **スラグ化の前に**、現行composeの出力を全技ぶんスナップショットして保存(`reference/_desc_before_slug.json`)。これが比較の基準になる(=**sim自己出力をゴールデンにするのとは違う**。ここで見るのは「意図せず変わった箇所」の検出であって、正しさの判定ではない)。
2. スラグ化+compose読み替えを実施 → 全技ぶん再生成(`_desc_after_slug.json`)。
3. **3者を並べる**: ①旧compose出力 ②新compose出力 ③**`description_legacy`(ヤックンの意味)**。
   - ①=②(差分なし) → 素通り(ただし**②が③に意味で戻れるか**は別問題。元から穴があった技はここでは見つからない)
   - ①≠② → **全件を人が見る**。改善(細粒度で正確になった)か、退化(訳が消えた/誤訳)かを判定。
   - **①=②でも、kind に新フェーズ(`charge_execute`/`move_call`/`multi_hit_count` 等)が付いた技は必ず目視**=旧5値では表現できず**元から訳が落ちていた**可能性が高い場所。
4. **合否はヤックン耳テスト**(不変): ①意味が戻るか ②声がやさしい子ども口調か ③機械が漏れてないか(true/0.125/英語/キー名) ④legacyのverbatimコピーになっていないか。**Claudeは★→✓に上げない**(耳の最終判定は阿部さん)。
5. 確認の場は `review/waza_list_confirm.html`(効果カテゴリー別)。**差分があった技を先頭に集める専用ビュー**を作ると人の目が効率化する。

**チェックの観点(今回の設計変更で新たに増えた分)**
- `fire_level=per-hit` の技: 訳に「**ヒットするたびに**」が入っているか(旧5値では表現できなかった)。
- `rng_timing=開始時一括`: **内部確定値は説明文に出さない**のが正しい(ねむりの残りターン等)。「何ターン眠るかはわからない」が正しい訳。
- 共有Section(`.can/.modify/.do/.react`)を経由する効果: 「〜されると」「〜したとき」の**主体と順序**が訳に出ているか。
- `対象外(野生戦専用)` の効果: 説明文に**バトル外の話を混ぜない**(ものひろい等)。

**規模の見積り**: 919技×3者。差分が出るのは phase を実際に読んでいる分岐(回復/設置/遅延/急所/必中まわり)に集中するはずなので、実作業は**差分技のみ数十〜百件台**の見込み。全数の機械比較はスクリプトで安く回せる。

## 14. ★特殊機構の洗い出し方(機械で「危ないもの」を先に見つける・2026-07-28 阿部さん)

阿部さん「**明らかに変わったやつ、本当にヤバいやつ**=交代技・入れ替え・場を変える・特性を変える。**ダブルも注意**」
汎用的な機構は機械処理でほぼ間違わない。危険なのは①**オンリーワン**(どこにも分類できない) ②**「ただし」の例外** ③**ダブル特有の挙動**。この3つを**機械で先に炙り出してから**、そこだけ丁寧にやる。

### 検出方法(いずれも機械で可能・実測済み)
1. **オンリーワン検出**: effect kindの技数が1件=61種(+2件が20種)。「他に類似がない」は定義上機械で分かる。イリュージョン型はここに出る。→ **分類をあきらめて1件ずつ原文を読む対象**。
2. **★ルールページ名指し検出(最も効く)**: **例外は技ページでなくルールページに書かれている**。実例=ドラゴンアローの「2体攻撃でも威力が落ちない」は**ダブルバトルのページ**にあった。ルールページ(ダブルバトル/ダメージ/連続攻撃/命中/急所/まもる/天気/フィールド/状態異常…)を取得し、**そこに名指しされた技・特性を索引化**する=「どのルールの例外か」まで分かる。実測: ダブル170技・連続攻撃58技+特性38・ダメージ89技+特性33。
3. **状態書き換え×ダブルの交差=最危険19件**(2026-07-28実測): いえき/うつしえ/コアパニッシャー(特性変更)・トリック/やきつくす/ふしょくガス(持ち物)・コートチェンジ/**サイドチェンジ**(入れ替え)・グラスフィールド/ミストフィールド/どろあそび/みずあそび(場)・**ワイドガード/トリックガード/たたみがえし**(範囲技防御)・**ワイドフォース/テラクラスター**(範囲変更)・シンクロノイズ/プラズマシャワー。
   - ★**サイドチェンジ**は「味方と位置を入れ替える」=**隊列の位置という状態が層0の表に存在しない**。ダブル対応時に状態の追加が必要(既に分類作業で起票済み)。
4. **例外語検出**(補助): 効果欄に「ただし/例外/〜のみ/〜を除く/落ちない/変化しない」等 → 336/900件。単独では絞れないので2と併用する。

### 運用
**全数を機械処理 → 上記1〜3でフラグ → フラグ分だけ丁寧に精査 → さらに無作為50件で誤り率を推定**。
「機械が自信満々に一貫して間違える」型は検査で出ないので、**無作為抽出が唯一の防波堤**(目についたものを選ぶと見やすいものばかり選んでしまう)。

## 15. ★条件つきで挙動が変わる技(2026-07-28 阿部さん指摘・最も危険な型)

阿部さん「**相手が先制技を出したら自分も先制技で返す**とか、条件がついてくる場合。**この場合はこう、それ以外だったらこう**、というのが注意点」

**問題**: うちのデータは `priority: 1` のように**単一の値**しか持てない。条件つきの技は**どちらかが必ず嘘**になる。
- 実例: **じんらい**(相手が攻撃技を選んでいる時だけ先制)=kindはあるが**effectsにphaseキーすら無い**(分類作業で起票済み)。**グラススライダー**(グラスフィールド中だけ+1)=`条件付き優先`として既に特別扱い。**いかりのこな/このゆびとまれ**=権威と優先度が食い違い(世代差)。
- 現状は**エンジンに特例ifを書いて逃げている** → 憲法§1「箱が足りないから特例が生える」そのもの。

**あるべき形**: 値を「条件つきの値」で持てるようにする。
```
priority: { base: 0, when: "相手が攻撃技を選択している", value: 1, source: "権威引用" }
```
同様の「条件つき」は優先度以外にもある: 威力(条件威力13種)・命中(天候必中)・タイプ(しぜんのちから/テラクラスター)・対象(ワイドフォース=フィールド中は範囲拡大)・分類(不定=Zワザ)。
→ **全部同じ形(base + when + value)で持てるはず**。個別に特例を足さない。

**やること(順序)**: ①コーパスから「条件つき」の技を全数抽出(「〜のとき」「〜中は」「〜だと」+数値変化のパターン) ②表現形式を決める ③データに入れる ④エンジンの特例ifを、この宣言を読む共通処理へ寄せる。

## 16. ★公開ページ(waza-list.html)の見直しは"バトル確定後"に(2026-07-28 阿部さん)

- 公開の技一覧 https://pchamdb.com/waza-list.html は既に **対象(全対象/ダブル専用)・接触(接○/接×)・まもる(守○/守×)・優先度(+5〜-7)** でフィルタしており、**今回バトル側で正した属性と同じデータを表示している**(データ源=`pokechan_data.js`)。
- **2026-07-28 実測: Champions版(公開が読む側)は手キュレーションされており「自分/場が対象なのに protect:true」は0件=公開ページは無事**。今回の系統バグは**全部版(ビルダー自動生成側)のみ**だった。
- **予定**: バトル(フェーズ設計・対象・条件つき値)が確定したら、**waza-listのタグ付け・フィルター・グループ分けを最終見直し**する。既存の[[national-moves-grouping-todo]](全国版445技のグループ分けは暫定)もここで正式化する。
- **逆に参考にする**: waza-listの既存グループ分け/フィルター軸は「人が実際に技を探す時の切り口」なので、**フェーズ語彙や辞書の分類の妥当性チェックに使える**。説明文チェック用の `review/waza_list_confirm.html`(効果カテゴリー別)も同様に、辞書の分類と突き合わせる材料になる。

## 17. ★条件分岐(if)は2種類ある — 「値が変わる」と「流れが変わる」(2026-07-28 阿部さん)

阿部さん「条件付きっていうのはif文だよね。**ifが入るとフェーズからずれてくる**。違うフェーズに移行したりするから、下流だったり上流だったり中間だったり、**別フェーズに流れていく**という考え方も必要。**if条件分岐が入っているものは注意**」

§15(条件つき値)を一段深めた話。**同じ「条件つき」でも、危険度が全く違う2種類がある**。

### 型A: 値条件(同じフェーズ内で値だけ変わる)= 安全
- 例: もうか(HP1/3以下なら攻撃×1.5)/テクニシャン(威力60以下なら×1.5)/こんじょう(状態異常なら×1.5)
- 表現: `{ base, when, value, source }`(§15)。**フェーズは1つのまま**。
- 検証も簡単(条件の真偽で値が2通りになるだけ)。

### 型B: フロー条件(条件が成立すると**別のフェーズへ流れる**)= ★危険
- **上流へ**: じんらい/グラススライダー(条件成立で**優先度が変わる**=行動順の決定そのものに影響)
- **横へ差し込む**: とんぼがえり/すてゼリフ(ダメージ後に**交代パイプライン**〈退場→入場→設置物→登場特性〉へ飛ぶ)
- **飛ばす**: ばけのかわ(剥がれていなければ**ダメージ計算を丸ごと飛ばす**)/まもる(攻撃パイプライン全体をスキップ)/みがわり(**ダメージの宛先が変わり追加効果が起きない**)
- **呼び出す**: じゃくてんほけん/だっしゅつパック(被弾を引き金に**ランク変化/強制交代のSectionを呼ぶ**)
- **回数・対象が変わる**: ドラゴンアロー(相手が2体なら**各1回+範囲補正の対象外**)/ワイドフォース(フィールド中は**対象範囲が広がる**)

**型Bは「この機構はこのフェーズ」と1つ書いた時点で嘘になる。** データが**飛び先**を持っていないと、必ずエンジンに特例ifが生える(=憲法§1の再発源)。

### 表現(案・第2段で確定させる)
```
flow: { when: "<条件>", goto: "<フェーズのスラグ>", mode: "skip|insert|call|reorder", source: "<権威引用>" }
```
- `skip`=以降を飛ばす / `insert`=別パイプラインを途中に差し込む / `call`=共有Sectionを呼んで戻る / `reorder`=上流の順序決定に影響
- **型Bは必ず`設計_割り込みと分岐の規律_2026-07-26.md`の5安全装置の対象**(発生源タグ/深さ上限/決定的順序/再入禁止/本流復帰保証)。飛び先があるということは**カスケードが起きるということ**だから。

### 機械での洗い出し(実測 2026-07-28)
- **複数フェーズを持つ機構=198件**(特性108/持ち物10/技kind80)、**共有Sectionを呼ぶ=299件**。
- ただしこの中には**型Bでないもの**が混ざる: 「同じ処理が複数の場所から呼ばれるだけ」(例: 状態付与は5経路から呼ばれるが、どこから呼ばれても**やることは同じ**)。
- **→ 次の作業: 198件を「型A/型B/単に多経路」に3分類する**。型Bだけが flow 宣言と5安全装置を要する。
- 判別の目安: **条件によって『次にどこへ行くか』が変わるなら型B**。条件によって『値だけ』が変わるなら型A。呼ばれる場所が複数でも毎回同じ場所へ戻るなら多経路(安全)。

## 18. ★CEDEC図の再読で分かったこと — 型Bはほぼ「Section呼び出し」1種類に畳める(2026-07-28)

§17(条件分岐の型A/型B)を持った状態でCEDECのスライドを読み直したら、初回に見落としていた構造が見えた。

### 発見1: CEDECのEventは2種類ある(=型A/型Bと完全に一致)
「割り込み/連鎖(せいでんき+クラボのみ)」の図をよく見ると:
| Eventの種類 | 図の例 | Handlerがすること |
|---|---|---|
| **補正Event** | 攻撃力補正Event ← 天気「晴れ」Handler | **値を変える**だけ(=§17の型A) |
| **リアクションEvent** | リアクションEvent ← せいでんきHandler / 状態異常Event ← クラボのみHandler | **別のSectionを呼ぶ**(=§17の型B) |
→ ゲーフリも最初からこの2つを分けている。**うちの型A/型Bの区別は独立に同じ結論に着いた**。

### 発見2: Handlerの箱の中にSectionが入っている
黄色いHandlerの箱の内側に「状態異常付与」「状態異常回復」という**Section**が描かれている。記事原文=「**イベントハンドラーには、任意のセクションを呼び出す機能も持たせている**」。
→ **型Bの実体は「HandlerがSectionを呼ぶ」ことだけ**。特別な仕組みではない。

### 発見3: 「状態異常付与」Sectionが図に2回出る=多経路は正常
1回目=技の追加効果(やけど)/2回目=せいでんきHandler内(まひ)。**同じSectionが別の呼び出し元から使われる**=§17で「安全な多経路」と呼んだもの。**Sectionは再利用可能な部品**なので、誰から呼ばれても同じ動きをするのが正しい。

### ★発見4(最重要): skip と reorder はCEDEC図に出てこない=Eventで扱う話ではない
- **skip系**(まもる/ばけのかわ/みがわり=以降を飛ばす)は Event ではなく、**Sectionの「可否判定(can段)」**として持つのが正しい。うちの3段(can/do/react)の第1段がそれ。
- **reorder系**(じんらい/グラススライダー=優先度が変わる)は、**行動順決定Sectionが「優先度補正Event」を持てば、ただの型A(値)になる**。
  → **じんらいが型Bに見えたのは、うちに「優先度補正Event」が無いから**。正しい場所にEventを置けば上流へ飛ぶ必要が消える。
- **insert系**(とんぼがえり=交代パイプラインへ差し込む)も、結局**Handlerが交代Sectionを呼ぶ**=型B-callと同じ。

### 結論(設計が1段シンプルになった)
```
型A(値)      = 補正Event。Handlerは値を返すだけ。フェーズは動かない。
             ※「優先度補正Event」を新設すれば じんらい系もここに入る
型B(呼び出し) = リアクションEvent → Handlerが任意のSectionを呼ぶ。カスケードの本体。
             ※5安全装置(発生源タグ/深さ上限/決定的順序/再入禁止/本流復帰)の対象
skip        = Eventではない。Sectionのcan段(可否判定)で表す。
```
**→ §17で4つ(reorder/insert/skip/call)に見えたモードは、実質「補正Event」「Section呼び出し」「can段」の3つに畳める。**
やること: ①**優先度補正Eventの新設**(語彙v3の priority_sort に補正Eventを持たせる) ②skip系をcan段へ寄せる ③198件の3分類(§17)はこの基準で行う。

## 19. ★時間軸は2本ある — 持続状態は「常設Handler」(2026-07-28 阿部さん)

阿部さん「**軸のズレというか、別ラインでの動き**みたいなのが他にもあちこち入ってくる。**そこで回る**みたいな。例えばトリックルームとか。**何ターンだけ何かが変わる**とかは注意した方がいい」

§18で「優先度補正Eventを作れば じんらい は型Aになる」と分かったが、**これは一例で、同じ形が場・状態のすべてに当てはまる**。

### 2本の時間軸
```
軸1: 行動の流れ   ターン開始 → 行動ループ(繰り返し) → ターン終了
軸2: 状態の寿命   5 → 4 → 3 → 2 → 1 → 消滅(ターン終了フェーズで減る)
                  ↑ 軸1とは独立に回っている
        この2本が交差する点 = Event
```

### 実測(2026-07-28): 持続ターンを持つ場の状態は6種類、すべて特例ifで実装されている
`weatherTurns / fieldTurns / screenTurns / tailwindTurns / gravityTurns / trickRoomTurns`
- **トリックルームの実装** = `real_battle_simulator.html:2341` の
  `const selfFaster = env.trickRoom ? (s1 < s2) : (s1 > s2);`
  → **行動順の計算式に直接if文が埋め込まれている**。新しい場の技が来るたびに、また別の場所にifが増える構造。

### あるべき形: 持続状態は「常設Handler」として、差し込み先を宣言する
```
トリックルーム: { lifetime: 5ターン, hooks:[ 行動順決定Event ], effect: 速度比較を反転 }
おいかぜ:      { lifetime: 4ターン, hooks:[ すばやさ算出Event ], effect: ×2 }
リフレクター:  { lifetime: 5ターン, hooks:[ 防御補正Event ],    effect: 物理×0.5 }
天候(晴れ):    { lifetime: 5ターン, hooks:[ 威力補正Event, ターン終了Event ], ... }
じゅうりょく:  { lifetime: 5ターン, hooks:[ 命中補正Event, 接地判定Event ], ... }
```
- 語彙v3の**場・状態レーン(F01〜F07)で既に「状態そのものがHandlerを持つ」と書いた**もの。その具体形がこれ。
- **登録はP00(バトル開始前)ではなく、状態が発生した瞬間**(場の設置Section=`field_set.do`)。消滅は`end_turn_field_count`で寿命が尽きた時に**登録解除**。
- ★これで「新しい場の技が来たら宣言を1行足すだけ」になる。エンジンに if を足さない。

### 注意点(阿部さん指摘の核心)
- **「何ターンだけ変わる」ものは、変わっている間だけ別の計算になる**=テストが素通りしやすい(展開していない状態でしかテストしていないと気づかない)。
  → **持続状態は「展開中/非展開」の両方でテストする**ことを規約にする。
- **軸2で回るものは undo/オンライン対戦に直結**(残りターン数は per_battle_field の状態=スナップショット対象・§3.5)。
- 個体の持続状態(こんらん/ちょうはつ/バインド/アンコール/でんじふゆう等)も**同じ構造**(軸2で回る・特定Eventに差し込む)。場だけの話ではない。

### やること
①持続状態6種+個体の持続状態を**常設Handlerの宣言形式**に整理 ②差し込み先のEvent(すばやさ算出/行動順決定/命中補正/威力補正/防御補正/接地判定)を語彙v3に不足なく用意 ③トリックルームのif(L2341)を第一号として宣言駆動へ移す(§8の増分リファクタの手順で・全ハーネス緑をゲートに)。

## 20. ★「個体の中身が丸ごと差し替わる」機構(2026-07-28 阿部さん・第4の軸)

阿部さん「イリュージョンとか、メタモンとか、**自分の何かが変わる**のもイレギュラーな動き。**こだわりスカーフを持たせると同じ強さで素早さだけ速いから、同じ対面で必ず勝てない**」

フェーズ(いつ)・対象(誰に)・条件(どんな時)とは**軸が違う第4の型**。**個体そのものの中身が入れ替わる**。

### 権威で確定した仕様(コーパス `abilities/かわりもの.json`・`abilities/イリュージョン.json` より)
**へんしん/かわりもの がコピーするもの**: 見た目・タイプ・**特性**・覚えている技・**HP以外のステータス実数値**・**相手のランク補正**・たかさ/おもさ・性別
**コピーしないもの**: 種族 / **HP(現在値・最大値とも)** / レベル / 性格・個体値 / なつき度 / **持ち物** / 状態異常 / ほとんどの状態変化 / **技のPP(最大PP1の技のみ1/1、それ以外は5/5)** / 専用アイテムの効果(ふといホネ等) / めざめるパワーのタイプ・威力
- ★**この「コピーしない例外リスト」が仕様の本体**。1つ間違うと戦術が成立しなくなる。
- **こだわりスカーフ・メタモンが必ず先手を取れる理由**: 実数値はコピーするので素の素早さは同値 → **持ち物はコピーしないのでスカーフの×1.5だけが乗る** → 必ず先手。加えて**相手のランク補正もコピー**するので、積んだ相手ほど返り討ちになる。

**イリュージョンの化ける基準**(今日まで未確認だった箇所・確定):
- **場に出る直前の時点で、手持ちの一番後ろにいるひんしでないポケモン**に化ける。
- 先発なら**選出時に一番最後に選んだポケモン**が対象。
- **場にいる間に最後尾が変わっても、交代しない限り対象は変わらない**。
- 自分が最後尾なら**化けずにそのまま出る**。
- 解除=**他のポケモンから攻撃技のダメージを受けた時**。

### 設計上の扱い
```
入場フェーズ(switch_in / entry_ability)で、個体の中身が丸ごと差し替わる
   ↓ 以後の全フェーズは「差し替わった後の値」で動く
   ↓ 寿命 = per_switch(場を離れると元に戻る)
```
- **語彙の置き場**: `type_change`(S06)/`ability_change`(S07)より広い=**個体そのものの差し替え**。専用のSection(仮 `identity_swap`)を検討する。
- 該当機構: へんしん(技)/かわりもの・イリュージョン・トレース(特性の一部)/へんげんじざい・リベロ(タイプ)/メガシンカ・フォルムチェンジ(種族値)/ばけのかわ・イルカマン(見た目と実数値)。
- ★**undoスナップショット対象**(§3.5)。「差し替わる前の自分」を保持しないと巻き戻せない(既に `megaBase` で同型の処理をしている)。
- ★**テストの注意**: コピー**する**ものだけ確認しても足りない。**コピーしない側(持ち物・HP・PP・状態異常)を明示的に検証する**テストが必要(スカーフメタモンの成立条件そのもの)。

## 21. ★★正典は「Champions」— SVでも他世代でもない(2026-07-28 阿部さん・以後不変の大原則)

阿部さん「**チャンピオンズベースでやりましょう、うちは。チャンピオンズに全てルールを合わせて**」
「今後を見越して**全部のポケモンを裏では入れておいて**、いつ公式で実装されても対応できるようにしておく」
「面白いのが、**ラボではChampions以外のポケモンでもバトルできる・遊べる・試せる**というところ」

### 三層の方針(これがうちの独自性)
| 層 | 方針 |
|---|---|
| **ルール(仕様)** | **Champions準拠が正典**。権威に「Champions」欄があればそれが正。SV/他世代の値は採用しない |
| **データ(ポケモン/技/特性/持ち物)** | **全部持つ**(全部版)。公式が実装した時にすぐ対応できる状態にしておく |
| **遊べる範囲** | 本番=Champions実装分 / **ラボ=全部で遊べる**(Champions外のポケモンも試せる=うちの面白さ) |

### ★権威コーパスに「Champions」記述が410ページある(2026-07-28実測)
ルール7 / 技58 / **特性200** / 持ち物145。**ポケモンWikiはChampions固有の仕様を独立して記載している**。
うちは今までSV基準で実装してきた箇所があり、**既に2件の食い違いを確認**:

| 項目 | うちの実装(SV基準) | **Champions(正)** | 出典 |
|---|---|---|---|
| **まひ** | 行動不能25% | **12.5%**(すばやさ1/2は同じ) | rules/まひ「Champions \| 12.5% \| 1/2」+説明文「Champions 12.5%の確率で技が使えない。」 |
| **ねむり** | カウント2〜4の等確率(`2+rand(3)`) | **カウント2〜3**・**2が1/3、3が2/3** | rules/ねむり「Champions \| 2〜3 \| 1〜2」「Championsではカウント2になる確率が1/3、カウント3になる確率が2/3」 |

他にも: **はたきおとす**(SVでは落とせない状況でもChampionsは落とせる)/**さめはだ**(Championsは特性バーのみでメッセージ無し・技の発動判定が異なる)/**きんちょうかん**(Ver.1.0.3の挙動が明記)など。

### やること
1. **410ページからChampions固有の記述を全数抽出**し、うちの実装と全数照合(機械+精査)。→ まひ/ねむりと同型の食い違いが他にもあるはず。
2. 以後、**権威を読む時は必ず「Champions」欄の有無を先に確認**する。無ければSV/最新世代を採用(これまで通り)。
3. **バージョン差(Ver.1.0.2 / 1.0.3 等)にも言及がある** → どのVerを基準にするかは阿部さん判断が要る場合あり(基本は最新)。

## 22. ★世代とバトルシステムの整理 — テラスタル/ダイマックスは「実装しない」(2026-07-28 阿部さん)

### 前提: Championsは本編の世代とは別のゲーム
- **本編(正当な世代)**: ゲームボーイ → DS/3DS(ウルトラサン・ムーン等) → Switch(ソード・シールド/**スカーレット・バイオレット**)。
- **Champions は別枠**=**バトルだけをやるゲーム**。世代番号の系列に乗らない。
- したがって「第九世代の仕様」は**参考にはなるが正典ではない**(正典はChampions・§21)。

### 各バトルシステムの帰属と、うちの扱い
| システム | 本編での初出 | Championsでの実装 | **うちの扱い** |
|---|---|---|---|
| **メガシンカ** | 第六世代(XY) | **実装済み(現在Championsで動いているのはこれだけ)** | ✅**実装する**(engine 18箇所) |
| **テラスタル** | スカーレット・バイオレット | **未実装** | ❌**入れない**(2026-07-28 阿部さん明言「ややこしくなるので」) |
| **ダイマックス/キョダイマックス** | ソード・シールド | 未実装 | ❌**入れない** |
| **Zワザ** | サン・ムーン | 未実装 | ❌**入れない** |
- ★**Champions側は季節制で順次搭載される見込み**([[champions-battle-systems]])だが、**2026-07時点で動いているのはメガのみ**。**公式が実装した時に着手する**(データは持っているので対応は速い)。

### データは「持っているが使わない」状態にする
- ヤックン/徹底攻略/ポケモンWikiを土台にしているため、**Z・ダイマックスの威力値などが自然に混ざっている**(実測: コーパスの技ページ200件中**99件**に「ダイマックスわざ威力」欄がある。うちのデータにもテラスタル4/ダイマックス1/Zワザ1のヒット)。
- **方針**: **消さない・使わない**。公式が実装した時にすぐ出せるように保持する(§21の「データは全部持つ」と同じ考え)。
- ただし**バトルの計算・フェーズ設計には一切入れない**。分類作業で「不定(Zワザ/ダイマックス依存)」として出てくるものは**対象外扱い**でよい。

### ★棚卸しレポートの訂正
`review/現状棚卸し_ルール実装点検_2026-07-28.html` は「**テラスタルが完全に未実装**」を影響度=高の穴として挙げているが、**これは穴ではなく意図的な不実装**。同レポートの優先順位からテラスタル/ダイマックス/Zワザ関連を除外して読むこと。

## 23. ★バトルメッセージはフェーズに紐づいている(2026-07-28 阿部さん)

阿部さん「バトル中にコメント(**バトルメッセージ**)が出るが、**それもフェーズに合わせて乗っかってくる**。この場合はこうメッセージを出す、というルールがフェーズによって出ていると思う」

**その通りで、エンジンには既にフェーズタグが付いている**(実測 2026-07-28):
```
log('9',...)235件 / log('0c',...)100件 / log('10',...)48件 / log('8',...)35件
log('Init-A',...)15件 / log('10a',...)10件 / 他(0a/0b/10c/Turn/Undo/Reset)少数
```
= 旧`PHASES`定数(`Init-A / 0a / 0b / 0c / 1〜8 / 9 / 10a〜10h`)のタグ。

### 分かること2つ
1. **タグが旧語彙のまま**。新スラグ語彙(§0.8)に移行する時、**メッセージのタグも一緒に動かす**必要がある。放置すると「設計上のフェーズ」と「ログ上のフェーズ」がズレて、次に読む人が混乱する。
2. **`log('9')`に235件が集中している**=「攻撃後の効果適用」という巨大な1フェーズに全部押し込まれている。**ログの偏りが、そのまま「箱が足りない」ことの可視化**になっている(§1の原理の観測可能な証拠)。

### ★ここが効く: ログは「フェーズ順序」の機械可読トレース
メッセージのタグ列 = **どのフェーズがどの順で発火したかの記録**。つまり:
- **順序の明示検査(§5-3)を実測できる**。今までテストは「ダメージの数値が合うか」しか見ていなかったが、**順序そのものをアサートできる**。
- 例: 「相手のまもるで防がれた時、`技を使った`のログが先に出て、その後に`防いだ`が出る」(2026-07-16に阿部さんFBで確定した表示順)のような**順序仕様**がテストで守られる。
- **カスケードの深さ(§割り込み規律)もログから測れる**(タグの入れ子の深さ)。

### やること
1. **フェーズをスラグ化する時、`log()`の第1引数も同時に移行**する(旧タグ→新スラグの対応表を作る)。
2. **ログのタグ列を検証するテスト**を新設(期待するフェーズ順序をアサート)。既存の`_spec_*_test.js`はログ本文を見ているが、**タグ順序は見ていない**。
3. `log('9')`の235件を、新語彙の細分フェーズに振り分ける(=巨大フェーズの分割が、ログの分類として実際に進む)。
4. ★**多言語化との関係**: バトルログは`battle_log_i18n.js`のパターン翻訳(案X)で9言語化されている。**タグを変えても翻訳パターンは本文に依存**するので影響しないはずだが、移行時は`retranslateLogDom`まで含めて実機確認する([[i18n-static-and-battlelog-arch]])。
