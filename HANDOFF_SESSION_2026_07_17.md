# HANDOFF 2026-07-17(バトルラボ磨き込み=実FB即応ラウンド・全部本番ライブ)

前: `HANDOFF_SESSION_2026_07_16.md`(1日3スプリントの大回・必読)。本セッション=その続きで**阿部さんが実プレイ→FB→即修正**のループ。体制=実装Sonnet/検証・commit=Opus(同一ファイル直列を厳守)。

## 1. 今日やったこと(全部push・CDN確認済み)

| 内容 | 要点 |
|---|---|
| **⏩⏸ボタンのベージュ化** | 交代確認の2ボタンが素の白でパネルと同化→薄い黄色/ベージュ2段階(⏩濃いめ) |
| **架空ポップ数字の廃止** | まもる/ばけのかわで防がれた時にエディタ見本値「−42」が出ていた→**実ダメージが無い再生(dmgText:null)はpopnumを出さない**(fx_primitives)。ポップは常に「simが計算した実際に与えた数字」(マルチスケイル等込み)=構造は元々正・フォールバックの穴だけ塞いだ |
| **まもる行にバリアfx本番配線** | shieldFxはツクールプレビュー専用だった→「守りの体勢に入った！」行で発火(3ページ・SHIELD_FX_COLOR共有マップ=キングシールド金/トーチカ紫/ニードルガード緑) |
| **交代画面の左右固定再設計** | スワップ方式の弊害(相手のばんで左右・色が入れ替わり「交代できない」と混乱)→**左=じぶん(青)/右=あいて(赤)を常に固定**・今のばんの側だけ交代可(⏩⏸)・反対側は閲覧。**中央=編成エディタ流用**(バトル中に技〔PP再設定込み〕/特性/持ち物/努力値66Pを両側・控えまで編集可=次の行動から反映) |
| **実ブラウザGIF検証** | Claude in Chromeで本番URLを操作し、自分⏸交代→ターン渡し→相手のばんの⏩交代までGIF録画(`~/Downloads/battle_lab_switch_demo.gif`・阿部さんへ送付済み)。横向き誘導タグ修正/ベージュボタン/左右固定を本番で目視確認 |
| **様子パネル常時表示+⚙両側** | 様子を見るパネル=バトル中常時(10px・コンパクト・pointer-events:none・renderAllで自動更新・👀=一時隠しトグル)。⚙能力値+持ち物モーダルを自分(持ち物左)/相手(HP%左)両HPボックスに(openStatsModal(side)化・位置はJS動的計算・stopPropagationで#field罠回避) |

現`?v`: fx_primitives.js=**20260717a**(4ページ)。他は昨日のまま(HANDOFF_2026_07_16 §2参照)。

## 2. ★注意事項(今日の追加分)

1. **ポップ数字の契約**: 本番のtext/popnumは「dmgText=null(明示)→出さない/undefined→エディタ見本」の区別。cue再生に新しい呼び出し経路を足す時はdmgTextの渡し方に注意。
2. **交代画面はもうスワップに依存しない**: 描画は常に真のself/opp(labSwappedはボタン出し分けのみ)。今後この画面を触る時に「アクティブ側が左」前提のコードを書かないこと。
3. **HPボックス内のボタンは位置をJSで動的計算**(positionPbGears)。CSSの固定pxは持ち物名の長さで必ず重なる(実際に一度重なってPlaywrightが検出)。
4. 昨日の教訓(HANDOFF_2026_07_16 §4)は全部有効: 同一ファイル並列厳禁/GLMコードは親の初回実行検証必須/全数照合/クローンCSS機械監査/単一ソース化。

## 3. 🙋 阿部さんの確認事項(たまっているもの)

**バトルラボ(⌘+Shift+Rで https://pchamdb.com/battle_lab.html)**
1. 交代画面の**左右固定**の使い心地(GIF送付済み=battle_lab_switch_demo.gif)
2. **様子パネル常時表示**の文字サイズ/位置・**⚙両側**の位置(持ち物左/HP%左)
3. バトル中リロード=バトル消失を許容か(保存するなら実装)

**演出(リアルバトル/ツクール)**
4. まもる時の**バリアドーム**・音符・巻き付き・terrain(グラス葉/サイコ紫もや)の見た目
5. 連続技の連打速度(1発約0.9s)・牙の形
6. **変化技Phase2**(ツクール調整を本番の変化技にも効かせる)に進むか

**サジェスト(https://pchamdb.com/suggest_partner.html)**
7. **赤枠の条件**: 今=重み付き≥2(1体の4倍弱点でも赤)/代案=2体以上が弱い時だけ
8. トップページへの導線を付けるか(今はURL直打ちのみ)

## 4. 次にやること(実装キュー)

### ★Lab-3=無限バトルシミュレーション化(阿部さん 2026-07-17・次にやりたいと明言)
1. **途中からAI切替**: バトル中に相手(や自分)の操作を「手動⇔AI」でいつでも切り替えられるトグル(real_battleのAI行動ロジックを流用。AIに任せて観察→気になる所で自分が引き取る、を自由に)。
2. **チームの動的拡張**: 交代画面で3〜6体で終わりでなく、**バトル中に新しいポケモンをその場で追加**できる(ボックスから選んで編成に足す→そのまま交代候補に)。
3. **ひんし復活**: ひんしの子を復活できる(神の手の延長=HP%スライダーにひんし解除を含める or 交代画面に「復活」)。
4. ゴールイメージ=「延々と交代・追加しながら永遠にバトルできる実験場」(勝敗で終わらない・今も続行はするが、上記3点で完全に無限化)。
- 阿部さん判断後: 変化技Phase2(text i18nキー化必須=設計_変化技演出_2026-07-16.md §2)/Lab-2小物(ダメージピン留め・1手戻す・同種制限解除)/サジェスト赤枠調整・監査対象登録
- 小物: メガ儀式climaxのcolor対応(fx_primitives 2行)/PokeAPI訳なし119件=API側データ待ち
- 継続: 再戦フロー設計(阿部さんと)/演出量産ループ(ツクール調整→done:true統合)
- 参照: `ラボスプリント_状態_2026-07-16.md`(2026-07-17追補あり)/設計書5本(バトルラボ/エフェクト絵/変化技/連続技/ツクール本格化)
