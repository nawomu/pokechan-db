# 設計: ポケモンWiki 権威コーパスをローカルに持ち、それを土台に仕様・フェーズ・テストを組む

作成: 2026-07-28 JST / 発案: 阿部さん「ポケモンWikiは技/特性/道具をかなり細かく分析している。全部持ってきてうちのデータの中に残して、それをベースに一個一個チェックして、仕様・フェーズ決め・テストに使うのが一番正確」
位置づけ: `設計_バトルエンジン原理_2026-07-26.md`(憲法)の**根拠層**。憲法が「どう考えるか」なら、本書は「**その考えの材料をどこから取るか**」。

---

## 0. なぜやるか(3つ・順に大きい)

1. **★出典の捏造を機械検出できるようになる**(最大の効果)。今回の作業で**実在しない引用を計10件以上**検出した(ひらいしん/よわき/ポルターガイスト/しろいハーブ 等)。原文がローカルにあれば「その引用が実ページにあるか」は**grep一発**。人が気づくかどうかに頼らなくなる。
2. **エージェントが毎回WebFetchしなくて済む**=枠の節約と速度。1400ページを毎回取りに行く現状は高コストで、しかも取得の成否がその日の運に依存する。
3. **再現性**。同じ判断を後から辿れる(「なぜこのフェーズにしたか」の根拠が固定される)。世代差の記述も残るので、Champions=第9世代相当という判断が毎回できる。

## 1. ★法務・倫理の線引き(最初に決める・破らない)

- **保存は内部参照専用**。`reference/_authority_corpus/` に置き、**Webページ・公開ファイルとして再配布しない**(.gitignore 対象にするか、リポジトリに入れるなら非公開前提を明記)。
- **うちの説明文は独自作成のまま**(北極星: verbatim一致は盗用リスク)。コーパスは**意味の照合と仕様確認に使うのであって、文章のコピー元にしない**。既に `description_legacy`(ヤックン)で同じ線引きをしている=それを踏襲。
- **取得は丁寧に**: 1リクエスト/1〜2秒・並列1〜2・User-Agent明示・再取得は差分のみ。相手のサーバに負荷をかけない。
- **既存の前例**: `moves_yakkun.json`(ヤックン効果文の内部保存)と同じ扱い。[[yakkun-scrape-method]]

## 2. 取得経路(実測ベース)

| 経路 | 可否 | 用途 |
|---|---|---|
| `curl` / headless | ❌ Cloudflareで403(2026-07-28実測) | 使えない |
| **Claude in Chrome の同一オリジンfetch** | ✅ ヤックンで実績あり([[yakkun-scrape-method]]) | **本命=バルク取得**。ブラウザ内JSでN件ずつ取得しファイル書き出し |
| WebFetch(Claude/GLM) | ✅ 動作確認済み | 少数・確認用。**1400件の一括取得には使わない**(LLM経由=高コスト+要約が入る) |

**★重要**: 保存するのは**LLMの要約ではなく原文テキスト**。要約を保存すると「引用の実在確認」という最大の目的が果たせない。

## 3. コーパスの形

```
reference/_authority_corpus/
  moves/<技名>.json      … {title, url, fetched_at, sections:{効果, 仕様, 世代変更, 備考, ...}, raw_text}
  abilities/<特性名>.json
  items/<持ち物名>.json
  _index.json            … 取得状況(取得済/失敗/未取得・fetched_at・ハッシュ)= 再開の要
```
- `sections` = 見出しごとに分割したテキスト(ポケモンWikiは「効果」「仕様」「備考」「世代による違い」等の構造が安定している)。
- `raw_text` = 見出し込みの全文(引用照合用の正典)。
- 対象規模: 技919 + 特性309 + 持ち物169 = **約1400ページ**。

## 4. 段取り(フェーズ制・各段で止まっても次から再開できる)

### F0. パイロット(30ページ・まずこれだけ)
- 技10/特性10/持ち物10を取得 → セクション抽出が安定するか・取得間隔が妥当かを確認。
- **ここで抽出スクリプトの形を確定**させてから本番へ(1400件やり直しを避ける)。

### F1. バルク取得(約1400ページ・resumable)
- Chrome同一オリジンfetchで **50件/バッチ**・**1〜2秒間隔**。バッチごとに `_index.json` を更新。
- 失敗は記録して次へ(止めない)。最後に失敗分だけ再試行。
- **中断・枠切れがあっても`_index.json`から再開**(やり直しゼロ)。

### F2. 構造化(LLM不要・純コード)
- HTML→セクション分割→JSON。`tools/_build_authority_corpus.js`。
- **ここはLLMを使わない**=安い・決定的・何度でもやり直せる。

### F3. 機械照合(LLM不要・ここで大量の差分が出るはず)
- うちのデータ(WAZA_MAP/ABILITY_DESC/items_database)と**数値・属性を全数突き合わせ**: 威力/命中/PP/タイプ/分類/優先度/接触/音技/まもり貫通 等。
- 出力=`reference/_authority_diff.json`(食い違いリスト)。**これは純粋な機械比較なので全数やっても安い**。
- 期待: いじげんラッシュ型(名前の不一致)・ホズのみ型(条件の取りこぼし)がここで芋づる式に出る。

### F4. 差分のみLLM精査(ここで初めてエージェントを使う)
- F3で出た差分だけを1件ずつ: 権威原文(ローカル)を読む→うちが正しいか権威が正しいか判定→実バグなら修正タスク化。
- **コーパスがローカルにあるので、引用は必ず実在**(捏造が構造的に不可能になる)。

### F5. フェーズ割り当ての再検証
- 既に済んでいる627機構の割り当て(`reference/_phase_assign/`)を、**コーパスの「効果」「仕様」節と突き合わせて**再チェック。
- 特に `confidence:中/低` と `未確認` を優先。**source欄をローカルコーパスの該当セクションへの参照に張り替える**(URL+引用→ファイル+行)。

### F6. テストの自動生成(ここが最終目的)
- コーパスの「仕様」節から**テストケースの候補**を機械抽出(「〜の場合は失敗する」「〜は無効」「第七世代以降は〜」等のパターン)。
- 人/LLMが期待値を確定 → `tools/_spec_*_test.js` に追加。**今日作った11本の機構別テストの拡張版**。

## 5. 分担(GLM解約7/30を踏まえて)

| 作業 | 担当 | 理由 |
|---|---|---|
| F1 バルク取得 | **Claude in Chrome(私)** | 同一オリジンfetchはブラウザ必須 |
| F2 構造化 | **GLM**(〜7/30)→以降Sonnet | 純コード・機械作業の典型 |
| F3 機械照合 | **GLM**(〜7/30)→以降Sonnet | 同上・LLM判断ゼロ |
| F4 差分精査 | Sonnet(バッチ)+ Claude検証 | 判断が要る。**必ず検証パスを付ける**(作る側だけだと今日の誤報2件が通っていた) |
| F5 フェーズ再検証 | Sonnet + Claude | 同上 |
| F6 テスト生成 | Sonnet + Claude検証 | 期待値は権威由来・sim出力をゴールデンにしない |
| 進行管理・commit | Claude | 枠ガード(`tools/_quota_guard.sh`)→時限再開([[quota-guard-self-stop]]) |

## 6. 回し方(止めない・急がない)

- **ゆっくりでよい**(阿部さん)。1日にF1のバッチを何本か+F3の照合、くらいのペースで十分。
- 各ウェーブ前に `bash tools/_quota_guard.sh` → go=falseなら `ScheduleWakeup(ブロック終了+5分)`。
- **すべての段が resumable**(`_index.json`/進捗JSON)。中断が損失にならない設計にすることが最優先。
- 1ウェーブ終わるごとにハンドオフ更新。

## 7. 期待される成果(見積り)

- **出典捏造=構造的にゼロ**(ローカルgrepで機械検証)
- **データ差分**: いじげんラッシュ/ホズのみ型のバグが数十件出る想定(今回の断片的な調査だけで実バグ9件出た実績から外挿)
- **未実装の可視化**: 60件の未実装特性に加え、技側の未実装(kind140のうちengine_lines空=20件)も仕様つきで一覧化
- **テスト網**: 機構別テストが11本→数十本規模へ。以後の回帰が守られる

## 8. やらないこと(スコープ外)

- 画像・スプライトの取得(不要)
- 野生戦/フィールド専用の記述(語彙v3 §3.65=対象外)
- ダブル専用の実装(語彙に置き場は作るが実装は後)
- **コーパスを元にした説明文の生成**(北極星違反。説明文は独自作成のまま)
