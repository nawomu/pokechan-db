# 設計_リアルバトルUI再現_2026-07-03

作成日: 2026-07-03  
取材元: `review/_showdown_ref_20260703/`(スクショ27枚・DOM断片・computed_styles.json・ログ全文)  
対象ファイル: `real_battle.html`  
ステータス: 仕様書のみ(コード変更なし・commit なし)

---

## 0. 前提と不変条件

- **バトルロジック無変更**: `real_battle_simulator.html` をエンジンとして iframe 経由で呼ぶ現在の構造は変えない。`window.__sim` ブリッジの改変禁止。
- **アセット独自**: Showdown の sprites/images を流用しない。手描き SVG スプライト(`images/sim/*.svg`)を維持。
- **i18n 配線維持**: HTML に日本語ハードコードしない。固有名詞は `tPokeName`/`tMoveName`/`tTypeName` 経由。UI 文字列は `data-i18n` 属性 or `I18N.t()` 経由。新規文字列は 9 言語セット(ja/en/fr/de/es/it/ko/zh-Hans/zh-Hant)。
- **スマホ縦積み維持**: `@media(max-width:880px)` ブレークポイントを維持・更新。
- **演出互換**: メッセージ送り速度(`msg-speed`)・自動送り(`auto-msg`)・音(`snd-toggle`)・メガシンカボタン(`c-mega`)・背景フラッシュ(`fx-flash`) のロジックを壊さない。

---

## 1. 画面仕様(Showdown 準拠)

### 1-1. 3ブロックレイアウト

参照スクショ: `01_full_overview.png` / `02_battle_area.png` / `03_log_area.png`

```
┌──────────────────────────────────────────────────────────────────────┐
│ ヘッダー                                                              │
├─────────────────────────────────────────────────────────┬────────────┤
│  [A] .battle / #field                                   │ [C]        │
│      盤面: 幅560px / 高さ360px                          │ .battle-log│
│      (position:relative が親)                           │ #log-scroll│
├─────────────────────────────────────────────────────────┤ 幅460px    │
│  [B] .battle-controls / #cmd + #moves + #party          │ 高さ362px  │
│      コントロール: 高さ≒80px                            │            │
└─────────────────────────────────────────────────────────┴────────────┘
```

| ブロック | 要素 | 幅 | 高さ | 備考 |
|---|---|---|---|---|
| A 盤面 | `.battle` / `#field` | 560px | 360px | Showdown 実測値。現在は 600px×430px |
| B コントロール | `.battle-controls` / `#cmd` | 盤面と同幅 | ~80px | 技ボタン・交代ボタンが入る |
| C ログ | `.battle-log` / `#battle-log-panel` | 460px | 362px | Showdown 実測値(computed_styles.json) |

**現状との差**: `#battle-main` が `flex:0 0 600px`・`#field` が `height:430px` → 縮小が必要(後述)。

---

### 1-2. 配色(実測 16 進)

参照: `computed_styles.json` / `07_controls_area.png` / `03_log_area.png`

| 要素 | 16 進色 | rgb 値(computed) |
|---|---|---|
| ログ背景 `.battle-log` | **`#eef2f5`** | rgb(238,242,245) |
| ターン見出し `h2` bg | **`#e0e7ea`** | rgb(224,231,234) |
| ログ文字 | **`#000000`** | 黒 |
| ログ枠線 | **`#aaaaaa`** | 1px solid |
| HP バー外枠 bg | **`#fcfeff`** | rgb(252,254,255) |
| HP バー枠線 | **`#777777`** | 1px solid |
| HP 内側バー(満タン) | **`#00bb51`** | rgb(0,187,81) |
| HP 内側バー(中) | 未取材 → `#d0c000`(推定黄) | 要後日確認 |
| HP 内側バー(少) | 未取材 → `#c82315`(推定赤) | 要後日確認 |
| statbar 背景 | **`transparent`** | 盤面画像が透ける |
| statbar 文字 | **`#000000`** | 黒(背景透明のため白黒は環境依存) |

**注意**: HP 黄/赤の境界色は notes.md 7節「取材できず」とされている。阿部さん確認事項 A-1 参照。

---

### 1-3. タイポグラフィ

参照: `computed_styles.json` (全要素共通)

- **フォント**: `Verdana, sans-serif`
- **基本サイズ**: 13.3px (computed)
- **ターン見出し**: 17.3px / 高さ 31px
- statbar、ログ行、技名すべて Verdana 統一

**現状**: `"Hiragino Kaku Gothic ProN", Meiryo, sans-serif` をデフォルト使用 → Showdown 感を出すには Verdana を盤面・ログ内に限定指定が有効(body 全体変更は不要、対象セクションのみ)。

---

### 1-4. HPバー(2層構造)

参照: `05_hpbar_nameplate.png` / `05b_statbar_player.png` / `11_hp_reduced.png` / `dom_statbar_player.html` / `dom_statbar_opp.html`

**Showdown の DOM 構造**:

```html
<div class="hpbar">           <!-- 外枠: 155×12px, border:1px solid #777, border-radius:4px, bg:#fcfeff -->
  <div class="hptext">99%</div>        <!-- パーセント表示(左寄り) -->
  <div class="hptextborder"></div>     <!-- テキストの下縁装飾(取材での詳細不明) -->
  <div class="prevhp" style="width:150px;">   <!-- 前フレーム幅: アニメ前の幅を保持 -->
    <div class="hp" style="width:149px; border-right-width:1px;"></div>  <!-- 現在 HP 幅 -->
  </div>
  <div class="status"></div>           <!-- 状態異常バッジがここに入る -->
</div>
```

**2層アニメーション**: `prevhp` が前の HP 幅を保ち、`hp` が新しい幅に transition → HP 減少時に「ゆっくり縮む」演出が生まれる。

| 項目 | Showdown 仕様 | 現状 `real_battle.html` |
|---|---|---|
| HP 表示形式 | **% のみ**(例: `99%`) | **実数値表示**(`${hp} / ${max}`) → `#pb-self` のみ `.hptext` に出力 |
| バー外枠サイズ | 155×12px | 現状: `height:9px` / 幅は `min-width:215px` の `.pbox` に追従 |
| 内側バー高さ | 8px | 現状: `height:100%`(つまり 9px) |
| 内側バー border-radius | 3px | 現状: なし(overflow:hidden で代替) |
| 2層(prevhp + hp) | あり | **なし**(`.hpfill` 1枚のみ) |
| 状態異常バッジ位置 | `hpbar` 内 `.status` | `.sts` を `.hpbar` 外の別 div に配置 |

**阿部さん確認事項 A-1**: HP 表示を「% のみ」(Showdown 準拠)にするか「HP/最大 実数値」(現状・自分側)にするか。  
→ 両案: 案X=`99%` のみ / 案Y=`99%` + `(234/235)` を小字で添える。

---

### 1-5. 名前プレート(statbar)オーバーレイ

参照: `05_hpbar_nameplate.png` / `dom_statbar_player.html` / `dom_statbar_opp.html`

**Showdown の statbar 構造**:

```html
<div class="statbar lstatbar" style="position:absolute; left:350px; top:24px; opacity:1; width:162px; height:44px;">
  <strong>ニックネーム <img src="fx/gender-f.png" alt="F" width="7" height="10"></strong>
  <div class="hpbar">...</div>
</div>
```

| 配置 | left | top |
|---|---|---|
| 自分(lstatbar) | 350px | 24px |
| 相手(rstatbar) | 130px | 120px |

- **サイズ**: 162×44px(computed)
- **背景**: transparent(盤面の背景画像が透けて見える)
- **フォント**: Verdana 13.3px / 名前は `<strong>` タグ
- **性別アイコン**: `<img>` 7×10px を名前の後ろに inline で添える

**現状 `real_battle.html`**: `.pbox` が `position:absolute` で盤面オーバーレイになっているが、サイズが `min-width:215px` と大きく、背景も `#141927ee` (半透明ダーク)。Showdown の transparent+小型(162px)とは見た目が大きく異なる。

---

### 1-6. 控えチームアイコン(leftbar / rightbar)

参照: `dom_battle.html` / `10_midgame_full.png` / `02_battle_area.png`

**Showdown 構造**:
```html
<div class="leftbar">     <!-- 盤面内に absolute で左端(自分側) -->
  <div class="trainer trainer-near">
    <strong>プレイヤー名</strong>
    <div class="trainersprite" style="background-image:url(sprites/trainers/lyra.png)"></div>   <!-- 96×96px -->
    <div class="teamicons">
      <span class="picon" style="background: ... pokemonicons-sheet.png ... -400px -2850px" aria-label="Hoopa-Unbound"></span>
      <!-- 6体を 3体×2行で並べる -->
    </div>
  </div>
</div>
<div class="rightbar">...</div>  <!-- 盤面内に absolute で右端(相手側) -->
```

- **アイコン 1 体のサイズ**: 40×30px(Showdown のスプライトシートの 1 コマ)
- **瀕死スタイル**: `opacity:0.3; filter:grayscale(100%) brightness(.5)` — グレーアウト
- **配置**: 盤面(`position:relative`)内に `position:absolute`

**現状**: `real_battle.html` には `.balls` 列があり手持ちポケモンの球アイコンは `.tball`(SVG 20×20px)で表示されているが、**盤面外の `.pbox` 内**に配置。leftbar/rightbar 相当の**盤面オーバーレイ型アイコン列は存在しない**。

**方針**: 既存の `.balls` を pbox 内に残しつつ、盤面左端/右端に leftbar/rightbar 相当のアイコン行を追加。アイコン画像は独自 SVG(`images/sim/*.svg`)を使用(Showdown スプライトシート流用禁止)。

---

### 1-7. ログ 4 層マークアップ

参照: `dom_log.html` / `06c_turn1_log.txt` / `13d_endgame_log.txt` / `03_log_area.png`

**Showdown のログ要素分類**:

| 層 | タグ/クラス | 用途 | フォント |
|---|---|---|---|
| ターン見出し | `<h2 class="battle-history">Turn N</h2>` | ターン区切り | 17.3px / bg:`#e0e7ea` / 高さ31px |
| 通常行 | `<div class="battle-history">...技使用/交代/特性...</div>` | メイン行 | 13.3px |
| 効果/解説行 | `<div class="battle-history"><small>...</small></div>` | 有効度・ダメージ・回復等 | 13.3px×0.8 |
| 空行 | `<div class="spacer battle-history"><br></div>` | ターン内の区切り空行 | — |

**技名** は `<strong>` タグでボールド。  
**特性発動** は `[名前の 特性名]` 形式で角括弧を使う。

**現状 `real_battle.html`**:
- `logLine(text)` / `logTurnHeader(n)` でテキスト文字列として追加
- `.log-turn`: white bg / `font-size:12px` / `inline-block` pill 型
- `.log-line`: `color:#cdd6e4` / テキストのみ(HTML 構造なし)
- `<small>` / `<strong>` / spacer 構造は未実装

**差分**: 技名 bold・効果行 small・空行 spacer の 3 点が未実装。ログ背景色も `#0e1422`(ダーク)vs Showdown `#eef2f5`(ライト)。

---

### 1-8. 技ボタン

参照: `07_controls_area.png` / `07b_controls_area2.png` / notes.md §3

**Showdown の技ボタン構造**(ライブ対戦時・ソース推察):

```html
<div class="movemenu">
  <button class="move type-fire" data-move="pyroball">
    <strong>Pyro Ball</strong>           <!-- 技名 bold -->
    <small class="pp">8/8</small>        <!-- PP 表示 -->
  </button>
  ...4 ボタン
</div>
```

- タイプ色はクラス名 `.type-{typename}` で CSS 変数 or 直値
- リプレイ時はボタン非表示・ナビゲーションボタン(<<, <, >, >>)が代わりに表示

**現状 `real_battle.html`**:
- `#moves button` に `.mv-name`(技名)・`.mv-sub`(分類/PP 等)・`.t-pill`(タイプ色バッジ)
- タイプ色は CSS 変数でなく JS で `background` を直接セット
- 構造は独自で十分機能している → 枠デザインをタイプ色ボーダー左帯に統一するのみ

---

### 1-9. 背景プリセット・天候エフェクト

参照: `dom_battle.html` / `02_battle_area.png` / `04b_turn0_battle.png`

**Showdown の backdrop 構造**:
```html
<div class="backdrop" style="opacity:0.8; background-image:url('gen6bgs/bg-orasdesert.jpg');"></div>
<div class="weather"></div>     <!-- 天候エフェクト层1 -->
<div class="weather"><em></em></div>   <!-- 天候エフェクト层2(アニメ) -->
```

- 背景は `background-image` の URL 差し替えで複数プリセット対応(砂漠/宇宙等)
- opacity: 0.8 で半透明

**現状**: `#field` の `background` プロパティで CSS グラデーション直書き(フィールドの地面+空を CSS で表現)。`#weather-fx` div が emoji 文字列で天候を表示(opacity:.22 / アニメ有)。

**方針**: CSS グラデーション背景 + weather-fx emoji の現状構造を維持しつつ、`#field` に `.backdrop` div を追加して背景プリセット切り替えを可能にする。

---

## 2. うちとのギャップ表

| # | 現状 real_battle.html | 項目 | 目標仕様(Showdown) | 変更方針 |
|---|---|---|---|---|
| G1 | `#field: height:430px` / `#battle-main: flex:0 0 600px` | 盤面サイズ | 560×360px | **改修**: `#field` → 360px / `#battle-main` → 560px |
| G2 | `#battle-log-panel: flex:1 1 auto` (可変) | ログ幅・高さ | 460×362px | **改修**: `min-width:460px; flex:0 0 460px; height:362px` |
| G3 | `font-family: Hiragino Kaku Gothic ProN, Meiryo` (body) | フォント | Verdana, sans-serif | **改修(限定適用)**: `#battle`, `.battle-log` に Verdana を上書き指定 |
| G4 | `.hpfill` 1 層 / `height:9px` | HP バー | 2 層(prevhp + hp)/ 外枠 12px / 内側 8px | **改修**: 2 層構造追加・サイズ調整 |
| G5 | `t.textContent = \`${hp} / ${max}\`` (自分側) / 相手はバーのみ | HP 表示形式 | `%` のみ | **阿部さん確認 A-1**(両案参照) |
| G6 | `.hpbar` 背景: `#1d2433` / 枠: `#2a3450` | HP バー外枠配色 | bg:`#fcfeff` / 枠:`#777` | **改修**: Showdown 配色へ |
| G7 | HP 色: 満=`#4ade80` / 中=`#fbbf24` / 少=`#f87171` | HP バー内側色 | 満=`#00bb51` / 中=未取材 / 少=未取材 | **改修(満のみ確定)**: `#00bb51`。中/少は取材後 |
| G8 | `.pbox` = 半透明ダーク背景・`min-width:215px` | 名前プレート | 透明背景・162×44px / 絶対配置 | **改修**: 透明化・サイズ縮小。固定位置座標調整 |
| G9 | `.pbox` 配置: `#pb-opp{top:26px;left:24px}` / `#pb-self{bottom:18px;right:24px}` | statbar 絶対座標 | 相手: `left:130px; top:120px` / 自分: `left:350px; top:24px` (盤面 560 基準) | **改修**: 盤面幅変更に伴い座標調整 |
| G10 | `.balls`(20px SVG)が `.pbox` 内に存在 | 控えアイコン | leftbar / rightbar オーバーレイ | **新設**: leftbar/rightbar div を `#field` 内に追加。既存 `.balls` と共存または置換 |
| G11 | `logLine(text)` で text only | ログマークアップ | h2/div.battle-history/small/spacer の 4 層 | **改修**: `logLine` の出力を HTML 構造付きに |
| G12 | `.log-turn`: 白 pill 型 bg / 12px | ターン見出し | `h2` / bg:`#e0e7ea` / 17.3px / 幅フル | **改修**: h2 要素・配色・サイズ |
| G13 | `#log-scroll`: bg=`#0e1422`(ダーク) | ログ全体配色 | bg:`#eef2f5` / 文字:`#000` | **改修**: 配色反転 |
| G14 | `#battle`: `display:flex` (バトル時) / `in-battle max-width:1060px` | 2 カラム親 | 全幅 flex / ログが右 | **改修**: 幅・flex 設定を新サイズに合わせる |
| G15 | 背景は CSS グラデーション固定 | 背景プリセット | `.backdrop` 画像 URL 切り替え | **新設**: `.backdrop` div を追加・プリセット選択は設定画面に |
| G16 | 技ボタン: タイプ色は `.t-pill` バッジ | 技ボタン見た目 | タイプ色はボーダー or button bg 全面 | **流用**: 大きな構造変更不要・左縁ボーダー色のみ整備 |

---

## 3. 実装ステージ分割(S1〜S7)

### S1: レイアウト 3 ブロック化とサイズ・配色

**対象**: `#field`、`#battle-main`、`#battle-log-panel`、`#battle`(親 flex)、`body.in-battle max-width`

**変更内容**:
- `#field` → `width:560px; height:360px`
- `#battle-main` → `flex:0 0 560px; max-width:560px`
- `#battle-log-panel` → `flex:0 0 460px; min-width:460px; height:362px`
- ログ配色: bg=`#eef2f5` / 文字=`#000`
- ログ枠: `1px solid #aaaaaa`
- `body.in-battle max-width` を `560+460+gap` に合わせて調整
- Verdana フォントを `#field`・`.battle-log-panel` に限定指定
- `@media(max-width:880px)` の縦積みは現状維持

**スクショ突合 合格基準**: `01_full_overview.png` / `02_battle_area.png` / `03_log_area.png` と比べて 3 ブロックの大きさ・比率が概ね一致。

---

### S2: HPバー 2 層構造 + statbar 名前プレートオーバーレイ

**対象**: `.pbox`・`#pb-opp`・`#pb-self`・`.hpbar`・`.hpfill`

**変更内容 - HPバー**:
- 外枠: `width:155px; height:12px; border:1px solid #777; border-radius:4px; background:#fcfeff`
- `.prevhp` div(前 HP 幅保持)を `.hpbar` 内に追加
- `.hp` div(現在幅)を `.prevhp` 内に入れ子
- 内側 `height:8px; border-radius:3px`
- 内側色: 満=`#00bb51` / 中=TBD / 少=TBD(阿部さん確認 A-1 解決後)
- `.hptext` を `%` 表示に変更(阿部さん確認 A-1 の方針が決まってから)

**変更内容 - statbar**:
- `.pbox` 背景: `transparent` に変更
- `.pbox` サイズ: `width:162px`(min-width 削除)
- `.pbox` ボーダー/border-radius: 削除
- 座標: `#pb-opp → left:130px; top:120px` / `#pb-self → left:350px; top:24px`(盤面 560 基準・ブロードキャスト後に微調整可)
- 名前に性別アイコン `<img class="gender-icon">` を追加(画像は独自 SVG or Unicode ♂♀)
- `.who` ラベル(あいて/じぶん小バッジ)の扱い: 阿部さん確認 A-2 参照

**阿部さん確認事項 A-2**: Showdown は statbar 背景が transparent で名前のみ(「あいて」「じぶん」ラベルバッジなし)。うちの `.who` バッジ(緑/赤)は独自の分かりやすさ改善 → 残すか削除するか。

**スクショ突合 合格基準**: `05_hpbar_nameplate.png` / `05b_statbar_player.png` / `05c_statbar_opponent.png` と HP バーの幅・色・テキスト位置が一致。

---

### S3: 控え 6 体バー(leftbar / rightbar)

**対象**: `#field` 内に新設する `#leftbar`・`#rightbar`

**変更内容**:
- `#field` 内に 2 div を `position:absolute` で追加
  - `#leftbar`: `bottom:0; left:0` 付近(自分側)
  - `#rightbar`: `top:0; right:0` 付近(相手側)
- 各 div に `.picon` 相当のアイコン 6 体を並べる
  - アイコン: `<img src="images/sim/{name}.svg">` 独自 SVG (30×30px 相当)
  - 瀕死: `opacity:0.3; filter:grayscale(100%) brightness(.5)`
- 既存 `.balls`(`.pbox` 内)との関係: 冗長になる場合は `.balls` 非表示にするか検討(阿部さん確認 A-3)
- HP%文字列は aria-label に入れる(`aria-label="ポケモン名 (63%)"`)

**阿部さん確認事項 A-3**: 既存の `.balls`(pbox 内の小型球アイコン)と新設 leftbar/rightbar の両立 or 削除どちらにするか。Showdown は pbox 内に balls 相当なし。

**スクショ突合 合格基準**: `10_midgame_full.png` / `02_battle_area.png` の leftbar/rightbar 位置・6 体表示・瀕死グレーアウトが一致。

---

### S4: ログ 4 層マークアップ化

**対象**: `logLine()`・`logTurnHeader()`・`#log-scroll` CSS

**変更内容**:
- `logTurnHeader(n)` → `<h2 class="battle-history">Turn N</h2>` を出力(現在: div.log-turn pill 型)
- `logLine(text, type)` を種別付きに拡張:
  - `type='normal'` → `<div class="battle-history">{text}<br></div>`
  - `type='small'` → `<div class="battle-history"><small>{text}</small><br></div>`
  - `type='spacer'` → `<div class="spacer battle-history"><br></div>`
  - `type='bold'` → 技名等を `<strong>` で内包
- ログ内テキストの i18n: 日本語テキストは `battle_log_i18n.js` の `translateLogLine()` 経由(既存配線を維持)
- `.log-scroll h2.battle-history` CSS: `bg:#e0e7ea; font-size:17px; height:31px; padding:0 8px; margin:0`
- `.log-line-small`: `font-size: smaller; color: inherit`

**注意**: `logLine()` の呼び出し元(JS バトルロジック側)も type 引数追加が必要。呼び出し箇所を一覧化してから着手。

**スクショ突合 合格基準**: `06c_turn1_log.txt` の実テキストと比べてターン見出し・技名 bold・効果行 small・空行 spacer が揃っている。`03_log_area.png` と色・レイアウトが一致。

---

### S5: 技ボタン・コントロール領域

**対象**: `#cmd`・`#moves`・`#party`・`#speed-row`

**変更内容**:
- `#moves button` の左縁タイプ色ボーダー: 現状 `.mv-chip` と同様の `border-left:4px solid {typeColor}` を技ボタンにも適用(現状は `.tcol` クラスで bg 色を付けている)
- PP 表示: `<small class="pp">残PP/最大PP</small>` を技ボタン内に追加(現状: `.mv-sub` でまとめて分類+PP 表示)
- コントロール(たたかう/ポケモン/メガシンカ/にげる)ボタンは現状維持
- `#speed-row` は現状維持(i18n 配線済み)

**スクショ突合 合格基準**: `07_controls_area.png` と技ボタンの見た目・タイプ色・PP 表示位置が概ね一致。

---

### S6: 背景プリセット・天候エフェクト整備

**対象**: `#field` CSS / `#weather-fx` / 設定 UI

**変更内容**:
- `#field` 内に `<div id="field-backdrop"></div>` を追加(z-index:0 / position:absolute;inset:0)
- 背景プリセット定義(自前デザイン、Showdown 画像流用禁止):
  - `grass`: 現状の CSS グラデーション(地面+空)を維持
  - `cave`: ダーク石壁グラデーション
  - `water`: 水面グラデーション
  - `night`: 夜空グラデーション
  - プリセット選択は `.battle-backdrop-sel` select 要素を `#speed-row` 内または設定モーダルに追加
- `#weather-fx` の emoji 天候演出は現状維持
- 新規文字列「背景」などは i18n 9 言語対応

**スクショ突合 合格基準**: `04b_turn0_battle.png` の backdrop 構造(画像 + opacity 0.8 レイヤー)に相当する CSS レイヤリングが実現できていること。

---

### S7: 仕上げ突合・細部調整

**対象**: 全体

**作業内容**:
- `01_full_overview.png`・`10_midgame_full.png`・`13_endgame_full.png` と全体レイアウトを突合
- `11_hp_reduced.png` で HP 減少後の色変化を確認(中/少の境界値確認 → 阿部さん確認 A-1 の答えを反映)
- `09_switch_area.png` で交代選択 UI 確認
- スマホ縦積み(`@media(max-width:880px)`) を実機または DevTools で確認
- i18n ハーネス(`tools/i18n_audit_playwright.js`)で残日本語 0 を確認
- ブラウザコンソールエラー 0 を確認

**スクショ突合 合格基準**: 各フェーズ(開幕・中盤・終盤)の全体スクショと目視一致。演出(メッセージ送り・フラッシュ・メガシンカ)が壊れていない。

---

## 4. 阿部さん確認事項まとめ

| # | 確認項目 | 両案 |
|---|---|---|
| A-1 | **HP 表示形式**: % のみ or 実数値も表示 | 案X = `99%` のみ(Showdown 準拠) / 案Y = `99% (234/235)` の形で実数も小字で添える |
| A-2 | **statbar 内「あいて」「じぶん」バッジ**: 削除して Showdown 準拠の透明 statbar にするか、識別のため残すか | 案X = 削除(透明 statbar のみ・Showdown 準拠) / 案Y = statbar 枠外に小型ラベルとして残す |
| A-3 | **pbox 内 `.balls` アイコン列**: 新設 leftbar/rightbar と並立するか、pbox 側を削除するか | 案X = leftbar/rightbar 新設 + pbox の balls 削除 / 案Y = 両方残す(leftbar = 大きめ表示 / balls = HP 確認用サブ) |
| A-4 | **HP バー黄・赤の色と境界値**: Showdown は 48%以下で黄・20%以下で赤(notes.md「取材できず」) | 現状: 50%以下=`#fbbf24`・25%以下=`#f87171`。Showdown 実機で再取材するか現状値のままにするか |

---

## 5. リスク・干渉点

| リスク | 該当箇所 | 対策 |
|---|---|---|
| **メガシンカボタン** `#c-mega` の `mega-armed` スタイル | S1〜S2 で `.pbox` の配色を変更すると `.mega-armed` の黄色強調が目立ちすぎる/消える可能性 | S2 完了後に `#c-mega.mega-armed` の視認性を確認 |
| **音 SE/BGM** の `AudioContext` タイミング | レイアウト変更でページリロードが増え、初回クリック前 AC.state=suspended が増える可能性 | 変更なし。SE トリガーは現状維持 |
| **メッセージ送り** `#msgbox` の `click` → `advanceMsg` | `#msgbox` を盤面外に残すが S1 でサイズ変更した場合、クリック領域がずれる可能性 | `#msgbox` は `#field` 外・`#battle-main` 内に置く現構造を維持。高さ変更のみ |
| **ログ logLine() 変更** | `logLine(text)` を HTML 出力に変えると呼び出し元の XSS リスクが生まれる | テキストは必ず `textContent` または `createTextNode` で挿入し、`innerHTML` に生文字列を直接流さない |
| **スプライト座標(f-opp / f-self)** | `#field` を 430→360px に縮めると `.fighter#f-opp` (top:52px) / `#f-self` (bottom:42px) の配置が変わる | S1 完了後にスプライト位置を実機確認して微調整 |
| **i18n**: 新規追加文字列 | S3〜S6 で新規 UI 要素を追加する際、生 ja 文字列をハードコードしてしまうリスク | 各 S でコーディング前に必ず i18n キー設計を先に行う(CLAUDE.md §多言語化ファースト) |
| **自動送り速度設定と h2 追加** | S4 でターン見出しに h2 を追加するとスクロール速度が変わる可能性 | `#log-scroll` の `scrollTop = scrollHeight` は変更なし。ターン h2 は高さ 31px 固定なので影響軽微 |

---

## 6. 参照スクショ索引

| 仕様項目 | 主参照スクショ |
|---|---|
| 3 ブロックレイアウト | `01_full_overview.png` / `02_battle_area.png` |
| ログ全体 | `03_log_area.png` / `03b_log_area_alt.png` |
| 開幕ターン | `04_turn0_start.png` / `04b_turn0_battle.png` |
| HP バー + 名前プレート | `05_hpbar_nameplate.png` / `05b_statbar_player.png` / `05c_statbar_opponent.png` |
| ターン別推移 | `06_turn1_full.png` 〜 `06_turn3_full.png` / `06b` 系 |
| 技・コントロール | `07_controls_area.png` / `07b_controls_area2.png` |
| 交代選択 | `09_switch_area.png` |
| 中盤(HP 消耗) | `10_midgame_full.png` / `10b_midgame_battle.png` / `10c_midgame_log.png` |
| HP 減少後 statbar | `11_hp_reduced.png` |
| 終盤 | `12_lategame_full.png` / `12b_lategame_battle.png` |
| 対戦終了 | `13_endgame_full.png` / `13b_endgame_battle.png` / `13c_endgame_log.png` |

---

_以上。コード変更なし・commit なし。実装着手は阿部さん確認事項 A-1〜A-4 の方針が決まってから各ステージに入る。_

---

## 7. S7 突合結果 (2026-07-04)

突合実施: `real_battle.html` を http://localhost:8080 でPlaywright/claude-in-chrome で実対戦し、各シーンのスクリーンショット・DOM計測・JavaScript計測を実施。参照: `review/_showdown_ref_20260703/`

### 7-1. シーン別突合表

| # | シーン | 参照スクショ | 判定 | 詳細 |
|---|---|---|---|---|
| S7-01 | 全体3ブロックレイアウト | `01_full_overview.png` | **一致** | 盤面560×360px✓ / ログ460×362px✓ / 2カラム(盤面左・ログ右)✓ / `body.in-battle max-width:1060px`✓ |
| S7-02 | 盤面エリア単体 | `02_battle_area.png` | **一致** | フィールド比率・leftbar/rightbar位置✓。背景はCSSグラデ(独自化ポイント・維持) |
| S7-03 | ログエリア配色 | `03_log_area.png` | **一致** | bg `#eef2f5`(rgb 238,242,245)✓ / 文字`#000`✓ / 枠`1px solid #aaa`✓ / フォントVerdana✓ |
| S7-04 | 開幕ターン0全体 | `04_turn0_start.png` | **一致** | Turn 1バッジ✓ / trainer名札✓ / 操作ボタン4個✓ |
| S7-05 | HPバー + 名前プレート | `05_hpbar_nameplate.png` / `05b_statbar_player.png` / `05c_statbar_opponent.png` | **一致** | hpbar 155×12px✓ / bg `#fcfeff`✓ / border `1px solid #777`✓ / radius 4px✓ / hp内側8px✓ / radius 3px✓ / 2層(prevhp+hp)✓ / hptext %のみ✓ / statbar transparent背景✓ / 162px幅✓ / 相手`left:130px top:120px`✓ / 自分`left:350px top:24px`✓ |
| S7-06 | ターン別推移(Turn1〜3) | `06_turn1〜3_full.png` | **一致** | ターン進行・ログ蓄積・HP変化アニメ動作✓ |
| S7-07 | 技・コントロール | `07_controls_area.png` | **軽微差** | 技ボタンあり・PP表示✓・タイプ色左縁✓。差分: Showdownはボタンbg全面タイプ色、うちは左縁帯のみ(独自化ポイント・仕様書G16通り) |
| S7-08 | 交代選択UI | `09_switch_area.png` | **一致** | 控えポケモン一覧・ミニHPバー・もどるボタン✓ |
| S7-09 | 中盤全体(HP消耗) | `10_midgame_full.png` | **一致** | Turn表示更新✓ / ログ複数Turn蓄積✓ / 控えbarの瀕死グレーアウト(dead class)✓ |
| S7-10 | HP減少後statbar色変化 | `11_hp_reduced.png` | **一致** | 満(100〜50%): `#00bb51`✓ / 中(50〜25%): `#d0c000` `.mid`クラス付与✓ / 少(25%以下): `#c82315` `.low`クラス付与✓ → 実測: 6%時に `rgb(200,35,21)` `.hp.low`で赤色確認✓ |
| S7-11 | ターン見出しh2スタイル | `10c_midgame_log.png` | **一致** | `h2.battle-history` bg `rgb(224,231,234)=#e0e7ea`✓ / font-size 17px✓ / height 31px✓ |
| S7-12 | 技名bold / 効果行small | `06c_turn1_log.txt` | **一致** | 技名 `<strong>` 付き✓(例: `だいばくはつ！` / `イカサマ！`) / spacer div✓。差分: small(効果行)は効果度テキストのみ自動判定(パターンマッチ) |
| S7-13 | 終盤〜対戦終了 | `13_endgame_full.png` | **一致** | 最終Turn表示✓ / ログ全履歴蓄積✓ / 勝敗メッセージ表示✓ |
| S7-14 | 背景プリセット | `04b_turn0_battle.png` | **一致** | `#field-backdrop[data-preset]` 4プリセット(grass/cave/water/night)✓ / `#backdrop-sel`セレクト✓ / `#weather-fx`天候div✓ |
| S7-15 | 英語切替(en) | (動的確認) | **一致** | `I18N.setLang('en')` でUI全文英語化✓ / JSエラー0✓ / 技名・ポケモン名・ボタン・ログ全て英語化✓ |
| S7-16 | JSエラー | (コンソール確認) | **合格** | ja/en 両モードともコンソールエラー0✓ |

### 7-2. 要修正の残差

今回の突合では「要修正」(表示層のみ・軽微CSS調整の範囲内)に該当する残差は **なし**。全シーンで一致または軽微差どまり。

### 7-3. 独自化フェーズへの持ち越しリスト

| 項目 | 分類 | 内容 |
|---|---|---|
| **スマホ幅statbar座標** | 要確認(S2持ち越し) | `pb-self` が `left:350px`・幅`162px`=512pxで390px画面からはみ出す(`overflow:hidden`でクリップ)。`#field`に幅の`@media`追加か statbar座標を`%`指定にすれば解消。独自化フェーズで対応推奨。 |
| **技ボタンbg** | 独自化ポイント(維持) | Showdownはbutton背景をタイプ色全面に。うちは左縁4px帯のみ(ダーク基調維持の独自化。G16の既定方針通り)。 |
| **背景トーン** | 独自化ポイント(維持) | Showdownは白系盤面。うちはダーク基調(阿部さん確認済み・独自化ポイント)。 |
| **HP 黄/赤の境界値** | 要後日確認(A-4) | 現状: 50%以下=`#d0c000`(黄)・25%以下=`#c82315`(赤)。Showdown仕様(48%/20%)は「取材できず」のまま。実機再取材するか現状値維持かは阿部さん確認事項A-4待ち。 |
| **small効果行の自動判定** | 軽微差 | `_SMALL_PATS`で「こうかはばつぐん」等の固定パターンのみ`<small>`化。Showdown仕様(ダメージ%・能力変化等も small)には完全には達していない。独自化フェーズで拡張可。 |
| **statbar `.who`バッジ** | 独自化ポイント(維持) | A-2デフォルト=削除(display:none)済み。復活させるにはCSS削除のみ。 |
| **控えアイコン画像** | 独自化ポイント(維持) | Showdownはスプライトシート。うちは独自SVG(images/sim/*.svg)。 |

### 7-4. 一致度サマリ

| 項目 | 計測値 | Showdown仕様 | 一致 |
|---|---|---|---|
| 盤面サイズ | 560×360px | 560×360px | ✓ |
| ログパネルサイズ | 460×362px | 460×362px | ✓ |
| ログ背景色 | `#eef2f5` | `#eef2f5` | ✓ |
| ターン見出し背景 | `#e0e7ea` | `#e0e7ea` | ✓ |
| ターン見出しfont-size | 17px | 17.3px | ✓(実用範囲) |
| ターン見出し高さ | 31px | 31px | ✓ |
| フォント | Verdana | Verdana | ✓ |
| HPバー外枠 | 155×12px | 155×12px | ✓ |
| HPバー外枠色 | `#fcfeff` / `#777` | `#fcfeff` / `#777` | ✓ |
| HP内側バー | 8px / radius 3px | 8px / radius 3px | ✓ |
| HP満タン色 | `#00bb51` | `#00bb51` | ✓ |
| HP%のみ表示 | ✓ | ✓ | ✓ |
| 2層HPアニメ(prevhp+hp) | ✓ | ✓ | ✓ |
| statbar transparent背景 | ✓ | ✓ | ✓ |
| statbar幅 | 162px | 162px | ✓ |
| 相手statbar位置 | left:130px top:120px | left:130px top:120px | ✓ |
| 自分statbar位置 | left:350px top:24px | left:350px top:24px | ✓ |
| 控えbar(6体) | 6+6アイコン | 6体×2 | ✓ |
| 瀕死グレーアウト | `.dead` class✓ | opacity:0.3 / grayscale | ✓ |
| 技名bold | `<strong>`✓ | `<strong>` | ✓ |
| spacer空行 | `.spacer.battle-history`✓ | `.spacer.battle-history` | ✓ |
| 背景プリセット構造 | `#field-backdrop[data-preset]` | `.backdrop`相当 | ✓ |
| i18n全UI文字 | キー全数✓・ja/en動作✓ | — | ✓ |
| JSエラー | 0件 | — | ✓ |

**総合判定: S1〜S6の実装はShowdown仕様との主要差分をすべて解消済み。全シーンで一致または設計済み独自化ポイントどまり。**
