# GLMタスク T7: 特殊機構11種の権威仕様テスト(Anthropic枠温存のためGLMで実施・7/30解約前の使い倒し)

送信元: claude-design → glm-impl
目的: 特殊な特性/技が権威仕様どおりに動くかを、専用テストスクリプトで1機構ずつ確かめる。**修正はしない**(バグ候補の報告が成果物)。

## 対象11機構(1個ずつ順番に・全部やる)
1. イリュージョン(化ける基準=手持ちの並び?・被ダメで解除・再発動条件)
2. スキルスワップ+トレース(特性入れ替え/コピーの対象制限・入れ替え後に登場時特性が再発動するか)
3. ミイラ(接触で相手特性を書き換え・書き換え不可リスト)
4. すてゼリフ(相手-1してから自分交代・まもられた時・クリアボディ相手・控え無し)
5. みがわり(HP1/4で分身・音技貫通・変化技ブロック・ドレイン/連続技との関係)
6. マルチスケイル(HP満タン時のみ半減・連続技は1発目だけ・かたやぶり無効)
7. さいせいりょく+しぜんかいふく(交代で回復・強制交代でも発動するか)
8. マジックガード(間接ダメージ無効の範囲・付与自体は防がない・こんらん自傷は?)
9. マジックミラー+マジックコート(跳ね返し対象/非対象・跳ね返し合い)
10. ばけのかわ(初撃無効+1/8削り・per-battleフラグ・連続技1発目のみ・かたやぶり貫通)
11. きあいのタスキ+がんじょう+こらえる(条件の違い・連続技との関係・優先順)

## 各機構の手順(省略禁止)
1. **権威仕様の確定**: ポケモンWiki日本語版(https://wiki.pokemonwiki.com/wiki/<名前>)をWebFetchで実読。**引用は実在する文字列のみ**(捏造厳禁)。曖昧はBulbapediaでクロスチェック。
2. **エンジン実装確認**: real_battle_simulator.html をgrepし実装行を読む。未実装なら「未実装」と報告してテストは書かない。
3. **専用テスト作成**: `tools/_spec_<slug>_test.js` を新規作成。既存 `tools/_sim_hard_interaction_test.js` のエンジン読み込みパターン(_sim_engine.js)を流用。**最低4ケース**(基本+連続技/交代/かたやぶり等の境界)。期待値の出典をコメントに(sim自身の出力をゴールデンにしない)。
4. **実行**: node で全ケース pass/fail。failは「エンジンのバグ」か「期待値の誤り」かを権威再読で切り分け。バグと確定した分だけ記録。
5. 1機構終わるごとに `reference/_spec_test_results_2026_07_28.json` に追記(mech/implemented/cases/pass/bugs[]/script)。

## 規律
- **エンジン・既存ハーネスの編集禁止**。新規ファイル(tools/_spec_*_test.js と結果JSON)のみ。commit/git add禁止。
- 決めつけない: failを即バグと断定せず期待値の側を先に疑う。
- 全部終わったら agmsg で claude-design へ: 機構ごとの pass/fail 表+バグ候補一覧+結果JSONのパス。
- 途中でz.aiの5時間枠に当たったら、その時点までの結果JSONを保存して「ここまで・枠待ち」と報告(やり直し不要の作りにする)。
