# 設計: 割り込み・連鎖・分岐の規律(ディスパッチ規律)

作成: 2026-07-26 / 発端: 阿部さん「入れ子になって差し込みになって、そこから分岐する。変なループとか止まったりしないように、ちゃんと終わりまで行く。そこの設計をしっかりしないといけない」
関連: [[phase-key-architecture-north-star]] / `設計_フェーズ語彙v3_2026-07-26.md`(どのフェーズがあるか) / 本書(そのフェーズ間をどう行き来するか)

---

## 0. これは何の設計か

語彙v3が「**箱の一覧**」なら、本書は「**箱と箱の間をどう飛ぶか**」の規律。
半年潰し続けた再発バグ(どくびし自ガード/交代入場特性の全不発/さめはだper-hit/ミイラ/じゃくてんほけん/だっしゅつパック/undo復元漏れ)は、**すべてこの「飛び方」に規律が無かったこと1つ**に帰着する(2026-07-23 監査D)。

---

## 1. 採用方式: 即時再帰カスケード + 5つの安全装置

- **見た目・タイミング=即時再帰**(CEDEC式)。Handlerが処理の途中で新しいEventを焚き、その場で潜って処理して戻る。実機の体感(せいでんき→即まひ→クラボのみ→即回復)に一致。
- **中身の規律=決定的**(MtG式スタックの良い所を借りる)。順序が毎回同じ・必ず終わる・undoできる。
- 理由: うちは**オンライン対戦(ロックステップ=両者が同じ計算をする)**と**undo(一手もどす)**があるため、「非決定的な即時再帰」は許されない。かといって全部キューにすると実機の体感からズレる。→ **即時だが決定的**が唯一の解。

## 2. 安全装置(5つ・全部必須)

### ① 発生源タグ(origin) — 自爆事故を構造的に殺す
すべてのEvent発火に `origin = {side, source, cause, eventId}` を必ず持たせる。
- **効く例**: どくびしT3バグ=「自分が どくびしを設置した宣言」が「自分のどく吸収ガード」を叩いて即消える事故。originに「自分の設置宣言」が入っていれば、ガード側が「自分起因は無視」と判断できる=**特例if文でなくデータで防げる**。
- 既存の `fromOpponent` 引数(applyRankStageGuarded)はこの概念の断片。**全Eventに一般化する**。

### ② 深さ上限(maxDepth) — 「止まらない」を保証
カスケードの深さを数え、既定 **10段** を超えたら打ち切り、`console.warn`+バトルログに記録。
- 実機で10段を超える連鎖は事実上存在しない(せいでんき→クラボで2段)。超えたら**それはバグ**なので、黙って無限ループせずに**見える形で止める**。
- 打ち切りは「バトルを壊さない」形で行う(その割り込みだけ捨て、本流は続行)。

### ③ 同時発火は決定的順序 — ロックステップを壊さない
同じEventに複数のHandlerが同時に反応する場合の順序は、必ず次で確定:
1. **すばやさ順**(速い方が先。トリックルーム中は逆転) ← 実機準拠
2. すばやさ同値なら **`canonSides()` の正準順**(ホスト基準・既存のオンライン用ヘルパー)
3. 同一個体内の複数Handlerは **登録順**(Handlerリストの配列順=コードで固定)
- ✗禁止: `['self','opp'].forEach` のような**側の固定順**(現エンジンの残存gap=メガシンカ順・ターン終了スリップ順)。

### ④ 再入禁止フラグ — A→B→A の輪を殺す
1つのEvent解決の中で、**同じ(Handler × 対象)の組は1回だけ**発火する。
- 既に `ものまねハーブ` の二重発動防止フラグで局所的に使っている手法を、**ディスパッチャの一般規律に格上げ**する。
- 「1ターンに1回」「1回の技に1回」など**発火レベル**(語彙v3の per-hit/per-move/per-turn)と組み合わせて判定する。

### ⑤ 本流への復帰保証 — 枝の奥でターンを終わらせない
割り込みの中で **ターン終了・勝敗確定・強制交代を直接実行してはいけない**。
- 割り込みは「ひんしになった」「交代が必要」を**フラグとして立てるだけ**にし、**本流(フェーズの進行)が拾って処理する**。
- 理由: 枝の奥で終了処理を走らせると、戻り先を失って状態が壊れる(=undo復元漏れ・死に出し不具合の温床)。
- 例外なし。ひんし処理は S05(faint Section)を**本流から**呼ぶ。

---

## 3. undo との関係(一手もどす)

- カスケードは**1つのトランザクション**として扱う。undoは「Event発火前のスナップショット」に戻す。
- 途中の状態(割り込みの最中)をundo地点にしない。
- 2026-07-21/22に根治したundo復元漏れ(壁/場8フィールド・ばけのかわdisguiseBroken)は、**カスケードで書き換わる状態を漏れなくスナップショットに含める**ことで再発を防ぐ → 新しいEventを足す時は「このEventが書き換える状態」を宣言させ、**スナップショット対象に自動で入る**ようにするのが理想形(手編集の漏れを構造的に無くす)。

## 4. 実装の進め方(一括書き直し禁止)

1. ✅**第1歩(完了・2026-07-26)**: `applyRankStageGuarded` → `rbFireEvent('stat_stage')` + 名前つきHandler 9個。全ハーネス緑(825/0・919/0・174/0・flag0)。
2. **第2歩**: ディスパッチャに①origin ②maxDepth ④再入禁止 を追加(stat_stageで先に効かせる)。既存挙動は不変を保証(stdout diffゼロをゲートに)。
3. **第3歩**: ③決定的順序をエンジン全体に適用(メガシンカ順・ターン終了スリップ順のgap修正)。**オンライン対戦のE2Eを必ず回す**(乱数消費順が変わるため)。
4. **第4歩**: S02(状態異常付与)をEvent化 → クラボのみ型の連鎖を一級市民に。以後、語彙v3のSectionを1つずつ。
5. 各段階で**全ハーネス緑+stdout diff**をゲートにする。赤なら戻す。

## 5. 検証(この規律が守られているかを機械で測る)

- **カスケード深さログ**: テスト実行時に最大深さを記録し、想定超え(>3程度)を検出したらレポート。
- **決定性テスト**: 同じ入力・同じシードで2回走らせて全ログ一致(既存のsim_test stdout diffの拡張)。
- **オンライン鏡写しテスト**: 既存の2ブラウザE2E(`scratchpad/e2e_online.js`)を第3歩の必須ゲートに。
- **敵対的テスト**: [[adversarial-scenario-testing]]の方針で「割り込みの裏を突く」シナリオを設計(まもる連打×じゃくてんほけん、みがわり×せいでんき、ひんし同時発生など)。
