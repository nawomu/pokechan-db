# HANDOFF 2026-07-28 PART2(午後〜夕・バトル修正 + ★データSSOT問題の発覚)

★次に読む人へ: **この回で「データが分裂している」という土台の問題が見つかった**。バトルの個別修正より、こちらが根。
★**枠の方針(2026-07-28 阿部さん)**: 週間枠は**金曜22:00リセットまで温存**。**少しずつやる。使い切らない。大きな作り直しは金曜以降**。

---

## 0. この回の一行まとめ

**バトルのバグを4件根治して全緑にした一方で、その過程で「Champions版と全国版のデータが別物になっている」という土台の破綻を発見し、絶対ルールとして文書化した。**

---

## 1. 完了したもの(すべて全緑・**未commit**)

| # | 中身 | 検証 |
|---|---|---|
| 1 | **メイカー4種のフィールド永続バグ**(エレキ/グラス/ミスト/サイコ)。`env.fieldTurns` を設定しておらず**フィールドが永久に消えなかった**。同名フィールドなら発動しないガードも追加 | sim825/0・hard174/0・sweep919/0 |
| 2 | **フォーカスレンズの死にコード**。`atk.actedAfterTarget` がどこにも立っておらず、命中1.2倍が**一度も発動していなかった** | 同上 |
| 3 | **メンタルハーブ**が `encore/disable/tormented` の3つしか解かず、**メロメロ・ちょうはつ・かいふくふうじが治らなかった**(権威は6状態) | 同上 |
| 4 | **メトロノーム**。連続使用回数を `calcDamage`(純粋な計算関数)の中で書き換えていたため、**技リストのダメージ表示・AIの読み・みらいよちが呼ぶだけで回数が進む/リセットされる**実バグ。数える場所を「技を実際に使った」確定点 `noteMoveUsed` **1箇所**に集約。**交代で回数がリセットされず控えへ持ち越されていた**のも同時に根治 | 同上+**専用テスト新設 `tools/_spec_metronome_test.js` 8/8** |

実装=1〜3はGLM/4はClaude。検証(回帰)は全部Claude。**エンジンを直したので commit時は `?v` bump が必要**。

## 2. ★新しい権威基盤: ヤックン `/ch/` チャンピオンズページ(315件取得)

- 保存: `reference/_authority_corpus_ch/abilities_ch.json`(特性315件・効果文・英語名・**Champions登場ポケモン数**)
- 取り方: **curlは403**。**Chromeの同一オリジンfetch**のみ。EUC-JP、**タイプ名はimgなのでalt復元が必須**。
  データはページ→ローカル受け口(POST)へ直接送って保存(**LLMの文脈を通さない=トークンゼロ**)。
  ★ CSPでhttps→http直POSTは不可 → **`window.name` に載せて localhost へ遷移してからPOST**する手が有効だった。
- **Champions実在の特性は201件**(残り114件はChampionsに使うポケモンがいない=全部版用)。

## 3. ★特性の三者照合(Champions文 ↔ うちのABILITY_DESC ↔ エンジン実装)

- 結果: **不一致90件**(未実装73/条件欠落14/数値相違1/未確認2)。**影響度=高が43件**。関わるChampionsポケモン延べ**186体**。
- 機械で裏取り: 「未実装」73件のうちエンジンに文字列が在るのは**1件だけ**(さまようたましい=要再確認)。
- データ: `reference/_ch_ability_check_all.json`(**Championsでの保有体数で降順**に並べ替え済み=着手順)。
- 重い順の例: ちからずく9体(条件欠落)/くいしんぼう7体(未実装)/ふみん7体/マイペース7体/プレッシャー7体/ものひろい6体/**どくのトゲ5体(未実装)**/かいりきバサミ4体/わるいてぐせ4体/トレース4体(除外リスト3件しかない・本来28件)。
- ★**型の指摘**: 「**同型の隣は実装済みなのに、これだけ抜けている**」が多発(せいでんき✅↔どくのトゲ❌ / あついしぼう✅↔たいねつ❌ / シェルアーマー✅↔カブトアーマー❌)。**単一ディスパッチ点が無く手で足していたから**=フェーズ設計で解こうとしている問題そのもの。

## 4. ★★土台の破綻(この回の最重要発見)

### 発端
阿部さんが画面で「**サーフゴーはおうごんのからだを持っているのに説明が出ない**」を発見。

### 実測した事実
- 技のキー体系が**完全に別**: Champions版=ローマ字 `reitoupanchi` / 全国版=英語スラグ `karate-chop` → **共通キー0件**
- 特性の説明: Champions版**195件** / 全国版**309件**
- `champions.in` の印が**10件立っていない** → 説明が生成されない(`うなぎのぼり/おうごんのからだ/きゅうばん/どくのトゲ/ほうし/ほのおのたてがみ/もふもふ/よちむ/エレキメイカー/カブトアーマー`)
- **★`build_champions_view` を素で回すと5件の説明文が消える**(どくのトゲ/メイカー4種=本番にだけ手で足された分)← **踏む寸前だった地雷**
- 名前の切り詰め「フラエッテ(えいえん)」→ **フラエッテナイトのメガシンカが到達不能**(実バグ)
- 配線が**循環**し移行が途中停止: `pokechan_data.js` は今も直接編集、正しい生成物 `pokechan_data.new.js` は**7/4で停止・未昇格**
- **生成コンテンツ4,900枚はデータが焼き込み**(第3の写し)= SSOTを直しても再生成しないと古いまま

### バトルは何を見ているか(実測)
| 画面 | データ |
|---|---|
| リアルバトル / オンライン | **Champions版 固定**(`?data=all` を付けない) |
| バトルラボ | 切替式(`mode==='all'` で全国版) |
| エンジン | **1本**。`?data=all` で読むファイルを差し替え |
→ **エンジンは1本、川(データ)が2本**。ブリッジ(`moveByChampKey`/`pokeByName`)で誤魔化してきたのが限界。

### 文書化した(=忘れる対策)
- **`CLAUDE.md` の最上位に「★★★データの絶対ルール」を新設**(北極星の直前)
- **`設計_データSSOT一本化_2026-07-28.md`**(現状の実測・配線図・移行手順)
- **`review/データ配線_現状調査_2026-07-28.html`**(全4,999ページ×全データの機械走査レポート)
- 記憶: `data-ssot-one-source-rule` / `champions-plus-alpha-canon-rule` / `yakkun-champions-authority`

---

## 5. ★反省点(次も必ず同じことをやる。だから書く)

1. **橋を架けて誤魔化した**。キー体系が違う2つのデータに `moveByChampKey`/`pokeByName` というブリッジを書いた時点で、「**川が2本あること自体がおかしい**」と気づくべきだった。**変換層を書きたくなったら、それは設計の破綻の合図**。
2. **権威を1つしか見ていなかった**。ポケモンWikiだけを正典扱いし、**Champions固有の記述(ヤックン `/ch/`)を見ていなかった**。危うく すいほう/たいねつ を第九世代仕様(相手のこうげき半減)で実装するところだった。正しくは Champions=**ダメージ半減/威力2倍**。
3. **手持ちのヤックンデータを「Champions」と思い込みかけた**。実際は `/ch/` を含まない**全部版用**(427技)。→ 全数検証したら **Champions側は無汚染(0件)** で、消さなくて正解だった。**「やばい」と思っても、まず全数で確かめてから動く**。
4. **「見つけたら直す」モグラ叩きを続けていた**。全数照合に切り替えた瞬間に90件出た。**サンプルの"ほぼOK"は偽の安心**。
5. **ビルダーを気軽に回さなくてよかった**。`build_champions_view` を素で実行していたら説明文5件が静かに消えていた。**再生成の前に「何が消えるか」を必ず全数で出す**。
6. **枠の使い方**。午後の前半で5時間枠を使い切って待機が発生した。**重い調査はワークフロー(Sonnet)+GLMに回し、Opusは判断と検証に絞る**([[fable-token-economy]])。

---

## 6. ★次にやること(小分けリスト・上から順・**1つずつ**)

### A. 今日の続き(軽い・枠を使わない範囲)
1. **今日の4件をcommit**(🙋阿部さん可否待ち)+ エンジン `?v` bump
2. `reference/_ch_ability_check_all.json` の**重い順に1件ずつ**実装(権威確認→フェーズ確認→実装→専用テスト)。**まとめて直さない**
   - 第1号候補=**どくのトゲ**(せいでんきの同型がすぐ隣にある=最も安全)

### B. データSSOT一本化(★金曜以降・大仕事)
1. **消える5件の説明文をSSOTへ退避**(これを先にやらないと失われる)
2. `champions.in` の印を**10件立てる**
3. **フラエッテを正式名称へ**(SSOT側。31箇所は再生成で追随)
4. 説明文が2種類ある5件の**正典を決める**(🙋阿部さんの耳)
5. **統一版 `pokechan_data_v2.js` を SSOT から生成**(キー=英語スラグ統一/名前=正式名称)
6. **`?data=v2` で二重公開** → 回帰+実機 → 安定後に旧削除 → ブリッジ撤去 → 逆流を断つ → コンテンツ4,900枚を再生成

### C. フェーズ設計(設計書は出来ている・実装はこれから)
- `reference/_phase_design_a1〜a4.json`(行動順の再評価/優先度補正Event/skip→can段/持続状態→常設Handler)
- `reference/_t11_spec_g1〜g4.json`(高影響11件の実装仕様)
- **接地判定 Q01 ground_check** は `設計_フェーズ語彙v3` §2.05 に記載済み(保存フラグにしない・攻撃側も引数=かたやぶり対応)
- ★設計中に見つかった未修正バグ: `runSingleAttack` と `runTurn` の**二重実装(こんらん・ひるみが片方に欠落)**/ばけのかわがみがわり・タイプ相性より先に評価/ちょうはつのPP消費順が権威と逆/ちょうはつの継続ターンが固定値/マジックルームを15箇所超が自己申告

---

## 7. 🙋 阿部さん判断待ち(たまり)

1. **今日の4件をcommitしてよいか**
2. **フラエッテの正式名称化**(31箇所波及)をやるか → 阿部さん「**正式名称の方がいい**」と表明済み。**B-3で実行予定**
3. **説明文が2種類ある5件**、どちらの言い方を正典にするか(例: どくのトゲ「…することがある」vs「30%の確率で…」。Champions正典は確率明記)
4. **キーを英語スラグに統一**してよいか(多言語と将来の追加が楽になる)
5. **接地判定 ground_check** を独立した箱として新設してよいか(語彙v3に記載済み・実装は未着手)

---

## 8. 現在の状態

- **全ハーネス緑**: sim 825/0・hard 174/0/skip11・sweep 919/0・**新設 metronome 8/8**
- エンジン `?v=20260728b`(**未bump=commit時に上げる**)。作業ツリーに**未commitの修正4件**
- 枠: Claude週間**64%**(金22:00リセット)/ GLM 5時間枠は消費済み・週間0%(8/4リセット)
- **GLMは待機中**(枠回復まで新規編集をしないよう指示済み)

---

## 9. ★夜の追記(データ一本化・決定的な発見と、作った道具)

### 9.1 ★★「全国版を土台にする」は間違いだった(危うく大事故)
Champions権威(**ヤックン `/ch/move_list.htm`・全497技の 威力/命中/PP/接触/守る**)を取得して全数照合:

| 照合 | 結果 |
|---|---|
| **うちのChampions版 vs Champions権威** | **491件一致 / 不一致5件** ✅ |
| **全国版のPP vs Champions権威** | **396件が食い違う** ❌ |

**Championsは引き分け防止でPP・回復系を大きく絞っている**(阿部さん)。
→ **全国版を土台に作り直していたら、396技のPPが静かに旧世代へ巻き戻っていた。**

**修正した設計** = **「器」と「中身」を分ける**:
- **器(全部を入れる箱)= 全国版の範囲**(1219体/919技/310特性/167持ち物)
- **中身(値)の正典 = Champions**。①Championsに在る→Champions権威で上書き ②無い→最新世代を参考として入れる ③後から追加されたら**その1件だけ**調べ直して上書き
- ★これなら **Championsに新しいポケモン/技/持ち物が追加されても、旧来の値を少し変えるだけで済む**

**うちのChampions版の取りこぼし5件**: たきのぼり(権威PP16/うち12)・どくばりセンボン(12/10)・ソウルクラッシュ(16/15)・はいすいのじん(8/5)・ふんどのこぶし(12/10)

### 9.2 作った道具(2つ)
| 道具 | 中身 |
|---|---|
| **`pokedb.js`**(新設) | ★**参照元の一本化**。ページは**この1枚だけ**を読む(既存グローバル `POKEMON_LIST` 等はそのまま生えるので**ページのロジックは1行も変えなくてよい**)。供給元の差し替えは**この中だけ**で完結。`PokeDB.healthCheck()` で自己診断可 |
| **`tools/_ssot_guard_test.js`**(新設) | ★**破ったら落ちる番人**。G1消える資産5件 / G2説明なし特性8件 / G3名前の切り詰め**17件** / G4 DB間の説明文差0件。**基準(`reference/_ssot_guard_baseline.json`)より悪化したら exit 1**。★**基準を上げて通すのは禁止**(コード内に明記) |

### 9.3 番人が掘り当てたもの
- **名前の切り詰めは17件**(ビビヨン/フラエッテ/フラージェス/トリミアン/ギルガルド2/ルガルガン3/ミミッキュ/ポットデス/マホイップ/モルペコ/イッカネズミ/イルカマン2/ヤバソチャ)
- **到達不能のメガが2つ**: **フラエッテナイト**(対象名が不一致)+ **ニャオニクスナイト**(対象「ニャオニクス」に対し実名は「ニャオニクス♀/♂」)
  → **メガフラエッテ・メガニャオニクス♀・メガニャオニクス♂ の3体は誰もメガシンカできない**

### 9.4 GLMの現状把握(T12・4本完了)
| 棚卸し | 出力 | 要点 |
|---|---|---|
| 1 項目の差 | `_inv1_field_gap.json` | Champions側に**技習得フラグ169列**(POKEMON_WAZAのサブセット=捨てられる見込み)。全国版のみ gen/legend/season・priority/tags/availability |
| 2 値の差 | `_inv2_value_gap.json` | **技のPP差392件**(→§9.1でChampionsが正と判明)・ポケモン名の食い違い68件・**特性の説明文差は0**(Championsは全国版の部分集合) |
| 3 手作り資産 | `_inv3_handmade_assets.json` | 危ないのは**特性5件のChampions独自文**(再ビルドで公式文に上書きされ消える)+**技flags4件**(ブレイク/ドラゴン/シャドー/フェイタルクロー)が生成物のみ |
| 4 生成系I/O | `_inv4_builders.json` | ★**循環を確認**: `build_master.js`(生成物→SSOT)↔ `build_champions_view.js`(SSOT→生成物)。停滞ファイル多数・**旧 `*_master.json` と新 `master_*.json` の名前衝突**あり |

### 9.5 ★priorityは失われていなかった(誤報を訂正)
「全国版にしか priority が無い」という棚卸し1の指摘は、**置き場所の違い**だった。
Champions版は `battle_data.priority` に持ち、**エンジンもそこから読む**(`real_battle_simulator.html:2290`)。バトルは正常。引っ越し時にschemaを揃える。

### 9.6 次にやること(更新)
1. **Champions権威との全数照合を、ポケモン・特性・持ち物にも広げる** → これが**新DBの中身の正解表**になる(技は完了=497件)
2. 新DB設計の確定(器=全国版の範囲 / 中身=Champions優先)
3. `pokedb.js` を**1ページずつ**適用(まず1枚で実証 → 全ページ)
4. 番人を `release_check` に組み込む
5. 今日のエンジン修正4件のcommit(🙋可否待ち)

---

# 【2026-07-29 追記】管理ツール新設 + AIの交代ロジック強化

## A. 完了したもの

### A-1. エンジンのバグ4件を **commit 済み**(`db61dce4`・**pushはまだ**)
メイカーのフィールド永続 / フォーカスレンズの死にコード / メンタルハーブ3→6状態 / メトロノームの計算副作用+交代持ち越し。
エンジン `?v`: 20260728c → **20260729a**(battle_lab / real_battle / online_battle / **damage_calc=取り残されていたのを発見して統一**)。

### A-2. ★管理ツール(阿部さんが見る画面)を新設 — **ローカル専用**
| もの | 中身 |
|---|---|
| **`PchamDB管理ツールを開く.command`** | **ダブルクリックで起動**(ターミナル不要)。すでに起動中なら開くだけ |
| `PchamDBサーバーを止める.command` | サーバー停止 |
| **`管理ツール.html`** | 入口。全リンク**新規タブ**で開く。各ページ右下に **🏠 管理ツール** ボタン(`internal_home.js`・**localhostでだけ表示**) |
| **`data_browser.html`** | ★**データ一覧**(エクセル風)。ポケモン313/技498/特性200/持ち物169/覚える技313。タイプ色・棒グラフ・列の出し入れ・名前列固定・Champions⇄全国切替。**参照元は `pokedb.js` 1枚**(引っ越し第1号) |
| **`review/内部構造仕様書.html`** | `node tools/_build_internal_spec.js`。**随時チェック**。差分の基準は「**阿部さんが最後に確認した時点**」(`--reviewed` で更新) |
| **`review/AIの仕組み.html`** | `node tools/_build_ai_spec.js`。★**数字と行番号はソースから直接抜く**(コードを変えればページも変わる。抜けなくなったら「★抽出できず」と赤で出る) |
| 公開しない対策 | noindex + robots.txt(`data_browser` / `管理ツール` / `review/`) |

### A-3. ★AIの交代ロジックを強化(**全緑・専用テスト7本 新設**)
`tools/_spec_ai_switch_test.js`(7/7 pass)。実装は `aiChooseAction` の **C) ブロック** + ヘルパ3つ。

| 追加した判断 | 中身 |
|---|---|
| **C) 相性による予防交代** | 相手の一撃で**残HPの半分以上**を持っていかれる & **こちらに効果抜群が無い** & **後攻** → 受けられる控えへ |
| **受け役の条件** | 脅威技を**半減以下**で受けられる(★**特性による無効/半減も込み**)。同率なら**相手に効果抜群を返せる子**を優先 |
| **★特性を見る** | `aiBenchTakes()`。ふゆう/もらいび/ちょすい/よびみず/かんそうはだ/そうしょく/ひらいしん/でんきエンジン/ちくでん=**無効**、あついしぼう/たいねつ=**半減**。**全件Champions権威で裏取り済み** |
| **★設置物のコスト** | `aiHazardCost()` `aiSwitchAffordable()`。ステルスロック(いわ相性÷8)+まきびし(層で1/8〜1/4)を見積もり、**出た瞬間に倒れる/残HPの半分以上減る控えには逃がさない**。あつぞこブーツ・接地判定も考慮。**A)B)C)の全交代に適用** |
| **★先制技で倒せるなら殴る** | 確定KOが複数ある時は**優先度の高い技**を選ぶ(後攻でも先に倒せる) |
| **先手を取れるなら殴る** | 速い側は交代しない(手数を捨てない。既存契約 T282b) |
| **★判断理由の記録** | `st._aiReason`(`move_確定KO` / `switch_B_受け交代` / `switch_C_相性で退く` / `switch_A_手詰まり` 等)。**テストと将来のフェーズ・ビューアで使う** |

## B. ★注意書き(今日つまずいた所・次も必ず起きる)

1. **★「数が合わない」時は、まず自分の取り方を疑う**。今日3回やらかした:
   - 種族値39件差 → **番号だけで突合**した私のミス(名前で照合し直したら**0件差**)
   - learners食い違い17,109件 → **データ構造を確認せず**書いた私のミス(実際は**完全一致**)
   - 権威の学習データ208件 → **図鑑番号だけで巡回して地方フォーム/メガを取り漏らし**(313体あるのに208件=サインは出ていた)
2. **突き合わせは「名前」と「番号」の両方を使う**。片方だけでは必ず取り違える。**比較前に全角→半角の正規化**も必須。
3. **★「履歴にあるから正しい」とも決めつけない**。コインビームは「阿部さん依頼で追加」というコミット履歴があったが**実在しない技**だった。**両方向(権威に無い/履歴にある)とも個別に素性を調べる**。
4. **★「?」つきのメモを、確認せずにデータへ昇格させない**。コインビームの発生源は `tools/_research_mb_signature_moves.js:46` の「**専用技あり?**」という仮説。それがデータ化→ビルダーの特例→9言語辞書まで拡散した。
5. **テストが落ちたら、まず「テストの設定が悪いのでは」を疑う**。今日はAIテストで3回とも私の設定ミス(リザードンはじめん無効/いわ4倍は既存B)が先に発動/設置ダメージ4では死なない)。**`_aiReason` のような「なぜそうしたか」の記録があると一発で分かる**。
6. **GLMに途中結論を渡すと、それが記録に残る**。コインビームで実際に起きた(誤結論がGLMのメモに固定された→訂正を送った)。**確定していない話は「暫定」と明示して渡す**。

## C. ★次にやること(優先順)

### C-1. すぐやる(データ一本化・①の続き)
1. **フォーム219件の取得を完了 → `learnsets_ch.json` に統合**(いま巡回中。地方フォーム/メガが埋まる)
2. **GLMにT18(学習データの全数照合)をやり直させる** — 最重要は「**うちに在るが権威では没収されている技**」
3. **学習データが空の38体を埋める**(サーフゴー・オーロンゲ等。権威に38/38すべて存在を確認済み)

### C-2. マスターデータの構築(①の本体)
4. 消える資産の退避 → `champions.in` の印10件 → **名前を正式名称へ**(`display_name` は別欄)
5. **`master/` に6本を生成**(pokemon/moves/abilities/items/learnsets/regulations)
   - ★**コインビームは持ち込まない**(削除決定済み)
   - ★特性・持ち物の説明は**公式のゲーム内テキスト(Champions)をそのまま**。**世代で変わるので「Champions対応か」を必ず確認**
6. **番人に検査を追加**: ①説明文==effectsから再生成した文 ②オーバーレイ件数 ③フラグ列↔POKEMON_WAZA ④名前の正規化

### C-3. ②ページの作り変え
7. `pokedb.js` に新DBを1行足す → **`?data=v2` で二重公開** → 1ページずつ切り替え → 安定後に旧削除

### C-4. ③バトル(①②の完了後)
8. 特性の不一致90件 / フェーズ差し込み4本 / 接地判定 Q01
9. ★**フェーズ・ビューア**(1フェーズずつ止めて見る画面)。`_aiReason` があるので**AIが何点つけて何を選んだかも一緒に出せる**

## D. 🙋 阿部さん判断待ち
1. **`db61dce4` を本番pushするか**(バグ修正4件・全緑)
2. AIの交代ロジックも**同様にcommit/pushするか**(いま未commit)
