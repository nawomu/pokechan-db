# Codex引き継ぎ実験(2026-09-02 阿部さん指示)

> 「ChatGPT Pro(¥16,800)で、今Claude Codeにやらせてるのを全部引き継いで試したい。最初はClaudeが監視、良さそうなら監視もやめてCodexだけに任せる」

## 目的
同じ作業・同じ品質ゲートで **性能とコスト感覚を実測比較** する。Claude側の実測基準(既取得): 技監査R1 100技=照合+反証200エージェント・≈15Mトークン・≈18分・失敗0。

## 段階
| 段階 | 体制 | 内容 |
|---|---|---|
| P0 | 阿部さん | ChatGPT Pro課金 → `! npm i -g @openai/codex` → `! codex login` |
| P1 | Codex実行+**Claude監視** | まず小さく: バッチ8の先頭10技だけ `bash tools/codex_move_audit.sh <材料dir> 3 10`。Claudeが結果の形式・引用実在・判定品質を抜き打ち検証→OKならバッチ8全量→9→10 |
| P2 | Codex実行+Claude最終確認のみ | Codexが自走(ランナー+ゲート)。Claudeはcommit前の差分確認だけ |
| P3 | **Codex単独** | 監視なしで1タスク任せる(候補: 全国限定learners残差51件の調査、非ja孤児ページの検証)。結果を後からClaudeが監査して比較レポート |

## 計測(比較レポートの軸)
- 消費: Codex側=メッセージ数/週間枠の%(阿部さんの画面)・Claude側=subagentトークン(帳簿 `_window_ledger.json`)
- 速度: 100技あたりの実時間
- 品質: ①ゲート通過 ②引用の実在(機械検査) ③Claudeによる抜き打ち反証(10件) ④発見数と精度(偽陽性率)
- 運用性: 枠切れ時の再開・自動化(cron/Automations)の組みやすさ

## 引き継げるもの(全部そのまま)
リポジトリの全道具(番人・ビルド・PDCA・X投稿スクリプト)。ルール=CLAUDE.md(Codexは AGENTS.md 経由で読む)。
## Claude固有で作り直しが要るもの
Workflowオーケストレーション(→ `tools/codex_move_audit.sh` に移植済み・未実測)・ScheduleWakeup(→ cron+`codex exec` or CodexアプリのAutomations)。

## 注意
- ランナーは**未実測**(Codex未インストールのため)。P1の最初の10件で `codex exec` のフラグ(--output-last-message 等)を実環境に合わせて直すこと。
- agmsg(GLM時代の連絡インフラ)は併用フェーズで使う: Codex→Claudeの報告や依頼は `~/.agents/skills/agmsg/scripts/send.sh`。
