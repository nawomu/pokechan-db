# 設計: データのSSOT一本化(一箇所を見る・一箇所を変えれば全部変わる)

作成 2026-07-28 JST / 指示=阿部さん(「**データは一つ。一箇所変えれば全部変わるようにしなきゃいけない**」「**前に言ってたんだけど、やっぱ忘れてくるんだよね**」)
★**この文書は「忘れる」ことへの対策**。データを触る作業の前に必ず読む。

---

## 0. 結論(これだけ覚える)

1. **SSOT = `reference/master_{pokemon,moves,abilities,items}.json`。ここだけを直す。**
2. **`pokechan_data.js`(Champions版)と `pokechan_data_all.js`(全国版)は"生成物"**。直接編集しない。
3. **名前は正式名称を使う**(略さない・切り詰めない)。例: ✗「フラエッテ(えいえん)」→ ✓「**フラエッテ(えいえんのはな)**」。英語名も PokeAPI の**完全名**を使う。
4. **作り直しは"別で作ってから入れ替える"**。新版を別ファイルで生成 → 差分を確認 → 本番へ昇格 → **古いものを消すのは最後**。
5. **多言語(i18n)も同じSSOTから引く**。

---

## 1. いま何が起きているか(2026-07-28 実測・推測ではない)

### 発端
阿部さんが画面で発見: **サーフゴーは「おうごんのからだ」を持っているのに、説明が出ない**。
調べると **Champions版と全国版で、同じはずのデータが別物になっていた**。

| ファイル | ABILITY_DESC | おうごんのからだ |
|---|---|---|
| `pokechan_data.js`(Champions本番) | **195件** | **無い** |
| `pokechan_data_all.js`(全国版) | **309件** | 有る |
| `reference/master_abilities.json`(SSOTのはず) | 313件(全部 effect_ja あり) | 有る(が `champions.in` の印が立っていない) |

### 原因 = **移行が途中で止まり、配線が循環している**

```
   [手で直す] ──▶ pokechan_data.js ──(build_master.js)──▶ master_*.json
                        ▲                                      │
                        │                          (build_champions_view.js)
                        │                                      ▼
                    ※昇格されていない ◀────────────── pokechan_data.new.js(7/4で停止)
```

- `pokechan_data.js` は **7/28 14:16** に更新(=**いまも直接編集されている**)
- `pokechan_data.new.js`(master から作る正しい生成物)は **7/4 で止まったまま=昇格されていない**
- つまり **master は「pokechan_data.js から作った写し」**にすぎず、**SSOTとして機能していない**

### その結果の実害(全部実測)
1. **`champions.in` の印が10件立っていない** → Championsのポケモンが実際に持っているのに説明文が生成されない
   `うなぎのぼり` `おうごんのからだ` `きゅうばん` `どくのトゲ` `ほうし` `ほのおのたてがみ` `もふもふ` `よちむ` `エレキメイカー` `カブトアーマー`
   → 画面は「(説明データなし)」。**説明が無い=存在に気づかない=エンジン未実装の温床**になっていた。
2. **★いま `build_champions_view` を素で回すと5件の説明文が消える**
   `どくのトゲ` `エレキメイカー` `グラスメイカー` `サイコメイカー` `ミストメイカー`(本番にだけ手で足された分)
3. **同じ特性の説明文が2種類ある**(例: どくのトゲ = 本番「…どく状態にすることがある。」/ master「…30%の確率でどく状態にする。」)
4. **名前が切り詰められている**: Championsデータの「フラエッテ(えいえん)」に対し、権威・全国版は「フラエッテ(えいえんのはな)」
   → メガシンカの持ち物 `フラエッテナイト` が **applies_to と名前が一致せず、到達不能**(実バグ)

---

## 2. あるべき形(一方向・一箇所)

```
                    ┌──────────────────────────────┐
   権威(Champions)  │  SSOT = reference/master_*.json │
   ヤックン /ch/  ──▶│  ここ「だけ」を直す                │
   ポケモンWiki      └──────────────┬───────────────┘
                                   │(生成は一方向・逆流禁止)
        ┌──────────────┬───────────┴────────┬──────────────┐
        ▼              ▼                    ▼              ▼
  pokechan_data.js  pokechan_data_all.js   i18n/*.json   バトル仕様書
   (Champions版)      (全国版)             (9言語)      (自動生成)
```

- **逆流禁止**: 生成物 → SSOT の向き(`build_master.js` が `pokechan_data.js` を読む形)は**移行完了時に断つ**。
- **Champions と 全国版の関係**: [[champions-plus-alpha-canon-rule]] のとおり **①Champions仕様が正典 → ②無ければ最新世代**。
  **同じ機構の説明・数値がファイルごとに違う状態を許さない**(違ってよいのは「Championsに在るか」の印だけ)。

---

## 3. 作り直しの手順(★阿部さん指示=別で作ってから入れ替える)

**壊れるかもしれない作り直しを、本番の上で直接やらない。**

1. **旧はそのまま残す**(消さない)
2. **新版を別ファイルで生成**(`*.new.js` / `*_v2.json` 等)
3. **差分を全数で確認**(消える項目・変わる文言を1件ずつ。★「消える5件」のような事故はここで捕まえる)
4. **回帰テストを通す**(sim / hard / sweep / i18n監査)
5. **本番へ昇格**(置き換え)
6. **本番で動作確認してから、古いものを削除**

---

## 4. 名前の規律(2026-07-28 阿部さん)

- **正式名称を使う。略さない・切り詰めない。** 例: 「フラエッテ(えいえんのはな)」
- **英語名も同様**。PokeAPI の **`pokemon_name`(完全名)** を使う。`form_names`(『Hisuian Form』等のフォームラベル)を種名の代わりに使わない([[i18n-pipeline]] の468件事故と同じ罠)。
- 名前を変えると**波及が大きい**(フラエッテの例で31箇所)。だから **SSOTで1回変えて、全部を再生成する**形にしておく必要がある。

---

## 5. 追加・最新化のときの規律

- **技・特性・持ち物・ポケモンを追加/更新する時は、SSOTだけを直す**。両方のDBを手で直さない。
- **最新世代への差し替え**が必要になったら、上の§3の手順(別で作る→差分→昇格→旧削除)で行う。
- **Championsに追加・変更があったら、新しい情報で調べ直して上書きする**([[champions-plus-alpha-canon-rule]])。
- 各項目に「**Champions確認済みか / 最新世代からの暫定か**」の印を持たせる(どれを上書きすべきか後から分かるように)。

---

## 6. いますぐやること(順番厳守・順番を間違えるとデータが消える)

1. **消えると分かっている5件の説明文をSSOTへ退避**(`どくのトゲ`/メイカー4種)
2. **`champions.in` の印を10件立てる**(Championsのポケモンが実際に持っている特性)
3. **説明文が2種類ある5件はどちらを正典にするか決める**(★阿部さんの耳=北極星。機械では決められない)
4. **フラエッテの名前を正式名称へ**(SSOT側・31箇所は再生成で追随)
5. `build_champions_view` → `.new` で生成 → **全数差分** → 回帰 → 昇格 → 旧削除
6. 昇格後、**`build_master.js` の逆流(生成物→SSOT)を断つ**

★ 説明文の文言は **権威の丸写しにしない**(盗用リスク)。**意味の参照に使い、言葉はうちの流儀で書く**(北極星=子どもが読んで意味に戻れるか)。

---

## 7. 関連

- [[champions-plus-alpha-canon-rule]] 出典の優先順位(①Champions ②最新世代 ③判明したら上書き)
- [[yakkun-champions-authority]] 権威の二本立て(Wiki + ヤックン `/ch/`)/ 手持ちヤックン427件は非Champions版
- `設計_バトルエンジン原理_2026-07-26.md` バトル設計の憲法
- `reference/_ch_ability_flag_gap.json` 今回の実測データ(印が立っていない10件ほか)

---

## 8. ★引っ越し方式(2026-07-28 阿部さん確定)= 「今のは直さない。新しいDBを先に作って、供給元を向け替える」

> 阿部さんの言葉: **「今のはその都度合わせない方がいい。中のデータをどこから持ってくるかを一本化するだけ。先にDBを作っておいて、そこから持ってこいってやればいい。上っ面は残して、引っ越す。」**

### 8.1 原則
1. **今のデータは凍結する**(直さない・その都度合わせない)。**直すと、直した分だけ差が増えてぐちゃぐちゃになる**。
2. **新しいDBを先に完成させる**。それができるまで、上っ面(HTML/エンジン/演出)は**一切触らない**。
3. **引っ越し = 各ページの「データの供給元」を1つに向け替えるだけ**。ページの中身は変えない。
4. **1ページずつ切り替える**。全部を一斉に切り替えない(戻れなくなる)。
5. **旧は動いたまま残す** → 新が安定してから捨てる。

### 8.2 新DBの形(1本)
- **全国版(上位集合)を土台にする**。Championsは「絞り込み」= `champions: true/false` の**印**で表す。
  (逆にしない。Championsを土台にすると、また"外側を後から足す"形になり同じ失敗をする)
- **キーは英語スラグに統一**(PokeAPI準拠)。Champions側のローマ字キー(`reitoupanchi`)は**対応表を作って捨てる**。
- **名前は正式名称**(「フラエッテ(えいえんのはな)」)。
- **1件に「どこ由来か」を持たせる**: `source: "champions" | "gen9" | …` + `verified_at`。
  → [[champions-plus-alpha-canon-rule]] の①②③(Champions優先→無ければ最新世代→判明したら上書き)が**データ上で見える**ようになる。
- **道具も同じDBに入れる**(いまは `items_database.js` だけ別建て)。

### 8.3 順番(この順にしか進めない)
| 段 | やること | なぜこの順か |
|---|---|---|
| **0** | **現状把握=資産の棚卸し**(片方にしか無い情報を全部出す) | ★**これをやらないと静かに失う**(実例: 再ビルドで説明文5件が消える) |
| **1** | **「止める仕組み」を先に作る**(§8.4) | **作り直しの最中こそ事故が起きる**から先に |
| **2** | **キー対応表**(Championsローマ字 ↔ 英語スラグ ↔ 図鑑番号) | 全部の土台。ここが無いと繋がらない |
| **3** | **ポケモン** → **4. 技** → **5. 特性** → **6. 道具** を新DBへ | 依存の順(技/特性/道具はポケモンから参照される) |
| **7** | **ビューを生成**(Champions/全国/コンテンツ4,900枚/多言語) | DBが固まってから |
| **8** | **`?data=v2` で二重公開** → 実機とハーネスで確認 | **戻れる状態を保ったまま試す** |
| **9** | **1ページずつ供給元を切り替える** | 一斉にやらない |
| **10** | 安定後に**旧を削除** → **ブリッジ撤去** → **逆流を断つ** | 最後 |

### 8.4 ★「ルールが保持されない」への構造的対策(=文章で書くだけでは守られない)
**実証済みの事実**: 「データは一本化」と決めていたのに分裂した。**セッションが変わると文章のルールは失われる**。
→ **機械が拒否する形**にする。三重:

| 層 | 仕組み | 効果 |
|---|---|---|
| 読ませる | `CLAUDE.md` 最上位 + 記憶(**済み**) | 読めば分かる。だが**読み飛ばしうる** |
| **止める** | ①**生成物の先頭に「自動生成・直接編集禁止・SSOT=…」のヘッダ** ②**hookで生成物への Edit/Write をブロック** | **セッションが変わっても効く**。人もAIも物理的に書けない |
| **落ちる** | **SSOT監査テストを常設**: ①生成物 == SSOTから再生成した内容か ②同じ機構の値が2つのDBで一致するか ③名前の切り詰めが無いか ④生成物にしか無い資産が無いか → `release_check` に組み込む | **破ったら赤くなる**。忘れても検出される |

★**文章は忘れられるが、落ちるテストは忘れられない。**

### 8.5 引っ越しで「作り直さない」もの(資産として運ぶ)
- 技の**独自説明文**(`description`)と `description_legacy`、**effects/flags/tags**
- **ABILITY_DESC**(家の流儀の文言)
- 演出データ(`battle_fx_cues` 等)・スプライト・i18n辞書
→ **移送するだけ**。北極星で積み上げた文言を作り直さない。

---

## 9. ★★決定的な発見(2026-07-28 夜)= 「器」と「中身」を分けて考える

### 何が起きたか
§8.2で「**全国版(上位集合)を土台にする**」と書いた。**これは危うく大事故になるところだった。**

Champions権威(ヤックン `/ch/move_list.htm`・全497技の 威力/命中/PP/接触/守る)を取得して全数照合した結果:

| 照合 | 結果 |
|---|---|
| **うちのChampions版 vs Champions権威** | **491件一致 / 不一致わずか5件** ✅ |
| **全国版のPP vs Champions権威** | **396件が食い違う** ❌ |

**Championsは引き分け防止のためにPP・回復系を大きく絞っている**(2026-07-28 阿部さん)。
→ **全国版を土台にして作り直していたら、396技のPPが静かに旧世代へ巻き戻っていた。**

### 直した設計
- ❌ 「全国版を土台にして、Championsの印を立てる」
- ✅ **「全国版を『器』にして、Championsに在るものは Champions値で上書きする」**

**器(=全ポケモン・全技・全特性・全持ち物を入れる箱)は全国版**でよい。
**しかし値の正典はChampions。** 器と中身を分けて考える。

```
   器(全部入る箱) = 全国版の範囲(1219体 / 919技 / 310特性 / 167持ち物)
        └ 中身の値 = ①Championsに在る → **Champions権威の値で上書き**(最新・正典)
                     ②Championsに無い → 最新世代(第九→第八→第七…実在する最新)の値を入れる(**参考として置く**)
                     ③後からChampionsに追加されたら → **その1件だけ調べ直して上書き**すればよい
```
★この形にしておけば、**Championsに新しいポケモン/技/持ち物が追加されても、旧来の値を少し変えるだけで済む**。

### ついでに見つかった、うちのChampions版の取りこぼし(5件)
`たきのぼり`(権威PP16/うち12) `どくばりセンボン`(12/10) `ソウルクラッシュ`(16/15) `はいすいのじん`(8/5) `ふんどのこぶし`(12/10)

---

## 10. ★失敗の記録(この設計は「やらかし」から生まれた。忘れたらまた同じことをする)

**★この節が本体。** ルールだけ読んでも守られない。**なぜそのルールが要るのか=何をやらかしたか**を残す。

| # | やらかし | 何が起きたか | そこから出たルール |
|---|---|---|---|
| 1 | **「データは一本化」と決めたのに、全国版を別に取ってきた** | Champions版と全国版が**別体系**に(技キーの共通0件・説明195 vs 309) | **データは一つ。絶対に一つ。ページごとに作らない** |
| 2 | **変換層(ブリッジ)を書いて誤魔化した** | `moveByChampKey`/`pokeByName` を書いた時点で気づくべきだった | **変換層を書きたくなったら設計破綻の合図** |
| 3 | **生成物を手で直した** | `pokechan_data.js` に手で足した説明文5件が、**再ビルドで消える状態**になった | **生成物は直接編集しない。SSOTを直す** |
| 4 | **印(フラグ)の整合を誰も見ていなかった** | `champions.in` が10件立っておらず、サーフゴーの「おうごんのからだ」が**説明なし・エンジン未実装** | **印の整合は機械で検査する**(番人) |
| 5 | **名前を切り詰めた** | 「フラエッテ(えいえん)」等**17件**。フラエッテナイト/ニャオニクスナイトの**メガシンカが到達不能** | **名前は正式名称。略さない** |
| 6 | **権威を1つしか見ていなかった** | ポケモンWikiだけを正典扱いし、Champions固有(ヤックン`/ch/`)を見ていなかった。すいほう/たいねつを第九世代仕様で実装しかけた | **権威は二本立て。Champions固有を必ず確認** |
| 7 | **手持ちのヤックンデータをChampionsだと思い込みかけた** | 実際は非Champions(全部版用427技)。全数検証したら**Champions側は無汚染(0件)**で、消さなくて正解だった | **「やばい」と思っても、まず全数で確かめてから動く** |
| 8 | **★新しい方が正しいと思い込んだ** | 「全国版の方が後から作ったから新しい」→ **PPは396件が旧世代の値だった**。危うく巻き戻すところだった | **新しく作った方が正しいとは限らない。正典に当てて確かめる** |
| 9 | **見つけたら直すモグラ叩き** | 全数照合に切り替えた瞬間に特性の不一致が90件出た | **全数でやる。サンプルの「ほぼOK」は偽の安心** |
| 10 | **再生成を気軽に回しかけた** | `build_champions_view` を素で実行していたら説明文5件が静かに消えていた | **再生成の前に「何が消えるか」を全数で出す** |

★**共通する根**: 「**足りないから、もう一つ作る**」を繰り返したこと。足りないときは**一つのものを広げる**。

---

## 11. ★なぜ一本化するのか(理由。ルールだけでは守られないので、理由を残す)

### 11.1 メンテナンス(2026-07-28 阿部さん)
> **「一箇所直せば全部変わる、それが大事。ページが増えれば増えるほど、参照箇所がずれる・全部直さないといけない・チェックをやり直さないといけない。メンテナンスと今後のことを考えてデータを一つにする。」**

実際にページは **4,999枚**まで増えている。データが分かれていると、
1件直すたびに **「どのページが古いまま残っているか」を探す作業**が発生し、**検証もページ数ぶん増える**。

### 11.2 多言語化(2026-07-28 阿部さん)
> **「一本化がまた多言語化にもつながる。一本であれば全部多言語で変わる。ページの見出しとか項目名とかも全部。技の説明とかも。一本化すれば多言語化も一本化できる。無駄が減る。効率がいい。」**

- いまは **9言語 × 生成ページ4,900枚**。データが分かれていると、**同じ翻訳を何度も当て直す**ことになる。
- **データが一本なら、翻訳も「そのデータに1回だけ」当てればよい**。名前・技名・特性名・説明文・見出し・項目名が**全部そこから出る**。
- 逆に、いまの分裂状態は **i18n事故の温床**でもあった(468件のフォーム名事故・技名辞書845件の系統バグは、いずれも「参照元が複数あって、どれが正か分からない」ことが背景)。
- → **一本化 = 多言語化の一本化**。[[i18n-ssot-render-rule]] とも一致(データはIDで参照して描画する)。

### 11.3 追加のしやすさ
Championsに新しいポケモン/技/持ち物が追加されたとき、**器はもう全部入っている**ので、
**その1件の値をChampions権威で上書きするだけ**で済む(§9)。分かれていると**追加のたびに2箇所+生成ページ**を触ることになる。

---

## 12. 実装した道具(2026-07-28)

| 道具 | 役割 | 確認 |
|---|---|---|
| **`pokedb.js`** | ★**参照元の一本化**。ページはこの1枚だけを読む。供給元の差し替えはこの中だけで完結 | 実機確認済(mode/pokemon313/moves498/abilityDesc195/learnsets275/ok=true・**既存グローバルも従来どおり生える**=ページのロジック変更ゼロ) |
| **`tools/_ssot_guard_test.js`** | ★**破ったら落ちる番人**(消える資産/説明なし特性/名前の切り詰め/DB間の食い違い) | 実行済。基準=`reference/_ssot_guard_baseline.json`。**基準を上げて通すのは禁止** |
| **Champions権威コーパス** | `reference/_authority_corpus_ch/`(特性315件・**技497件**=威力/命中/PP/接触/守る) | 技はうちのChampions版と**491/497一致**を確認 |

★**pokedb.js の実装メモ(次に触る人へ)**: データ側は `const POKEMON_LIST = …` のトップレベル宣言なので **`window.POKEMON_LIST` では取れない**(script直下の const/let は「グローバル字句環境」に入る)。窓口は**識別子を直接参照**すること。2026-07-28 の実機確認で判明(healthCheckが全部0を返した)。

---

## 13. ★技のグループ分け(ポケモンDB右側の技列)も一本化の対象(2026-07-28 阿部さん)

> **「ポケモンDBを作る時にグループ分けが毎回ぐちゃぐちゃになる。DBの右側にダーッとある技の列がずれてくる気がする。技リストとも繋がっているので、威力いくら・命中率いくらも含めて全部一本化してほしい。」**

### 正体(棚卸しで判明)
Champions版の **ポケモン1件に「その技を覚えるか」のフラグ列が169個**焼き込まれている
(`chouhatsu` `jikosaisei` `haneyasume` `makibishi` … = DBの右側に並ぶ技の列)。
一方で **`POKEMON_WAZA`(1219体分の覚える技)と `learners` も別に存在する**。
→ **同じ「誰がどの技を覚えるか」が3箇所にある**。だから**ずれる**。
(GLM棚卸し1のサンプル検証では、169フラグは `POKEMON_WAZA` の部分集合=**導出可能**)

### 一本化のルール
1. **フラグ列は持たない。`POKEMON_WAZA` から導出する**(=一箇所を直せば右側の列も全部変わる)。
2. **グループ分け(どの技を右側の列に出すか)は「タグ」で決める**。タグもSSOTの技データに持たせる
   (例: 回復技/積み技/状態異常技/設置技/優先技/多段技/反動技/急所技 …=いま `waza-list.html` のフィルタが持っている分類)。
   → **わざリストとポケモンDBの右側が、同じタグを見る**ようになる。**片方だけずれることが構造的に起きなくなる。**
3. **技の値(威力・命中・PP・タイプ・分類・優先度)も同じSSOTから**。
   ★Champions版は `battle_data.priority` に優先度を持ち、全国版は最上位 `priority` に持つ、という**置き場所の違い**がある(2026-07-28 実測)。**新DBではどちらか一方に統一する**。
4. [[national-moves-grouping-todo]](全国版新技445のグループ分けが暫定=自動導出のまま)は、**この一本化と同時に正式化する**。

### 検査(番人に追加すべき項目)
- 「フラグ列」と「`POKEMON_WAZA` から導出した結果」が**一致しているか**(ずれたら赤)
- わざリストのタグと、ポケモンDB右側の列の**定義が同じか**

---

## 14. ★名前の持ち方(2026-07-28 阿部さん決定)= 正式名称に統一 + 表示名を別に持つ

| データの名前(正式・照合はこれで行う) | 画面の表示名(display_name) |
|---|---|
| ニャオニクス(オスのすがた) | ニャオニクス♂ |
| イダイトウ(メスのすがた) | イダイトウ♀ |
| ロトム(ヒートロトム) | ヒートロトム |
| パンプジン(ちいさいサイズ) | パンプジン(小) |
| フラエッテ(えいえんのはな) | フラエッテ(えいえん) |

- **持ち物・エンジン・i18n・権威との照合は、必ず「正式名称」で行う**
  → **フラエッテナイト / ニャオニクスナイトの到達不能が解消**(メガフラエッテ・メガニャオニクス♀♂が使えるようになる)
- **画面の見た目は今のまま**(短い表示名を使うので変わらない)
- 新DBのスキーマに **`name`(正式) と `display_name`(表示)** の2欄を必ず持たせる

### 名前のズレは3つの型(全部このルールで解消する)
1. **切り詰め**(17件): `フラエッテ(えいえん)` ← `フラエッテ(えいえんのはな)`
2. **表記の違い**(16体): `ニャオニクス♀` / `ヒートロトム` / `パンプジン(小)` / `イダイトウ♂`
3. **★全角/半角の混在**(技5件): `10まんボルト` vs `１０まんボルト`、`DDラリアット` vs `ＤＤラリアット`、`Gのちから` vs `Ｇのちから`
   → **比較の前に必ず全角→半角へ正規化する**(これを忘れると静かに外れる)

★参照箇所は **74件**(grepで実在確認済・`reference/_name_normalize.json` の `affects`)。SSOTで1回直して全部を再生成する。
