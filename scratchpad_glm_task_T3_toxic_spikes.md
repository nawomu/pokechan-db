# GLMタスク T3: sim_test 既知2fail(T185d どくびし)の根治調査→修正

送信元: claude-design → glm-impl
方針: 長年「既知」として許容されてきた sim_test の2failを閉じる。客観ゲート=sim_test 825/0。**本番commit/push/git addしない**。

## 現状
`node tools/_sim_test.js` が **823 pass / 2 fail** で、failは両方 T185d:
- 「T185d どくびしが置かれた」
- 「T185d 接地カビゴンはどく状態になる」

このT185dは何セッションも「既知」として除外扱いされてきたが、**なぜfailするのかの根本原因が記録に無い**。今回それを突き止めて閉じる。

## やること
1. **調査**: tools/_sim_test.js の T185d テスト本体を読み、何を期待しているか把握。→ real_battle_simulator.html のどくびし(Toxic Spikes)実装(設置・交代入場時の付与・層数1/2・どくタイプ接地入場での吸収)を追い、**期待とどこでズレるか**根本原因を特定する。
2. **権威裏取り**: どくびしの正仕様をBulbapedia "Toxic Spikes (move)" / ポケモンWiki「どくびし」で確認(接地・非どく/はがねに入場時どく[2層でもうどく]・接地どくタイプが入場すると解除・ひこう/ふゆうは無効 等)。**テストの期待値自体が仕様と食い違う可能性もある**=その場合はテストが誤りである根拠(出典)を添えて報告(テスト期待値を無断で書き換えて緑にしない)。
3. **修正**: 原因がエンジン/データ側なら修正。原因がテスト側(期待が仕様違い/セットアップ不備)なら、出典つきで修正案を報告してから直す。
4. **検証**: `node tools/_sim_test.js`(**825/0** を目標)+ 回帰 `node tools/_sim_sweep_all.js`(919/0)+ `node tools/_sim_hard_interaction_test.js`(174/0/skip11)。どうしても825/0にできない場合は、根本原因の説明と「なぜ閉じられないか」を報告(無理に緑化しない)。

## 制約
- 変更は最小限・関係箇所のみ。エンジンの無関係なロジックに手を出さない。
- テストの期待値変更は出典必須(自己出力をゴールデンにしない=[[battle-sim-reproduction-pivot]]の原則)。
- **commit/push/git addしない**。完了報告に: ①根本原因 ②直した側(エンジン/データ/テスト)と根拠出典 ③3ハーネスの数値 ④触ったファイル/行。
