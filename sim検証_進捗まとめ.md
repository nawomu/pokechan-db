# sim検証 進捗まとめ（検証→修正→再検証ループ）

最終更新: 2026-06-09 JST
全490技を「sim挙動 vs 説明(legacy)」でワークフロー検証 → 直す → また検証、を繰り返した記録。**正解は権威ソースに当てる**([[バトル再現_羅針盤]])。

---

## 1. 検証トレンド(ワークフロー18体・全490技を毎回判定)
| 判定 | run1 | run2 | run3 | 向き |
|---|---:|---:|---:|---|
| **match(説明どおり)** | 234 | 250 | **258** | ↑ 改善 |
| partial(主効果は出るが付随欠落) | 92 | 90 | 87 | ↓ |
| unimplemented(主効果が出ない) | 69 | 88 | **54** | ↓ 改善 |
| **mismatch(誤実装)** | 2 | 0 | 1 | → **現在0**(下記で全修正) |
| na(単発プローブで観測不能) | 92 | 61 | 90 | (LLM判定のばらつき大) |

- **match が 234→258(+24)、unimplemented が 88→54** と着実に改善。
- **mismatch(誤実装)は発見した分すべて修正済み**(run3の1件=あくびも修正完了→次回は0想定)。
- ※na/partial の絶対数は**LLM判定のばらつき**を含むので、トレンド(match↑/mismatch→0)で見るのが正直。

## 2. これまでに修正/実装したもの(各テスト緑・`tools/_sim_test.js` 52/52)
| 項目 | 内容 | 検出元 |
|---|---|---|
| mismatch: はらだいこ | `to_max`(最大まで上げる)未処理→+6に | run1検証 |
| mismatch: つぼをつく | ランダム1能力のはず→全上げ を1つ抽選に | run1検証 |
| 反動 | 与ダメ×fraction を自分も受ける(11技) | レポート頻度1位 |
| 吸収(ドレイン) | 与ダメ×fraction 回復(9技) | 頻度上位 |
| 連続攻撃 | hits固定/2〜5(第5世代確率)/max_hits(ネズミざん等) | 頻度1位 |
| 回復(変化技) | fraction×maxHP・天候条件(あさのひざし等)対応 | run2検証・頻度1位 |
| mismatch: あくび | 即眠り→**遅延(次ターン終わりに眠る)** 機構を新設 | run3検証 |
| (基盤) | ひるみ / ねこだまし最初のターン限定 / 状態異常 / 能力ランク / 天候ダメージ 等 | 段①〜⑨ |

## 3. 残りの実装ロードマップ(最新レポートの頻度順=優先度)
詳細レポート: `sim検証_実装ギャップレポート.md`(run3) / `sim検証レポート_2026-06-09.md`(run1) / `_v2.md`(run2)。
| 優先 | 効果ファミリー | 概数 | 備考 |
|---|---|---:|---|
| 1 | **power=null のダメージ**(固定/一撃必殺/可変威力) | ~20 | calcDamageが0を返す。状態管理不要で着手しやすい |
| 2 | **継続状態管理**(バインド/天候設置/フィールド/壁/設置物) | ~45 | ターンをまたぐ状態構造が要る。基盤を作れば一気に対象化 |
| 3 | **こんらん付与** | ~10 | statusマップ外で全スキップ中 |
| 4 | 強制交代(ふきとばし/とんぼがえり 等) | ~7 | |
| 5 | 特性/タイプ/能力の上書き・交換(へんしん/トリック等) | ~14 | |
| 6 | 次ターン行動不能(はかいこうせん等)/自分瀕死(じばく) | ~12 | |
| — | 反動の厳密な丸め(0.33 vs 1/3) | park | 権威で要確認 |
| — | amount回復(ちからをすいとる)/トリプルアクセル威力逓増 | park | 特殊計算 |

## 4. 進め方(このループを続ける)
1. 1ファミリーずつ「テスト赤先行 → sim最小実装 → 緑 → commit(緑のみ)」。
2. 数バッチごとに**再検証ワークフロー**を回し match率/mismatchを測る(毎回はトークン高なので適度に)。
3. **mismatch(誤実装)は最優先**で即修正。観測限界(単発プローブ)由来の na は probe拡張で切り分け。
4. 正解値は権威ソース([[バトル再現_羅針盤]]参照)に当てる。

## 5. 関連ファイル
- テスト: `tools/_sim_test.js`(52/52) / 観測: `review/_sim_probe_all.json`(`tools/_sim_probe.js`)
- 検証WF: `verify-all-moves`(workflow) / 詳細レポート: `sim検証_実装ギャップレポート.md` ほか
- 流れ: `バトルフロー一覧表.md` / `バトルの流れ_実機リファレンス.md` / 進捗: `HANDOFF_SESSION_2026_06_08.md`
