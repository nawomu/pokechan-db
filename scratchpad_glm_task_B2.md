# GLM実装タスク: B2「こうかなし理由表示」+ 無効ログのi18n漏れ修正

役割: 設計=Claude/実装=あなた(GLM)/検証=Claude/声の判定=阿部さん。

## 0. まず読む
- `CLAUDE.md`(特に「★バトルログ/msgboxはJA正典→translateLogLineパターン翻訳までがセット」)
- CLAUDE.md の i18n教訓・北極星(子ども口調・意味が戻る)

## 1. 背景(調査済みの事実)
無効(こうかなし)のログは `real_battle_simulator.html` の `calcDamage` が返す `reason` を、行 **2667** で `log('8', \`${result.reason}！\`)` として出している。現状の reason:
- 純タイプ無効(行1642): `'こうかなし'` → 出力「こうかなし！」= **理由なし**
- ふしぎなまもり(1650): `'ふしぎなまもり'`
- ふゆう/でんじふゆう(1661): `'こうかなし(ふゆうで 浮いている)'` 等
- 吸収特性(1674): `'${pname(def)} は ${_dAb}で 効かない'`(absorbAb付き)

**問題**: これらの文字列は `battle_log_i18n.js` の PATTERNS(行450〜)のどれにもマッチしない → **英語/他言語モードで日本語が漏れる**(EN切替で確認可能)。かつ純タイプ無効は理由が出ていない。

既に翻訳済みで使える型: `no_effect_t`(TPL行158 / PATTERN行487 = `^((?:相手の )?\S+) には こうかが ないようだ…$`)。

## 2. やること
### (A) エンジンの reason を統一フォーマットに(real_battle_simulator.html)
無効理由を、**既存の標準無効文＋括弧で理由**の形に統一する。阿部さんの例フォーマット=「◯◯には効果がない(とくせい ◯◯のため)」。実装案(JA正典・要 阿部さん最終OK):
- 純タイプ無効(1642): `${pname(def)} には こうかが ないようだ…`(= no_effect_t にマッチ=即翻訳される)
- ふしぎなまもり(1650): `${pname(def)} には こうかが ないようだ…（とくせい ふしぎなまもりのため）`
- ふゆう(1661 _isLevitate): `${pname(def)} には こうかが ないようだ…（とくせい ふゆうのため）`
- でんじふゆう(1661): `${pname(def)} には こうかが ないようだ…（でんじふゆうで 浮いているため）`
- 吸収特性(1674): `${pname(def)} には こうかが ないようだ…（とくせい ${_dAb}のため）`

行2667 の `！` 付与をやめ、`log('8', result.reason)` として reason 全文をそのまま出す(reason末尾は「…」や「）」で完結させる)。**ばけのかわ(disguiseBlock)・吸収おまけ(absorbAb)の後続処理(2669〜)は壊さないこと**。

### (B) i18n パターン/テンプレを追加(battle_log_i18n.js)
上の新フォーマットに対応する PATTERNS(行450〜)と TPL(行125〜)を**9言語**追加:
- `no_effect_ability`: re = `^((?:相手の )?\S+) には こうかが ないようだ…（とくせい (\S+)のため）$`、slots p=poke, ab={g:2,kind:'ability'}。TPL例(en): `It doesn't affect {p}… (due to its {ab})`。fr/de/es/it/ko/zh-Hans/zh-Hant も。
- `no_effect_levitate_state`: re = `^((?:相手の )?\S+) には こうかが ないようだ…（でんじふゆうで 浮いているため）$`、slots p=poke。各言語固定文。
- ability名(ふゆう/ふしぎなまもり/ちょすい等)は kind:'ability' で既存の特性辞書から翻訳されること。翻訳が無い特性があれば ja フォールバックで壊れないことを確認。

### (C) キャッシュバスター
`real_battle.html` の engine iframe `?v=YYYYMMDD`(現状 20260707)を **今日の日付にbump**(エンジンを変えたので必須。これを忘れると届かない=CLAUDE.md教訓#1)。※これは real_battle.html を1行だけ触る。B1(別タスク)と同じファイルなので、**B1がマージされた後に**bumpすること(Claudeが指示する)。

## 3. 検証(必ずやる)
- `node tools/_sim_test.js`(814/2既知=T185dどくびし のみ。**それ以外のfail増=無効文言のアサート回帰**を疑い、テストが古い文字列を見ていたら実挙動に合わせて更新可否をClaudeに相談)。
- ローカルで real_battle を開き、①じめん技をふゆう持ちに撃つ→「…（とくせい ふゆうのため）」②でんき技をちょすい持ちに→「…（とくせい ちょすいのため）」③ノーマル技をゴーストに→「… には こうかが ないようだ…」を確認。**言語をENに切替**→上記が英語化して**日本語が残らない**ことを確認(これがB2の主目的)。

## 4. 厳守
- 編集は real_battle_simulator.html と battle_log_i18n.js のみ(cache-busterのreal_battle.htmlはClaude指示後)。
- git commit/push禁止。実装後、変更箇所・追加パターン/TPL・テスト結果・EN実機確認結果・阿部さんに要判断のJA文言を報告。
