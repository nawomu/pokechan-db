# ▶ 次回ここから(説明文の声チェック)

最終更新 2026-06-15。今回(2026-06-15)の進捗は `HANDOFF_SESSION_2026_06_15.md` を見る。

---

## いまの状態(1行)
**機械チェック満点維持**(463 OK / 0 / 0)。**今回workflowで kind 別構造抽出を完了**(120 kinds・409 findings)し、その中の「明らかなcompose出力バグ」約30〜40技ぶんを修正済。残りは「人間の耳でしか判断できない言い回し」と「補足情報の追加可否」が中心。

---

## 30秒セットアップ(コピペ)
```bash
cd "/Users/masamichi/Documents/ポケモンDB"
# ローカルサーバー(切れてたら起動。8000でも8765でもよい)
python3 -m http.server 8000 --bind 127.0.0.1 >/dev/null 2>&1 &
# チェック用ページを最新化(composeを直すたびに、この3行で再生成)
node tools/_waza_compose.js              # 1
node tools/_waza_list_confirm.js         # 2
node tools/_waza_verify_reclassify.js    # 機械判定し直し(無料)
node tools/_waza_verify_report.js        # 3
open -a "Google Chrome" "http://localhost:8000/review/waza_list_confirm.html"
open -a "Google Chrome" "http://localhost:8000/review/rules.html"
```

---

## 今回やること = 声チェックの作業ループ
**confirmページを効果kind別に上から見て、「効果(compose)↔ヤック(legacy)」を意味照合**する(ヤックン耳テスト)。

1. **直すのは `tools/_waza_compose.js` の clause()**(該当kindのcase)
2. effects自体が足りない時だけ `tools/_fix_*.js` で データ追加(dry-run→件数アサート→--write)
3. `node tools/_waza_compose.js` →（上のセットアップの再生成3〜4行）でページ更新
4. 直したkindにチェック。次のkindへ

**進め方の単位 = 効果kind**(似た技は同じ直し方)。

---

## ★前セッション(2026-06-15)の構造抽出 findings は cache 済

- **wave1(top30 kinds・250 findings)**: `/private/tmp/claude-501/.../tasks/whfoy5awu.output`
- **wave2(残90 kinds・159 findings)**: `/private/tmp/claude-501/.../tasks/wh4b7urij.output`
- 集計済 JSON: `/tmp/all_findings.json` / `/tmp/all_findings2.json`
- ★sonnet判定者の判定は鵜呑み禁止(rules.html)。findingsは「機械的diff候補」=参考。声OKは阿部さんの耳のみ。

### workflow再投入したい時
```js
// scripts dir
Workflow({ scriptPath: ".../workflows/scripts/waza-voice-gap-scan-wf_6b438dcc-76b.js" })
Workflow({ scriptPath: ".../workflows/scripts/waza-voice-gap-scan-2.js" })
```
※同一スクリプトで再投入する場合は `resumeFromRunId` で cache hit(ただし findings 自体は変化する可能性=新たに走らせて再集計が安全)。

---

## ⚠ やってはいけない(罠)
- **判定者(sonnet)の判定列を鵜呑みにしない**。信用できるのは「空白」「機械漏れ」「具体的な抜けpattern」だけ
- **legacyの言い回しを写さない**(verbatim=盗用リスク)
- **★→✓(完成判定)はしない**。声の最終OKは阿部さんの耳だけ
- 本番(waza-list.html等)は今もlegacy維持。composeは確認ページ内のみ
- **ダイマックス系の取りこぼし指摘は無視**(SYSTEMS_IN_GAME=false でゲート中・設計どおり)

---

## 困ったら読む
- ルール集約: `review/rules.html`(元データ `tools/_rules.js`)
- 具体例の山: `ヤックン耳_判断ログ.md`(作業前に必ず読む)
- 手順書: `わざ説明文_開通手順.md`
- 今回(2026-06-15)の詳しい背景: `HANDOFF_SESSION_2026_06_15.md`
