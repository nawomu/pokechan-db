# ▶ 次回ここから(説明文の声チェック)

最終更新 2026-06-14。詳しい背景は `HANDOFF_SESSION_2026_06_14.md`。これは「すぐ手を動かす」用。

---

## いまの状態(1行)
**機械チェックは満点**(compose空っぽ0・機械漏れ0・全463技に下書きあり)。**残りは"声・意味"が正しいかの人間チェック=阿部さんの耳**。それをやるのが今回。

---

## 30秒セットアップ(コピペ)
```bash
cd "/Users/masamichi/Documents/ポケモンDB"
# ローカルサーバー(切れてたら起動。8000でも8765でもよい)
python3 -m http.server 8000 --bind 127.0.0.1 >/dev/null 2>&1 &
# チェック用ページを最新化(composeを直すたびに、この3行で再生成)
node tools/_waza_list_confirm.js        # ① confirm再生成(effects↔compose↔ヤック)
node tools/_waza_verify_reclassify.js   # 機械チェックで判定し直し(空白/機械漏れ/穴だけ)
node tools/_waza_verify_report.js       # ② verify_report再生成(①と同じ列+判定)
open -a "Google Chrome" "http://localhost:8000/review/waza_list_confirm.html"
open -a "Google Chrome" "http://localhost:8000/review/rules.html"
```
※ composeを直したら上の `① → 判定 → ②` の3コマンドを再実行すれば両ページが更新される。

---

## 今回やること = 声チェックの作業ループ
**confirmページを効果kind別に上から見て、「効果(compose)↔ヤック(legacy)」を意味照合**する。

1枚見て、下書きが**①意味が戻るか ②やさしい子ども口調か ③機械が漏れてないか ④legacyの写しでないか**(=ヤックン耳テスト)。ヘンなものを見つけたら:

1. **直すのは `tools/_waza_compose.js` の clause()**(該当kindのcase)。effects自体が足りない時だけ `tools/_fix_*.js` でデータ追加(dry-run→--write・件数アサート)。
2. `node tools/_waza_compose.js` →（上のセットアップの再生成3行）でページ更新。
3. 直したkindにチェック。次のkindへ。

**進め方の単位 = 効果kind**(似た技は同じ直し方)。confirm/verifyの目次チップで「OK率低い順」「気になるkind」から。

---

## ⚠ やってはいけない(罠)
- **判定者(sonnet)の判定列を鵜呑みにしない**。信用できるのは「空白」「機械漏れ」だけ。
  - ✗「急所+1」を「急所に当たりやすい」に直す ← **ルール逆**(急所+1が正・rules.html / 判断ログ参照)
  - ✗ 優先度がeffectsに無い/ダイマックス無効/ダメージkind無し ← 全部誤検知(設計上正常・Champions対象外)
- **legacyの言い回しを写さない**(verbatim=盗用リスク)。ただし「30%でひるませる」など言い方が1つしかないものは一致OK(無理に変えない)。
- **★→✓(完成判定)はしない**。声の最終OKは阿部さんの耳だけ。
- 本番(waza-list.html等)は今もlegacy維持。composeは確認ページ内のみ。本番反映は阿部さんGO後。

---

## 困ったら読む
- ルール集約: `review/rules.html`(元データ `tools/_rules.js`)
- 具体例の山: `ヤックン耳_判断ログ.md`(作業前に必ず読む)
- 手順書: `わざ説明文_開通手順.md`
- 背景: `HANDOFF_SESSION_2026_06_14.md`
