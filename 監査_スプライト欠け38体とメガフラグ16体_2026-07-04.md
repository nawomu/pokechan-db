# 監査: スプライト欠け38体+新メガのmegaフラグ未設定16体 (2026-07-04)

連戦モード通しテスト中の404から発見。**データの変更はしていない**(メガの扱い=阿部さん判断待ち事項②に直結するため)。

## 1. スプライト欠け(svg/pngとも無し・38体)

ランダム相手や編成でこれらを引くと**タイプ色の円+名前のフォールバック表示**になる(壊れはしない・設計内)。
2026-06-12の275体一括作画より後にDBへ追加されたシーズン追加分とみられる。

**ベースポケモン22体(描き足し候補・落書きテイスト)**:
ラフレシア, ハリーセン, ジュカイン, バシャーモ, ラグラージ, クチート, メタグロス, ムクホーク, ムシャーナ, ペンドラー, ズルズキン, シビルドン, カエンジシ, カラマネロ, ガメノデス, ドラミドロ, オーロンゲ, タイレーツ, ハリーマン, ハカドッグ, コノヨザル, サーフゴー

**新メガ16体**(下記フラグ問題と同じ個体・フラグ解決後に作画判断):
メガライチュウX, メガライチュウY, メガジュカイン, メガバシャーモ, メガラグラージ, メガクチート, メガメタグロス, メガムクホーク, メガペンドラー, メガズルズキン, メガシビルドン, メガカエンジシ, メガカラマネロ, メガガメノデス, メガドラミドロ, メガタイレーツ

## 2. 新メガ16体の `mega:false` は【確定済み仕様】(2026-07-05訂正)

**HANDOFF_SESSION_2026_07_04.md L23 で阿部さん確定済み**: M-Bメガ16体=現状維持(手持ちに直接選べる)。
`mega:true`化は `isPickable(!p.mega)` に波及して選択不可になるため意図的にフラグを立てていない。
→ **バグではない・対応不要**。当監査の初版で「判断待ち」と書いたのは誤り(ハンドオフ確認漏れ)。

## 3. 残る実タスク = スプライト38体の描き足しのみ

- 直接選択できる新メガ16体こそスプライトが必要(現状は円フォールバック)。
- ベース22体+新メガ16体=38体を、既存の落書きテイストパイプラインで描き足すのが唯一の残作業(優先度は阿部さん判断)。

## 4. 検出方法(再現)

```bash
node -e "const fs=require('fs');const d=require('./pokechan_data.js');
const files=new Set(fs.readdirSync('images/sim'));
console.log(d.POKEMON_LIST.filter(p=>!files.has(p.name+'.svg')&&!files.has(p.name+'.png')).map(p=>p.name).join(','));"
```
