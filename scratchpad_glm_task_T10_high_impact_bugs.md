# GLMタスク T10: 高影響バグ 第1波(既存コードの欠陥5件・エンジン行特定済み)

送信元: claude-design → glm-impl
背景: 権威コーパス全数照合で出た204件の差分のうち **impact=高** の中から、**「新機能でなく、既にあるコードが壊れている/到達不能/死にコード」** の5件を先に潰す。

## 大原則(破ったら成果物は捨てる)
- ★**1件ずつ**。1件直したら**その場で報告**(まとめて最後に報告しない=途中で枠が切れても成果が残る)。
- ★**全コマンドはフォアグラウンド同期実行**。バックグラウンド待ちで止まるな。止まらず完走。
- ★権威=`reference/_authority_corpus/` の**ローカル原文をgrepで読む**(WebFetch不要・トークンゼロ)。引用は**実在文字列のみ**(捏造厳禁。コーパスでgrepできない引用は書くな)。
- ★**うちのABILITY_DESC/items_database原文も必ず読む**。Champions独自仕様があればそちらが優先。食い違えば**実装せず報告**。
- ★ランク変化は必ず共有パイプライン `applyRankStageGuarded` 経由(直接 `st.rank.X` を書かない)。
- ★変更は最小限・**出典コメント必須**(権威URL+引用)。**commit/push/git add しない**。
- ★**推測で埋めない**。分からない所は「未確認」と正直に書いて報告。
- ★対象ファイル: `real_battle_simulator.html`(エンジン)。データは**ビルダー経由**で直す(生成物の直接編集禁止)。

## 報告フォーマット(1件ごとに agmsg で送る・15行以内)
`[T10-n 完了] <名前> / 直した所=file:line / 権威引用=「…」/ 想定テスト=… / 未確認=…`

## 対象5件(この順で)

### 1. エレキメイカー(数値の相違 / impact=中)
- **権威**: 場に出たとき、フィールドを5ターンの間エレキフィールド状態にする(グランドコート所持で8ターン)。
- **引用**: 場に出たとき、フィールドを5ターンの間エレキフィールド状態にする。
- **うちの現状**: エンジンは env.field='electric' を代入するだけで env.fieldTurns を設定しない(L2519-2521)。技でフィールドを張る側(L4822)は ef.duration||5 を入れているのに、特性側は据え置き=直前の残ターンを引き継ぐか、未設定なら null(=無期限)のまま。
- **該当行**: L2519, L2520, L4822, L7340
- **補足**: グラスメイカー/ミストメイカー/サイコメイカー(L2523-2531)も同じ書き方=同一原因。天候側は abilityWeatherDuration() を通しているのにフィールド側だけ対になる関数が無い。

### 2. フォーカスレンズ(未実装 / impact=高)
- **権威**: そのターンにすでに行動している相手(=自分が後攻)に対して使う技の命中率が1.2倍(第5世代以降は4915/4096)。Champions欄も「相手よりも 行動順が遅い時 技の命中率が1.2倍になる。」
- **引用**: ・効果:そのターンに、すでに行動している相手に対して使う技の命中率が1.2倍になる。
- **うちの現状**: items_database は effect『相手より遅く攻撃したとき技の命中率が1.2倍になる』implemented_in_pokechan:true。エンジンは L2662 で `if (atk.actedAfterTarget) acc = acc * 1.2;` と書いているが、`actedAfterTarget` はリポジトリ全体で L2661(コメント)・L2662(参照)の2箇所しか存在せず、どこからも代入されない=常に undefined。命中1.2倍は一度も発動しない死にコード。
- **該当行**: L2654, L2659, L2661, L2662
- **補足**: `grep -rn actedAfterTarget .` の結果が real_battle_simulator.html の2行のみ(代入なし)であることを確認済み。レギュM-Bで追加・『実装確認済』と notes にあるがフックが未配線。付随して権威の『一撃必殺技の命中率は上がらない』も、そもそも発動しないため未確認のまま。

### 3. フラエッテナイト(未実装 / impact=高)
- **権威**: フラエッテ(えいえんのはな)がメガシンカできるようになる持ち物。
- **引用**: フラエッテ えいえんのはな が メガシンカすることができる持ち物。
- **うちの現状**: items_database: applies_to='フラエッテ(えいえんのはな)' / mega_form='メガフラエッテ'。ところが pokechan_data.js の実名は『フラエッテ(えいえん)』(のはな が無い)。canMegaEvolve(L6516) の専用分岐 `it.applies_to === st.poke.name` が名前不一致で外れ、フォールバック megaFormFor(L6507) は 'メガフラエッテ(えいえん)' / 'メガフラエッテ(えいえん)X' を探して両方存在しないため null。結果、フラエッテはどのストーンを持ってもメガシンカできない(データ上 'メガフラエッテ' は存在するのに到達不能)。
- **該当行**: L6505, L6507, L6509, L6516, L6517
- **補足**: pokechan_data.js を grep して「フラエッテ(えいえん)」1件・「メガフラエッテ」1件のみを確認。本バッチのメガストーン55件を同じロジックで機械シミュレーションし、到達不能になるのはこの1件だけだった(ヤドラン(ガラル)/ライチュウ(アローラ)が到達不能なのは権威の『ガラルのすがたには効果がない』『アローラのすがたには効果がない』と一致=正しい挙動)。

### 4. メンタルハーブ(条件の欠落 / impact=高)
- **権威**: メロメロ・ちょうはつ・いちゃもん(連続不可)・かなしばり(わざふうじ)・かいふくふうじ・アンコールを回復して1度で消費(Champions欄)。
- **引用**: メロメロ ちょうはつ 連続不可 わざふうじ かいふくふうじ アンコール状態を回復する。1度使うと その対戦中は 無くなる。
- **うちの現状**: エンジン L6250-6257 は `if (st.encore || st.disable || st.tormented)` の3つしか見ておらず、ちょうはつ(st.tauntTurns)・かいふくふうじ(st.healBlockTurns)・メロメロ(st.attracted)を回復しない。3つとも別機構としてエンジンに実装済み(tauntTurns L1172/L5788、healBlockTurns L1228/L4736、attracted L1122/L3106/L4313)なので『状態が無いから対象外』ではない。コード注釈の『メロメロは未実装=対象外』は古い(メロメロは段139で実装済み)。
- **該当行**: L6249, L6250, L6251, L6252, L1172, L1228, L1122
- **補足**: ちょうはつは対戦で最も踏む状態変化なので影響大。エンジン側の tauntTurns/healBlockTurns/attracted は grep で実在を確認済み。

### 5. メトロノーム(条件の欠落 / impact=高)
- **権威**: 同じ技を連続で使うと1回につき+20%、最大2倍。別の技を使うとリセット。交代でもリセット。連続攻撃技はヒット数に関わらず使用回数は1回分しか上がらない。
- **引用**: ・連続攻撃技は、ヒット数に関わらず使用回数は1回分しか上がらない。
- **うちの現状**: エンジンは連続使用カウンタ(atk.metronomeCount / atk._metronomeLastMoveKey)を calcDamage(L2006-2018)の内部で書き換えている。calcDamage は実際の技使用(L2857)以外に、①技リストのダメージ表示 dmgCache(L8169・renderMoves 内で全技ぶんループ) ②AIスコアリング(L6687/L6755) ③連続攻撃の2発目以降(L3004-3010・1ヒットごとに再呼び出し) からも呼ばれるため、実戦の使用回数と無関係にカウンタが加算/リセットされる。また交代時のリセット(clearVolatilesOnSwitch 系 L6087-6117)に metronomeCount / _metronomeLastMoveKey は含まれていない。
- **該当行**: L2006, L2008, L2009, L2012, L3010, L6687, L6755, L8169
- **補足**: 倍率テーブル自体(1回目1.0→5回目で2.0上限)は権威と一致。壊れているのは『いつ数えるか』。最も明確なのは連続攻撃技(L3010)で、1ヒットごとに calcDamage が呼ばれるため権威の『ヒット数に関わらず1回分』に反する。技リスト表示(L8169)経由の汚染は描画タイミング依存なので実機再現を1回踏んで確かめるのが望ましい(本タスクは修正禁止のため未実行)。交代リセットは権威『・交代すると使用回数はリセットされる。』(同ファイル 詳細な仕様)にも明記。
