# DB統一リビルド 実装タスク分解 — 2026-07-02

親設計: `設計_DB統一_2026-07-01.md`(schema案・ビルダー骨格・正規化表はそちら)。
体制: **設計/検証=Claude・実装=GLM+Sonnetサブエージェント・声=阿部さん**。

## ★進捗(2026-07-02時点)
- **T1 ✅**(GLM+Sonnet) / **T1.5 ✅**(GLM(1)(2)(3)+Sonnet(4)) / **T2 ✅**(Sonnet・compose呼び出しを全国版と統一=492/492 desc一致) / **T4 ✅合格**(PCHAM_DATA env var方式・814/2既知のみ・sweep919/0・behavior606OK/flag0・unexpected diff=0[総503=desc再生成491+move_no正規化7+ほか期待分])
- 補足: MDESC(手書きオーバーレイ)は「効果なし」kind開通で**0件に**。viewビルダーの現行pokechan_data.js参照はTYPES/NATURES等の定数群・WAZA_MAPのkey・POKEMON_LIST転記のみ(意図的・互換保証。POKEMON_LISTのmaster直参照化は移行完了後の後工程)。
- **残り: T3(全国版ビルダーのmaster参照化・軽) → 声サインオフ(阿部さん) → T5(本番差替+push)**。

---

## T1. `tools/build_master.js` — 統一マスターSSOT生成(GLM実装)

**出力**: `reference/master_pokemon.json`(1302) / `master_moves.json`(919+コインビーム) / `master_abilities.json`(311+独自) / `master_items.json`(2180)。schemaは親設計の「統一マスターschema案」に厳密準拠。

**入力(source of truth)**:
- PokeAPI base: `reference/*_master.json`(名前9言語/種族値/タイプ/効果en)
- effects SSOT: `reference/moves_battle_data_fix.json`(全919技・**これが唯一のeffects源**)
- Champions overlay: `pokechan_data.js` から抽出
  - ABILITY_DESC(独自JA説明・連結特性2件)/ 季(M-A/M-B)/ メガ可否 / 体重 / コインビーム / 独自特性(ドラゴンスキン等)
- 特性補助: `reference/abilities_master.json`(311・Conquest除外済) + abilities_desc_ja

**要件**:
1. `description_ja` は**焼き込まない**→ビルド時に `tools/_waza_compose.js` で全919技再生成(effects→compose一方通行・R9)
2. `move_no` 正規化: 独自採番7技→実番号(親設計の対応表: どくばりセンボン919→839 等)。コインビーム=独自id予約(926維持 or 10000番台、実装時に1行で決めて記録)
3. 結合キーは **id/slug**(JA名結合は全角/半角揺れで壊れる=禁止)
4. provenance フィールド(どの source から来たか)を各エントリに残す
5. Champions curated(effects/説明/季/メガ)は**1件も失わない**こと→ビルド後に突合カウンタをログ出力(overlay適用件数=期待値一致)

## T1.5 masterの自己完結化(後工程・T2実装後に判明)

T2実装(2026-07-02)は技の `type/power/accuracy/pp/contact/protect/flags` を**現行pokechan_data.jsからpass-through**した(PokeAPI=最新SV値でChampions実値と392件差→Champions実値が正、の判断=妥当)。ただしこれは**masterが自己完結していない**=view生成が旧pokechan_data.jsに依存し続ける、ということ。
→ **T1.5**: Champions実値(type/power/pp等の対戦数値)を master_moves の `champions.stats` overlayフィールドに取り込み、viewビルダーのpass-through参照を master参照に置換。表記ゆれ5技(10まんボルト等)も master側で champions.in=true+正slug結合に直す。**T5(本番差替)の前に必須**(でないと旧ファイルを永遠に捨てられない)。

## T2. `tools/build_champions_view.js` — Champions本番形の生成(GLM実装)

- master から `champions.in==true` をフィルタ→**現行 `pokechan_data.js` と同一schema**(POKEMON_LIST/WAZA_MAP{description,battle_data}/ABILITY_DESC/NATURES/TYPES)で `pokechan_data.new.js` に出力(**本番ファイルは直接上書きしない**)
- consumer(sim/HTML)無改変で動くこと=形の互換が絶対条件
- 現行 `pokechan_data.js` との**全フィールドdiffレポート**を出す(`reference/_unify_diff_report.json`)
  - 期待されるdiff = ①陳腐化した焼き込み説明の更新 ②move_no正規化(7技) のみ
  - **それ以外のdiffが出たら全部フラグ**(=overlay取りこぼしの疑い)

## T3. `tools/build_national_view.js` — 全国版の生成(GLM実装)

- master 全体 → `pokechan_data_all.js`(現 `_build_pokechan_data_all.js` を置換。焼込撲滅composeDescHの挙動は維持=masterがそもそも焼込レス)
- 生成後、現行 `pokechan_data_all.js` とのdiffレポート(期待=ほぼ無変化)

## T4. 回帰ゲート(GLM実行 → Claude判定)

pokechan_data.new.js を読ませて:
1. `node tools/_sim_test.js` → **814 pass / 2 fail(T185d既知)維持**
2. `tools/_sim_sweep_all.js` → 919技クラッシュ0
3. `tools/_sim_behavior_all.js` → 現状OK数維持(ほおばるスキップ込み)
4. T2のdiffレポート → Claudeが全数目視(期待diff以外ゼロ)
5. Playwright実機: pokemon_db_v9 / waza-list / real_battle_simulator / 全国版ページ = JSエラー0・描画OK(PDCA Check)
6. i18n監査ハーネス(残ja=0維持)

**ゲート全通過まで本番ファイルに触らない。**

## T5. 差し替え(阿部さん声サインオフ後)

1. `review/waza_list_confirm.html` で効果↔ヤック=阿部さんの耳(本番公開の前提)
2. `pokechan_data.js` バックアップ→new と差替→キャッシュバスター bump→再度ゲート1-6
3. commit分割(master生成/ビルダー/データ差替)→push(阿部さんOK後)

---

## 進め方・注意

- **順序は T1→T2→T4 を先に一気通貫**(小さく端まで通す=前回の教訓)。全1302/919を最初から回さず、まずChampions分だけで縦に通してからスケール、でも可。
- GLMには**1タスク=1メッセージ**で仕様を渡す(agmsg)。完了報告→Claude検証→次タスク。
- GLMクォータ切れ時はClaudeが引き取り(その時点のTを継続)。
- 本番 `pokechan_data.js`・公開HTMLは T5 まで一切変更しない。
- effects修正が必要になったら `moves_battle_data_fix.json`(SSOT)を直す→再ビルド(生成物を直に直さない)。
