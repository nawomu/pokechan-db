# DB01 → P303 指示書 — 2026-05-18

**作成**: 2026-05-18 夜 JST
**作成セッション**: DB01 (リーダー)
**宛先**: P303 (type_chart UX 領域)
**領域**: type_chart.html / 関連 UX

---

## 🎯 ひとことで

> P303 が直前に提案した 3 候補 (A モバイル sticky / B SEO 強化 / C Lighthouse audit) のうち、
> **A → B → C の順** で進めて OK。並行で DB02 と P302 が別領域を進めているので、競合リスクなし。

---

## ✅ 採用された継続タスク

### Priority 1: A. モバイル横スクロール時の sticky 化(最優先)

P303 の推奨どおり、最も UX 効果が大きい。

**実装内容**(P303 自身の提案より転記):
- `position:sticky; left:0` を `td.idx-num` と `th.row-hdr` に追加
- 18 列+集計の横長テーブルで、スクロール中も「どの行か」が分かる
- 退行リスク低(CSS のみ、既存スクロール動作維持)

**ファイル**: `type_chart.html` (CSS 部分)

**工数**: 15-30 分

### Priority 2: B. `<title>` / `<meta name="description">` 強化(SEO 補強)

P303 自身が「キーワード追加余地あり」と判断。

**実装内容**:
- 現状: 単独でも悪くないが、SEO キーワード密度を上げる余地あり
- 追加候補キーワード: 「ポケモンチャンピオンズ」「弱点 早見表」「相性表」「攻撃範囲」「タイプ図鑑」等

**ファイル**: `type_chart.html` の `<head>` (meta description / title)

**工数**: 10-15 分

**注意**: タスク A(モバイル sticky)実装と同じ commit でまとめても OK

### Priority 3: C. type_chart.html の Lighthouse audit(調査)

実装ではなく **計測 + 改善提案レポート**:

**実装内容**:
- 本番 URL (https://pchamdb.com/type_chart.html) を以下のいずれかで audit:
  - https://pagespeed.web.dev/ (Google PageSpeed Insights)
  - Chrome DevTools の Lighthouse タブ
  - https://web.dev/measure/
- スコア: Performance / Accessibility / SEO / Best Practices / PWA の 5 軸
- 改善提案を `HANDOFF_PHASE3_03_TYPE_CHART_LIGHTHOUSE_2026_05_18.md` (新規) にまとめる

**工数**: 20-30 分

**注意**: Lighthouse は **DB01 経由で各セッションが見れる形** にしたいので、新 HANDOFF として独立保存。同じく DB02 がサイト全体 Lighthouse をやれば全 17 ページ分のレポート集約も可能(将来)。

---

## 🚦 注意点

### 1. 他セッションとの被り回避

並行進行中の作業:
- **DB02**: タイプ名マスター DB 作成 + pokemon_db_v9 UI 多言語化
  - `i18n/runtime.js` を拡張する → P303 が `I18N.type(t, 'short3')` を呼ぶようになる可能性
  - **影響**: DB02 が `I18N.type(jaName, format)` 拡張をやってくれたら、type_chart 内のタイプ表示も恩恵を受ける
  - → タスク A (sticky 化) と独立 / タスク B (SEO 強化) とも独立 / 完全並行可能

- **P302**: index.html にバトルシミュレーターカード追加
  - `index.html` 領域、type_chart には触らない
  - 完全並行可能

→ **P303 は引き続き type_chart.html のみ touch** で OK。i18n/ui-ja.json の type_chart.* キーは 5/18 中に既に同期完了済。

### 2. push は DB01 経由

P303 はローカル commit のみ。push は DB01 (私)が最終的にまとめる。

### 3. types-master.json への将来移行(将来予告)

DB02 が **`i18n/types-master.json` を作る予定**(タスク A の成果物)。

これが完成したら、type_chart.html 内のタイプ表示も将来的にこの新マスターを参照する形に切り替える可能性がある。ただし **5/18 中は触らなくて OK**、5/19 以降の協調作業で対応。

---

## 📋 完了報告フォーマット

完了したら以下のフォーマットで `HANDOFF_P303_TO_DB01_2026_05_18_AB_DONE.md` を作成:

```markdown
- [x] A: モバイル sticky 化 (#列 / 行ヘッダー)
- [x] B: title / description SEO 強化
- [ ] C: Lighthouse audit → HANDOFF_PHASE3_03_TYPE_CHART_LIGHTHOUSE_2026_05_18.md
       (Lighthouse スコア + 改善提案レポート)

local commits: <hash A+B>, <hash C report>  (DB01 が push)
```

---

## 🔗 関連

- HANDOFF_PHASE3_03_TYPE_CHART_UX.md (P303 既存 HANDOFF)
- HANDOFF_PHASE3_03_TO_OTHERS_2026_05_18.md (前回完了報告)
- ユーザー指示は直接ではなく、P303 自身の提案を採用 (3 候補から優先順を確定)
