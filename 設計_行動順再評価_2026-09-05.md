# 設計: 行動順再評価とActionIntent

起案資料: 2026-09-05 Codex設計試験 / 清書: 2026-09-06。
状態: **承認された設計採用方針に基づく清書。技の性質フラグのマイルストーン後に統合担当が検収する。バトル実装はまだ変更していない。**

対象は B149 `action_reorder` / B150 `priority_modify` と、B064へつなぐ行動所有者の契約。上位規則は `設計_バトルエンジン原理_2026-07-26.md`、`設計_フェーズ語彙v3_2026-07-26.md`、`設計_割り込みと分岐の規律_2026-07-26.md`。語彙や憲法をこの文書だけで変更したことにはしない。正式キーはスラグ、番号は表示用とする。

## 1. 解く問題と採用する分離

先行調査では、ターン開始時に2要素の行動順を固定しており、未行動者の順番をライブ状態から再評価できない。また順序比較の中でせんせいのツメを抽選するため、その関数を呼び直すだけでは乱数を余計に消費する。根拠は `review/_codex_trial/round1_design_2026-09-05.md` §1/課題Aと、独立照合 `review/_codex_trial/round2_fable_reply.md`「照合結果」。これは9/5時点のコード調査結果であり、実装開始時には関数名で現物を再確認する。

設計では次の責務を分ける。

| 要素 | 決めること | 混ぜてはいけないこと |
|---|---|---|
| `orderingMove` | 行動順の計算に用いる技の識別子 | 実行技への機械的な上書き |
| `executionMove` | 今回実行する技の識別子 | すべての機構で行動順も同時に変わるという仮定 |
| `priority_modify` | 基本優先度と条件付き寄与から有効優先度を導出 | 同優先度内だけの先制効果・乱数抽選 |
| `order_modifier_snapshot` | ターン開始時に確定すべき順序補正を保存 | 毎再評価の再抽選・保存すべきでないライブ状態の焼き込み |
| `action_queue_override` | 既存の未実行行動への明示的な順番変更 | 有効優先度そのものの書換え |
| `action_reorder` | 生存し同じ在場期間にいる未行動者から次の1件を選ぶ | 新しく入場した個体への通常行動の追加 |

アンコールによる実行技の差替え後も、選択技の優先度を用いる。よって `orderingMove` と `executionMove` を別に持つ。**あばれ・溜め解放・こだわりロック・わるあがきへ同じ規則を無条件で拡張しない**。機構別の根拠確認を残す。B158は「全部、実行技で並べ直す」修正票として使わない。

## 2. 根拠の範囲と未確認

以下のcorpusは、worktreeには存在せず、今回 `/Users/masamichi/Documents/ポケモンDB/reference/_authority_corpus/` の既存ローカル保存を実読した。外部サイトの最新取得は行っていない。下表のパスはこのディレクトリからの相対パスで、行番号は保存JSONに対するもの。

| 根拠 | この文書で採る内容 | 範囲 |
|---|---|---|
| `rules/優先度.json:7,11` | 高優先度が先、同優先度はすばやさ比較。第八世代以降の優先度変化はそのターンへ反映。アンコールでの差替えは選択技の優先度を保持 | 本編記述。Champions固有の動的再評価は未確認 |
| `rules/すばやさ.json:19` | 第八世代ではターン途中の速度変更を反映。同優先度・同速はランダム | **再評価時に同速乱数を保持するか再抽選するかは未確認** |
| `moves/グラススライダー.json:13` | フィールドの恩恵を受ける/受けなくなる変化で優先度が即座に変化 | Champions完全同一は未確認 |
| `items/せんせいのツメ.json:7,12` | 毎ターン20%。優先度を変えず同優先度内で先制。発動判定はターン最初の行動決定時。発動後に失ってもそのターンは効く | 保存対象とライブ持ち物判定を区別する根拠。Championsの全細則の確認済みとはしない |
| `abilities/クイックドロウ.json:8,11` | 説明文にはChampionsで30%と明記。仕様欄には先制効果の相互作用・アナウンス時期がある | 説明文のChampions印を仕様欄全体へ拡張しない |
| `rules/優先度.json:13,14` | おさきにどうぞ等の優先度を越える操作と、せんせいのツメ等の同優先度内操作は別。さいはいは追加実行側の機構 | 同じ `orderBand` へ全部畳まない |
| `rules/ポケモンチェンジ.json:7` / `abilities/ききかいひ.json:12` | 交代先は次ターンまで通常行動しない。未行動でもききかいひで退場すれば行動しない | ActionIntent失効の根拠 |
| `items/だっしゅつパック.json:11` | 同ターン再入場で行動できない具体例。複数同時発動では補正なしの速度で比較しトリックルーム等を除外 | 再入場によるIntent復活を防ぐ根拠。例に登場するダイマックス自体は実装対象にしない |
| `rules/ひんし.json:20` / `moves/とんぼがえり.json:19` | 第八世代以降の死に出しはターン終了直前。とんぼがえりの交代先が設置技で倒れた場合も次選出はターン終了。死に出し時の設置技KOはターンを進めず追加選出 | 入場元に応じた復帰先・時期を保持する |

旧round1に記憶から書かれた外部URLは根拠に採用しない。訂正済みround2を優先する。特にChampionsのシングル/ダブル提供状況、動的再評価の完全な細則は、この文書のローカル調査では最新確認していない。

## 3. ActionIntentの所有者と寿命

以下はランタイム状態の**設計上の契約案**であり、masterの新スキーマではない。

| フィールド | 契約 |
|---|---|
| `turnId`, `actionId` | 選択された通常行動を一意に追跡する |
| `actorId` | バトル内の個体。表示名や画面のself/oppで識別しない |
| `presenceEpoch` | その個体の在場期間。退場後の再入場では新しい値にする |
| `sideId`, `slotId` | 陣営と現在位置。行動の所有者とは別。位置変更の詳細はB064側 |
| `orderingMove`, `executionMove` | 技はSSOTの識別子で参照する。コピーDBを作らない |
| `status` | `pending → executing → consumed` または `pending → cancelled` |
| `cancelReason` | `switched_out` / `fainted` 等。終端状態は復活させない |
| `origin` | 起点の行動・原因・イベント・カスケードを追跡する |

通常行動のIntentはターンの選択確定時に生成し、寿命は `per_turn`。在場世代のカウンタはバトル中の個体識別状態として保持する。`pending` からの選択直前にも、生存・現在の在場期間・行動権を検証する。

退場/ひんしが確定した時点で、その個体の未実行Intentを失効させる。別個体が同じスロットへ入っても、同じ個体が同ターンに戻っても引き継がない。実行中のとんぼがえりなどは、退場したことで新たな通常行動を得るのではなく、進行中の行動を本流が終端まで解決して `consumed` にする。

さいはい・おどりこ・技を呼び出す機構の追加実行は、ターン選択時の通常Intentとは別の実行経路。元のIntentを `pending` に戻す実装は禁止する。追加実行の親子ID、PP、使用履歴、優先度の継承は各機構を実装する段で根拠と共に定める。

## 4. 主ループと本流復帰

構造は次の順とする。これは実装方式であり、「ゲーム内部が必ずこのアルゴリズム」という主張ではない。

1. ターン選択を確定しIntentを作る。開始時確定の乱数・順序補正を専用段で保存する。
2. ターン開始の交代・入場等を既存の対応する段で解決する。退場者のIntentを失効させる。
3. 本流から `action_reorder` を呼び、存続する `pending` の次の1件を選ぶ。
4. `target_lock` から行動パイプラインへ進み、必要なSectionとカスケードを解決する。
5. 行動不能・失敗・交代技・ひんしを含む全経路を共通の本流復帰点へ集める。要求された交代/ひんし処理と入場効果を、根拠に合う時期で完了させる。
6. 今の通常行動を終端状態にする。次の行動を選ぶ境界で再び手順3へ進む。
7. 通常Intentが尽きたらターン終了へ。終了側の死に出し後は次ターンへ進み、その入場者を今ターンの手順3へ追加しない。

`action_reorder` は1行動の解決境界につき1回。本流の途中から重ねて呼ばない。入場効果で場や速度が変われば、残るIntentを次に選ぶ時にそのライブ状態を読む。設置技で倒れた個体の追加選出時期を「入場カスケードだから即時」と一括処理しない。`switch_in` の起点(交代技/通常交代/ターン終了死に出し)と本流の復帰地点を保持する。

## 5. 優先度・順序補正・乱数の契約

`priority_modify` は `(action, actorState, fieldState, ruleset)` を読み、`basePriority` / `contributions[{sourceId, delta, reason}]` / `effectivePriority` を返す純粋Query。条件不成立はその寄与0であり、先に成立した寄与を消す `return 0` ではない。置換・無効化等の非加算ルールを後から足す際は、加算へ丸めず別操作と競合解決を明記する。

先制防御・優先度に依存する無効判定・ログは、同じ行動コンテキストと寄与情報を受け取る。行動者なしで再計算しない。いたずらごころ由来の無効判定は、最終優先度の数値だけでなく寄与元も必要になる。防御側が参照する技や時点の機構差は、個別根拠がないまま `executionMove` または `orderingMove` に一括統一しない。

順序比較は概念上、明示キュー指示 → 有効優先度 → 同優先度内の補正帯 → その帯の速度比較規則 → 同値処理に分ける。さきおくり等の複数指示の競合、first/last帯の内部順序、トリックルームの適用範囲は個別ルールを持つ。**通常の速度比較方向反転を全帯・全Eventへ機械的に適用しない**。未確認の組合せを正準側順だけで埋めない。

`order_modifier_snapshot` は再評価のたびに乱数を引かない。せんせいのツメの発動後の所持変化は、保存結果を遡って消さない。対象機構ごとに `rng_timing` と適用期間を明記し、「持ち物はライブ」という一般則と「開始時に確定した発動結果」を区別する。表示の発火順と行動の同速順も同じ抽選と仮定しない。

**`tiePolicy` は未確定**。本編corpusは同優先度・同速をランダムとするが、再評価で保持/再抽選する時機は未確認。`canonSides()` は共有乱数を引く入力の正準化・同期に使えても、常に同じ側を勝たせる実機代替ではない。実装前に乱数の時点と回数を根拠で確定する。純粋schedulerの契約試験では注入した決定的結果を使い、その試験を実機再現の証明には数えない。

## 6. 5安全装置・undo・オンライン

| 装置 | 実装時の合否条件 |
|---|---|
| origin | 元の `origin={side,source,cause,eventId}` を維持し、行動/カスケード/個体/在場期間を追跡できる。子イベントの識別子と元の因果IDを混同しない |
| maxDepth | 規律の既定10段上限を適用。超過を可視化して枝を打ち切り、本流へ戻る。未解決の本流要求や状態を黙って成功扱いしない |
| 決定的順序 | 同じ入力・共有seedで行動選択列/乱数消費列/状態/ログが一致。Event別の比較ルールを持ち、だっしゅつパックへ通常のTR反転を流用しない |
| 再入禁止 | 同じ解決範囲で `Handler × actorId × presenceEpoch` の重複発火を防ぐ。子ごとにeventIdを変えただけで A→B→A の防止集合がリセットされない設計にする。実際の再入場は新epochとして区別 |
| 本流復帰 | Handlerは要求を返す。枝の奥でターン終了・勝敗・強制交代を直接確定しない。本流が適切なSectionを呼ぶ |

undo単位はカスケード開始前の整合したスナップショット。Intentの状態・epoch・開始時乱数結果・キュー指示・現在の本流位置・RNG状態を含む。`per_turn`だから保存不要としない。再計算で復元する登録表は、同じ個体/持ち物/特性/場から同じものを構築することまで検査する。ローカルself/oppの見え方に依存するIDを通信契約へ漏らさない。

## 7. 実装へ渡す順番とゲート

技フラグのマイルストーン検収 → 現行単体/実機の基準取得 → dispatcher安全装置 → B154 `ground_check` → B150 `priority_modify` → B149 scheduler → B153の速度/トリックルーム縦切り → B151 can段 → B152みがわりguard。各段に具体的機構を1つ載せて検証する。大規模な全機構移行を一括で行わない。ダブルの段階は `review/_b064_staged_plan_2026-09-06.md`。

最小の意味検証ケース:

- 4個体の純粋schedulerで速度変更後に未行動者同士が入れ替わる。シングルで残り1体の例を再評価の成立証明にしない。
- アンコールで `executionMove` が変わっても対応する `orderingMove` を保持する。
- 条件付き優先が消えても他の寄与を失わない。グラスフィールドの開始/消滅/接地変化を両方向で確認する。
- 再評価を複数回しても開始時抽選回数が増えず、先制効果だけで優先度0の技が先制防御対象に変わらない。
- とんぼがえり、未行動のききかいひ、同ターン再入場、未行動のひんしで旧Intentが復活しない。
- 入場元ごとの設置技KO・追加選出・登場効果の時期と、本流復帰先を確認する。
- 行動不能/失敗でも次の評価境界へ到達する。全Intentが終端ならループが止まる。
- 同seed再実行・undo再実行・オンライン鏡写しで、状態だけでなく行動/乱数/ログ列を照合する。

既存ハーネスの緑は退行検出、権威既知値との一致は仕様検証として別記する。新期待値をsim自己出力だけで作らない。新規phase_slugを実装側へ登録する前に語彙・分類・実機リファレンスへ反映する。effectsの既存phaseを置換するならcompose全919件の再照合が別途必要なので、この行動順作業へ紛れ込ませない。

## 8. 実装前に解決する限定的な論点

| 論点 | 進められること | 保留する部分 |
|---|---|---|
| Champions動的再評価・途中入場規則の細則 | 型・所有者・純粋関数・本編根拠付きケースの設計 | Champions確認済みという表示・全細則の無条件な本番適用 |
| 同速乱数 | RNG注入と同期/undoの契約試験 | 再評価時の抽選タイミングを推測で固定 |
| snapshotとアナウンス位置 | 開始時確定と再評価を分離 | 現語彙 `priority_interrupt` を前へ移す具体的な全順序の確定 |
| あばれ/溜め/こだわり等のorderingMove | アンコールで裏付けた分離を採用 | 全機構への一括置換 |
| 明示キュー操作・追加実行の相互作用 | 別責務とIDの契約を保持 | 複数指示の競合を推測で実装 |

これらは着手を再承認してもらうための一覧ではない。根拠のない依存部分を保留し、独立して検証可能な段を進めるための境界である。
